# issue6448 — leftover CELL/TAC 표 통째 배치

`tac_cell_leftover_fits.hwpx` 는 `samples/task2097/rowbreak_midpage_declared_fits.hwpx` 의
표 `treatAsChar="1"` 판이다. HWPX `pageBreak="CELL"`(모델 RowBreak) + leftover 선언-fit.
