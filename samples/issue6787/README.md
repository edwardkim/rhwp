# #6787 정식 회귀 입력

[이슈 #6787](https://github.com/edwardkim/rhwp/issues/6787)에서 식별한 공개 문서
**전자투표용지(사진 사용) 별지 제2호서식**(전자투표 및 개표에 관한 규칙)의 원본
바이트를 등록했다. 비공개 PC 경로 또는 환경 변수가 없어 검사를 건너뛰는 방식은
사용하지 않는다.

- 파일: [16774617-electronic-ballot-form.hwp](16774617-electronic-ballot-form.hwp)
- SHA-256: `b68b1d54a4e52f282d5ae699f4aa5d8cd5d580c982ec79a1d1e8099e69c1c088`
- 크기: 54272바이트
- 저장 제품: `hancom-office-2020`, `11.0.0.7571`
- 기준 엔진: **`2020`** (저장 제품이 `hancom-office-2020` 이므로
  [시각·fixture 증적](../../mydocs/manual/pr_review/visual_fixture_evidence.md) §3.5.1
  에 따라 `hwp2024Convert` MCP 의 `engine: 2020`).
  ⚠⚠ **engine 2020 기준 PDF 는 아직 없다** — 이 작업 PC 에는 그 MCP 의
  `.env.local`(endpoint·token)이 없어 산출하지 못했다.
- 현재 증적은 **한/글 2024** 출력이다: 쪽수 2 = 2, **공백 제거 글자 수가 쪽별로
  `[448, 582]` 로 정확히 일치**한다(수정 전 `[87, 582]`). 2020 저장본이라 정본과
  드리프트가 있을 수 있으므로, engine 2020 PDF 를 받는 대로 다시 대조한다.
- 회귀 계약: 칸 안 문단-기준 자리차지 중첩 표들이 **가로 오프셋으로 겹치지 않으면**
  같은 줄에 나란히 놓이고, 그 칸의 높이는 합이 아니라 **최댓값**이다.

새 sample이 기존 코퍼스 래칫에 주는 영향은 통합 검증에서 실측한다. 알려지지 않은
회귀를 숨기려고 기존 문서 기준선을 올리거나 보안 탐지 신호를 포괄적으로 무시하지 않는다.
