# Issue #7312 정식 회귀 fixture

- 원본: `samples/서울아리수본부 강서수도사업소 시설관리과/36494702_결재문서본문.hwpx`
- 형식: HWPX (1쪽)
- 용도: `treat_as_char=true` 표의 **저장 밴드**가 `om_top + 선언높이 + om_bottom` 로 그 표
  하나를 담고 있을 때(`tac_stored_band_is_outer_box`), 중간앵커 줄바꿈이 표를 다음 줄로
  내려보내지 않는지 검증한다.
- 저장소 코퍼스에서 이 형상을 재현하는 문서가 이 하나뿐이다(`samples/` 1,139문서 전수
  진단 스캔에서 해당 0건). 같은 계열(`결재문서본문`)은 이미
  `samples/hwpx/opengov/36385445_결재문서본문_…hwpx` 로 들어와 있다.
- 비밀·사용자별 경로 또는 환경변수에 의존하지 않는다.

## 이 문서가 잠그는 값

```text
문단 0.2  ls[0] lh=6896 == om_top 283 + 선언높이 6330 + om_bottom 283   (오차 0)
  결재란 표 3행×5열  treat_as_char=true  outer_margin 283 사방

  표 상단  종전 237.5 (= 줄 상단 140.4 + line_step 93.28 + om_top 3.77)
           기대 144.2 (= 줄 상단 140.4 + om_top 3.77)
  표 좌단  종전  79.4 (줄 시작으로 되돌림)   기대 373.4
  뒤 본문  종전 pi=11 1050.3                 기대 868.9
```

한/글 2022 정본 대조값(표 상단 145.7 · 좌단 373.9 · `pi=11` 869.6)은 이슈 #7312 에 있다.
정본 PDF 는 저장소 밖 캐시이므로 이 시험은 **저장 사다리와의 정합**을 계약으로 삼는다.
