# #7092 — 가운뎃점 `·`(U+00B7) 전진폭

한/글 정본은 `hwp2024Convert` engine 2020 으로 새로 생성했다(두 문서 모두 2020 버킷).
수정 전은 PR 기준 커밋 `897c6a3d8` 을 별도 워크트리·target 에서 새로 빌드한 바이너리,
수정 후는 이 브랜치 최종 커밋의 바이너리다.

| 이미지 | 내용 |
| --- | --- |
| `d23-p1-before-after-oracle.png` | 재현체(`tests/fixtures/issue_7092`) 1쪽 — **변화 사례** |
| `jubo-p2-before-after-oracle.png` | `samples/issues/2809/jubo_20260104.hwp` 2쪽 — **통제군** |

## 재현체 1쪽 — HY신명조(TTF · 대체 없음)

```text
  `·` 전진폭   정본 0.999em · 수정 전 0.299em → 수정 후 1.000em   (1쪽 13회, 문서 31회)
```

수정 전은 `굴취·채취` 처럼 점이 앞뒤 글자에 붙고, 수정 후와 정본은 점 앞뒤로 같은 간격이 난다.

## jubo 2쪽 — 신명 신신명조(HFT → HY신명조 대체) · 통제군

```text
  점선 리더 피치   수정 전 0.3em · 수정 후 0.3em (269회)
  text-overlap     수정 전 0건  · 수정 후 0건
```

대체된 글꼴은 빌려 온 표를 믿지 않아 종전 값을 유지한다. 26점이 150.6px 칸에 들어가야
하므로(전각이면 381px) 좁은 값이 옳다.
