# #7018 — 자리차지 표를 앵커한 문단의 baseline 출처

`samples/issue7018/2769535-records-inspection-plan.hwpx` 2쪽 상단을 `export-svg` 로 내
헤드리스 Chrome 으로 떴다(같은 clip: x 60, y 580, 640×290).

| 파일 | 내용 |
|---|---|
| `before_p2.png` | 수정 전 — `마. 행정박물류` 가 표 안으로 내려와 괘선을 가로지른다 |
| `after_p2.png`  | 수정 후 — 표 위 제자리 |

좌표 원장:

```text
                   줄 상자 상단   런 상자 높이          baseline
  수정 전            605.8        179.0 (표 줄 값)       784.8
  수정 후            605.8         13.6 (글자 줄 값)     619.4
  한/글 2020 잉크    605.5                               ..620.5
  표 상단            636.4 (수정 전후 동일)
```

기준 PDF 는 문서 저장 제품(한컴 2020)과 같은 엔진으로 새로 산출했다
(`pdf/2769535-records-inspection-plan-2020.pdf`).

저장 사다리가 정답지다 — 이 문단은 두 줄로 저장돼 있고(`seg[0] bl=1020`, `seg[1] bl=13423`,
표 줄 `textpos=11` = 본문 끝), 글자 줄 값으로 계산한 619.4 가 오라클 잉크와 맞는다.
