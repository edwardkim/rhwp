# 10k 한글 오라클 서베이 r39 — 정답지를 한글2024 로 바꾼 첫 기준선

- **표본**: hwpdocs 무작위 10,000건 — r15~r38 동일 표본(seed=20260716), 청크 md5 동일 확인
- **바이너리**: `rhwp_r39.exe` = 순수 `upstream/devel b9eb55107` (열린 PR 0건 반영),
  release + native-skia, SHA1 `649e2977704577d3763f71bfd2acc7bd33bfd0de`
- **실행 환경(필수)**: `RHWP_FONT_PATH=ttfs/hwp;ttfs/windows`, 자식 프로세스 cwd = 저장소 루트
- **오라클**: **한컴오피스 한글 2024 (13.0.0.564)** — r15~r38 은 전부 한글2022 였다
- **실행**: 본선 2갈래 병렬(25+25청크) 13.5시간 + 단일 프로세스 회수 패스 662건 1.2시간
- **하니스·원자료**: `output/poc/survey10k_r39_20260822/` (gitignore)

작업지시자 결정으로 정답지를 한글2024 로 바꿨다. 2024 를 쓴 런은 r32(2026-08-06)가 유일했고
당시 PI 계측 오염으로 되돌렸으므로, 이번이 **2024 기준의 실질적 첫 기준선**이다.

바이너리도 r37 이후 842커밋 움직였다. 오라클과 바이너리가 동시에 바뀌면 차이를 한 덩어리로
보는 순간 귀속이 불가능해진다. 그래서 이 보고서는 **`hwp_pages`(정답지 쪽수)와
`rhwp_pages`(우리 쪽수)를 따로 보고 3축으로 갈랐다.**

## 1. 집계 — 픽셀은 그대로, PI 만 −0.97pp

| 지표 | r37 (한글2022) | **r39 (한글2024)** |
|---|---|---|
| MATCH | 9,216 | **9,099** |
| PI_MISMATCH | 208 | 237 |
| PI_MISMATCH_CARET | 56 | 64 |
| PAGE_DELTA | 340 | **405** |
| PARA_COUNT / PROTECTED_SKIP | 74 / 28 | 91 / 42 |
| PI 일치율(graded) | 94.42% (9,820) | **93.45%** (9,805) |
| 픽셀 평균(OK+TRUNC) | 94.60% (9,026) | **94.69%** (9,036) |
| ERR / STALL / ARTIFACT | 52 / 26 / — | 56 / **0** / 6 |

시각 대조 분포: `OK` 8,924 · `OK2UP` 518 · `BIG` 196 · `SCALE` 132 · `TRUNC` 112 · `VERR` 24.

**픽셀 축은 버전 이동이 없다** (+0.09pp, 런 간 표류 ~0.2pp 안). r32 실측(Δ −0.010pp)의
재확인이다. 즉 두 버전의 **그리기**는 같고 **쪽 나눔**만 갈린다.

## 2. 3축 분해

`output/poc/survey10k_r39_20260822/analyze_axes.py`.

| 축 | 정의 | 건수 | 처리 |
|---|---|---|---|
| A 오라클 이동 | `hwp_pages` 가 r37 ≠ r39 | 101 | 이슈 #5940 |
| B rhwp 이동 | `rhwp_pages` 만 움직임 | 151 (회귀 73 / 개선 77) | 이슈 #5941 |
| C 잔존 이탈 | 양쪽 불변인데 rhwp ≠ 한글 | 285 | 기존 총괄 #5585 |

### 2.1 축 A — 한글2024 가 더 길게 조판한다 (89건)

| Δ(2024−2022) | +1 | +2 | +3 | +4 | +5~+11 | +13~+15 | +33 | +48 |
|---|---|---|---|---|---|---|---|---|
| 문서 | 63 | 8 | 3 | 3 | 6 | 3 | 1 | 1 |

**rhwp 는 한글2022 값을 재현한다** — 89건 중 55건이 2022 쪽수와 정확히 같다. 그래서 오라클을
바꾸는 순간 이 코호트가 통째로 `PAGE_DELTA` 로 넘어간다(신규 이탈 69건). 이것이 PI 하락
−0.97pp 중 **약 −0.70pp** 다.

계측 착시가 아님을 실증했다 — 4문서에서 ① 열자마자 읽은 `PageCount` ② 20초 대기 후
`PageCount` ③ 한컴이 내보낸 PDF 의 실제 쪽수가 **셋 다 같았다**(692 / 348 / 186 / 155,
`producer=Hancom PDF 1.3.0.547`).

같은 코퍼스를 직접 2022↔2024 로 대조한
[한글 버전 오라클 대조 r1](hangul_version_oracle_r1_20260807.md)은 `PAGE_DELTA` 121건을
보고했다 — r39 의 오라클 이동 101건과 같은 크기다. (r1 의 원자료가 남아 있지 않아 문서 단위
겹침은 확인하지 못했다.)

**기존 `--compat 2024` 는 이 코호트에 반응하지 않는다.** #5524 / #5543 으로 들어온 플래그를
89건 전수에 걸었더니 **완전 해소 0 · 가까워짐 0 · 악화 0 · 변화없음 89** 였다. 현재 플래그가
다루는 기전(Δ1 자리차지 표 앵커 줄 계상)과 이 코호트의 기전이 다르다.

### 2.2 축 B — devel 842커밋 구간의 쪽수 회귀 (73건)

회귀 73 · 개선 77 · 거리동일 1. 순증은 +4 개선이라 **총량 지표로는 침묵한다.**
악화폭 분포 `+1 × 67 · +2 × 2 · +3 × 3 · +8 × 1`.

두 동결 바이너리로 `dump-pages` 를 돌려 **COM 없이 151/151 재현**했다
(`axisB_ab.tsv`). 오라클과 무관한 우리 쪽 변화임이 확정됐다.

| 악화 | r37 | r39 | 한글2024 | 문서 |
|---|---|---|---|---|
| +8 | 304 | **312** | 302 | `1490000-201600081_…비정규직 목표관리 로드맵 마련을 위한 기초연구.hwp` |
| +3 | 79 | **82** | 79 | `1490000-200600032_…06년과제[1].근로시간등.중간보고.060817.hwp` |
| +3 | 427 | **430** | 417 | `6440000-202000006_…2019충남사회조사(시군편).hwp` |

**#5585 총괄 표의 일부가 이 구간 산물이다.** #5585 는 devel `9d352d56d`(08-19)에서 쟀고
격차 상위에 `06687 312/302` · `08291 430/417` 을 올렸는데, r37(08-14)에서는 각각 **304 · 427**
로 한글에 훨씬 가까웠다. 총괄 이슈가 회귀 후 상태를 기준선처럼 기록하고 있다.

### 2.3 축 C — 잔존 이탈 (285건)

Δ 분포 `{-1: 104, +1: 145, |Δ|≥2: 36}`. 기존 #5585 코호트이며 이 런의 신규가 아니다.

## 3. 오라클 전환에서 새로 생긴 계측 결손

1. **2024 가 일부 HWPX 를 못 연다** — `PageCount=1` + 문단 1개(caret-blocked) 6건.
   주파수 분배표(2022 에서 290쪽 → 1), 전자파적합성 기준(14 → 1), 질병청 보도자료(6 → 1) 등.
   **rhwp 는 6건 전부 정상 파싱하고 2022 값과 일치한다**(287 / 14 / 6 / 3 / 2 / 2) —
   정답지 결손이지 rhwp 결함이 아니다.
2. **COM RPC 끊김** — `hwp:com_error` 34건(한글2022 는 0건). 단일 프로세스 회수로 29건 복구.

## 4. 하니스 — 2024 로 오면 반드시 고쳐야 하는 두 곳

절차 상세는 [한글 버전별 페이지네이션 대조 가이드](../manual/verification/hangul_version_oracle.md) 8절.

### 4.1 정답지 PDF 가 통째로 안 나온다

pyhwpx `save_as(format="PDF")` 는 `HParameterSet.HFileOpenSave.filename` 을 쓰는데
**한글2024 타입라이브러리는 그 프로퍼티를 `HScriptMacro` 로 선언**한다. gen_py 캐시를 지우고
재생성해도 같다 — 캐시가 아니라 선언이 다르다. 착수 스모크 8건이 전부
`vis=VERR / no_pdf` 였는데 **PI 축은 멀쩡해서 놓치기 쉬웠다.**

평범한 `SaveAs(path, "PDF", "")` 는 정상 동작한다. `save_pdf()` 헬퍼로 raw `SaveAs` 를
먼저 쓰고 실패 시 종전 경로로 되돌린다.

### 4.2 r32 정착 게이트의 전제가 틀렸다

r32 가 도입한 가드는 "끝 문단의 `current_page` == `PageCount` 여야 조판이 끝난 것"이었다.
**문서의 마지막 문단이 마지막 쪽에 있어야 할 이유가 없다.** 회수 첫 50건 중 **32건이 오발동**
이었고 값도 `end_page=176/177` · `497/519` · `1/2` 처럼 조판 결과로 자연스러웠다(전부 한글2022
에서 MATCH 였던 문서). 오발동 1건마다 settle 만료 × 재측정 2회를 태워 **문서당 89초**를 썼다.

정착은 값이 **멎을 때까지**(연속 2회 동일) 기다리고, 만료돼도 격리하지 않도록 바꿨다.
격리 근거는 **"쪽 번호가 물리적으로 불가능한가"** 로만 좁혔다.

| 가드 | 조건 |
|---|---|
| 1쪽 역행 | 직전이 2쪽 이상인데 1 |
| 범위 밖 | `current_page > PageCount` 또는 `< 1` |
| 끝 문단 불안정 | 같은 문단을 두 번 읽어 값이 달라짐 |
| 축퇴 분포 | 쪽수 3 이상·문단이 쪽수의 2배 이상인데 서로 다른 쪽 값이 2종 이하 |
| 역행 일반형 | 뒤 문단이 앞 문단보다 앞쪽 쪽번호 |

축퇴형이 실제 아티팩트다 — 15쪽 문서의 문단이 처음부터 끝까지 전부 "1쪽". r32 의 1쪽 역행
가드는 시작부터 1이면 못 잡는다.

결과: 오발동 50건 중 0건, 문서당 89초 → 21초. 본선 ARTIFACT 436 중 **326건이 MATCH 로 회수**
됐고 최종 ARTIFACT 는 6건, STALL 은 0건이다.

## 5. 운영 결론

- **2병렬 본선 → 단일 프로세스 회수 패스** 2단 구성은 2024 에서 필수다. 아티팩트는 부하 의존
  이라 특정 청크에 몰린다(r39 실측: 청크별 0 ~ 47건).
- **r37 이전 PI 수치와 직접 비교하지 말 것** — 정답지가 다르다. 픽셀 축만 연속 비교 가능하다.
- 오라클 버전이 바뀌면 **지표가 아니라 측정 절차부터 재검증**한다. 한 축(PI)만 무너지고 다른
  축(픽셀·쪽수)이 멀쩡하면 결함이 아니라 계측을 의심하라.
- **r39 = 신규 기준선** (한글2024 기준).
