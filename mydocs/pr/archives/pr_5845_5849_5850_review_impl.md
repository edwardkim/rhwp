# PR #5845, #5849, #5850 통합 구현 검토 기록

## 범위와 반영 순서

최신 `upstream/devel` `1b8c39b6c32baf00574564016bd9d9f7d1d88198`에서 시작했다.

1. #5845 `530fd2790476a7233f860d37ac68e4b5e411743b`
2. #5849 `1942ad24267b883b11b5c5d75387102f8ecb4e6c`
3. #5850 `0251c0a218dfc1d7701a1a735ae885787b7b0e32`

#5845의 앞선 두 test-only source commit도 함께 반영했다. upstream에 이미 있던 #5845 commit은
중복 적용하지 않았다. #5850의 #5723, #5729 후속 renderer commit도 같은 원 PR head에서 누적 적용했다.
통합 code head는 `05e3789ebb222ddb7b4653462ff85d6678de6006`이다.

## 최종 사전 판정

| 원 PR | 판정 | 근거 |
| --- | --- | --- |
| #5845 | 수용 예정 | dot leader 공통 계약, focused test, visual scope 확인 |
| #5849 | 수용 예정 | 5개 focused regression, #5798 기준선 대비 visual 개선, #5821 text 무차이 |
| #5850 | 수용 예정 | HWPX x2x `vertpos`/합성 lineseg 방출, #5723 displaced-line, #5729 TAC stored-band 계약 |

원 PR 모두 최신 head가 `OPEN`, `MERGEABLE`, `CLEAN`이며 source CI 성공을 확인했다.
이 통합 PR의 최신 head Full CI, CodeQL, Render Diff 성공 뒤에만 최종 수용 및 merge를 실행한다.

## 공통 로컬 검증

- `cargo fmt --all -- --check`
- `node scripts/rust-unit-test-tiers.mjs --check`
- `node scripts/rust-test-suite-manifest.mjs --prepare` 및 `--check`
- `git diff --check upstream/devel...HEAD`
- `cargo clippy --locked --all-targets --target-dir target/pr-review -- -D warnings`
- `scripts/wasm-pack-locked.sh --target web --out-dir pkg`
- native-Skia library test
- 전체 nextest: 8121 passed, 3 slow, 39 skipped (217.679s)

## 증적 보존

- Hancom 2020 기준 PDF: `pdf/pr_5849/hancom2020/`
- 대표 review PNG: `mydocs/pr/assets/pr_5845_ktx_p002_visual_review.png`,
  `mydocs/pr/assets/pr_5849_issue5798_p001_visual_review.png`,
  `mydocs/pr/assets/pr_5849_issue5821_p001_visual_review.png`
- 임시 visual sweep output은 `output/fidelity-*-20260822`; GitHub comment에는 merge SHA가 고정된
asset raw URL만 사용한다.

## 남은 절차

1. 통합 PR 생성 전 `upstream/devel` 및 세 원 PR head를 재확인하고 rebase한다.
2. 통합 PR CI를 모니터링한다.
3. 녹색이면 실제 CI 결과를 trailing docs-only commit으로 기록하고 자동 승인·admin merge한다.
4. merge SHA가 devel의 조상임을 확인한 뒤 원 PR과 #5843, #5715, #5798, #5821, #5822, #5828, #5847, #5723,
   #5729에
증적 기반 comment 및 close를 처리한다.
