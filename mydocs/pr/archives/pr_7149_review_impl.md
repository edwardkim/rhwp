# PR #7149 체리픽 검토 처리 기록

1. 최신 `upstream/devel` `4fddb1bb7`을 확인하고 `codex/pr7145-pr7149-review-20260915`를 생성했다.
2. #7145 `0eefe065f`를 `9f5a63a6a`로, #7149 `42d1493ff`를 `65df76e61`로 순차 체리픽했다. 충돌은 없었으며 `-x` 출처와 원 저자를 보존했다.
3. #7149 검사 10개, 삭제 전 registry 음성 대조, TypeScript 및 프런트엔드 전체 테스트를 완료했다. 제품 코드 메인터너 보정은 없다. Copilot 오타 지적 두 곳은 `fec435fa9`에서 수정하고 검사 10개를 재실행해 통과했다.
4. #7145의 Native·WASM·한컴 비교 및 기준 PDF 보존은 해당 [검토 문서](pr_7145_review.md)에서 판정한다.
5. 통합 PR 생성 지시 후 해당 사전 검증과 최종 head CI를 완료한다. owner를 reviewer로 자동 지정하지 않는다.
6. merge 승인 및 CI 조건 충족 후에만 통합 merge와 원 PR·이슈 후속 처리를 한다. 원 PR을 먼저 닫지 않는다.
7. cleanup은 이 작업이 만든 branch와 전용 `target/pr7145-pr7149-review-20260915`에 한정하며 실행 중인 Cargo/Rust 작업이 없음을 확인한 뒤 수행한다.

#7141은 사용자 지시에 따라 Draft 제외, #7142~#7144는 이미 merged, #7146~#7148은 PR이 아닌 이슈다.
이번 적용 대상은 #7145·#7149 두 건이며, 이전 #7104·#7113·#7115 보정 요청은 취소되어 실행하지 않았다.
