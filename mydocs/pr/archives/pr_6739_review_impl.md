# PR #6739·#6770 통합 처리 계획

## 현재 단계

2026-09-05, 두 source PR의 reviewer 지정과 최신 `upstream/devel` 동기화, 출처를 보존한
로컬 체리픽 및 정적 검토를 완료했다. 코드 후보는 `5bc8e87f5`이며, 그 위에 #6770의 음의 0 비교
보정을 추가해 Studio 테스트·타입 검사·빌드와 직접 브라우저 검증을 완료했다. 보정은 미커밋이다.
통합 PR 생성·push·원 PR 댓글·close·GitHub merge는 수행하지 않았다.

## 기준과 출처

- 기준: `1f861362ab372f9fa26e38f9a534a89286f641c4` (`upstream/devel`).
- 작업공간: `/Users/tsjang/rhwp`.
- 검토 브랜치: `review/lpaiu-6739-6770-20260905`.
- 원 author를 유지하고 `git cherry-pick -x`로 출처를 남겼다. 체리픽 충돌은 없었다. 이후 #6770의 음의 0 비교 결함을 메인터너 보정하고 회귀 테스트 10개를 추가했다.

| 원 PR | 원 커밋 | 로컬 커밋 | 제목 |
| --- | --- | --- | --- |
| #6739 | `4f1cacec4795f774f27c5aca21678d916d9bf683` | `7b3bcccf2` | Task #6738: 조합 오버레이가 성립하지 않는 기하를 폴백 폭으로 덮지 않는다 |
| #6739 | `facfdf93ea5ef1b5e43a30121251bce0365a0775` | `85ac66281` | Task #6738: 자기리뷰 P3 정산, lookup 실패 계약 통일과 쪽 넘김 시 셀 bbox 폐기 |
| #6770 | `d07f721fade0e2397284b5b1119898d1c42f54e0` | `5bc8e87f5` | Task #6769: 건드리지 않은 위치 오프셋을 패치에 싣지 않는다 |

#6770의 선행 `390eff1f`와 `fc2166de`는 이미 `devel`에 동등 패치가 있어 중복 제외했다.
다른 열린 PR은 이번 통합에 포함하지 않았다.

## 실행 검증 완료와 승인 경계

1. 사용자의 테스트·보정 지시에 따라 로컬 실행 검증을 마쳤다. 보정 후 Studio 1,424 통과·1 건너뜀·실패 0, 타입 검사·WASM·Studio 빌드가 통과했다.
2. 원 코드의 `-1 HWPUNIT` 무변경 실패를 재현하고 숫자 기준 표시값 비교로 최소 보정했다. 추가 40개 경계값 시나리오·160개 assertion과 실제 셀 그림 속성창 검증도 통과했다.
3. [#6739 리뷰](pr_6739_review.md)는 승인, [#6770 리뷰](pr_6770_review.md)는 메인터너 보정 완료·수용 가능으로 갱신했다. [직접 증적](pr_6739_6770_visual_sweep.md)과 코멘트용 PNG 5개만 보존하고 로그·임시 산출물은 제외했다.
4. 기준 `upstream/devel`을 반영한 오늘할일을 읽고 기존 기록을 보존해 이 작업의 완료 검증만 추가했다. 보정 코드·리뷰·증적·오늘할일은 아직 미커밋이며, 검증 diff SHA-256은 `e00d6c035ce675020915a2c00a089e86792ba70a6a668d0c46589132ff74fd67`이다.
5. 작업지시자의 승인을 받은 뒤에만 임시 upstream head로 push하고 `devel` 대상 통합 PR을 생성한다. fork source branch는 변경하지 않는다.
6. 최종 head의 required check·실행 worker·CodeQL·필요한 Render Diff와 merge 가능 상태를 확인한다. merge 승인 없이 source PR을 닫거나 통합 PR을 merge하지 않는다.
7. 승인 후 merge SHA를 고정하고 devel 동기화·push CI 확인을 마친 다음 `post_merge.md`에 따라 원 PR과 실제 연결 이슈를 중복 없이 처리한다. 그림은 댓글에서 직접 볼 수 있게 삽입한다.
8. 실행 중인 작업·미커밋 변경·merge 포함 여부를 확인한 뒤 승인된 이번 작업의 branch·검토 전용 산출물만 정리한다. 기본 작업공간, 공유 target과 기여자 브랜치는 보존한다.

## 검증 범위

이번 신규 변경은 Studio TypeScript 여섯 파일이다. 기본 검증은 Studio 테스트·타입 검사와
직접 브라우저 검증이며, Rust 전체 회귀를 자동으로 실행하는 계획은 아니다.
IME 화면과 속성창 동작은 정적 PDF 비교만으로 판정하지 않는다.

## 중단 및 되돌림 원칙

검증 실패나 source head 변경이 생기면 기존 결과를 새 후보에 그대로 적용하지 않는다.
사용자 변경을 덮어쓰거나 다른 작업 브랜치·worktree를 정리하지 않는다.
이번 브랜치의 폐기·기능 제외도 별도 지시를 받은 범위에서만 수행한다.

## 2026-09-05 PR 게시 승인 후 확정 기록

- 작업지시자가 메인터너 보정과 검토 기록의 커밋 및 통합 PR 생성을 승인했다. 앞의 미커밋/승인 대기 표기는 로컬 검증 당시 상태이며, 게시 준비 단계에서는 이 절을 따른다.
- #6739의 최신 source head는 `facfdf93ea5ef1b5e43a30121251bce0365a0775`로 유지됐다.
- #6770의 최신 source head는 `d8d8828f5ea6acf7506db20f541ac1186f00755d`다. 기여자가 선행 #6763의 통합을 반영하여 `1f861362ab372f9fa26e38f9a534a89286f641c4` 위로 재배치했다.
- `git range-diff`에서 이전 `d07f721fade0e2397284b5b1119898d1c42f54e0`과 최신 `d8d8828f5ea6acf7506db20f541ac1186f00755d`의 기능 패치는 `=`로 확인됐고, `git cherry HEAD <최신 source head>`도 이미 적용된 패치(`-`)로 판정했다. 로컬 `5bc8e87f5879d3308ae006bcc8e53410477ee580`의 기존 provenance를 보존하며 같은 기능을 중복 체리픽하지 않았다.
- 검토 branch의 기준 `upstream/devel`도 `1f861362ab372f9fa26e38f9a534a89286f641c4`로 동일하다. 검증한 메인터너 보정 diff의 SHA-256 `e00d6c035ce675020915a2c00a089e86792ba70a6a668d0c46589132ff74fd67`과 커밋 직전 diff가 일치했다.
- 실제 소스 변경이 없으므로 기존 Studio 1,424 통과/실패 0/skip 1, TypeScript 검사, Studio 및 WASM 빌드, CDP 검증 결과를 재사용한다. 이번 게시 단계에서 테스트를 다시 실행했다고 기록하지 않는다. 통합 PR 최신 head의 GitHub CI 결과는 별도로 기다려야 한다.
- 최종 판정은 #6739 **승인**, #6770 **메인터너 보정 완료, 수용 가능**이다. 원 PR/관련 이슈 comment와 close, 통합 PR merge, branch/target 정리는 이번 게시 작업에서 수행하지 않는다.

- 메인터너 code/test 보정 커밋: `f7c53792ca966521567a79078247fb28a2b1111a`. 검토 문서, 오늘할일 및 코멘트용 PNG 5개는 이 커밋 뒤의 별도 기록 커밋으로 포함한다.
