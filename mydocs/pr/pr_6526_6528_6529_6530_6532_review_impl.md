# planet6897 CI-green PR 통합 검토 및 구현 기록

- 작업일: 2026-08-31
- 통합 브랜치: `review/planet6897-green-20260831`
- 시작 base: `upstream/devel@99419b6b2`
- 범위: CI green이었던 #6526, #6528, #6529, #6530, #6532
- 제외: #6514는 CI 실패이므로 cherry-pick 대상에서 제외
- reviewer 요청: `postmelee`를 #6526, #6528, #6529, #6530, #6532에 요청

## 적용 계보

| 순서 | PR | 원 head | 통합 commit | 비고 |
| --- | --- | --- | --- | --- |
| 1 | #6526 | `76a7e0e62edcad0207a0d543f68ef450359830b8` | `1a0923e9ed`, `7fb1ae1e03` | renderer 및 regression fixture |
| 2 | #6528 | `7eed781260257c1f54cd79b9d72bb57d1ceab0b5` | `a9727942c` | TSV 충돌을 두 fixture 행 보존으로 해결 |
| 3 | #6529 | `02ccd4bb70884ae3cb6726e69b8a6f907fe46a75` | `e1e12c9fb` | #6528이 조상이므로 unique commit만 적용 |
| 4 | #6530 | `e374be85c554c8b4b502f1f253aaa7c3d7b425f0` | `b98b9de60` | 원 PR conflict를 메인 터너 보정으로 승계 |
| 5 | #6532 | `ef0d03fc74ac34bae709f5e24c61c51f4b412fcf` | `1d43b1f5f` | Studio substitution 계약 |

## #6530 메인 터너 보정

- GitHub 원 PR은 `CONFLICTING`/`DIRTY`여서 직접 merge할 수 없었다.
- maintainer 보정은 현재 `devel`의 renderer/golden 기준을 보존하면서 legacy 한양 face의 HCR/함초롬 fallback만 합쳤다.
- 보정 대상: `한양중고딕`, `HY중고딕`, `한양견고딕`, `한양견명조`.
- 유지 대상: `한양신명조` 기존 체인.
- 따라서 #6530은 원 PR을 다시 merge하는 대신, `b98b9de60`이 포함된 통합 PR로만 승계한다.

## 워크플로 검증

| 게이트 | 결과 |
| --- | --- |
| `node scripts/rust-test-suite-manifest.mjs --prepare` | 통과: 48/48 targets, 최소 6559 cases inventory |
| `cargo fmt --all && cargo fmt --all -- --check` | 통과 |
| native Clippy | 통과 |
| WASM Clippy | 통과 |
| workspace build | 통과 |
| all-target Clippy | 통과 |
| `node scripts/rust-test-suite-manifest.mjs --check` | 통과 |
| `node scripts/rust-unit-test-tiers.mjs --check` | 통과: 4221 tests / 299 modules |
| #6524, #6181, #6180, #6171 focused regression | 모두 종료 코드 `0` |
| Studio #6263 contract | 2 pass / 0 fail |
| full `cargo nextest run --locked --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast` | 종료 코드 `0` |

## 시각 검증

| PR | 입력 / 대상 페이지 | Hancom 기준 | 결과 |
| --- | --- | --- | --- |
| #6526 | `30098_float_host_split_lineseg.hwp` p3 | 2020 direct-dll-host, SHA `d3157eb5...9962df` | flagged 0, [review](assets/pr_6526_issue6524_p3_review.png) |
| #6528 | `156562368_inline_tac_table_line_advance.hwpx` p5 | 2020 direct-dll-host, SHA `e22b77ca...48353a` | flagged 0, [review](assets/pr_6528_issue6181_p5_review.png) |
| #6529 | `156745974_tac_object_line_spacing.hwpx` p7 | 2020 direct-dll-host, SHA `d9539968...460589` | flagged 0, [review](assets/pr_6529_issue6180_p7_review.png) |

- 모든 기준 PDF는 HWP 2024 MCP client의 asynchronous `start -> status -> download` 경로로 생성했고, client-local 다운로드 SHA-256과 서버 SHA-256을 대조했다.
- visual sweep는 `/snap/bin/chromium`을 `VISUAL_SWEEP_CHROME`으로 지정해 webfont rasterizer를 실행했다.
- pixel/ink match는 글꼴 rasterizer 차이를 포함하므로 보조 지표로만 사용했고, review panel과 위험 항목 목록을 함께 확인했다.

## 결론과 후속 조건

- 확인된 차단 결함은 없다.
- #6526, #6528, #6529, #6532는 통합 검증본에서 승인한다.
- #6530은 메인 터너 보정이 포함된 통합본으로 수용하며, 원 PR 직접 merge는 금지한다.
- 현재 작업은 로컬 integration branch까지만 생성했다. remote push, 통합 PR 생성, GitHub comment는 수행하지 않았다.
- 원격 상태는 변동 가능하므로, 다음 상태 변경 직전에 각 원 PR head와 CI green, 통합 branch head를 재확인해야 한다.

