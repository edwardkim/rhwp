# 2026-09-14 오늘할일

## #3790 CI Impact Policy 정상화 — PR #7129 self-review

- [PR #7129](https://github.com/edwardkim/rhwp/pull/7129)의 후보 `b7ab86240`에서 Full CI·CodeQL·Render Diff·Adapter·Proptest와 최종 Policy 감사가 성공했다.
- 신규 연결 테스트의 기대 목록 누락을 보정했고 이전 CI 실패 단계도 성공했다. self-review에서 Node 22개·Python 19개를 재실행해 통과했다.
- [self-review](../pr/archives/pr_7129_review.md)의 판정은 승인. advisory 유지, v6 발행·소비 정합성과 devel 전용 경계를 확인했다. Rust·Studio·문서 fixture 변경 없음.
- 실행 순서·빈도 재설계는 추가하지 않는다. 현재 원격 성공은 `v=6;cv=7;rfp=0` Full 감사이며 main 새 YAML 활성화나 후행 기록 재사용 성공을 뜻하지 않는다.
- 이번 기록의 push·최신 head CI·병합은 남아 있다. 승인된 병합 후 최종 결정과 결과를 게시하고 #3790을 종료하며, main 배선은 정규 승격에서 적용한다.

## #7084 이모지 진행폭 — C 통합 검증 계속

- 9월 13일 HWP/HWPX Studio 이모티콘 렌더링의 메인테이너 시각 판정 통과 후 C 진입 승인.
- [Stage 4](../working/task_m100_7084_stage4.md)에 최초 실패 및 최소 보정을 기록했다.
  기존 테스트 초기화 6곳의 새 필드 누락과 새 세션 API 5개의 #2724 분류 누락만 보정했다.
  조판 구현·sample·baseline/golden은 변경하지 않았다.
- release lib 4,055 PASS / 13 ignored, 전체 nextest 재실행 9,769 PASS / 51 skipped / 0 FAIL.
- Native Skia 공식 3종(lib 4,112 PASS 및 집중 2+4 PASS)도 통과했다.
- 세 Clippy·workspace build·doc test·TypeScript 통과. 줌 테스트 fixture의 새 WASM 메서드 누락을
  보완한 뒤 Studio 1,678 PASS / 2 skipped. 기본 Render Diff 3쪽 및 직접 PDF gate 3쪽 통과.
- 실제 Chrome 비용 측정 완료. 두 원본 cold 1.0~1.3ms, warm 추가 측정 0건.
  큰 문서에서 warm 47.6ms·동기 print SVG 확보 550.6ms 관측은 보고서에 제한과 함께 공개했다.
- 서식 run 경계를 가로지르는 VS16 묶음의 부분 적용을 HWP/HWPX 모두 메모리 편집으로 재현했다.
  원본 시각 통과는 유지하지만 C 완료 판정은 보류한다. [보완 계획](../plans/task_m100_7084_impl.md#8-c-통합-검증-후-보완-요청) 승인 후 [Stage 5](../working/task_m100_7084_stage5.md) 진행 중.
- Stage 4 제품은 `bdd63167c`이며 Stage 5에서는 논리 문단 결합열 경계 보호를 보완했다.
  중간 후보의 기존 shaping run 보존 6건 실패를 정정했고 제품/테스트 `281fb2826`으로 보존했다.
  집중 55건, lib 4,055건, integration 9,773건, Native Skia, 세 Clippy, Docker WASM,
  Studio 1,678건, 기본 Render Diff 3쪽·직접 PDF gate 3쪽이 통과했다.
  실제 Chrome의 원본 비율 및 VS16/ZWJ 경계 보호를 확인했다. 큰 문서 warm 51.9ms·print 574.5ms,
  warm 추가 측정 0건이다. 통제한 descriptor 오류에서는 명시적 복구만 성공하고 화면 자동 재준비는
  호출되지 않아 C 최종 완료를 보류한다. 같은 이슈 안의 최소 예외 복구가 다음 절편 후보이다.
  중간 Docker 빌드는 최종 증거에서 제외하며 상세 기록은 Stage 5를 따른다.
  원격 push·PR·GitHub 댓글은 이번 승인 범위가 아니다.
- [Stage 6](../working/task_m100_7084_stage6.md) 승인 후 descriptor 불일치의 제한된 자동 복구를
  `230f80113`에 구현했다. 같은 문서/폰트 세대에서 1회만 시도하고 오래된 결과는 거부한다.
  실제 Chrome HWP/HWPX 12개 조건, 집중 15건, npm 1,687건, TypeScript·production build,
  Docker WASM·원본/결합열 smoke·Render Diff 3쪽 통과. 음성 대조는 복구 누락을 검출했다.
  WASM bytes는 Stage 5와 동일하다. 이 절편은 완료했으며 다음은 최종 보고서 통합·완료 판정이다.
  원격 push·PR·이슈 close는 수행하지 않았다.
- 최종 보고서 통합 승인을 받아 [결과보고서](../report/task_m100_7084_report.md)를 작성했다.
  원인 계보·보호 규칙·소스별 검증·시각 판정·비용·지원 제한과 남은 제출 순서를 통합했다.
  제품/테스트 `230f80113`은 변경하지 않았다. 문서 검증·로컬 커밋 후 최종 결과 승인을 요청한다.
  원격 push·PR·GitHub 댓글·close는 하지 않는다.

## #7084 제출 완료 — PR #7119

- 원격 push·Open PR 생성 승인을 받아 [PR #7119](https://github.com/edwardkim/rhwp/pull/7119)를 등록했다.
  assignee `edwardkim`, milestone `v1.0.0`, bug/Studio/rendering/font 라벨, reviewer 지정 없음.
- 통합 검증 후보 `d3a189d5d`, base `11860a9f4`, 최초 제출 `78f372a5b`.
  전체 nextest 9,814 PASS/51 skipped, Native Skia 3종·세 Clippy·fresh Docker WASM,
  Studio 1,692 PASS/2 skipped 및 실제 Chrome·Render Diff·직접 PDF 검증을 완료했다.
- [self-review 준비 기록](../pr/archives/pr_7119_review.md)과 대표 PNG를 같은 PR에 보존한다.
  원격 CI·정식 self-review·병합 승인·이슈 close는 아직 남아 있다. 앞선 항목은 당시 단계 기록이다.

- 이후 self-review 진행 승인을 받아 원격 `6434189bc`의 CI·CodeQL·Render Diff·Adapter·Proptest와
  GHAS/Policy 성공을 확인했다. 코드·계약·기준값 변경을 대조한 review 판정은 승인이다.
  Studio 집중 25건·Rust 집중 32건을 추가 통과했다. 검토 기록 push·그 새 head CI·병합 승인은 다음 절차다.

## #7096 TAC 표 겹침 — 원 PR #7103 통합 PR #7110

- source `1c5fd9676`을 최신 devel 위에 저자·SHA를 보존해 cherry-pick하고 `6bb907fa9`·`ef489fd02`에서 메인터너 보정했다. 원시 줄 소속과 저장 표 위치·advance를 측정/배치가 공유한다.
- [개별 review](../pr/archives/pr_7103_review.md)는 메인터너 보정 후 수용 가능이다. 초기 회귀 2건은 기존 기대값 변경 없이 해결했고 최종 전체 Summary [ 320.358s] 9753 tests run: 9753 passed (2 slow), 51 skipped, 세 Clippy·Native Skia 3종·fresh WASM/Chrome 검증을 통과했다.
- 한컴 1쪽 전체·표 경계를 확인하고 OVR5 142페이지/48표 회귀 0을 확인했다. 입력 18개가 Git blob/SHA와 일치하며 기존 HWP/HWPX/PDF의 중복 재명명은 없다.
- [통합 PR #7110](https://github.com/edwardkim/rhwp/pull/7110)의 코드 CI가 성공한 뒤 이 review/오늘할일/최종 증적을 같은 PR의 trailing 문서 commit으로 추가했다. CI 모니터링 후 후속처리 승인을 받았으며 최종 trailing head CI를 확인한 뒤 병합한다.
- 원 PR 코멘트에 통합 경로 및 fork의 `jangster77` Write 권한 요청을 게시했다. 초기 일반 fork 권한 부재와 maintainer edit을 구분했고, 요청 후 API `push: true`로 변경됨을 재확인해 코멘트·PR 설명을 갱신했다.
- `Closes #7096`을 지정했다. 병합 뒤 원 #7103에 통합·merge SHA/대표 이미지를 알리고 종료하며 #7096 상태, devel 동기화·duration refresh·이번 소유 산출물 정리를 확인한다.
