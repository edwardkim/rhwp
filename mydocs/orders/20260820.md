---
kind: daily-order
status: active
date: 2026-08-20
---

# 2026-08-20 오늘할 일

## #5760 rhwp-q-more volume probe 공통화와 CodeQL 분석량 축소

- [#5761](https://github.com/edwardkim/rhwp/pull/5761)은 `volume-probe`의 50개 생성 shard와 반복 문서
  순회 함수를 공통 수집기로 대체한다. 문서는 한 번만 순회하며 기존 slot/probe 가중치와 wrapping 산술은
  유지한다.
- 생성 shard 50개, 890,280 LOC를 제거했으며 `rhwp-q-more` source 총량은 891,950 LOC에서 1,768 LOC로
  축소됐다. CodeQL 제외 규칙은 추가하지 않았다.
- 3개 실제 HWP의 150개 golden 결과와 slot 범위 거부 계약, `clippy -D warnings`가 통과했다. 전체
  release-test는 8,010 passed, 3 slow, 39 skipped, exit 0, 197.514초로 완료했다.
- 최신 `upstream/devel@bba0ba2d` 위 rebase는 충돌 없이 완료됐다. 사용자 지시에 따라 rebase 뒤의
  로컬 회귀는 반복하지 않았고, code candidate CI의 Build & Test, Lint, property roundtrip,
  adapter inter-diff와 Rust CodeQL은 모두 성공했다.
- Rust CodeQL은 #5735 대비 job 전체가 29분 22초에서 20분 2초로 9분 20초(31.8%) 단축됐다. 36개 query
  실행도 20분 51초에서 12분 36초로 8분 15초(39.6%) 줄었다. 827개 Rust 파일 성공 추출, extractor
  진단 2건, branch 기준 open alert 0건을 확인했다.
- [#5760](https://github.com/edwardkim/rhwp/issues/5760)은 PR 본문의 closing keyword로 연결했다. 현재
  [#5761](https://github.com/edwardkim/rhwp/pull/5761)은 `MERGEABLE/CLEAN`이며, 이 문서와 검토 기록을
  trailing docs-only commit으로 push한 뒤 작업지시자 승인 후 merge한다. 상세 판정은
  [PR #5761 검토 기록](../pr/archives/pr_5761_review.md)에 남긴다.

## #5746 nextest archive A/B/C 분할과 worker 재배치 완료

- [#5746](https://github.com/edwardkim/rhwp/pull/5746)은 library test archive A와 integration archive B/C를
  독립 builder로 만들고 A shard 1/2, B, C worker 네 개로 실행하도록 CI를 재구성했다.
- 최종 CI에서 A는 3,893건, B는 integration target 41개 중 21개와 2,000건, C는 나머지 20개와 1,922건을
  빌드했다. B/C는 각각 약 12분 14초와 12분 18초에 병렬 archive 생성을 마쳤고 aggregate가 전체 count와
  네 worker 성공을 확인했다.
- [#5737](https://github.com/edwardkim/rhwp/issues/5737)은 merge로 자동 종료됐다. 상세 검토 증적은
  [PR #5746 기록](../pr/archives/pr_5746_review.md)에 남긴다.
- 이 항목은 code PR의 CI를 다시 유발하지 않도록 review-only docs PR로 보존한다. docs PR이 merge되면
  이슈/원 PR 후속 기록을 반복하지 않고 `post_merge.md`에 따라 devel sync와 해당 branch 정리만 수행한다.

## #5719 Studio 올드스쿨 스킨과 첫 실행 선택 안내

- [#5719](https://github.com/edwardkim/rhwp/pull/5719)는 `oldschool` 옵트인 스킨과
  클래식·모던·올드스쿨 첫 실행 선택 대화상자를 추가한다. 기존 `default`·`flat` 저장값은
  사용자 노출 명칭만 바꾸고 호환한다.
- `skinChosen` 마이그레이션으로 기존에 스킨을 골랐던 사용자는 안내를 다시 보지 않으며,
  FOUC 초기화·런타임 전환·보기 메뉴·온보딩 카드가 같은 스킨 값을 사용한다. 다크 모드에는
  올드스쿨의 라이트 팔레트가 덮이지 않도록 별도 토큰 블록을 둔다.
- 최신 `upstream/devel` 병합 시뮬레이션, focused Studio test 17건, 개발용 WASM build,
  Studio production build가 성공했다. 구현 후보 CI의 Frontend package gates와 Canvas visual diff도
  성공했다.
- 이 오늘할일과 [#5719 검토 기록](../pr/archives/pr_5719_review.md)을 원 PR head에 trailing
  docs commit으로 push한다. 새 head의 CI와 mergeability가 통과하면 병합하고, source branch,
  review worktree, 검토 전용 WASM target을 `post_merge.md` 순서로 정리한다. 관련 #4714는 이미
  병합된 선행 PR이므로 별도 issue close는 없다.
## #5647 B2 구조 편집 한컴 판정 번들 검토 완료

- [#5647](https://github.com/edwardkim/rhwp/pull/5647)은 원본 HWP/HWPX 38개, 한컴 2022 PDF, SHA-256
  원장과 경계 사례 시각 근거를 추가해 #5447 B2 스파이크의 재현 가능한 판정 근거를 고정한다.
- 최신 `upstream/devel` 위 병합 시뮬레이션, format·manifest·unit-tier 정책, focused Rust 계약
  38 passed/2 ignored와 `pdftoppm` 144dpi 원장 재계산 262건 전건 일치를 완료했다. `--prepare`가
  Cargo 파일을 건드리지 않는 #5683 경로도 SHA-256 비교와 계약 test 4건으로 확인했다.
- 원 PR에는 최종 승인 검토 기록을 trailing commit으로 push했다. 이 오늘할일은 병합 뒤에도 남는
  운영 기록이며, #5447은 구현 B2 후속 범위를 계속 추적하므로 닫지 않는다.

## #5671 rhwp-q-pack 조회 CLI 50개와 Control 필드 조회 코드 완료

- [#5672](https://github.com/edwardkim/rhwp/pull/5672)는 최종 검토 head `47b2fab`의 CI 성공 뒤
  `f9616a95fdffb917e9a5a74d4cdf4f4ad774b32e`로 `devel`에 squash merge되었다.
- 메인터너 보정으로 `caption-tables`, `ctrl-kinds`, `page-starts-on`이 실제 문서 모델의 의미를
  직렬화하도록 맞췄고, 50개 query command의 help/JSON/volume-probe 계약을 integration test로 고정했다.
- focused contract 4건, 전체 release-test 7,995 passed (38 skipped), `clippy -D warnings`, suite/unit-test
  정책 검사와 최신 CI의 Build & Test, Lint, Rust CodeQL, adapter inter-diff, proptest가 성공했다.
- 연결 이슈 [#5671](https://github.com/edwardkim/rhwp/issues/5671)은 merge로 종료됐으며, 이슈와 기여자 PR에
  해결 범위와 검증 근거를 안내했다. 상세 검토는 [PR #5672 기록](../pr/archives/pr_5672_review.md)과
  [보정 기록](../pr/archives/pr_5672_review_impl.md)에 남긴다.

## #5674 kevin9327 조회 CLI 54개 누적 통합

- 통합 PR은 [#5674](https://github.com/edwardkim/rhwp/pull/5674)이며, 최신 `upstream/devel@c2a36398d` 위에 [#5659](https://github.com/edwardkim/rhwp/pull/5659), [#5662](https://github.com/edwardkim/rhwp/pull/5662), [#5663](https://github.com/edwardkim/rhwp/pull/5663), [#5664](https://github.com/edwardkim/rhwp/pull/5664), [#5668](https://github.com/edwardkim/rhwp/pull/5668)을 `-x` 계보로 누적했다.
- 단건 query CLI 4개와 `rhwp-q-kit` 50개 하위 query를 통합한다. 각 원 PR의 범위와 결론은 [#5659](../pr/archives/pr_5659_review.md), [#5662](../pr/archives/pr_5662_review.md), [#5663](../pr/archives/pr_5663_review.md), [#5664](../pr/archives/pr_5664_review.md), [#5668](../pr/archives/pr_5668_review.md)에 기록했다.
- q-kit의 하위 명령 `--help`가 handler 오류로 종료 코드 2를 내던 결함은 메인터너 보정으로 해결했다. 전역 목록 기반 50/50 help 실행과 focused contract 6/6이 통과했다.
- 로컬 manifest·unit tier·fmt·clippy와 전체 release-test nextest 7,978/7,978건(38 skipped)이 통과했다. renderer·layout 변경이 없어 시각 검증은 적용하지 않는다.
- 이 오늘할일과 [#5674 self-review](../pr/archives/pr_5674_review.md), 구현 기록을 trailing docs commit으로 push한다. 최신 head의 CI와 mergeability가 성공하면 병합하고, 원 PR·issue comment/close 및 review branch/worktree/전용 target 정리를 `post_merge.md` 순서로 수행한다.

## #5685 lpaiu-cs Studio 변경 3건 통합

- 통합 PR은 [#5685](https://github.com/edwardkim/rhwp/pull/5685)이며, 최신 `upstream/devel` 위에
  [#5675](https://github.com/edwardkim/rhwp/pull/5675), [#5676](https://github.com/edwardkim/rhwp/pull/5676),
  [#5684](https://github.com/edwardkim/rhwp/pull/5684)을 오래된 번호순으로 충돌 없이 체리픽했다.
- 도형의 회전·대칭은 양식 모드 차단을 거치는 command 역연산으로, 그림·OLE는 저장 바이트 보존을 위해
  snapshot으로 유지한다. 선택 범위의 실재성은 `CursorState.selectRange`가 소유하고, CI unit WASM stub은
  생성 선언과 같은 `exportHwp`·`exportHwpx` 메서드를 명시한다.
- `cargo fmt --all`, format check, `git diff --check`, Studio TypeScript, Studio 전체 test
  `1,019 passed, 1 skipped`, 로컬 headless Chrome의 그림·표 undo/redo E2E를 통과했다. 원격 VM Chrome
  CDP 주소의 도달 불가는 로컬 headless 재검증으로 환경 문제로 분리했다.
- code candidate CI의 Build & Test, Lint, Frontend package gate, Native Skia, Canvas visual diff,
  CodeQL, archive·regular/slow shard, Proptest, Adapter inter-diff가 모두 통과했다.
- 이 오늘할일과 [#5685 통합 검토 기록](../pr/archives/pr_5685_review.md),
  [구현 기록](../pr/archives/pr_5685_review_impl.md)을 하나의 trailing commit으로 push한다. 최신 문서
  head의 fast-pass CI가 성공하면 작업지시자 승인 뒤 병합하며, 원 PR·관련 issue·통합 branch는
  `post_merge.md` 순서로 정리한다.
## #5682 파생 suite 준비의 Cargo 파일 격리

- [#5683](https://github.com/edwardkim/rhwp/pull/5683)은 review/CI용 suite 준비와 루트 Cargo registry 동기화를 분리해 일반 `--prepare`가 `Cargo.toml`·`Cargo.lock`을 변경하지 않도록 한다.
- manifest·Cargo 격리·`--locked` 계약 테스트 22건, `--prepare`/`--check`, prepare 전후 두 Cargo 파일 SHA-256 비교, fmt·diff 검증을 완료했다.
- self-review 기록은 [pr_5683_review.md](../pr/archives/pr_5683_review.md)에 남겼다. 최신 trailing head의 CI와 mergeability가 통과하고 작업지시자 승인 후 merge한다.

## #5692 마스터 페이지 그림의 본문 입력 가로채기 보정

- [#5693](https://github.com/edwardkim/rhwp/pull/5693)은 `plane: 1` master-page 장식의 직렬화된
  `inFrontOfText` 값이 본문 클릭을 그림 선택으로 전환하던 결함을 보정한다.
- 물리 37쪽에서 재현한 전체 배경 이미지 hit-test를 입력 경로에서 제외하고, 일반 본문 foreground 및
  header/footer 그림 선택은 유지하는 회귀 테스트를 추가했다.
- Studio TypeScript, 정책 test 2건, Studio 전체 test 1,021 passed/1 skipped, fmt·diff check를 완료했다.
  출력 렌더링 변경은 없으므로 PDF/SVG visual sweep은 적용하지 않는다.
- 상세 판정은 [PR #5693 self-review](../pr/archives/pr_5693_review.md)에 남겼다. 최신 trailing head의
  CI가 통과하고 작업지시자 승인을 받은 뒤 merge하며, #5692와 branch 정리는 `post_merge.md`를 따른다.

## #5733 planet6897 #5709·#5710·#5718 통합 검토

- 최신 `upstream/devel` 기준 검토 브랜치 `review/planet6897-20260820`에 #5709, #5710, #5718을
  번호순으로 충돌 없이 체리픽했다. 개별 검토는 [#5709](../pr/archives/pr_5709_review.md),
  [#5710](../pr/archives/pr_5710_review.md), [#5718](../pr/archives/pr_5718_review.md)에,
  통합 적용 기록은 [implementation 기록](../pr/archives/pr_5709_5710_5718_review_impl.md)에 남긴다.
- 로컬 manifest prepare/check, fmt, diff check, 세 focused test와 전체 release-test nextest를
  완료했다. 전체 결과는 8,001 passed, 38 skipped이다. 추가 메인터너 보정은 없었다.
- 통합 PR [#5733](https://github.com/edwardkim/rhwp/pull/5733)의 code candidate CI에서
  adapter inter-diff, Full CI, CodeQL, Proptest, Render Diff, Native Skia, regular/slow shard가
  모두 성공했고 Rust-only 변경에 따라 frontend gates는 skip됐다.
- 이 항목은 #5733의 trailing docs commit으로 반영한다. 문서 head의 fast-pass CI와 최신
  mergeability를 확인한 뒤 병합하고, #5709·#5710·#5718 및 관련 issue의 수용 결과 코멘트,
  close, 원격·로컬 review branch와 검토 전용 산출물 정리는 `post_merge.md` 절차에 따라 진행한다.

## #5736 planet6897 규제영향분석서 표 body_bottom 클램프 검토

- [#5736](https://github.com/edwardkim/rhwp/pull/5736)은 `TopAndBottom` 순수 흐름 표가
  `body_bottom` 상향 클램프로 직전 페인트 줄을 침범하는 #5699 H3-J3 결함을 3중 형상 가드와
  `37787_regulatory_impact.hwp` 회귀 테스트로 보정한다.
- 최신 `upstream/devel@b32113be` 위 체리픽 검토에서 manifest prepare/check, fmt, diff check,
  신규·관련 focused test와 전체 release-test nextest **8,009 passed, 39 skipped**를 완료했다.
  원 PR head의 CI, CodeQL, Native Skia, Canvas Render Diff, Proptest, Adapter inter-diff도 성공했다.
- [#5736 검토 기록](../pr/archives/pr_5736_review.md)을 원 source head에 trailing docs-only commit으로
  push한다. 새 head의 required aggregate와 mergeability가 통과하면 merge하고, #5699의 실제 상태를
  확인해 merge SHA·검증 결과 comment 및 필요 시 수동 close를 처리한다.
- merge 뒤 최종 `upstream/devel` sync, review branch·원격 source branch·검토 전용 산출물 정리는
  `mydocs/manual/pr_review/post_merge.md` 순서로 완료한다.
## #5689·#5691·#5695·#5707 외부 기여 PR 누적 검토 완료

- 기존 VS Code 작업공간의 `review/open-prs-20260820`은 최신 `upstream/devel` 위에서 [#5689](https://github.com/edwardkim/rhwp/pull/5689), [#5691](https://github.com/edwardkim/rhwp/pull/5691), [#5695](https://github.com/edwardkim/rhwp/pull/5695), [#5707](https://github.com/edwardkim/rhwp/pull/5707)을 번호순 `-x` 체리픽했다. 원 작성자와 원 SHA는 각 커밋 메시지에 보존된다.
- focused Rust contract `4/4`, Studio block-selection contract `10/10`, WASM build와 headless Track 4 프로브, skill router의 반복 gate·route·catalog·probe 검증을 완료했다. 원 source head의 완료된 CI도 모두 성공이다.
- 메인터너 보정으로 #5707의 증적 텍스트 줄 끝을 정규화하고, skill gate가 stale local Cargo binary 대신 명시한 검토 후보만 검사하게 했다. #5695의 0ms timer-resolution 경계는 `NaN` 실패 대신 측정 해상도 이하 PASS로 기록하도록 보정했다.
- 개별 판정은 [#5689](../pr/archives/pr_5689_review.md), [#5691](../pr/archives/pr_5691_review.md), [#5695](../pr/archives/pr_5695_review.md), [#5707](../pr/archives/pr_5707_review.md)에, 누적 적용은 [구현 기록](../pr/archives/pr_5689_5691_5695_5707_review_impl.md)에 남긴다.
- 다음 단계는 이 검토 기록을 포함한 통합 후보 PR의 CI다. 성공하면 #5688·#5690·#5706과 원 PR을 `post_merge.md` 순서로 수용 코멘트·close하며, #3315·#5694는 성능 후속 이슈로 유지한다.
