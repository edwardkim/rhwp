# 2026-08-22 오늘할일

## #4962 W4 조판 위험 순위

- [x] 최신 `upstream/devel@0f9ceeb19`을 `task_m100_4962`에 정상 병합하고 W3 Node 계약 47건,
  #4962·장평·저장 frame·LineSeg focused Rust 14건을 통과했다.
- [x] private 32건 ingress가 32/32 성공했고 기존 Stage 4-B와 failure·count·category·join·usage row 및
  semantic body hash가 exact임을 확인했다. 새 10k 전수 계측은 필요하지 않다.
- [x] W3 decision aggregate와 POC 장평·자간·context·stored LineSeg의 compatibility를 감사하고
  [W4 수행계획](../plans/task_m100_4962_w4.md)을 작성했다.
- [x] Stage W4-1에서 W3 동결 입력·historical 정의·candidate identity·risk 식·privacy 계약과 schema를
  고정하고, 공개 fixture 기반 ranker 요구사항이 미구현 모듈로 RED임을 확인했다.
- [x] Stage W4-2에서 결정론적 streaming ranker를 구현하고 보존된 W3 r2 단일 입력을 두 번 실행했다.
  위험 문자 2,061,732자의 category·format·LineSeg lane·후보 분모와 privacy가 exact이고 두 출력 bytes와
  hash도 일치했다.
- [x] Stage W4-3에서 근거 flag와 민감도 변형을 결합해 351개 face의 안정 band를 판정했다.
- [x] Stage W4-4에서 A+B 17개 W5 action queue와 후보별 다섯 질문을 공개 projection으로 고정했다.
- [x] 메인테이너가 최종 보고와 W5 action queue를 승인해 W4 종료 게이트를 통과했다.
- [x] #4962 대형 PR 제출 범위와 최신 `upstream/devel@0f9ceeb19` merge simulation을 검증했다.
- [x] detached review worktree에서 integration manifest, Node·Rust focused, release build·lib, 전체
  nextest 8,129건, Native Skia 3종, clippy, doc test, Markdown 링크와 표준 Docker WASM을 통과했다.
- [ ] 원격 작업 branch push와 PR 생성은 각각 별도 승인을 받는다.

## 진행 중

- [ ] #5845, #5849, #5850 통합 검토 PR의 trailing docs-only fast-pass 확인 후 자동 승인·admin merge
  - 원 PR별 archive 검토 기록과 Hancom 2020 PDF/visual sweep 증적을 같은 통합 PR에 포함했다.
  - code candidate는 Full CI, CodeQL, Canvas visual diff, Native Skia, archive shard를 포함해 모두 성공했다.
  - 이 커밋으로 실제 CI 시간과 각 PR의 수용 판정을 기록한다.

## merge 후

- [ ] 실제 merge SHA의 `upstream/devel` 포함을 확인한다.
- [ ] #5843, #5715, #5798, #5821, #5822, #5828, #5847, #5723, #5729와 원 PR #5845, #5849, #5850에
  검증·시각 증적·수용 사유 comment를 남기고 close한다.
- [ ] 통합 branch와 이 검토를 위해 만든 baseline worktree 및 전용 target을 정리한다.
