# 오늘 할일 - 2026년 4월 29일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #435 | exam_kor.hwp 24페이지 → 20페이지 정합 (한컴 PDF 호환) | **부분 완료 (24→22)** | `typeset.rs::compute_body_wide_top_reserve_for_para` 의 `VertRelTo::Paper` 분기 정정 (Paper-rel `vertical_offset` 을 body-rel 변환 안 하던 버그). 효과: page 1 단 1 +54.5px overflow / page 14 단 1 +106.2px overflow 해소 → pi=0.30, pi=1.25 split 차단 → page 2, 15 orphan 페이지 사라짐. 회귀: exam_eng 8 / k-water-rfp 28 / hwpspec 177 / synam-001 35 페이지 유지, cargo test 1062 passed. **잔여 22→20 미달성**: 섹션 1 페이지 14 (Square wrap 표 col 0 over-fill + col 1 under-use), 섹션 1 페이지 15 (단정의 2단인데 단 1 누락), 섹션 2 페이지 18 (pi=11 split orphan-like) 의 3 가지 별도 메커니즘. Stage 3 조사로 원래 가설 (누적 -100~-300px) 은 메트릭 차이일 뿐임 확인. 잔여 작업은 별도 task 로 분리 (옵션 A 종료) |
| #439 | Square wrap 표 직후 col 0 over-fill (exam_kor 페이지 14 단 0 1225>1211) | **완료 (22→20)** | `typeset.rs::place_table_with_text` 에서 Square wrap (어울림) 표일 때 `current_height` 누적을 `pre_height + table_total` → `max(pre_height, v_off + table_total)` 로 변경. 호스트 텍스트와 어울림 표가 같은 수직 영역을 공유하므로 더 큰 쪽만 한 번 누적. **Stage 1 부수 발견**: 활성 페이지네이션 엔진은 `engine.rs::Paginator` 가 아닌 `typeset.rs::TypesetEngine` (`engine.rs` 는 `RHWP_USE_PAGINATOR=1` 환경변수 fallback). 이슈 가설은 fallback 경로를 지목. 효과: 페이지 14 col 0 used 1225.8→1036.1 px, col 1 items 2→18, exam_kor 22→20 페이지 (목표 21 달성, 추가 -1 단축). 회귀 검증: 149개 sample HWP 중 exam_kor 만 변화, 148개 동일. cargo test 1066 passed. **#435 잔여 작업 (섹션 1 페이지 14 over-fill) 부분 해소** |
| #393 | 다단 col 1 시작 reserve 과대 산정 (exam_kor 페이지 1 pi=30 split) | **#435 Stage 2 로 수정 적용** | `compute_body_wide_top_reserve_for_para` Paper-rel 분기 정정. page 14 (pi=1.25 split) 동일 메커니즘 회귀 케이스 함께 해소. close 가능 (작업지시자 결정) |
| #445 | exam_kor 페이지 가운데 세로선 + 페이지 번호 박스 시각 결함 | **완료** | 두 결함 동시 해결. (1) Paragraph border (`border_fill_id`) 가 PartialParagraph 에서 col_bottom 을 초과해 페이지 바깥(p8: y=1671 / 페이지 1587 초과)까지 그려지던 버그 — `layout.rs build_single_column` merge 그룹을 col_area 바닥/꼭대기로 클램프 (Stage 1+2). (2) 꼬리말 paragraph 의 `wrap=TopAndBottom + vert=Para` 표(페이지 번호 박스)가 paragraph top 에 정렬되어 column line 끝과 박스 top 이 만나 "붙어 보이는" 문제 — `layout_header_footer_paragraphs` 에서 표 anchor y 에 `line_height/2` 를 더해 PDF 일치 (Stage 3). 검증: exam_kor p1~20 모든 페이지에서 박스 top y=1439.47 (PDF 380.6mm 일치), column line - 박스 갭 16.3~17.0px (PDF 16px 일치). cargo test 1117 passed. snapshot 1건 갱신 (`tests/golden_svg/issue-267/ktx-toc-page.svg` invisible 구조 rect height 5.34px 변경, 가시 변화 없음) |

## 버그

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #430 | exam_kor 헤더 "국어 영역(A 형)" — 그림 crop 미적용으로 원본 전체 강제 스트레치 | 완료 | `ImageNode.original_size_hu` 신규 필드 + `picture_footnote.rs` 2곳·`layout.rs` 1곳에서 `pic.shape_attr.original_{width,height}` 채움. svg.rs 의 crop 스케일 공식 `cr/img_w` → `original_size_hu/img_size_px` 로 교정 (헬퍼 `compute_image_crop_src` 추출). web_canvas.rs 동일 적용 (단일 진실 원천). 단위 테스트 4건 추가, 1027 unit + 7 integration 통과. 페이지 1 헤더 viewBox=`0 0 1364.88 354` → "국어 영역" 만 표시(PDF 일치) |
| #452 | 단락 마지막 줄 trailing line_spacing 누락 → 다음 단락 첫 줄이 ls 만큼 위로 당겨짐 (exam_kor pi=1↔pi=2) | **완료** | `paragraph_layout.rs:2511-2520` 의 `is_para_last_line` 분기 제거 (셀 내 `is_cell_last_line` 만 trailing 제외 보존). 본문 단락 모든 줄에서 `y += lh + ls` 통일 → `pagination/engine.rs` 의 `current_height += para_height` 누적과 정합. 효과: exam_kor pi=1.line9↔pi=2.line0 step 15.34→24.50 px (단락내 step 과 동일, PDF 정합). cargo test --lib 1066 passed. 10종 샘플 페이지 수 변동 0 (exam_kor 20 / aift 77 / biz_plan 6 / 2022국립국어원 40 / exam_eng 8 / k-water-rfp 28 / kps-ai 80 / synam-001 35 / 21_언어 15). Task #332 회귀 점검: 21_언어 p1 col 1 pi=26+보기①②③ fit 유지. golden SVG 2건 (issue-147, issue-157) baseline 재갱신 (Task #332 stage 5 갱신본 → 본 정합 재갱신, PNG 시각 검토 통과). 부수: 페이지 마지막 단락 trailing ls(~10.9 px) 가 col_bottom 살짝 넘는 cosmetic 오버플로 1건 (빈 공간, 시각 무영향) |

## PR 처리

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| PR #434 | 그림 자동 크롭 (FitToSize+crop) 공식 교정 + 테두리 문단 inner padding (#430, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. PR 의 5 commits 모두 cherry-pick — Task #430 4 commits + 별도 fix (테두리 inner padding) 1 commit. 1062→1066 passed (+4 신규 단위 테스트) + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,182,395 bytes. 광범위 byte 비교 309 페이지 중 40 페이지 변화 (의도된 정정). **검토 중 한컴 PDF 환경 의존성 II 발견** — 한컴 2010 / 2020 / 한컴독스 모두 다르게 조판. 작업지시자 코멘트: "이 시험지를 조판한 사람의 의도에 맞다는 생각이 들 정도". 옵션 3 (3종 PDF 비교 후 머지). SVG + Canvas 양 경로 작업지시자 시각 판정 통과 |
| 위키 보강 | 한컴 PDF 환경 의존성 위키 페이지에 "발견 정황 II (PR #434 / 이슈 #430)" 섹션 추가 | 완료 | 한컴 2010 ↔ 2020 ↔ 한컴독스 차이 정황 (단일 한컴 정답지 가정의 한계 재확인). rhwp 의 현재 출력이 시험지 조판자 의도에 더 부합 가능성. wiki commit `302adbf` push 완료 |
| samples 추가 | 한컴 3종 정답지 PDF (exam_kor 비교용) | 완료 | samples/2010-exam_kor.pdf (4.57 MB) + samples/2020-exam_kor.pdf (4.57 MB) + samples/hancomdocs-exam_kor.pdf (6.05 MB). 모든 컨트리뷰터와 fork 한 사람들이 환경별 차이 시각 비교 가능. devel commit `2714211` |
| README 보강 | Wiki 자료 섹션 추가 + 한컴 PDF 환경 의존성 안내 | 완료 | README.md / README_EN.md 의 Contributing 섹션에 "한컴 PDF 는 정답지가 아닙니다" 항목 추가 + 신규 "위키 자료 (Wiki Resources)" 서브섹션 (위키 9개 페이지 링크). devel commit `a45c78b` |
| v0.7.8 릴리즈 | devel → main 머지 + 태그 + GitHub Release | 완료 | PR #436 (devel → main, admin merge). 태그 v0.7.8 push. GitHub Release 생성 → npm-publish.yml 자동 트리거 (npm @rhwp/core + @rhwp/editor + VS Code Marketplace + Open VSX). CHANGELOG.md (한글) + CHANGELOG_EN.md 신규 (영문, 전체 이전 버전 포함). main commit `42cf91b` |
| Discussion #438 | 한컴의 기술부채와 운영의 기술 — v0.7.8 사이클 회고 (General) | 완료 | 메인테이너 인사이트 시리즈 6번째 (#183 → #184 → #188 → #328 → #329 → #438). 5 층위 정리 (정답지 분열 / 자동 처리 자의성 / case-by-case / 자기 라운드트립 ≠ 한컴 호환 / 영역 long tail) + 5 운영 전략. 신규 메모리 `feedback_small_batch_release_strategy` 등록 |
| PR #437 | 브라우저 확장 문서 URL 해석 및 응답 검증 (#432, @postmelee) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. 1차 검토 (옵션 C): PR 본문 명시 자동 테스트 명령이 rhwp-shared/package.json 부재로 ESM 충돌 → 실행 불가 정정 요청. **작성자 17분 만에 권장안 정확 채택** — `rhwp-shared/package.json` 신규 추가 (`"type": "module"`). 6 commits cherry-pick (5 본질 + 1 후속 정정). 1066 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + Rust core 영향 0 (byte 동일). **자동 테스트 38 건 통과**: document-url-resolver 12 + download-interceptor-common 26 (보너스 — ESM 선언으로 다른 테스트도 정상화). 본질: provider adapter 구조 일반화 (특정 wiki rule-based 회피) + 두 계층 분리 (URL 해석 + 응답 검증). Rust core 영향 0 패턴 (시각 판정 불필요) |
| PR #442 | exam_kor.hwp 24→22 페이지 정합 (#435, #393, @planet6897) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터. typeset.rs::compute_body_wide_top_reserve_for_para 의 VertRelTo::Paper 분기에서 paper-rel 좌표를 body-rel 변환 없이 reserve 누적하던 버그 정정 (32 lines). 4 commits cherry-pick (merge commit 1 제외). 1066 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,174,855 bytes. 광범위 byte 비교: 309 중 24 차이 (exam_kor 한정, 다른 9 샘플 회귀 0). col 1 reserve 306.1→94.5px (HWP 실측 ±0.2). 페이지 24→22, page 2/15 orphan 제거. 작업지시자 Canvas (WASM) 시각 판정 통과. 잔여 22→20 정합 작업 #439/#440/#441 별도 이슈 분리 — 메인테이너 작은 단위 회전 정책 정확 인지 |
| PR #443 | Square wrap 표 직후 col 0 over-fill 정정 (#439, @planet6897, exam_kor 22→20) | 완료 (cherry-pick 머지) | 신뢰 컨트리뷰터, 본 사이클 9번째 PR (가장 활발). PR #442 머지 직후 잔여 작업 즉시 처리. typeset.rs::place_table_with_text Square wrap 표 누적 정책 `pre_height + table_total` → `max(pre_height, v_off + table_total)` (32 lines). 어울림 = 본문이 표 옆을 흐름 (같은 수직 영역 공유). PR 의 11 commits 중 Task #435 (4) PR #442 흡수 + merge (3) 제외 → Task #439 본질 4 commits 분리 cherry-pick. 1066 passed + svg_snapshot 6/6 + issue_418 1/1 + clippy 0 + WASM 4,178,523 bytes. 광범위 byte 비교 307 중 9 차이 (exam_kor 한정). 페이지 14 col 0 1225.8→1036.1px (-189.7), col 1 64.3→1016.9px (+952.6) 균형 분배. 페이지 14+15 통합. 부수 발견: engine.rs::Paginator 가 활성 엔진 아님 (RHWP_USE_PAGINATOR=1 만) → typeset.rs 정정. 작업지시자 SVG + Canvas 양 경로 통과 (코멘트: "점점 개선이 되어가고 있는게 보입니다. 좋은 공략 전략") |

## M100 — 브라우저 확장 URL 해석/검증

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #432 | 실제 HWP/HWPX 응답 검증 및 파일 URL 해석 계층 도입 | 완료 | 수동 검증 통과, 완료: 11:57 |
