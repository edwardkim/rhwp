# 오늘 할일 - 2026년 5월 6일

## M100 — aift.hwp 18페이지 1줄 다음 페이지로 밀림 (Task #631)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #631** | aift.hwp 18페이지 하단 pi=222 1줄이 다음 페이지로 밀림 — 페이지 누적 +15.4px 초과 | **완료** | aift.hwp page 18 의 pi=222 단락이 PDF 는 첫 2줄을 page 18 에 담지만 우리 렌더러는 1줄만 담고 line 1~3 을 page 19 로 밀던 회귀. **원인**: `typeset.rs::typeset_paragraph` 줄 단위 분할 루프에서 `LAYOUT_DRIFT_SAFETY_PX = 10` 이 `base_available` (line 1046) 과 `avail_for_lines` (line 1074) 두 곳에서 차감되어 합계 20px 보수 마진. `pi=222` 에서 잔여 59.2px → 마진 적용 후 39.2px → 2줄 합 41.6px > 39.2 → line 1 탈락. **수정**: HWP 권위값 더블체크. 다음 줄(`li+1`) 의 `vpos==0` (페이지 경계 신호) AND 현재 줄(`li`) 의 `vpos+lh ≤ body_available_height` 이면 typeset 누적 마진을 우회하여 줄 포함. **검증**: aift page 18 = 2줄 (PDF 일치), cargo test --lib 1125 passed, 전체 155 샘플 회귀 0건. **부가 효과**: hwpctl_API_v2.4 (+460 text), hwpspec (+855 text) 등 6개 샘플 1300+ 누락 콘텐츠 복구. Task #332 stage4b 시점부터 알려진 회귀 (aift pi=222) 의 후속(Stage 5) 해결. 산출물: `mydocs/plans/task_m100_631.md` + `task_m100_631_impl.md` + `mydocs/working/task_m100_631_stage{1,2,3}.md` + `mydocs/report/task_m100_631_report.md`. |

## 메모리 정합

- Task #332 stage4b 의 LAYOUT_DRIFT_SAFETY_PX 이중 차감(20px) 보수 마진은 layout drift 흡수 목적이지만 HWP 가 명시적으로 페이지 경계를 인코딩한 케이스에서는 콘텐츠 손실 유발
- 본 task 의 권위값 더블체크는 vpos-reset 인접 줄에 한정되어 회귀 위험 최소
