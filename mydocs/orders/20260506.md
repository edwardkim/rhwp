# 오늘 할일 - 2026년 5월 6일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **#630** | aift.hwp 4페이지 목차 `(페이지 표기)` 위치 오류 — `·` 포함 라인 8.67px 좌측 이탈 정정 | **PR #636 OPEN (planet6897:pr-task630 → edwardkim:devel)** | 본질 결함: `is_halfwidth_punct` 가 U+00B7 강제 반각 처리. 한컴 저장본의 tab_extended 가 전각 측정 기반. 8.67px = 한 개의 `·` 전각/반각 차이. **5 단계 TDD 흐름** — Stage 1 베이스라인 (164 fixture / 1614 페이지 / 1134 cargo test / `·` 포함 6 라인 8.67px 이탈 정량) + Stage 2 단위 테스트 RED + Stage 3 정정 1 (`·` 측정 통일, spread 9.08 → 1.13 px) + **Stage 4 정정 2 (RIGHT 정확 매치) 시도 → 113px 회귀 발견 → 철회 (HWP5 ext[0] 가 이미 right-tab 결과 위치로 저장됨 발견, 코드 코멘트 240-243 의 진단 정확)** + Stage 5 광범위 sweep + 골든 갱신. 단일 본질 정정 (text_measurement.rs:859-862 1 라인 제거 + 코멘트). 검증: cargo test --lib --release **1135 passed** (1134 + 1) / 통합 테스트 16 파일 **87 passed** / svg_snapshot **6 passed** (issue_147 갱신, issue_267 KTX byte-identical) / 164 fixture 페이지 회귀 0 / Task #546/#554 회귀 가드 통과. **권위 케이스**: aift p4 `·` 포함 6 라인 모두 정확히 +8.67px 우측 이동 (1-1 592.36→601.03 등), `·` 미포함 라인 영향 없음 (케이스별 명시 정합). **잔여 영역 (별도 task)**: PDF 비교 시 모든 라인 PDF 대비 1.05 mm 좌측 — Stage 1 베이스라인부터 동일, in-run RIGHT 탭 본문 우측 끝 클램프 누락 본질. **Issue #635 등록**. 처리 보고서: `mydocs/report/task_m100_630_report.md`. |
| **#635** | aift p4 right-tab 1.05 mm 잔여 마진 — TabDef.position in-run RIGHT 처리에서 본문 우측 끝 클램프 누락 | **OPEN (M100, 후속 task — Task #630 잔여)** | Task #630 정정 후에도 PDF (한컴 2022 출력) 와 정밀 비교 시 모든 (페이지 표기) 라인이 1.05 mm 좌측 (우측 마진: PDF 0.11 mm vs SVG 1.16 mm). Task #630 베이스라인부터 동일 결함. 근본 원인: `compute_char_positions` 의 in-run RIGHT 탭 (text_measurement.rs:386~) 이 본문 우측 끝까지 클램프 누락. cross-run pending tab handler (paragraph_layout.rs:1402) 는 leader 시 클램프 로직 보유하지만 in-run 미적용. 영향 범위 광범위 (목차/표 우측 정렬/dot leader). 검증 실험 (`/2` 제거) 으로 `style_resolver.rs:643` 가 본질 아님 확인. 별도 task 5+ 단계 + 광범위 sweep + 시각 판정 필수. |

## 작업 메모

### Task #630 본질
- 본질 결함은 **`·` 측정 불일치 단독** (`is_halfwidth_punct` 의 U+00B7 강제 반각). 당초 가설 (원인 B: tab_type fallback) 은 Stage 4 검증에서 기각.
- **HWP5 의 `tab_extended[0]` 인코딩 의도 발견** — 이미 right-tab 결과 위치 (= 우측 끝 - 한컴_seg_w) 로 저장됨. 따라서 LEFT fallback 으로 처리하는 것이 한컴 인코딩 의도와 정합. RIGHT 정확 매치 시 seg_w 이중 차감 → ≈seg_w (≈112px) 좌측 이탈 회귀.
- `text_measurement.rs:240-243` 의 코드 코멘트 ("기존 golden SVG 가 LEFT fallback 의존") 가 정확한 진단이었음. **의심하기 전에 검증 우선** — 메모리 학습 가치.
- 합성 데이터 단위 테스트 (`test_630_native_inline_tab_right_align`) 가 GREEN 이지만 통합 테스트에서 회귀 발견 패턴 — TDD 의 통합 테스트 분리 가치 (`feedback_essential_fix_regression_risk` 정합).
- PR #636 변경 통계: `text_measurement.rs` 1 라인 제거 + 코멘트 갱신 + 단위 테스트 1개 / `tests/issue_630.rs` 신규 / `tests/golden_svg/issue-147/aift-page3.svg` 갱신 (+48 bytes) / 문서 7 개.

### Issue #635 본질
- Task #630 정정 후 잔여 1.05 mm = 4 px 마진. 본문 우측 끝 (170 mm) vs TabDef.pos `/ 2.0 = 168.75 mm` 의 1.25 mm 차이.
- 검증 실험 결과: `style_resolver.rs:643` 의 `/ 2.0` 제거해도 paren_x 변화 없음 → **`/ 2.0` 자체는 본질 아님**. 다른 코드 경로 (in-run RIGHT 탭 처리) 가 본문 우측 끝까지 클램프하지 않는 것이 본질.
- 정정 영역 후보: `compute_char_positions` 의 has_custom_tabs 분기 RIGHT 탭에 leader (fill_type ≠ 0) 인 경우 본문 우측 끝 클램프 추가. 또는 `find_next_tab_stop` 의 type=1 분기에 leader 클램프 통합.

## 인프라 / 정합 작업

- 없음 (본 task 외)
