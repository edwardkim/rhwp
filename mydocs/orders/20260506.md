# 오늘 할일 - 2026년 5월 6일

## M100 — aift.hwp 페이지 2, 3 (큰 표만 있는 cover-style) 쪽번호 미표시 메커니즘 분석 (Task #637)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #637** | Task #634 종료 후 분리된 후속 issue. samples/aift.pdf 페이지 2 (사업계획서 표지, 35×27 표) 와 페이지 3 (요약문, 14×17 표) 은 한컴 PDF 미표시이지만 PageHide page_num=true 컨트롤이 없음. rhwp 는 PageNumberPos 등록 후 정상 표시 → 차이 발생. 5가지 가설 후보 (cover-style 휴리스틱 / 셀 내부 PageHide / paragraph header 비트 / 표 attr 비트 / 한컴 자체 휴리스틱) 데이터 기반 검증. | **완료 (분석 close, fix issue #639 분리)** | **결과**: H1 이 결정적 룰로 확정 — `페이지 items=1 + 완전한 Table (PartialTable 아님) + tac=false → 한컴 미표시`. 174 샘플 전수 조사 결과 aift p2, p3 만 매칭 (전체의 0.06%). H2/H3/H4 최종 기각 (페이지 2 host = 페이지 6 host raw paragraph header byte-for-byte 동일, 페이지 6 표시 표 attr = 페이지 2 미표시 표 attr 동일). 회귀 위험 매우 낮음 (영향 페이지 2건, 모두 정합성 개선). **시나리오 (a) 채택**: 본 issue close-as-analysis-complete (gh issue close 637), fix issue **#639** 등록 (한컴 호환: cover-style 페이지 자동 쪽번호 미표시). 코드 변경 0 (examples/inspect_637.rs 분석 도구 추가만). 보고서: `mydocs/report/task_m100_637_report.md`. **참고**: Task #634 의 orders/working/report 은 별도 브랜치 `task634-pr` 에 있으며 local/devel 미머지 상태. milestone v1.0.0 설정은 메인테이너 권한 (planet6897 권한 부족) — 메인테이너 수동 설정 필요. |
| **M100 #639** | (#637 후속 fix issue) 한컴 호환: cover-style 페이지 (items=1 + 완전한 Table + tac=false) 자동 쪽번호 미표시. Task #637 분석에서 도출된 결정적 룰의 구현 task. 검증: aift p2, p3 미표시 (FAIL→PASS) + 다른 173 샘플 회귀 0. | **완료 (Stage 0~3 모두 PASS, close 대기)** | **본질 발견**: rhwp 페이지네이션은 두 경로 존재 — TypesetEngine (기본 main) + Paginator (RHWP_USE_PAGINATOR=1 fallback). 기본 main path 가 typeset.rs 임을 발견하여 두 경로 (engine.rs + typeset.rs) 모두에 동일 룰 적용. **fix**: finalize_pages 시그니처에 paragraphs 추가 + 신규 헬퍼 is_cover_style_page (단일 단 + items=1 + 완전 Table + tac=false) + page.page_hide.is_none() 가드 후 hide_page_num=true 자동 설정. **검증**: Stage 1 RED 5건 → Stage 2 GREEN 5건, 전체 cargo test 1139 + 76+ 통합 PASS / 0 failed, clippy warning 0, 174 샘플 룰 매칭 정확히 aift p2/p3 (Task #637 결과 일치), aift.hwp 페이지 카운트 77 무변화. 회귀 위험 0 확정. **코드**: src/renderer/typeset.rs +29, src/renderer/pagination/engine.rs +29, integration_tests.rs +99. 보고서: `mydocs/report/task_m100_639_report.md`. |
## v0.7.10 릴리즈 (5/6 자정 직후 — Issue #608/#612 정합)

| 항목 | 상태 | 비고 |
|------|------|------|
| **Issue #612** CLI 바이너리 릴리즈 파이프라인 (4 플랫폼 matrix) | **완료** | `.github/workflows/release-binary.yml` 신규 — Linux x86_64 / macOS x86_64 (cross-compile from macos-14) / macOS aarch64 / Windows x86_64 + SHA256SUMS.txt + softprops/action-gh-release@v2. 메인테이너 결정: native-skia 스킵 / 코드 서명 미포함 / 4 플랫폼만. workflow_dispatch 옵션 C dry-run 정합 후 v0.7.10 태그 트리거. **macos-13 runner 큐 적체 영역 회피** — Apple Silicon (macos-14) 에서 cross-compile 로 Intel binary 산출. v0.7.10 retag 후 4 플랫폼 모두 성공. |
| **v0.7.10 PATCH 릴리즈** | **완료** | `Cargo.toml` 0.7.9 → 0.7.10 + CHANGELOG.md / CHANGELOG_EN.md [0.7.10] entries. devel → main 직접 머지 + `v0.7.10` 태그 + GitHub Release 본문 (한영 동기화) + 4 플랫폼 자산 첨부 (tar.gz/zip + SHA256SUMS.txt). 본 사이클 v0.7.9 (5/1) → v0.7.10 (5/6, 5일 5 PATCH 회전 패턴 정합 — `feedback_small_batch_release_strategy`). |
| **Issue #608** 영문 알림 (@almet) + Issue #612 close | **완료** | 한영 알림 댓글 등록 + Issue close. |

## M100 — v1.0.0 조판 엔진 (PR 처리 — v0.7.10 후 사이클)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #578 Task #576** | 수식 토크나이저 times/sim 키워드 prefix-split 정정 (closes #576) | **완료 (핀셋 cherry-pick 머지 + push, PR #578/Issue #576 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). v0.7.10 후 첫 처리 PR. PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (`6f3d94aa`) 충돌 0 — PR base 시점 차이. **본질 결함**: `src/renderer/equation/tokenizer.rs::read_command` (L104) 의 prefix-split 키워드 list 가 폰트 스타일 모디파이어 3개 (`bold/it/rm`) 만 처리 → `times` / `sim` 등 연산자 키워드가 변수와 인접 시 단일 식별자로 토큰화 (`"a timesm"` → 단일 `"timesm"` italic / `"X simZ"` → 단일 `"simZ"` italic). **본질 정정 (단일 파일 +53 LOC)**: list 확장 `bold/it/rm` → `bold/it/rm/times/sim/TIMES/SIM` (광범위 sweep 158 fixture / 563 unique 수식 scripts 의 발현 4 키워드만 추가, 회귀 위험 0 — `feedback_hancom_compat_specific_over_general` 정합). **회귀 차단 (6 신규 unit tests)**: `test_task576_times_lowercase_prefix_split` / `test_task576_sim_lowercase_prefix_split` / `test_task576_times_uppercase_prefix_split` / `test_task576_sim_uppercase_prefix_split` / `test_task576_alpha_no_split` (alpha/alphabet 분리 안 됨 회귀 차단) / `test_task576_times_followed_by_space` (공백 구분 보존). 정량 측정: `{b} over {a timesm}` 분모 "a timesm" italic → **"a × m"** (× = U+00D7) / `rm X simZ` "X simZ" italic → **"X ∼ Z"** (∼ = U+223C) / `1TIMES10^-14` → **"1 × 10⁻¹⁴"** / `rmA SIMC` → **"A ∼ C"**. 검증: cargo test --lib --release **1140 passed** (회귀 0) / task576 unit tests **6/6 passed** / svg_snapshot 6/6 / clippy 0 / build --release / Docker WASM **4,583,156 bytes** (v0.7.10 baseline +1,691 — tokenizer.rs +53 LOC + 6 unit tests 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. SVG byte 차이: exam_science page 1/2 byte-identical / page 3 differ (15번 본문/보기 `rm W simY/Z`, `rmA SIMC`) / page 4 differ (20번 본문/응답 `{b} over {a timesm}`, `1TIMES10^-14`, `rm X simZ`) — PR 본문 100% 재현. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "웹 시각판정 통과입니다." cherry-pick author Jaeook Ryu 보존 (`fda98d2`). devel merge `72cd9fd` + push 완료. PR #578 close + 한글 댓글 등록 + Issue #576 close (closes #576 자동 처리). **PR #579 영역 (5/4 협업 PR 권유 시도)**: PR #579 (@oksure) 가 self-close → PR #578 단독으로 영역 진행 (협업 PR 권유 본 사이클 학습 영역). 처리 보고서: `mydocs/pr/archives/pr_578_report.md`. 검토 보고서: `mydocs/pr/archives/pr_578_review.md`. **v0.7.10 후 첫 처리 PR — 광범위 sweep + 명시 list 결합 + 6 신규 unit tests + 한글 답변 패턴 정합.** |
| **PR #629 Task #628** | nested cell inline_shape_positions 키 충돌 정정 — 글상자 안 이미지 미렌더링 (closes #628) | **완료 (핀셋 cherry-pick 머지 + push, PR #629/Issue #628 close)** | 외부 컨트리뷰터 @planet6897 (Jaeuk Ryu) / Jaeook Ryu (commit author). v0.7.10 후 두 번째 처리 PR. PR mergeable=UNKNOWN 표시였으나 본질 cherry-pick (`04ce0d22` src 7 파일 +62/-28) 충돌 0 (orders add/add 충돌은 ours 보존). **본질 결함**: `PageRenderTree.inline_shape_positions` 의 키 `(section, para, control)` 에서 `para` 가 두 가지 의미 혼용 — (1) paragraph_layout 호출 시 섹션 단위 paragraph 인덱스 (2) layout_table → 셀 paragraph 호출 시 셀 내부 paragraph 인덱스 (`cp_idx`). 서로 다른 셀 컨텍스트가 동일 키 `(0, 0, 1)` 등을 공유 → 다른 paragraph 의 double-nested 셀 처리가 키를 미리 점유 → 20번 외부 1×1 표 처리 시 `already_rendered_inline=true` 오판 → `table_layout.rs:1900` 분기에서 내부 2×3 표의 `layout_table` 재귀 호출 스킵 → 그 안의 그림 미렌더. **비대칭 발현**: 19번 (단일 nesting 2×3 표 → 셀 → 그림) 정상 / 20번 (이중 nesting 1×1 → 셀 → 2×3 → 셀 → 그림) 만 결함. **본질 정정**: 키에 `cell_path: Vec<(ctrl_idx, cell_idx, cell_para_idx)>` 차원 추가 (`InlineShapeKey` 신규 타입) — 섹션 단위는 빈 Vec, 셀 단위는 `CellContext.path` 전체. `set/get_inline_shape_position` 시그니처에 `cell_ctx: Option<&CellContext>` 추가, 호출처 13곳 일괄 패치 (셀 단위 9 + 섹션 단위 4) + `cursor_rect.rs::532` hit-test 가드 (`cell_path.is_empty()` 체크로 셀 내부 inline shape 의 cursor hit-test 별도 처리 영역 보존). **HWP IR 표준 직접 사용** (`CellContext.path` nesting 경로, `feedback_rule_not_heuristic` 정합) + **회귀 위험 영역 좁힘** (키 namespace 분리만 수행, 값/계산 로직 무변경, `feedback_hancom_compat_specific_over_general` 정합). 검증: cargo test --lib --release **1140 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 / issue_554 12 / clippy 0 / build --release / Docker WASM **4,590,307 bytes** (PR #578 baseline +7,151 — render_tree.rs +43 LOC + InlineShapeKey Vec allocation 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,684 페이지 (BEFORE/AFTER 동일) / 페이지 수 차이 0**. **정량 측정 (PR 본문 100% 재현)**: exam_science page 4 BEFORE 3 → AFTER 4 images (+1 = 20번 실린더 이미지 신규 emit), 추가 image 위치 `x=568.00 y=783.92 w=376.65 h=101.81` (PR 본문 명세 99.7×26.9mm IR **정확 일치**). page 1/2/3 byte-identical (회귀 0). **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "메인테이너 시각 판정 통과입니다." 권위 영역: page 4 20번 글상자 안 (이중 nesting) 실린더 이미지 정상 위치 출력. cherry-pick author Jaeook Ryu 보존 (`c353cfc`, committer edward). devel merge `08a5104` + push 완료. PR #629 close + 한글 댓글 등록 + Issue #628 수동 close (closes #628 키워드는 cherry-pick merge 로 자동 처리 안 됨, 안내 댓글 등록). 처리 보고서: `mydocs/pr/archives/pr_629_report.md`. 검토 보고서: `mydocs/pr/archives/pr_629_review.md`. **v0.7.10 후 두 번째 처리 PR — 근본 원인 정확 식별 (비대칭 발현의 origin) + 키 namespace 분리 (값/계산 로직 무변경) + cursor 가드 + HWP IR 표준 직접 사용 패턴 모두 정합.** |
| **PR #611 Task #458** | rhwp-studio 표/이미지 리사이즈 Undo 미동작 정정 (closes #458) | **완료 (핀셋 cherry-pick 머지 + push, PR #611/Issue #458 close)** | **첫 PR 컨트리뷰터 @kihyunnn (김기현, mable0927@gmail.com)** — v0.7.10 후 세 번째 처리 PR + **본 사이클 첫 web editor (rhwp-studio TypeScript) 본질 정정 PR**. PR mergeable=MERGEABLE 표시지만 PR base 283 commits 뒤 (4/29 등록 후 본 사이클 누적), 본질 cherry-pick (`a4944d2b` rhwp-studio TS 5 파일 +128/-19) 충돌 0 (auto-merge picture.ts 깨끗 통과). **본질 결함 (Issue #458 외부 사용자 등록)**: rhwp-studio editor 의 표 리사이즈 + 그림/도형 리사이즈 경로가 `CommandHistory` 우회 (WASM mutation 직접 호출) → Ctrl+Z / Ctrl+Y 미동작. **크롬 확장 사용자분들의 핵심 사용성 영역 — 외부 사용자 권위 영역**. **본질 정정**: (1) `command.ts` 신규 `ResizeObjectCommand` (+54 LOC, before/after snapshot, shape/picture 분기), (2) `input-handler-table.ts` (+30/-12) 표 리사이즈 3 분기 (`finishResizeDrag` / `resizeCellByKeyboard` / `resizeTableProportional`) 모두 `executeOperation kind: 'snapshot'` 패턴으로 통일, (3) `input-handler-picture.ts` (+40/-7) 다중 선택 (`historyTargets` 배열, **LIFO 스택 순서 정합**) + 단일 선택 모두 `ResizeObjectCommand` 로 record + **changed 가드** (`Object.keys(updated).some(...)` 무변경 record 차단, Undo 스택 오염 방지), (4) `input-handler-mouse.ts` + `input-handler.ts` (+2 each) state 에 `origHorzOffset/origVertOffset` 보조 추가. **회귀 위험 영역 좁힘**: 텍스트 undo 경로 무변경 + 표/개체 이동 undo 경로 (`MovePictureCommand`/`MoveShapeCommand`) 보존 + 기존 `executeOperation` API 재사용 (신규 인프라 0). 검증: cargo test --lib --release **1140 passed** (회귀 0) / clippy 0 / `rhwp-studio npm run build` (`tsc && vite build`) TypeScript 타입 체크 통과 + dist 빌드 성공 (`dist/index-cbRqvVRC.js` 689KB) / Docker WASM **4,590,307 bytes** (PR #629 baseline 과 **정확 일치** — Rust 영역 변경 0 정량 입증, 본 사이클 첫 web editor 영역 정정의 정합 입증). **광범위 페이지네이션 회귀 sweep**: 164 fixture / 1,684 페이지 / 차이 0. **메인테이너 시각 판정 ★ 통과 (web 환경 — 3개 권위 샘플)** — 작업지시자 평가: "모두 검증했습니다." 권위 샘플: `samples/calc-cell.hwp` (15,872 bytes, 표 리사이즈 Undo) + `samples/p122.hwp` (89,088 bytes, 단일 이미지 리사이즈 Undo) + `samples/mix-shape-01.hwp` (78,848 bytes, 다중 이미지 리사이즈 Undo LIFO 스택 순서). **3개 권위 샘플 영구 보존** — samples/ 직속 추가 (158 → 161 직속 hwp), 향후 회귀 차단 가드 영역 영구화 (작업지시자 직접 지정). cherry-pick author kihyunnn 보존 (`5cdf8e5`, committer edward). devel merge `40d1da8` + push 완료. PR #611 close + 한글 댓글 등록 ("**크롬 확장 사용자분들이 많이 요청해주셨던 기능을 첫 번째 기여로 구현해주셔서 감사합니다.**" 작업지시자 안내 정합) + Issue #458 수동 close. 처리 보고서: `mydocs/pr/archives/pr_611_report.md`. 검토 보고서: `mydocs/pr/archives/pr_611_review.md`. **첫 PR 컨트리뷰터 + 첫 web editor 본질 정정 + DTP 엔진 (`project_dtp_identity`) web editor 영역 정합 진입 + 외부 사용자 권위 영역 (Issue #458) 정정 + 3개 권위 샘플 영구 보존 패턴 모두 정합.** |

## 작업 메모

### v0.7.10 릴리즈 본질
- 5/1 v0.7.9 릴리즈 후 5일 만의 다음 PATCH (5/6) — 본 사이클 22 PR / 외부 컨트리뷰터 9명의 활발한 사이클을 PATCH 회전 운영으로 정합 (`feedback_small_batch_release_strategy`).
- macos-13 runner 큐 적체 영역 발견 → Apple Silicon (macos-14) cross-compile 로 우회 (Rust 표준 cross-compile, Intel binary 산출물 보존).
- 4 플랫폼 matrix + SHA256SUMS.txt + workflow_dispatch dry-run 옵션 C 가 본 사이클 첫 케이스 — Issue #612 가 v1.0.0 직전 인프라 영역의 본질.
- 코드 서명은 다음 사이클 영역 (Apple Developer ID / Microsoft EV 인증서 비용/계정 영역).

### PR #578 본질
- v0.7.10 후 첫 처리 PR — 본 사이클 (5/6) 의 첫 PR 처리 패턴 정합 영역 (한글 답변 / commit 단위 cherry-pick / 광범위 sweep).
- **PR #579 협업 영역 학습** — 5/4 작업지시자 협업 PR 권유 시도 후 PR #579 (@oksure) self-close, PR #578 단독으로 진행. @planet6897 답신 (5/4 22:23): "issue 처리시에 조금 더 주의해서 진행하겠습니다." (협업 영역 인지). 본 사이클 협업 PR 권유 시도 첫 사례.
- 광범위 sweep + 명시 list 결합 — 158 fixture / 563 scripts 의 정밀한 회귀 영역 측정 + 4 키워드 명시 list (case-specific 가드).
- 6 신규 unit tests — 회귀 차단 가드 (alpha 그리스 문자 prefix 충돌 + 공백 구분 보존).

### PR #629 본질
- 키 namespace 충돌의 근본 원인 정확 식별 + 비대칭 발현 (19번 정상 / 20번 결함) 의 origin (이중 nesting 의 다른 paragraph cp_idx=0 충돌) 정합 진단.
- `CellContext.path` (HWP IR nesting 경로) 직접 사용 — 휴리스틱 미도입, `feedback_rule_not_heuristic` 정합.
- 키 namespace 분리만 수행 (값/계산 로직 무변경) → 회귀 위험 영역 좁힘. 섹션 단위 호출 (`None` 전달) 은 기존 `(sec, para, ctrl, [])` 와 동등.
- `cursor_rect.rs` hit-test 가드 (`cell_path.is_empty()`) 로 cursor 동작 회귀 0 보존.

### PR #611 본질
- **본 사이클 첫 web editor (rhwp-studio TypeScript) 본질 정정 PR** — 이전 PR #561~#600 + #578 + #629 모두 Rust 렌더러 영역. DTP 엔진 (`project_dtp_identity`) 의 web editor 영역 정합 진입.
- **첫 PR 컨트리뷰터 @kihyunnn** — 외부 사용자 권위 영역 (Issue #458, "크롬 확장 사용자분들이 많이 요청해주셨던 기능") 의 핵심 사용성 본질 정정.
- 단일 영역 + 기존 `executeOperation` API 재사용 (신규 인프라 0) + 텍스트 undo / 이동 undo 경로 모두 보존 (회귀 위험 영역 좁힘).
- **다중 선택 LIFO 스택 정합** — `historyTargets` 배열 + `changed` 가드 (무변경 record 차단). `mix-shape-01.hwp` 권위 샘플 시각 판정으로 정합 입증.
- **WASM 영향 0 정량 입증** — PR #629 baseline 과 정확 일치 (4,590,307 bytes). Rust 영역 변경 없음 정합 측정.
- **3개 권위 샘플 영구 보존** — samples/ 직속 추가 (158 → 161). 향후 회귀 차단 가드 영역 영구화.

### 다음 영역 — 15 잔존 PR 처리
- 5/6 신규 PR (11건): #644, #642, #641, #638, #636, #632, #627, #626, #622, #621, #620
- 5/5 잔존 (4건, v0.7.11 영역 후보): #601, #602, #607 (검토 중 — 컨트리뷰터 응답 대기), #609
- v0.7.11 PATCH 사이클 영역 — 본 사이클 (5/6) PR 처리 누적에 따라 결정.
