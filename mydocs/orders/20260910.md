# 오늘 할일 — 2026-09-10

## #6950 문단 끝 자리차지 표의 공통 배치 계약

- 메인테이너 다음 절편 승인에 따라 `task_m100_6950`에서 Stage 2를 계속한다.
- 계산된 줄의 확정 앵커를 fit·예약·출력으로 전달했다. 무효 저장 줄과 무저장 상태, 후속 본문 비겹침을
  각각 검증했으며 최종 후보의 집중 검사 36/36이 통과했다.
- [Stage 2 기록](../working/task_m100_6950_stage2.md)에 수정 전 실패·수정 후 결과를 남긴다.
- 원본 3쪽 SVG는 같은 옵션으로 재생성하여 이전 후보와 바이트 동일함을 확인했다.
- 분할·이월 조각의 frame별 배치 전달과 실제 출력 검사를 추가했다. 호스트 자체가 현재 frame에
  들어가지 않는 경계의 실패는 같은 이슈 안에서 이어서 처리하며 Stage 2 전체 완료로 처리하지 않는다.
- 분할 절편 후보 `826e4192a` 집중 검사 47/47 통과. #5585 코퍼스 검사는 실제 파일을 지정해
  재실행했다. 원본 3쪽 SVG를 `output/6950/stage2-fragments-svg/`에 재생성했고 이전과 동일하다.
- Stage 3 전체 lint/회귀·Docker WASM·PR 준비와 push·PR·GitHub 변경은 아직 수행하지 않는다.
