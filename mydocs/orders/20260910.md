# 오늘 할일 — 2026-09-10

## #6950 문단 끝 자리차지 표의 공통 배치 계약

- 메인테이너 다음 절편 승인에 따라 `task_m100_6950`에서 Stage 2를 계속한다.
- 계산된 줄의 확정 앵커를 fit·예약·출력으로 전달했다. 무효 저장 줄과 무저장 상태, 후속 본문 비겹침을
  각각 검증했으며 최종 후보의 집중 검사 36/36이 통과했다.
- [Stage 2 기록](../working/task_m100_6950_stage2.md)에 수정 전 실패·수정 후 결과를 남긴다.
- 원본 3쪽 SVG는 같은 옵션으로 재생성하여 이전 후보와 바이트 동일함을 확인했다.
- 분할·이월 조각의 frame별 배치 전달과 실제 출력 검사를 추가했다. 이후 작은 용지 높이의
  합성 사례를 실패로 규정한 판단은 근거 불충분으로 철회했으며, 메인테이너 결정에 따라
  이번 범위·완료 차단 항목에서 제외한다. 해당 사례의 추가 구현·대체 실험은 하지 않는다.
- 분할 절편 후보 `826e4192a` 집중 검사 47/47 통과. #5585 코퍼스 검사는 실제 파일을 지정해
  재실행했다. 원본 3쪽 SVG를 `output/6950/stage2-fragments-svg/`에 재생성했고 이전과 동일하다.
- Stage 3 전체 lint/회귀·Docker WASM·PR 준비와 push·PR·GitHub 변경은 아직 수행하지 않는다.
- 메인테이너가 문단 끝 남은 너비에 들어가지 않는 표의 다음 줄 배치를 **시각 판정 통과**로
  확인했다. 결정과 검증 한계를 Stage 2 §9 및 두 계획서에 반영했다. 다음은 승인 후 Stage 3다.
- Stage 3 진행 승인 후 사전 점검: 최신 원격 `0d36da4096`과 오늘할일 문서 add/add 충돌,
  Docker Linux engine 접속 불가를 확인했다. [Stage 3 기록](../working/task_m100_6950_stage3.md)에
  양쪽 기록 보존 병합 방침과 검증 순서를 남겼다. 실제 merge·전체 검증은 아직 시작하지 않았다.
- 양쪽 기록 보존 병합 방침 승인 후 최신 devel을 반영한다. 시스템 날짜는 2026-09-10 KST이며,
  Docker 29.7.2 연결 복구를 확인했다. Stage 3 검증을 이어간다.
- Stage 3: 최신 devel 병합·포맷·native/WASM/workspace Clippy·workspace build 통과.
  전체 회귀는 9,378 통과 / 23 실패 / 46 skip. 대표 기존 문서4건의 base 대비 악화를 확인했다.
  작은 용지 실험 제외 결정은 유지한다. 기존 문서 회귀 정정 전 Native Skia·Docker WASM·PR은 보류한다.

## PR #6962 머리말 양쪽 정렬 및 HWP3 호환성 수정

- [x] [#6864](https://github.com/edwardkim/rhwp/issues/6864)의 저장된 머리말 마지막 줄 강제 분배를 제거했다. HWP3 원값 6/7을 구분해 #1692 공백 분배와 #5554 일반 양쪽 정렬·KEEP_WORD를 보존했다.
- [x] 코드 후보 `14df452d8f1114223d8082f9063a414a73e8274f`: 집중 15개, 전체 회귀 9,385개 통과(실패 0, skip 46개, 8 threads), fmt·native/WASM/workspace Clippy·workspace 빌드·Native Skia·실제 WASM API 3개를 통과했다.
- [x] 한컴 기준 PDF와 1/1페이지 Visual Sweep을 산출했다. 머리말 폭 567→63을 확인하고 원본 HWP·PDF·대표 전후 PNG를 포함했다. 임시 로그·SVG·JSON은 제외했다.
- [x] [PR #6962](https://github.com/edwardkim/rhwp/pull/6962)의 코드 head CI·CodeQL·Render Diff·Adapter·Proptest 및 CI Impact Policy 성공, `MERGEABLE/CLEAN`을 확인했다.
- [x] [개별 리뷰](../pr/archives/pr_6962_review.md)의 판정은 승인이다. 글꼴·래스터 미세 차이를 명시하고, 실제 이미지가 보이는 post-merge PR/issue 댓글 계획을 기록했다. 이 문서와 리뷰는 같은 PR의 문서 trailing commit에 포함한다.
- [ ] 문서 trailing head의 CI 완료 후 일반 merge commit으로 병합하고 devel 동기화·post-merge CI를 확인한다.
- [ ] #6864의 실제 종료 상태를 확인하고 PR·이슈에 merge SHA·실제 CI·시각 증적을 중복 없이 기록한다. 이번 local/upstream 임시 branch와 전용 target만 정리한다.
