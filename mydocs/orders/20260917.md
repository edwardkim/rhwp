# 2026-09-17 오늘할일

## PR #7200 — 중첩 표 정렬·빈 문단 앵커 메인터너 보정

- [PR #7200](https://github.com/edwardkim/rhwp/pull/7200)의 원 source `2543382e7`를 유지하고,
  메인터너 보정만 추가해 **`10107f443`**을 기여자 PR branch에 직접 push했다. force-push 없음.
  [검토 문서](../pr/archives/pr_7200_review.md)의 판정은 보정된 검토 범위에서 **승인**이다.
- 초기화된 저장 앵커와 정상 저장 앵커에서 빈 줄의 공간을 보존하고, 가로 앵커·문단/객체 테두리·
  표 뒤 흐름을 보정했다. 원래 합성 width의 무효 캐시는 정상 한컴 저장본으로 갱신하고, 손상 캐시
  복구 검사는 메모리 변형으로 유지했다. 기존 HWP 경로를 재사용하고 중복 원문 3개를 제거했다.
- 로컬 전체 nextest **9946 PASS / 51 skipped**, focused 7, Native Skia lib **4112 PASS / 13 ignored**,
  그림/PDF 2/4, fmt·Clippy 3종·workspace·suite 정책, 원문 보안 검사 **108 clean**을 완료했다.
- Native/fresh WASM **20쪽**의 compare·overlay·review를 재캡처했고 PNG 20/20 동일을 확인했다.
  정상 empty Top 표의 rhwp/PDF 위치는 **149.7/149.891px**다. 대표 PNG와 대응 HWP/HWPX/PDF를
  함께 보존했다. 비공개 7쪽 및 기존 #7150 차이를 해결했다고 보고하지 않는다.
- code head의 [Full CI](https://github.com/edwardkim/rhwp/actions/runs/35122939525),
  [CodeQL](https://github.com/edwardkim/rhwp/actions/runs/35122939551),
  [Render Diff](https://github.com/edwardkim/rhwp/actions/runs/35122939151), Adapter·Proptest가 통과했다.
  GitHub 실제 merge tree는 로컬에서 검증한 tree와 같았다.
- 이 기록과 review는 code CI 성공 뒤의 trailing 문서 commit으로 같은 PR에 포함한다.
  최종 head 검사, 실제 merge SHA, duration 갱신과 기여자 댓글·devel 동기화·전용 산출물 정리의
  완료 결과는 PR 후속 댓글과 작업 완료 보고에 남긴다. contributor fork branch는 보존한다.


## planet6897 PR 10개 체리픽 통합 검토

- `upstream/devel`을 `fcbd00e0f`로 동기화한 뒤 `codex/planet-review-20260917`에서 진행했다.
  원 PR #7210·#7211·#7214·#7215·#7217·#7220·#7221·#7223·#7225·#7228은 모두 non-draft였다.
  reviewer `jangster77`를 먼저 지정하고 stack 중복을 제외한 12개 source commit을 충돌 없이 적용했다.
- [전체 계보·공통 실행](../pr/archives/pr_7210_review.md#통합-검토-공통-실행-기록): code head `cd074a4da`,
  원문/PDF 증거 head `6600d48b2`. 개별 review 10개와 처리 계획 10개를 생성했다.
- **승인(변경 범위)**: #7210·#7211·#7217·#7220·#7223.
- **머지 보류**: [#7214](../pr/archives/pr_7214_review.md)의 중첩 셀 속성 조회 경로 누락,
  [#7221](../pr/archives/pr_7221_review.md)·[#7228](../pr/archives/pr_7228_review.md)의
  원점/예약/배치 높이 불일치, [#7225](../pr/archives/pr_7225_review.md)의 156676190 3→4쪽 회귀.
  [#7215](../pr/archives/pr_7215_review.md)는 자체 검사 통과와 별개로 보류된 stack을 포함해 보류다.
- Native/fresh WASM 빌드, Studio tsc·1755 PASS/2 skipped, Rust focused 34 PASS,
  원 PR 8개 원문 보안 검사(전달한 11경로 중 `tests/fixtures/` 3개는 검사 필터에서 제외), fmt·suite/unit 정책 검사 통과. 전체 회귀/Skia/Clippy bundle은 이 통합 tree에서 재실행하지 않았다.
- Visual Sweep은 각 backend 13입력·21선택쪽과 rhwp10↔PDF8/rhwp11↔PDF9 대응을 추가 검토했다.
  standalone overlay를 포함한 PNG 85개를 보존했다. Native/WASM 21개 중 19개 PNG 동일,
  hwpctl p52/p57은 raster 차이를 그대로 보존하고 render-tree 동일성을 별도 확인했다.
- 기존 Git 입력은 재사용했다. 없던 원문 3개와 실제 저장 제품에 맞춘 Hancom2020 PDF 3개만 추가했고,
  임시 log/tsv/json은 commit에 포함하지 않았다. 이 회차에는 원 PR 코드 보정이나 원격 push·통합 PR·merge·close를 하지 않았다.

## 통합 PR 메인터너 보정

- `c1c9e2047`: #7214의 중첩 표 제한 조회를 native/WASM/Studio 동일 CellPath로 연결했다.
- `2a9810642`·`95eed7197`: #7221·#7228의 저장 첫 조각 원점, 컷 높이, 예약 하단, paint 하단을 맞췄다. hwpctl 12쪽 마지막 줄 이월 반례도 보정했다.
- `54c24ebdd`: #7225의 음수 Percent 간격·개체 바깥여백·표 pre-flush 예약·쪽 위 저장 간격을 보정했다. 156676190은4→3쪽, 결재문서5쪽·셀 여백 문서6쪽을 유지한다.
- 집중 41개·전체 nextest **9,974 PASS / 51 skipped**, Skia lib **4,112 PASS / 13 ignored**, Skia 그림/PDF 2/4 PASS. Studio 1,758 PASS/2 skip, tsc·fmt·Clippy 3종·workspace build·suite/unit 정책 PASS.
- `f94dece59`: 테스트의 마지막 표 조회를 `rfind()`로 정리했다. fmt·workspace all-target Clippy 재검사와 실제 suite의 #7196 2개 재실행 PASS. 도우미가 다른 suite를 선택한 최초 0개 실행은 성공으로 세지 않았다.
- 최종 Native/fresh WASM **15입력·28선택쪽**과 #7196의 rhwp9↔PDF8/rhwp10↔PDF9 대응을 대조했다. PNG 22/28 byte 동일, 나머지는 render-tree 좌표·내용 동일을 별도 확인했다. standalone overlay 포함 새 PNG **62개**와 대조 PDF 1개를 보존한다.
- **#7214·#7221·#7228·#7225·#7215 모두 요청한 보류 사유 해소, 보정 범위 승인**으로 개별 review와 처리 계획을 갱신했다. [최종 공통 검증](../pr/archives/pr_7210_review.md#메인터너-보정-최종-검증)에 정확한 head·명령·범위·잔차를 기록했다.
- #7196 전체 11/10쪽 차이와 156676190의 기존 셀 그림 축소·글꼴 잔차는 해결 완료로 확대하지 않았다. 원격 push·통합 PR·merge·close는 이번 회차에서 수행하지 않았다.
- 기존 입력을 재사용하고 추가 대조 PDF1개만 생성했다. log/json/tsv는 임시 경로에 둔다.
