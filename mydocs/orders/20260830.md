---
kind: daily-order
status: active
date: 2026-08-30
---

# 오늘 할 일 - 2026년 8월 30일

## #6374 Oracle PDF 형식·엔진 fail-closed 선택

- [x] 최신 `upstream/devel@7592e9c99` 위에서 [PR #6466](https://github.com/edwardkim/rhwp/pull/6466)을
  생성했다. Oracle PDF 자동 선택을 원본 상대 경로·HWP/HWPX 형식·2020/2024 engine이 확인되는 canonical
  파일로 제한하고, 미확인 legacy PDF와 다중 engine 후보는 `--engine` 없이는 fail-closed로 처리한다.
- [x] MCP 재산출 대상 319건은 기존 canonical PDF 234건을 재사용하고 나머지를 endpoint별 단일 worker
  lane으로 처리해 실패 0으로 끝냈다. PR 범위 PDF 547개는 `pdfinfo` 양의 페이지 수를 확인했고, annotation
  destination 경고가 난 2개 PDF도 Acrobat에서 정상적으로 열리는 것을 확인했다.
- [x] focused 선택 계약 6/6과 format·diff gate를 통과했다. 상세 판단과 아직 미완료인 full CI 조건은
  [PR #6466 검토 문서](../pr/archives/pr_6466_review.md)에 기록했다.
## #6457 문서-only 병합 후 worker 재사용

- [x] [PR #6459](https://github.com/edwardkim/rhwp/pull/6459)에서 같은 저장소의 직접 review-only PR만
  merge tree, linear history, PR workflow의 preflight 성공 및 heavy worker `skipped` 증거를 모두 확인한
  경우 post-merge worker를 재사용하도록 보정했다. 불완전한 증거, fork, CI enforcement 변경은 Full로
  닫는다.
- [x] `7ec7f99b3`의 PR CI·CodeQL·Adapter·Proptest가 모두 성공했고, squash merge
  `46fc02706`의 `devel` CI·CodeQL도 성공했다. 구현 PR은 CI enforcement surface를 변경하므로
  post-merge Adapter와 Proptest worker가 Full 실행 후 각각 성공한 것이 의도된 fail-closed 결과다.
- [x] 상세 근거는 [PR #6459 검토 문서](../pr/archives/pr_6459_review.md)에 보관했다. 이 문서와
  오늘할일만 포함한 후속 PR을 실제로 병합해 PR 및 `devel` push에서 두 worker가 모두 `skipped`되는지
  검증한다. [#6457](https://github.com/edwardkim/rhwp/issues/6457)은 그 결과 전까지 열린 상태로 둔다.

## #6360 장기 baseline critical path 분할

- [x] [PR #6390](https://github.com/edwardkim/rhwp/pull/6390) code candidate
  `93c0bf16a76a761adec48ad43a3a3eeebf190528`의 Full CI를 검증했다. overflow-cell, off-canvas,
  oracle page-count baseline의 기존 191~385초 단일 critical path는 CI 실측 최대 31.332초,
  32.484초, 5.253초로 줄었다.
- [x] `issue2063_huge_cellbreak_table.hwp`의 중복 전수 스캔을 제거하되
  `tests/issue_2063.rs` 전용 sentinel의 완주 성능·page-count coverage는 유지했다. overflow-cell
  render-tree 집계도 기존 TSV와 diff가 없었다.
- [x] 상세 self-review와 CI artifact 측정값을
  [PR #6390 검토 문서](../pr/archives/pr_6390_review.md)에 보관했다.
- [x] trailing 문서 head의 CI와 mergeability를 확인해 squash merge했다. merge commit
  `067a8134bc5c6a39c614536fc6f769f94fdf17ea`의 devel CI와 trusted PR artifact duration refresh가
  성공했고, [#6360](https://github.com/edwardkim/rhwp/issues/6360)을 완료로 종료했다.

## PR 전 Rust lint gate 명확화

- [x] focused test·format만 실행하고 Rust test/baseline helper의 Clippy를 CI에 넘길 수 있었던 절차
  공백을 분석했다. native, WASM32, workspace all-target Clippy를 PR 생성 전 순차 gate로 명시했다.
- [x] [PR #6393](https://github.com/edwardkim/rhwp/pull/6393)의 self-review 기록을
  [archive 문서](../pr/archives/pr_6393_review.md)에 보관했다. 문서 4개만 변경한 review-only PR이므로
  최신 head의 fast-pass aggregate와 mergeability를 확인한 뒤 작업지시자 승인에 따라 squash merge한다.

## HWP 2024 MCP 무인 스크립트 사전 차단 가이드

- [x] 최신 `upstream/devel@97c4d7155` 위에서 [PR #6386](https://github.com/edwardkim/rhwp/pull/6386)을
  생성했다. HWP5 구조 손상과 `OnDocument_Open` 문서 열기 스크립트의 무인 사전 차단을 사용자 가이드에서
  구분하고, 암호 HWP5 재검사·비동기 `failed`·결과물 없음·download 금지를 반영했다.
- [x] 단일 사용자 가이드의 링크 검사·문서 메타데이터 검사와 `cargo fmt --all -- --check`를 통과했다.
- [x] self-review 기록을 [PR #6386 검토 문서](../pr/archives/pr_6386_review.md)에 보관했다. 문서 전용
  fast-pass의 최신 head CI와 병합 가능 상태를 다시 확인한 뒤 user 승인에 따라 squash merge한다.

## #4969 — W10-Q3-E4 최신 devel 재자격화

- [x] Q3-E4 checkpoint `880862177` 뒤 최신 `upstream/devel@965c050f0`을 merge commit `a457f289e`로
  충돌 없이 통합했다.
- [x] Q3-E4 focused 33건, 원격 #6373 회전 저장 2건, manifest·unit-tier, Studio 1,247건·build,
  Clippy·Docker WASM을 통과해 `qualified-merge-reconfirmed` 결과를 작성했다.
- [x] direct Cargo target과 stale generated suite 호출 오류를 공식 prepare/check/router 경로로 정정하고
  파생물을 stage하지 않았다.
- [x] 재자격화 결과와 증적 checkpoint 생성을 승인받았다.
- [x] Q3-E5 진입 전 최신 devel 병합·재자격화와 이어지는 성능·크기·전체 회귀·실제 CanvasKit E2E 착수를
  승인받았다.
- [x] Q3-E5 반복 layer build의 font 재준비 원인을 bounded PageLayerTree cache·font-byte 공유로 정정했다.
- [x] sub-millisecond baseline에서 상대비율이 왜곡되는 문제를 확인하고, 절대시간 noise floor와 cold first-build
  공개·회귀 ceiling을 추가하는 수행계획 보완안을 승인받았다.
- [x] font bytes 공유와 bounded page-layer cache 뒤 release 9회 재계측에서 explicit default 상대 gate와
  interior/max 절대 gate, 최종 Docker WASM +51,554 bytes/+0.528251% 크기 gate를 모두 통과했다.
- [x] Studio 1,276건·CanvasKit pixel E2E·Native Skia 3종·최종 nextest 8,759건과 lint/doc/Node WASM gate를
  통과해 [Q3-E5 qualified 결과 후보](../working/task_m100_4969_w10_q3_e5.md)를 작성했다.
- [x] 첫 full nextest가 탐지한 instance set/clear API를 문서 IR 비변경 `SessionState`로 근거 있게 분류하고,
  stale generated suite 0건 호출을 prepare/check로 정정한 뒤 focused 5/5와 full을 재통과했다.
- [x] 최종 diff 감사에서 빈 문서 교체와 머리글·바닥글 숨김의 새 page-layer cache 무효화 누락을 발견해
  공통 헬퍼와 보호 테스트 2건으로 정정하고 full 8,759/8,759·Native Skia·Docker WASM을 다시 통과했다.
- [x] 메인테이너가 Q3-E5 `qualified` 결과와 구현·결과 checkpoint 생성을 승인했다.

## #4969 — W10-Q4 vertical metrics·writing mode 인수

- [x] Q3-E5 checkpoint `4e0a3822c` 후 최신 `upstream/devel@a71ebf2141` 조상 관계와
  `34 ahead / 0 behind`, clean worktree를 확인했다.
- [x] exact vertical source, standalone shaping capability, 표 셀·글상자·구역·바탕쪽 의도,
  legacy 세로 배치, 공개 HWP/HWPX/PDF fixture를 cross-check했다.
- [x] capability 존재와 exact output oracle 부재를 구분했고, HWP5 1/2와 HWPX
  `VERTICAL`/`VERTICALALL`의 layout 의미 손실을 선행 결손으로 판정했다.
- [x] [Q4 인수 감사](../working/task_m100_4969_w10_q4_handoff_audit.md)와
  [수정 수행계획](../plans/task_m100_4969_w10_q4.md)을 작성했다.
- [x] 메인테이너가 Q4 수정 수행계획을 승인했다.
- [x] 인수·계획 checkpoint `73fe7768e`를 생성했다.
- [x] Q4-A에서 Noto Sans KR·Source Han Serif K의 TTB glyph/cluster/x-y position,
  RL/LR identity, `vert`/`vrt2` fixture 차이를 exact oracle로 고정했다.
- [x] native 19/19, 격리 Node WASM 3/3, focused Clippy, integration 정책 19/19,
  format·diff 검사를 통과했다. 격리 하네스 163 MiB는 실행 후 휴지통으로 이동했다.
- [x] Q0 WOFF2 판정 이력을 보존하면서 Q1·Q3에서 추가된 exact OTF/TTF
  source-readiness를 현행화했다.
- [x] Q4-A 결과 checkpoint `61d347139`를 생성했다.
- [x] Q4-B에서 OWPML 스키마와 공개 HWPX↔HWP5 변환 쌍을 대사해
  `VERTICALALL == HWP5 code 2 == 세로 영문 세움`을 확정했다.
- [x] 표면별 source intent→writing mode→Latin orientation typed contract와 미지 raw
  `unsupported-raw` 음성 계약을 focused test 3건으로 고정했다.
- [x] focused 22/22, HWP5·HWPX 표 셀/글상자 왕복 regression 0, legacy HWPX/HWP SVG
  byte mismatch 0을 확인했다. 제품 activation·Hyper-V·private corpus 사용은 0이다.
- [x] Q4-B 결과 checkpoint `f98cc0ea1`를 생성했다.
- [x] Q4-C에서 exact glyph·cluster·origin·bbox·inline/column next origin을 한 `Arc`가 소유하는
  dormant vertical transaction 결과 후보를 구현했다.
- [x] old Hangul, Latin sideways/upright, punctuation, RL/LR과 mixed/missing-table/variation 음성
  control을 native 27/27·격리 Node WASM 4/4로 검증했다. 제품 consumer·publication은 0이다.
- [x] 메인테이너가 Q4-C `qualified-dormant` 결과를 승인했다. 최종 gate를 다시 통과한 이 commit을
  checkpoint으로 고정하고 Q4-D는 수행계획 재검토·별도 승인 뒤 시작한다.
- [x] Q4-D 구현 전 감사를 통해 HWPX 표 셀의 `VERTICALALL` 의미 손실과 exact source·layout commit·
  publication·CanvasKit 선택의 분리된 실패 경계를 확인했다.
- [x] HWP5 code 2 pure-CJK 한 셀만 최초 target으로 여는
  [Q4-D 수정 수행계획](../plans/task_m100_4969_w10_q4_d.md)을 메인테이너가 승인했다.
- [x] Q4-D 계획 checkpoint `7b3a7bab4`를 생성했다.
- [x] Q4-D0 green 28/28, integration 정책 19/19, Studio control 1/1과 non-target SVG baseline을
  고정하고 제품 source 변경 없이 계획한 미구현 경계만 0/5 red로 분리했다.
- [x] 메인테이너가 Q4-D0 `qualified-red-baseline` 결과를 승인했다. 최종 gate를 다시 통과한 이 commit을
  D0 checkpoint으로 고정하고 D1은 별도 착수 승인 뒤 시작한다.
- [x] Q4-D0 checkpoint `87d7a13a2` 뒤 메인테이너가 D1 착수를 승인했다.
- [x] exact registry snapshot과 source handle·generation·동일 bytes `Arc`를 인증하는 dormant vertical
  context를 제품 module에 등록했다. layout·sidecar·paint·backend caller와 publication은 0이다.
- [x] D1 native green 30/30, 격리 wasm32+Node behavior 1/1, 표준 Docker WASM build, integration 정책
  19/19와 focused Clippy를 통과했다. D0 red는 D1 두 경계만 green이고 D2~D4 세 경계는 red다.
- [x] 메인테이너가 [Q4-D1 `qualified-dormant-certified` 결과](../working/task_m100_4969_w10_q4_d1.md)와
  checkpoint 진행을 승인했다. 최종 gate를 통과한 이 commit을 D1 checkpoint으로 고정한다.
- [x] D1 checkpoint `d0948166a` 위로 최신 `upstream/devel@d3b40a3d7` 25커밋을 정상 병합했다.
  오늘할일 2개와 passthrough guard 충돌은 양쪽 기록·면제 계약을 모두 보존했다.
- [x] D1 native 30/30·예상 red 3건, incoming #4121 Rust 7/7, passthrough guard 5/5, Studio
  1,314 pass/1 skip·244-module build·#4969 replay, Docker WASM 6m24s와 integration 정책 19/19를 통과했다.
- [x] Q4-D2 atomic table-cell layout commit을 `qualified-atomic-layout`로 판정하고 checkpoint
  `992fe4acaf92`로 고정했다.
- [x] 최신 `upstream/devel@bd78a53122e4`를 merge commit `049b73d0cd80`으로 충돌 없이 병합하고,
  focused gate·canonical controls·Docker WASM을 재통과해 기록 commit `7bfefff07a99`로 고정했다.
- [x] Q4-D3 재검토에서 D2의 문자별 fallback leaf와 기존 한 equivalence-group 계획의 충돌을 확인했다.
  line 전체 atomic publication과 leaf별 `text-N` group을 분리한
  [Q4-D3 수정 수행계획](../plans/task_m100_4969_w10_q4_d3_revised.md)을 작성했다.
- [x] 메인테이너가 Q4-D3 수정 수행계획을 승인했다.
- [ ] 계획 checkpoint를 고정한 뒤 D3-A leaf/source mapping shadow를 시작한다.

## #4121 머리말/꼬리말 텍스트 선택

- [x] 최신 `upstream/devel@2deb3dd61` 위로 정리한 뒤
  [PR #6394](https://github.com/edwardkim/rhwp/pull/6394)를 생성했다.
- [x] focused Rust 7/7, 관련 Studio Node test 43/43, production build 241 modules와 실제 Google Chrome
  E2E 56/56을 통과했다. 사용자의 최소 검증 요청에 따라 최종 rebase 후 전체 Rust/Clippy/WASM 검증은
  다시 실행하지 않았다.
- [x] 다문단 양쪽 머리말 선택과 홀짝 꼬리말 전환 화면을 ignored 로컬 산출물로 생성했다. 작업지시자가
  PR 본문에 직접 첨부할 수 있으며 소스 PR에는 stage하지 않았다.
- [x] 상세 self-review를 [PR #6394 검토 문서](../pr/archives/pr_6394_review.md)에 보관했다. 최신 head의
  required checks와 `MERGEABLE/CLEAN`을 확인하고 작업지시자의 별도 승인 뒤 merge한다.

## `test-caption` false-pass 제거

- [x] 최신 `upstream/devel@f54408110` 위에서
  [Draft PR #6391](https://github.com/edwardkim/rhwp/pull/6391)을 생성했다. 고정 fixture의 네 caption
  mutation·verification 중 하나라도 실패하면 SVG와 `완료` 없이 exit 1로 종료한다.
- [x] all-fail·partial-fail·all-pass focused 3/3, clippy, 전체 integration 8,660/8,660과 format·정적
  정책 게이트를 통과했다.
- [x] 작성자 self-review를 [PR #6391 검토 문서](../pr/archives/pr_6391_review.md)에 기록했다. code
  candidate CI 뒤 trailing docs-only head의 required check를 확인하고, Draft 해제·merge는 별도
  작업지시자 판단을 받는다.
- [x] [PR review](https://github.com/edwardkim/rhwp/pull/6391#issuecomment-5464292086)에 따라 verifier의
  `Shape(Picture)`·Endnote 해석, verification 증적, 고정 fixture 자기서술을 보정했다. 최신
  `upstream/devel@2deb3dd61` 기준 focused 5/5·CLI catalog 20/20·전체 8,686/8,686과 새 Rust lint 묶음을
  통과했으며 code candidate는 `d8ab820b0`이다.

## HWPX 양식 컨트롤과 비동기 재시작 대응 가이드

- [x] 최신 `upstream/devel@a71ebf214` 위에서 [PR #6456](https://github.com/edwardkim/rhwp/pull/6456)을
  생성했다. HWPX 양식 컨트롤이 현재 무인 runtime과 호환되지 않아 worker 시작 전 `failed`로 끝나는
  계약과 암호 HWPX 재검사를 사용자 가이드에 반영했다.
- [x] `service_restarted`에서는 같은 job ID의 polling/download 대신 원본 local input으로 새 비동기
  start를 제출하고, 병렬 endpoint wrapper는 start/status/download endpoint를 고정해야 함을 명시했다.
- [x] 단일 사용자 가이드의 `git diff --check`, 링크·문서 메타데이터 검사와
  `cargo fmt --all -- --check`를 통과했다. self-review는
  [PR #6456 검토 문서](../pr/archives/pr_6456_review.md)에 기록했으며, 최신 trailing head의
  review-only CI와 mergeability를 확인한 뒤 user 승인에 따라 squash merge한다.

## M100 — v1.0.0

| Issue | 타스크 | 상태 | 비고 |
| --- | --- | --- | --- |
| [#6395](https://github.com/edwardkim/rhwp/issues/6395) | rhwp-studio 쪽 나누기 후 새 쪽 캐럿·화면 스크롤 동기화 | 진행중 | [PR #6396](https://github.com/edwardkim/rhwp/pull/6396) Open. 문서 전환 시 pending reveal 누수를 보정했고 TypeScript·전체 unit·build·실제 Chrome E2E를 다시 통과했다. [self-review](../pr/archives/pr_6396_review.md)에 보정 근거를 기록했으며 최신 head CI와 merge 승인을 기다린다. |
