# 오늘 할일 - 2026년 8월 13일

## planet6897 누적 renderer·serializer 보정 - #4688, #4695, #4696, #4697, #4700, #4705, #4707

- 최신 `upstream/devel` 위에 고유 변경만 누적 적용했다. #4695의 넓은 #4088 수정은 #4700의
  목적격 절 경계 보정으로 대체했고, #4707은 source ref의 과거 history가 아닌 고유 commit 하나만 적용했다.
  이후 #4688의 캡션·bookmark·Ruby 후속 세 commit을 원 head CI 녹색 근거와 focused 회귀로 추가했다.
- 누적 중 인라인 그림 줄/주입 탐지 경계, overlay 표 연속 조각 회귀, pagination fixture 초기화에
  메인터너 보정을 더했다. 기능을 넓힌 보정이 아니라 누적 적용에서 드러난 소유·테스트 계약을 고정한 것이다.
- 초기 누적 head의 전체 nextest 5,923건, focused table 35건, Native Skia library 58건과 placeholder 2건,
  fmt·clippy·diff check를 통과했다. 추가 #4688 code는 원 head의 Full CI와 integration focused 4건으로
  확인했다. HWP/HWPX renderer 변경의 시각 증적은 각 PR archive review에 보존했다.
- [PR #4688](../pr/archives/pr_4688_review.md), [#4695](../pr/archives/pr_4695_review.md),
  [#4696](../pr/archives/pr_4696_review.md), [#4697](../pr/archives/pr_4697_review.md),
  [#4700](../pr/archives/pr_4700_review.md), [#4705](../pr/archives/pr_4705_review.md),
  [#4707](../pr/archives/pr_4707_review.md) 및 [누적 구현 기록](../pr/archives/pr_4688_4695_4696_4697_4700_4705_4707_review_impl.md)에 판단 근거를 남겼다.
- 통합 PR의 최신 head CI·CodeQL·Render Diff 및 작업지시자 승인 뒤에만 merge하고, merge 뒤 원 PR과
  관련 이슈의 close/comment·branch 정리를 공식 post-merge 절차로 처리한다.

## PR #4703 - core-cli 기준 풀이 완결 및 트랙 L 정본화 통합

- kevin9327의 [PR #4691](https://github.com/edwardkim/rhwp/pull/4691)과
  [PR #4693](https://github.com/edwardkim/rhwp/pull/4693)을 최신 `devel` 위에 순서대로
  체리픽해 [통합 PR #4703](https://github.com/edwardkim/rhwp/pull/4703)으로 올렸다.
- `target/pr-review`에서 기준 풀이 100/100, 전체 gym 채점 221/221, Python 계약 테스트 32건,
  roadmap 진행률·변경 Markdown 링크·`git diff --check`를 모두 통과했다.
- 원 PR별 검토와 체리픽 적용 SHA는
  [#4703 review](../pr/archives/pr_4703_review.md) 및
  [이행 기록](../pr/archives/pr_4703_review_impl.md)에 남겼다.
- 최신 문서 후속 commit의 GitHub Actions와 mergeability를 다시 확인한 뒤 merge하고, #4689·#4692 및
  원 PR #4691·#4693의 close/comment·branch 정리를 post-merge 절차로 처리한다.

## Issue #4676 - HWPX curve 저장 한글 크래시

- [PR #4684](https://github.com/edwardkim/rhwp/pull/4684)의 `hp:curve` 저장을 `hc:pt` 나열에서
  `hp:seg` 체인으로 바꾸는 기여자 변경을 최신 `devel` 위에 적용했다.
- 원 PR의 HWPX `type="CURVE"`를 HWP5 Bezier `segment_types=1`로 옮기던 구현은 제어점 수 계약이
  달라 공개 HWPX 곡선 경로를 바꿀 수 있어, 메인터너 보정 `3741441a2`로 제거했다. writer의
  한글 호환 `hp:seg` 출력은 유지한다.
- HWPX CURVE XML→IR→XML 회귀를 추가해, 재저장 뒤 점 체인·`hp:seg` 출력은 유지하면서 HWP5
  Bezier 타입이 비어 있는지 자동 검증한다.
- 고정 `target/pr-review`에서 parser·serializer 집중 회귀, release-test 전체 5,910건, fmt·clippy,
  실제 HWPX `--verify`와 ZIP 구조 검증을 모두 통과했다. 보정 전후 HWPX 재저장 바이트는 동일하다.
- 원 #4684 source를 second parent로 보존한 merge commit `87fab352a`에서 parser·serializer 충돌을
  해소했다. 원 contributor history를 재작성하지 않고 기존 source branch에 fast-forward push한 뒤,
  최신 head Full CI·CodeQL을 확인한다.
- #4684 merge 뒤 충돌 해소 commit과 메인터너 보정 이유를 남겨 close한다. #4676은 한글 2022
  오라클 재개방 범위까지 완료 조건을 다시 확인한 뒤 close 여부를 결정한다.

## Issue #3438 - 스타일 저장 반환 계약 정리

- `lpaiu-cs`가 등록한 열린 이슈를 Windows 개발 환경에서 분류했다. iOS 전용(#3415), 한컴 Office
  실측 선결(#3351·#3416 및 #3438의 모달 판정), 메인터너 목표값 결정(#3230)은 이 작업에서 제외한다.
- #3438의 독립 항목인 `createStyle`/`updateStyle`/`updateStyleShapes` 반환 계약 보정은
  `local/task_m100_3438`에서 계획했다. 수행·구현 계획서는
  `mydocs/plans/task_m100_3438.md`, `mydocs/plans/task_m100_3438_impl.md`에 기록했다.
- 이슈는 `jangster77`에 할당해 병렬 세션 잠금을 설정했다. 소스 변경은 계획 승인 뒤에 시작한다.
- Stage 1에서 `createStyle`의 불가능한 음수 실패 가드를 없애고, 기존 스타일 수정 API의
  `false`는 snapshot no-op으로, 모양 수정 API의 `false`는 명시 오류로 처리했다. Rust API
  반환 계약까지 대조하는 Studio 가드를 추가했으며, focused 12건·Studio 전체 870 passed/1 skipped·
  TypeScript·프로덕션 번들 빌드를 통과했다. 상세는
  `mydocs/working/task_m100_3438_stage1.md`에 남겼다.
- 한컴 Office 2018·2022·2024 설치를 확인했다. 앞선 레지스트리 조회가 Assistant만 찾아
  Office 미설치로 판단한 것은 정정한다. 한컴 2022 실측 선결 항목은 후속 Windows 검증 대상으로
  유지한다.
- Stage 2에서 한컴 Office 2022의 새 빈 문서에 `oracle3438`을 입력하고 F6 스타일 대화상자에서
  Ctrl+Z를 실행했다. 대화상자는 열린 채로 남았고 Escape 뒤 상태 표시줄도 `10글자`를 유지했다.
  따라서 모달 중 undo 차단은 한컴과 같은 계약이며, `StyleDialog`의 도달 불가
  `history-jumped` 구독·동기화·해제를 제거했다.
- 스타일 저장의 `false`·예외는 이제 `StyleEditDialog` 확인 동작도 실패로 반환해 모달을 유지하고,
  실제 성공 때만 저장 후속 동작을 실행한다. focused 12건, Studio 전체 875 passed/1 skipped,
  TypeScript·production build, fixture를 연 로컬 Studio F6/Ctrl+Z 스모크를 통과했다. 상세는
  `mydocs/working/task_m100_3438_stage2.md`에 남겼다.
- 최신 `devel` 기준 [PR #4710](https://github.com/edwardkim/rhwp/pull/4710)을 열고 collaborator
  `jangster77`의 셀프 검토 코멘트를 제출했다. 외부 reviewer는 요청하지 않았다. code candidate
  `b3ceeb330`의 검토·검증 기록은 [PR #4710 review](../pr/archives/pr_4710_review.md)에 남겼다.
  최신 head의 required check, mergeability와 작업지시자 승인 전에는 merge·#3438 종료를 진행하지 않는다.

## Issue #3414 - 모달 종료 뒤 Ctrl+Z 입력 경로 복원

- `lpaiu-cs`의 열린 이슈를 현 Windows 환경에서 다시 분류했다. iOS 전용 #3415, 수치 목표의
  메인테이너 판단이 필요한 #3230, 한컴 동작 정답지부터 확정할 #3416·#3351은 이번 변경에서
  제외한다. #3315의 핵심 Track 3 PR #3672는 이미 merged 상태임을 확인했다.
- #3414는 마지막 `ModalDialog` 종료 때 활성 InputHandler의 textarea 포커스를 공통 복원하는
  범위로 진행한다. 중첩 모달에서는 부모 overlay가 남아 있으면 복원하지 않는다.
- 이슈를 `jangster77`에 할당해 병렬 세션 잠금을 설정했고, 계획서는
  `mydocs/plans/task_m100_3414.md`, `mydocs/plans/task_m100_3414_impl.md`에 기록했다.
- Stage 1에서 마지막 모달 종료 이벤트를 `main.ts`의 활성 InputHandler focus로 연결했다.
  부모 모달이 남은 자식 종료는 이벤트를 내지 않는다. focused 2건·TypeScript·Studio 전체
  871 passed/1 skipped·프로덕션 빌드를 통과했으며, 상세 기록은
  `mydocs/working/task_m100_3414_stage1.md`에 남겼다. 로컬 Studio 브라우저 스모크에서 일반
  모달과 F6 스타일 중첩 모달 모두 최종 모달 종료 뒤 textarea 포커스 복원과 Ctrl+Z 동작을
  확인했고, 자식 모달만 닫힌 시점에는 포커스가 복원되지 않음을 확인했다.
- Stage 2에서는 모달 외 body 포커스 공백의 undo/redo fallback과 개체 선택 Ctrl+C/X·Delete의
  dispatcher 단일화를 진행한다. 사용자 제스처를 요구하는 Ctrl+V native paste 이벤트는 유지한다.
- Stage 2 완료: body 포커스의 Ctrl+Z/Y는 dispatcher로 실행되고, 그림·표 선택 Ctrl+C/X·Delete는
  `edit:*` command를 공유한다. focused 5건·TypeScript·Studio 전체 874 passed/1 skipped·build와
  textarea 밖 버튼 포커스의 브라우저 Ctrl+Z 스모크를 통과했다. 상세는
  `mydocs/working/task_m100_3414_stage2.md`에 남겼다.
- 최신 `devel` 기준 [PR #4708](https://github.com/edwardkim/rhwp/pull/4708)을 열고 collaborator
  `jangster77`의 셀프 검토 코멘트를 제출했다. code candidate `f90f1a30a`의 검토와 검증 기록은
  [PR #4708 review](../pr/archives/pr_4708_review.md)에 남겼다. CI preflight, CodeQL preflight,
  Render Diff의 최신 head 성공과 승인 조건을 확인할 때까지 merge와 #3414 종료는 보류한다.
