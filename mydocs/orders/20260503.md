# 오늘 할일 - 2026년 5월 3일

<<<<<<< HEAD
## M100 — 21_언어_기출.hwp 줄간격 정합 (Task #537)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #537** | TAC `<보기>` 표 직후 첫 답안 ① 과 ② 사이 줄간격 716 HU(=9.55 px) 좁음 (작업지시자 보고 11곳: P2/3/5/6/8/9/12/13/14 답안 + P7/9 박스 안 문장) | **완료 (Stage 3/3)** | **본질**: 세 메커니즘 상호작용으로 lazy_base 에 716 HU drift 동결 — (1) `prev_tac_seg_applied` 가드로 TAC 직후 vpos 보정 건너뜀, (2) `paragraph_layout.rs` 마지막 줄 trailing-ls 제외 (Task #479), (3) lazy_base 가 sequential drift 를 base 로 박음. **정정**: `layout.rs:1494-1521` lazy_base 산출 시 prev_pi 의 last seg `line_spacing` 만큼 `y_delta_hu` 보정 (+14 / -2 LOC). **검증**: TDD 통합 테스트 1건 (FAIL→PASS), 전체 1117 통과 0회귀, 본 task 11곳 모두 IR vpos delta 와 정확 일치 (P2 q3: 63.09→**72.64** 등). 광범위 회귀 (synam-001 / 복학원서 / exam_math/kor/eng/science / 2010-01-06): 대부분 누적 drift 만큼 IR-정확 위치로 하향 보정 (정합성 개선). exam_kor pi=7 ① 직접 검증: y=1205.19 = IR 기대값. **PR #538 OPEN**. 보고서: `mydocs/report/task_m100_537_report.md` |

## M100 — 21_언어_기출.hwp 글박스 안 paragraph 줄간격 (Task #539, #537 후속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #539** | 글박스(InFrontOfText tac=true Shape) 호스트 paragraph 직후 다음 paragraph 줄간격 1 ls(=716 HU=9.55 px) 좁음 (그룹 B: 7p pi=146 르포르, 9p pi=182 더불어 수피즘) + 그룹 A 지문 시작 [X~Y] 직후 9곳 (별도 분리 권고) | **완료 (Stage 3/3, 그룹 B 만)** | **본질 (그룹 B)**: `layout.rs:1443-1462` 의 `prev_has_overlay_shape` 가드가 `treat_as_char` 무관하게 `InFrontOfText/BehindText` 면 true → 직후 paragraph 의 vpos correction 분기 자체가 skipped → trailing-ls drift 716 HU 잔존. **정정**: `Control::Shape` 분기에서 `treat_as_char=true` early return false 추가 (+9 / -0 LOC). 근거: tac=true Shape 는 LINE_SEG vpos 에 통합되어 overlay 영향 없음. **검증**: TDD 테스트 2건 (FAIL→PASS), 전체 1119 통과 0회귀, 7p/9p 두 케이스 모두 gap 14.67→**24.21 px** (IR 정합). 광범위 회귀 (8 샘플): synam-001/복학원서/exam_math/kor/science/2010-01-06 0건 변경, exam_eng_002 151 paragraph 양의 시프트(+7.68 px, 정합성 개선). 음의 시프트 0건 = 회귀 없음. 보고서: `mydocs/report/task_m100_539_report.md` |
| 그룹 A | 지문 시작 [X~Y] → 다음 문단 사이 줄간격 (9곳: 2/4/5/7/8/10/11/13/14 페이지) | **별도 issue 권고** | rhwp 는 IR vpos 정확 (33.01 px). 한컴 PDF 가 더 넓다면 60% line spacing + 음수 ls 처리 차이. 한컴 2010/2020 환경 검증 후 별도 task 등록. 메모리 룰 ("PDF 비교 절대 기준 아님", "룰 vs 휴리스틱") 적용 |

## M100 — 21_언어_기출.hwp 빈 paragraph 음수 ls floor (Task #540, #539 그룹 A)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #540** | Task #539 그룹 A 후속 — 빈 paragraph 의 음수 ls 를 한컴이 floor (무시) 하여 advance = lh 만큼 진행하는 동작 재현 (가설 H2 채택) | **완료 (Stage 3/3)** | **본질**: HWP IR 의 `LINE_SEG.line_spacing` 은 음수 값 그대로 저장되어 vpos correction 시 음수 ls 가 반영되지만, 한컴 렌더링은 빈 paragraph (text=∅, controls=∅) 의 음수 ls 를 floor (무시) 하여 advance = lh 적용. **정정**: `layout.rs` 컬럼 누적 보정 `vpos_neg_ls_floor_total` 도입 + 가드 `text.is_empty() && controls.is_empty()` + lazy_base 누적 보정 가산 (+30 LOC). **검증**: TDD 테스트 1건 (FAIL→PASS, gap 33.01→**38.88 px**, 한컴 측정값 일치), 전체 1120 통과 0회귀. 광범위 회귀 (6 샘플, 104 페이지): synam-001 (음수 ls 57건) 0 차분 (가드로 일반 paragraph 보존), 21_언어_기출 3 페이지 + exam_math 6 페이지 양의 시프트만 (+0.75/+3.04/+3.84/+5.87 px), exam_eng/kor/sci 0 차분, 음수 시프트 0건. **closes #540**. 보고서: `mydocs/report/task_m100_540_report.md` |

## M100 — 21_언어_기출 passage 박스 (paragraph border) 위치/크기 PDF 정합 (Task #544, #540 후속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #544** | Task #540 Stage 4 후속 — passage 박스 (paragraph border) 의 위치/크기 가 한컴 2010 PDF 와 다름 (작업지시자 시각 검증 보고: 위/오른쪽으로 쏠려 그려짐) | **완료 (Stage 3/3)** | **본질** (두 가지): (1) ParaShape margin_left/right 가 박스 outline 좌표에 적용되었으나 PDF 는 미적용 (col_area 전체). (2) vpos correction 가드 (`seg.vertical_pos == 0 && prev_pi > 0`) 로 페이지 시작 paragraph 직후 transition 의 trailing-ls 가 sequential y_offset 에서 누락되어 박스 top y 가 위로 쏠림. **정정**: `layout.rs` 의 `paragraph_border_y_correction_px` Cell 도입 (vpos correction 가드 skip 시 trailing-ls 보정값 set) + `paragraph_layout.rs` 의 (a) bg_y_start 산출 시 보정값 적용, (b) box_x/box_w 산식 정정 (col_area 전체) + Task #540 Stage 4 임시 우회 (push skip 가드) revert (Task #544 가 본질 해결). **검증**: TDD 테스트 1건 RED→GREEN, 전체 1120 통과 0회귀, 페이지 4 [7~9] 박스 PDF 일치 (top -1.84 / x +0.34 / w +0.04 px ±2 px tolerance). 광범위 회귀 (6 샘플): 텍스트 -시프트 0건. **closes #544**. 보고서: `mydocs/report/task_m100_544_report.md` |

## M100 — 21_언어_기출 passage 박스 안 본문 텍스트 inset (Task #547, #544 후속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #547** | Task #544 후속 — passage 박스 안 본문 텍스트의 좌측 여백이 PDF (한컴 2010) 보다 넓게 보임 (작업지시자 시각 검증: [13~15] 박스 안 본문 inset 우측으로 치우침) | **완료 (Stage 3/3)** | **본질**: Task #544 가 박스 outline 만 col_area 로 옮긴 후, paragraph_layout.rs:709-717 의 `inner_pad_left = box_margin_left` 분기 (paragraph border + visible stroke + border_spacing=0 케이스에 box_margin_left 한 번 더 더하는 logic) 가 이중 inset 부작용으로 남음 (Task #544 전 박스도 margin 적용 시에 의미 있던 보정). **정정**: inner_pad 분기 완전 제거 + 단일 룰 (margin_left = box_margin_left 한 번만) 적용 (+8 / -25 LOC). **검증**: TDD 테스트 1건 RED→GREEN (페이지 4 [7~9] 박스 안 본문 min x: 139.89→**128.53 px**, PDF 정합 ±0.03), 전체 1121 통과 0회귀. 광범위 회귀 (6 샘플): 텍스트 element 개수 무변경 (line wrap 무회귀), exam_math/science/synam-001 0 shift, paragraph border 본문 의도 시프트 (max ±box_margin_left). [13~15] 박스 직접 검증: 박스 안 좌측 여백 22.66→**11.36 px** 정상화 (556건 시프트). **closes #547**. 보고서: `mydocs/report/task_m100_547_report.md` |

## M100 — 21_언어_기출 셀 내부 inline TAC Shape margin_left + indent 누락 (Task #548, #547 후속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #548** | Task #547 후속 — 페이지 8 보기 표 (pi=167) 셀 5 (3-col 병합 본문) 의 첫줄 [푸코] inline rectangle Shape 가 cell 좌측 가장자리 (x=131.04 px) 에 렌더되어 PDF (한컴 2010, x≈155.6 px) 보다 24.6 px 좌측 시프트 (작업지시자 시각 검증) | **완료 (Stage 3/3)** | **본질**: `table_layout.rs:1486-1497` 의 셀 내부 inline TAC Shape 렌더링용 `inline_x` 초기화/리셋 (3 위치) 가 paragraph 의 `margin_left + first_line_indent` 미적용. paragraph_layout (텍스트 경로) 은 `effective_margin_left` 적용해 정확하지만, table_layout (shape 경로) 은 `inner_area.x` 만 사용 → 두 경로 불일치 (텍스트 정확, shape 만 어긋남). **정정**: `effective_margin_left_line` 헬퍼 함수 추가 (paragraph_layout.rs:851-858 와 동일 룰) + 3 분기 (초기 / Picture target_line reset / Shape target_line reset) Left/Justify 케이스에 line_margin 적용 (+30 / -3 LOC). **검증**: TDD 테스트 1건 RED→GREEN ([푸코] box: 131.04→**155.60 px**, PDF +0.0 ✓), 전체 1122 통과 0회귀 (Task #544/#547 무회귀). 광범위 회귀 (6 샘플): 텍스트+rect 음의 시프트 **0건**, exam_math 0 shift, 21_언어_기출 [푸코] +24.56 의도, synam-001 27 cell shape 의도 시프트. shape 와 직후 텍스트 정확 일치 (185.83 = 155.60 + 30.23). **closes #548**. 보고서: `mydocs/report/task_m100_548_report.md` |

## M100 — exam_science 8번 문제 인라인 수식+한글 중첩 (Task #525, exam_science 후속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #525** | exam_science p2 8번 문제 (pi=37) 인라인 화학식 글자 겹침 — 1.3~9px 다중 중첩 (이슈 본문 가설: Shape advance 폭 / ParaShape spacing) | **완료 (Stage 3/3)** | **본질 (이슈 가설 정정)**: 단순 글자 겹침이 아닌 **호스트 paragraph 텍스트 중복 emit**. `layout_wrap_around_paras` 가 비-TAC Picture wrap=Square host 의 호스트 자기 텍스트를 PageItem::FullParagraph 정상 경로 외에 두 곳 (`layout.rs:3106 layout_shape_item` + `:3534 layout_column_shapes_pass`) 에서 추가 emit → 같은 줄을 다른 col_w 정렬로 distinct x 위치 (4.46~13px 누적 오프셋) emit → 시각 중첩. **정정**: Picture Square wrap 의 wrap-around 호출 두 곳 모두 제거 (+14 / -69 LOC). Table Square wrap 호출 (`:2555`, `:2811`) 은 의도된 동작 (호스트=빈 텍스트+표 구조) 유지. 정상 PageItem 의 `has_picture_shape_square_wrap` 분기 (paragraph_layout.rs:822, 973-982) 가 ls[0..5] 좁은 영역 + ls[6..7] 넓은 영역 모두 처리. **검증**: cargo test --lib 1122/1122 + clippy 0 + svg_regression_diff 7 샘플 170 페이지 168 byte-identical (exam_science_001/_002 의도 정정 2페이지만, 회귀 0). **pi=37 직접 측정**: ls[0..7] 모든 줄 dup 0 (사용자 보고 핵심 행 y=775.01/796.48 완전 해소). dup-instances 19→5 (-74%, 잔존 5는 다른 본질). Stage 1 광범위 가설 (37 페이지) 부정확 — 실제 본 정정 영향은 exam_science 2 페이지 한정. 시각 정합 작업지시자 승인 ★. `mydocs/report/task_m100_525_report.md` |

## 회귀 정정 — exam_science p2 페이지네이션 (Task #546, #525 자연 해소)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #546** | exam_science.hwp 페이지 2 페이지네이션 회귀 — PR #506 머지 후 본문 누락 (4 → 6 페이지, p2 단 0 = 2 items / 132.7 px) | **자연 해소 / close** (코드 변경 0) | **본질**: PR #506 의 `layout_wrap_around_paras` 호출이 비-TAC Picture wrap=Square host 의 호스트 자기 텍스트를 두 곳 (`layout.rs:3106`, `:3534`) 에서 중복 emit → (1) 시각 결함 [Task #525 표면 증상] + (2) 페이지네이션 inflation [본 #546 표면 증상] 두 표면이 동일 root cause. **Task #525 정정** (Picture Square wrap 의 wrap-around 호출 두 곳 모두 제거) **이 본 회귀 자동 해소**. **검증**: `2dbbd07` (#525 push 직전 origin/devel) = 6 페이지 / p2 = 2 items 회귀 잔존 직접 확인. 현재 devel HEAD `f5ad122` = **4 페이지 / p2 = 37 items / 1133.6 px** = v0.7.9 baseline `65e275d` 정합 ★. **#524 + #525 역할 분담**: PR #506 회귀가 두 본질 결합 (그림 anchor 위치 + wrap-around 호출). #524 가 본질 1 정정, #525 가 본질 2 정정. **closes #546**. 보고서: `mydocs/report/task_m100_546_report.md` |

## 작업지시자 검토 대기 사항

1. Task #539 그룹 B (직접 대상 검증 완료) → **merge 진행 권고**
2. ~~Task #539 그룹 A → **별도 issue 등록 권고**~~ → Task #540 으로 등록·완료
3. Task #540 → **merge 완료 (devel push 됨)**
4. Task #544 → **merge 완료 (devel push 됨)**
5. Task #547 → **merge 완료 (devel push 됨)**
6. Task #548 → **merge 진행 (승인됨)**
7. Task #537 잔존 base=716 케이스: 본 task fix 로 일부 해소 가능 — 별도 검증 후 처리
8. 페이지 6/8/13/14/15 박스 자동 검출 한계 (paginator 차이) — 별도 분석 task 등록 가능
=======
## M100 — v1.0.0 조판 엔진 (PR 처리)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #507** | CASES+EQALIGN+MATRIX 중첩 토폴로지 분수 분실 정정 (closes #505) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @cskwork (Agentic-Worker) 의 첫 PR. PR #396 이 다루지 못한 토폴로지의 결함 정정 — `try_consume_infix_over_atop()` 헬퍼 추출 + 6 호출지점 통합 (parser DRY) + tokenizer skip_spaces 에 \\n/\\r 추가. 회귀 테스트 9건 + 시각 판정 fixture (`samples/issue-505-equations.hwp` + `examples/build_issue_505_fixture.rs`). cherry-pick `7bcbe2c`+`1f65919`+`c4ca403`+`6657bfc` (devel push 완료). 검증: cargo test --lib 1110 / issue_505 9/9 / svg_snapshot 6/6 / clippy 0 / WASM 4,461,235 bytes. 작업지시자 시각 판정 1차 (SVG 4/4) + 2차 (rhwp-studio web Canvas 4/4) 모두 통과. 처리 보고서: `mydocs/pr/archives/pr_507_report.md` |
| **PR #531** | TAC + Top caption 표 본문 y 오프셋 정정 (refs #530) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @postmelee (Taegyu Lee) 의 PR. `samples/basic/treatise sample.hwp` 5페이지 우측 단 TAC 표 (3x3 pi=60 ci=0 wrap=TopAndBottom) 의 Top caption 두 줄과 머리행이 같은 y 영역에 렌더링되어 겹치는 결함 정정 — `src/renderer/layout/table_layout.rs::layout_table` 의 inline (TAC) 경로에 `inline_top_caption_offset` 추가 (depth=0 + caption.is_some + Top direction). cherry-pick `f4af57e`+`a9367a9`+`ed652a3`+`0ca451e`+`17347b6` (devel push 완료). 검증: cargo test --lib 1110 / issue_530 1 / issue_418/501 회귀 0 / svg_snapshot 6/6 / clippy 0 / WASM 4,461,549 bytes. 작업지시자 시각 판정 1차 (SVG) + 2차 (rhwp-studio web Canvas) 모두 통과. **디버그 도구 적극 활용** (`--debug-overlay` + `dump-pages` + `dump`) 으로 본질 정밀 식별. 처리 보고서: `mydocs/pr/archives/pr_531_report.md` |
| **PR #538** | 21_언어_기출_편집가능본.hwp 줄간격 716 HU drift 정정 (Task #534 v1+v2 + #537 + #539, closes #534/#537/#539) | **완료 (본질 cherry-pick 머지)** | 외부 컨트리뷰터 @planet6897 (Jaeuk Ryu) 의 PR. fork devel 분기 누적으로 PR 가 비정상 상태 (129 files / +19,350 / -2,387 / 182 commits / 42 task / CONFLICTING) — 작업지시자 결정으로 본질 12 commits 만 cherry-pick. **Task #534 v1+v2** (`layout.rs::layout_shape_item` TAC Picture inner_pad + LINE_SEG.column_start 정합), **Task #537** (lazy_base trailing-ls 보정, TAC 표 직후 첫 답안 줄간격 716 HU drift, P2/3/5/6/8/9/12/13/14 11곳), **Task #539** (`prev_has_overlay_shape` 가드 완화 — treat_as_char 제외, 글박스 호스트 직후 paragraph 줄간격 P7/P9 2곳). 코드 변경 `src/renderer/layout.rs` +60/-4 + `integration_tests.rs` +228 (TDD 3건). cherry-pick `e7f1adb~fc32bd3` + `75f33a0` + `4d42a7e` (devel push 완료). 검증: cargo test --lib 1113 / issue_530/505/418/501 회귀 0 / svg_snapshot 6/6 / clippy 0 / WASM 4,461,870 bytes. 작업지시자 시각 판정 1차 (SVG) + 2차 (rhwp-studio web Canvas) 모두 통과. 처리 보고서: `mydocs/pr/archives/pr_538_report.md`. **컨트리뷰터에게 다음 PR 전 fork devel 동기화 안내** (close 댓글). 잔존: 이슈 #540/#544 (PR closes 자동 동작으로 close 됐으나 본질 정정 미반영, 작업지시자 처리 결정 대기) |

### Task 별 정정 본문 (PR #538 cherry-pick 영역)

| Task | 결함 | 정정 |
|------|------|------|
| **#534 v1** | exam_kor 18p 우측 단 [A]/[B] + 인라인 그림 위치 결함 — Square wrap 표 + tac=true 그림 동일 paragraph 그림 x 좌표 inner_pad 누락 (~11.33 px 좌측 시프트) | `layout.rs::layout_shape_item` TAC 분기에 has_visible_stroke + border_spacing 검사 + inner_pad_left 가산 (+25 / -2 LOC). 광범위 8 샘플 192 페이지 중 190 byte-identical, p18 image x 593.39 → 604.72 (+11.33 px) 정합 |
| **#534 v2** | layout_shape_item TAC Picture LINE_SEG.column_start 정합 보강 | layout.rs (+12 / -1 LOC) — v1 후속 정정 |
| **#537** | TAC `<보기>` 표 직후 첫 답안 줄간격 716 HU(=9.55 px) 좁음 (P2/3/5/6/8/9/12/13/14 11곳) | `layout.rs:1494-1521` lazy_base 산출 시 prev_pi 의 last seg `line_spacing` 만큼 `y_delta_hu` 보정 (+14 / -2 LOC). 본 task 11곳 모두 IR vpos delta 와 정확 일치 (P2 q3: 63.09 → 72.64 등) |
| **#539** | 글박스(InFrontOfText tac=true Shape) 호스트 paragraph 직후 다음 paragraph 줄간격 1 ls(=716 HU=9.55 px) 좁음 (그룹 B 7p/9p 2곳) | `Control::Shape` 분기에서 `treat_as_char=true` early return false 추가 (+9 / -0 LOC). 7p/9p 두 케이스 모두 gap 14.67 → 24.21 px (IR 정합). 그룹 A (지문 시작 [X~Y] 직후 9곳) 는 IR 정확 — 한컴 환경 검증 후 별도 issue 등록 권고 |

## 신규 이슈

| Issue | 제목 | 비고 |
|------|------|------|
| **#543** | treatise sample.hwp 5페이지 좌측 단의 TAC 그림 (s0:pi=56) 이 캡션과 겹침 — Shape 경로의 Top caption 오프셋 누락 | PR #531 시각 판정 중 작업지시자 발견. 본질이 PR #531 의 정정과 동일 클래스 (TAC + Top caption 누락) 이지만 Shape 경로 의 별도 결함. **방향 반대** — 표는 본문이 caption 아래로, 그림은 본문이 caption 위로 이동해야 함. 메모리 `feedback_image_renderer_paths_separate` 정합 사례. milestone v1.0.0, assignee edwardkim. |
| **#545** | aift.hwp 41페이지 표 위치 잘못 계산 — TAC 표 (s2:pi=496) 의 vpos 또는 layout 결함 | PR #538 시각 판정 중 작업지시자 발견. **PR #538 와 무관, 이전부터 발생한 결함**. 표: 2x1 TAC, [그림 8] AI 팩토리 정책 개념도. milestone v1.0.0, assignee edwardkim. |
| **#546** | exam_science.hwp 2페이지 페이지네이션 회귀 — PR #506 머지 후 본문 누락 (4페이지 → 6페이지) | PR #538 시각 판정 중 작업지시자 발견. **PR #538 와 무관**. bisect 결과 회귀 origin 정확히 확정 — PR #506 (HWP 3.0 파서 + Square wrap, @jangster77 51 commits) 의 머지 시점. PR #506 직전 (`65e275d`) 4 페이지 + p2 단 0 items=37 정상 → PR #506 머지 후 (`c7330cf`) 6 페이지 + p2 단 0 items=2 회귀. 후보: Task #460 의 Square wrap 어울림 렌더링 (가장 가능성 높음) / Task #460 보완 4-5 / Task #417 HWP 3.0 파서 (가능성 낮음). milestone v1.0.0, assignee edwardkim. |

## 폐기 처리

| Issue | 처리 | 비고 |
|------|------|------|
| **Task #535** | **폐기** | BMP 32bpp 알파 좁힘 진행 후 작업지시자 결정으로 전부 폐기. 워킹트리 변경 + untracked 파일 + WASM 산출물 모두 되돌림, `local/task535` 브랜치 삭제. 이슈 #535 본문 워터마크 본질로 복원 (open 유지). 분리했던 신규 이슈 #541 (hwpspec.hwp InFrontOfText) + #542 (water-mark.hwp 워터마크 시각) 도 함께 close. 다층 레이어 도입 이전 시점으로의 rebase 는 보류 (작업지시자 추후 결정). 본 사이클의 진행 결과 통찰 (사용자 리뷰 ↔ BMP 32bpp 매핑) 은 보존된 메모리 (`feedback_image_renderer_paths_separate` 등) 로 누적. |

## 작업 메모

- PR #507 의 cherry-pick 절차에서 `mydocs/orders/20260501.md` auto-merge 정합 (충돌 0)
- WASM 빌드 4,456,858 → 4,461,235 → 4,461,549 bytes (PR #507 / PR #531 누적)
- README 기여자 목록 갱신 (@cskwork 첫 PR + @postmelee + @planet6897) — 본 사이클 일괄 갱신 시점에 반영 예정
- pr-507-review / pr-531-review / pr-538-review 임시 브랜치 삭제 (cherry-pick 후 정합)
- main vs origin/main `0fb3e675` 동기화 정합 (메모리 `feedback_release_sync_check` 적용)
- PR #538 컨트리뷰터에게 fork devel 동기화 안내 (다음 PR 전 `git pull --ff-only origin devel` + 충돌 해소)

## 작업지시자 검토 사항 (PR #538)

1. 본 task 직접 대상 (Task #537 11곳 + Task #539 그룹 B 2곳) — IR 정확 정상화 (PR 본문 검증 완료) → 작업지시자 시각 판정 게이트
2. 광범위 시프트 (exam_kor / exam_eng / exam_science / 2010-01-06): IR 정합 개선. 한컴 2010/2020 PDF 시각 비교 권고
3. exam_math 음의 시프트 (수식 직후 paragraph): 시각 비교 후 회귀 시 후속 task 분리 가능
4. Task #539 그룹 A (지문 시작 [X~Y] 직후 9곳) — IR 정확. 한컴 환경 검증 후 별도 issue 등록 권고
5. 잔존 사항: base=716 일부 paragraph (별도 메커니즘), Clippy 기존 결함 (table_ops.rs:1007, object_ops.rs:298) → 별도 issue 검토 후보

## 메모리 정합

- `feedback_check_open_prs_first` — PR #507 / #531 / #538 처리 진행
- `feedback_pr_comment_tone` — close 댓글 차분/사실 중심 + 컨트리뷰터 안내
- `feedback_release_sync_check` — main 동기화 점검 후 cherry-pick
- `feedback_v076_regression_origin` / `feedback_visual_regression_grows` — 작업지시자 직접 시각 판정 게이트 (1차 SVG + 2차 web Canvas) 통과 후 머지
- `feedback_pdf_not_authoritative` / `reference_authoritative_hancom` — 한컴 2010/2020 직접 비교
- `feedback_image_renderer_paths_separate` — equation 영역의 web Canvas / SVG 시각 정합 입증 (image 외 영역도 정합 가능 사례), Task #534 v1+v2 의 layout_shape_item 정정도 동일 영역
- `feedback_assign_issue_before_work` — 이슈 #537/#539/#534 모두 assignee 부재 → 외부 컨트리뷰터에게 "오픈 타스크" 인식 사례

## M100 — 21_언어_기출 [28~30] [A] inline 표 body 위치 (Task #549 close → #550)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #549** | 페이지 14 [28~30] 박스 안 [A] inline TAC 표 — initial 진단 (cell padding hack) 잘못됨 | **close (사전 분석 결과)** | Stage 2 fix 적용 시 [A] 셀 렌더링 무변화 (`shrink_cell_padding_for_overflow` 가 보상하여 수학적 등가). 본질 cause = body text "반" (x=626.22) 이 cell_right (628.12) 보다 1.90 px LEFT 시작 → table_right overlap. PDF 한컴 2010/2020: gap = `outer_margin.right` 850 HU = 8.51 pt = 11.34 px. 우리 wrap_text_x = `col_x + IR cs` 가 margin.right 누락. cause 가 cell padding 이 아닌 wrap_text_x / table_x 산식 영역. revert 완료 (1122 baseline 통과). 보고서: `mydocs/report/task_m100_549_report.md` |
| **M100 #550** | wrap=Square 표 우측 outer_margin.right 미반영 — body text overlap 정정 | **close (새 baseline 검증)** | Task #517/#518/#519/#520/#521/#523/#528 merge 후 재측정: body "반" 626.22→637.59 px (gap -1.90→+9.47 px). PDF 기대값 +11.34 px. overlap visible 결함 해소, 잔존 1.87 px 시각 임계 이하 → close. |
>>>>>>> origin/devel
