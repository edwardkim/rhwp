# 오늘 할 일 - 2026년 8월 17일

## Issue #4961 - 문자별 Font Decision Trace

- 선행 #4939에서 고정한 Font Rule Ledger의 W2 인계 계약을 이어받아, 실제 문서의 한 글자가
  document face에서 layout metric과 backend paint 후보까지 도달한 이유를 읽기 전용으로 설명한다.
- 최신 `upstream/devel@418e5b191d23cf0618ce99f0cfec332c19ac1bc2`에서
  `local/task4961-font-decision-trace` 브랜치를 만들었다. #4961은 `edwardkim` 담당, `v1.0.0`
  milestone, `enhancement`·`api`·`rendering` label이며 동일 범위의 열린 PR은 없다.
- 기존 font 선택 결과와 기본 renderer 출력은 바꾸지 않는다. 별도 opt-in trace API, 문자별 결정 계보,
  bounded/fail-closed 출력, 결정론적 hash와 native/WASM·Canvas2D·CanvasKit capability 구분을
  [수행계획](../plans/task_m100_4961.md)에 기록한다.
- 수행계획 승인 전에는 production source와 test를 변경하지 않는다. Stage별 구현·검증·commit 및
  remote push·PR 생성은 각각 프로젝트 승인 게이트를 따른다.
- 메인테이너 승인 뒤 Stage 1을 완료했다. trace v1 schema, W1 candidate identity golden 4건,
  repository-tracked HWP/HWPX fixture manifest와 상한·hash·민감정보 계약 도구를 추가했다. 신규 계약
  11건과 W1 원장 회귀 10건, Draft 2020-12 schema compile, Markdown 링크와 diff 검사를 통과했으며
  production renderer·resolver·Studio source 변경은 없다.
- 메인테이너 승인 뒤 Stage 2를 완료했다. 기존 style·metric·문자폭 선택 결과를 바꾸지 않는 decision
  wrapper, bounded native query와 `getFontDecisionTrace` WASM binding을 추가했다. 메트릭 색인 전수
  등가성, 기존 text measurement 회귀 39건, release-test, wasm32 check와 표준 Docker WASM build를
  통과했으며 공개 HWP 첫 8문자의 native/WASM JSON·`layoutHash`·`normalizedHash`가 byte-identical했다.
  W1 원장은 historical snapshot으로 유지하고 변경된 Rust source 4개의 digest drift를 trace에
  `ledgerSourceDrift`로 명시했다. Studio·native Skia·Canvas2D·CanvasKit 관측은 Stage 3 승인 전까지
  구현하지 않는다.
- 메인테이너 승인 뒤 Stage 3을 완료했다. native Skia는 실제 replay와 같은 후보·glyph 검사 helper를,
  Studio는 Canvas2D local/web/generic supply와 CanvasKit SFNT/typeface snapshot을 읽기 전용으로 공유한다.
  CSS actual glyph face 미관찰, 권한 거부, SFNT 부재와 source join 실패를 서로 다른 certainty/reason으로
  유지하며 `@rhwp/editor`의 별도 capability와 RPC를 추가했다. focused 46건과 production build가 통과했다.
- 메인테이너 승인 뒤 Stage 4를 완료했다. 추적 중인 공개 HWP/HWPX 6개를 exact·missing·document
  substitute profile로 분리하고 반복·key order·font enumeration·상한·unsupported E2E 3건을 고정했다.
  header/footer의 target-dependent `usize::MAX` marker는 source 좌표가 아니므로 `null/unavailable`로
  정규화했고 native 64-bit와 wasm32의 portable `layoutHash`를 일치시켰다.
- 메인테이너 승인 뒤 Stage 5를 완료했다. W1·trace 계약, release lib 4,076 passed/13 ignored,
  nextest 6,523 passed/38 skipped, native Skia 59+2+4, Studio 940 passed/1 skipped, editor 24건,
  fresh Docker WASM과 공개 SVG 6문서 parity가 모두 통과했다. headless와 Windows 호스트 Chrome에서
  trace 전후 SVG·HWP bytes가 같고 fetch·FontFace·Local Font Access 호출이 0건임을 확인했다.
  FI-01~FI-14와 #4961 완료 조건은 [최종 보고서](../report/task_m100_4961_report.md)에 전항 판정했다.
  실제 10k coverage와 조판 위험 순위는 제품 동작을 바꾸지 않는 후속 #4962에 인계한다. remote push,
  PR 생성과 이슈 상태 변경은 다음 승인 전까지 수행하지 않는다.
