# 오늘 할 일 - 2026년 8월 17일

## PR #5179 파생 Rust suite 정책 CI 계보 보정

- [PR #5179](https://github.com/edwardkim/rhwp/pull/5179)의 첫 CI에서 `Validate Rust test suite manifest`가
  base SHA shallow fetch 뒤 `base...HEAD`의 공통 조상을 찾지 못해 실패했다. 파생 산출물 검증 자체가
  거부된 것이 아니라 checkout 계보가 부족한 CI 환경 결함이다.
- lint checkout을 `fetch-depth: 0`으로 전환하고 별도 `--depth=1` base fetch를 제거해, 기존의 정확한
  PR base 3-way diff 정책을 유지했다.
- Node manifest 계약과 CI workflow 계약을 로컬에서 재검증한 뒤, review 기록과 함께 PR branch에
  trailing push한다. 병합 전 재실행된 GitHub Actions의 성공을 최종 조건으로 둔다.
## planet6897 PR #4984, #5136, #5172 통합 검토

- 최신 `upstream/devel@d4cf27eeb` 기준 `review/planet6897-20260817`에 #4984, #5136, #5172를 의존·고유
  commit 순서로 누적 적용했다. PR 내부 devel merge commit은 제외했고 contributor source branch에는
  push하지 않았다.
- 원 source head는 #4984 `9281e6f7a`, #5136 `e2df9f9bb`, #5172 `301c3b038`이며, 작성 시점 원 PR은
  모두 `OPEN/DIRTY`였다. required checks와 CodeQL은 성공 또는 해당 없음으로 확인했다.
- generated manifest와 unit-tier 충돌을 해결하고 generator check를 통과시켰다. 전체 nextest는
  `6538 passed, 38 skipped, 8 slow`, clippy와 fmt도 통과했다.
- 개별 archive review는 `pr_4984_review.md`, `pr_5136_review.md`, `pr_5172_review.md`에 남겼고,
  누적 적용·충돌·검증 순서는 `pr_4984_5136_5172_review_impl.md`에 기록했다.
- 차단 결함과 추가 메인터너 코드 보정은 발견하지 못했다. 원격 push·통합 PR 생성·원 PR merge/close는
  작업지시자 승인과 merge 직전 최신 head 재확인 뒤 진행한다.

## Non-draft PR #4974, #4975, #4977, #4978, #4979, #4980, #4981 통합 준비

- 최신 `upstream/devel@8d4fb781c` 위 `review/nondraft-prs-20260817`에 원 PR 일곱 건을 의존 순서로
  누적 적용하고 rebase했다. 원 contributor branch는 수정하지 않았다.
- 원 PR head는 #4974 `2cb99453e`, #4975 `36f636be3`, #4977 `b14db80da`, #4978 `20a160b04`,
  #4979 `d51487c5a`, #4980 `920929ca5`, #4981 `bc43d392d`이며, 모두 `OPEN`·`MERGEABLE`이고
  실패·대기 check가 없다.
- 최신 `devel`의 generated integration suite 규약에 맞춰 #4974/#4977의 fixture를 `tests/cases/`에
  등록하고 생성 manifest·unit tier를 동기화하는 메인터너 보정 `a2907c573`을 추가했다. Native Skia
  개별 fixture를 resolver로 실행하도록 공식 local validation 명령도 갱신했다.
- 최종 후보의 release-test nextest는 6,529 passed, 38 skipped, 7 slow, 334.412초였다. fmt, clippy,
  doctest, manifest/unit-tier, Studio 954 tests·production build, desk 34 tests·fmt·clippy·build,
  Native Skia 58/2/4, 페이지 설정 headless E2E를 통과했다.
- WASM은 이 호스트에 Docker가 없어 `CARGO_TARGET_DIR=target/pr-review wasm-pack build --target web
  --out-dir pkg --no-opt` 진단 경로로 통과했다. 표준 Docker 최적화 build는 원격 CI에서 재확인한다.
- 각 원 PR의 archive 검토 기록을 같은 통합 PR에 포함할 준비가 됐다. 아직 원격 push·통합 PR 생성은
  수행하지 않았으며, 승인 뒤 최신 source head를 재확인하고 진행한다.

## Non-draft PR #4942, #4949, #4951, #4954, #4955, #4958, #4959 통합 후보

- 최신 `upstream/devel@418e5b191d23` 위 `review/non-draft-20260816`에 원 PR 일곱 건을 번호순으로 누적했다.
  적용과 리베이스는 conflict 없이 끝났으며, 원 contributor branch는 수정하지 않았다.
- #4959의 후속 source commit `9ffe4878d`는 검토 중 추가된 fixture 교체이므로 최신 head를 재확인해
  적용했고, 해당 `issue_3915_verify_axes_both_reported` 3건도 별도로 통과했다.
- 최종 후보의 release-test nextest는 6,519 passed, 38 skipped, 7 slow, 389.510초였다. fmt, clippy,
  diff 검사와 #4949 Python handoff 계약 28건도 통과했다.
- #4949에는 수거 output의 선언 SHA-256과 실제 파일 해시를 강제 비교하는 메인터너 보정 `566194be7`을
  추가했다. 각 원 PR별 archive review와 누적 이행 기록을 같은 통합 PR에 포함한다.
- 통합 PR merge 전 최신 원 PR head·mergeability·required Actions를 다시 확인한다. 승인 뒤 통합 PR을
  병합하고 원 PR에는 반영 사유와 링크를 남긴 후 close한다.

## PR #4976 Rust 회귀 테스트 링크 fan-out 자동 샤딩

- 최신 `upstream/devel@b9efb7c20` 위 `task_m100_4947`에서 565개 integration source를 32개 generated
  suite와 9개 단독 예외 target으로 재구성하고 [PR #4976](https://github.com/edwardkim/rhwp/pull/4976)을 갱신했다.
- source unit test 4,225개와 모듈 298개를 계층화하고, 신규 integration·source-side test가 정책을
  우회하지 못하도록 PR base 대비 CI 게이트를 추가했다.
- 로컬 release-test nextest는 6,521 passed, 38 skipped, 실패 0이었다. workspace clippy, release build,
  doc test, WASM32 clippy/check, Node 20건과 Python 427건도 통과했다.
- 검토 중 내부 계약 크레이트의 `crateVersion`이 `0.1.0`으로 노출되는 회귀를 발견해 루트 제품 버전
  주입 파사드로 보정했고, 전체 회귀를 다시 통과했다.
- code candidate `ccb8f518f`의 GitHub Actions는 20 successful, 3 skipped, 실패·대기 0으로 완료됐다.
  archive a/b/slow, regular shard 1~3, slow shard, Native Skia를 모두 통과했다.
- #4976을 먼저 병합하고, 아직 열린 #4974, #4977 등은 갱신된 `devel` 위로 재리베이스해 manifest,
  단위 계층, nextest 기준을 다시 계산한다. review·오늘할일 trailing head의 CI 완료를 최종 조건으로 둔다.

## PR #5170 단일 nextest archive와 Native Skia 병렬화

- [PR #5170](https://github.com/edwardkim/rhwp/pull/5170)은 default-feature archive를 preflight 직후
  한 번만 빌드하고 slow 1개와 regular 3개 shard가 같은 archive를 병렬 소비하도록 변경했다.
- code candidate `df4c76a61`의 CI run `32002991473`은 11분 27초에 성공했다. 이전 성공 run의
  14분 48초보다 3분 21초 단축됐고, 필수 check와 CodeQL은 실패·대기 0건이다.
- Native Skia와 archive builder는 preflight 직후 같은 시각에 시작했다. archive 완료 직후 네 shard가
  병렬 실행됐으며 최종 `Build & Test`가 runnable 합계와 모든 lane 결과를 승인했다.
- 32개 generated suite 직접 축소는 열린 PR 충돌을 유발하므로 제외한다. 후속 성능 개선은 기존 테스트
  배정 구조를 유지하면서 workspace compile/link 재사용과 조건부 디스크 정리를 검토한다.
- review·오늘할일 trailing 문서 head의 fast-pass와 `CLEAN` 상태를 확인한 뒤 병합하고 작업 브랜치와
  전용 산출물을 정리한다.

## PR #5182 디스크 여유 공간 조건부 정리

- [PR #5182](https://github.com/edwardkim/rhwp/pull/5182)는 [#5181](https://github.com/edwardkim/rhwp/issues/5181)의
  작업으로, Nextest archive 전 root 가용 공간을 측정하고 30GB 미만일 때만 Android/.NET 디렉터리를
  정리하도록 보완했다. 충분한 공간에서는 정리를 건너뛰며, 정리 전·후와 Rust cache 복원 후 `df`를
  기록한다.
- code candidate `b6582f6fe`의 로컬 workflow 검증은 unittest 12 passed, actionlint, diff 검사를
  통과했다. workflow-only 변경이므로 Cargo 전체 회귀 테스트는 생략했다.
- CI run [32019381731](https://github.com/edwardkim/rhwp/actions/runs/32019381731)과 CodeQL run
  [32019381393](https://github.com/edwardkim/rhwp/actions/runs/32019381393)은 archive, Native Skia,
  regular/slow shard, Rust/Python/JavaScript 분석까지 모두 성공했다.
- 검토 기록은 `mydocs/pr/archives/pr_5182_review.md`에 남겼다. 이 문서와 오늘할 일은 code candidate
  검증 완료 뒤 같은 PR에 trailing commit으로 추가하며, 최신 head의 CI와 mergeability를 다시 확인한다.
