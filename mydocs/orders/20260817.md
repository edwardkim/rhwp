# 오늘 할 일 - 2026년 8월 17일

## Non-draft PR #4942, #4949, #4951, #4954, #4955, #4958, #4959 통합 후보

- 최신 `upstream/devel@418e5b191d23` 위 `review/non-draft-20260816`에 원 PR 일곱 건을 번호순으로 누적했다.
  적용과 리베이스는 conflict 없이 끝났으며, 원 contributor branch는 수정하지 않았다.
- #4959의 후속 source commit `9ffe4878d`는 검토 중 추가된 fixture 교체이므로 최신 head를 재확인해
  적용했고, 해당 `issue_3915_verify_axes_both_reported` 3건도 별도로 통과했다.
- 최종 후보의 release-test nextest는 6,519 passed, 38 skipped, 7 slow, 389.510초였다. fmt, clippy,
  diff 검사와 #4949 Python handoff 계약 28건도 통과했다.
- #4949에는 수거 output의 선언 SHA-256과 실제 파일 해시를 강제 비교하는 메인터너 보정 `566194be7`을
  추가했다. 각 원 PR별 archive review와 누적 이행 기록을 같은 통합 PR에 포함한다.
- 통합 PR merge 전 최신 원 PR head·mergeability·required Actions를 다시 확인한다. 승인 뒤 통합 PR을
  병합하고 원 PR에는 반영 사유와 링크를 남긴 후 close한다.

## PR #4976 Rust 회귀 테스트 링크 fan-out 자동 샤딩

- 최신 `upstream/devel@b9efb7c20` 위 `task_m100_4947`에서 565개 integration source를 32개 generated
  suite와 9개 단독 예외 target으로 재구성하고 [PR #4976](https://github.com/edwardkim/rhwp/pull/4976)을 갱신했다.
- source unit test 4,225개와 모듈 298개를 계층화하고, 신규 integration·source-side test가 정책을
  우회하지 못하도록 PR base 대비 CI 게이트를 추가했다.
- 로컬 release-test nextest는 6,521 passed, 38 skipped, 실패 0이었다. workspace clippy, release build,
  doc test, WASM32 clippy/check, Node 20건과 Python 427건도 통과했다.
- 검토 중 내부 계약 크레이트의 `crateVersion`이 `0.1.0`으로 노출되는 회귀를 발견해 루트 제품 버전
  주입 파사드로 보정했고, 전체 회귀를 다시 통과했다.
- code candidate `ccb8f518f`의 GitHub Actions는 20 successful, 3 skipped, 실패·대기 0으로 완료됐다.
  archive a/b/slow, regular shard 1~3, slow shard, Native Skia를 모두 통과했다.
- #4976을 먼저 병합하고, 아직 열린 #4974, #4977 등은 갱신된 `devel` 위로 재리베이스해 manifest,
  단위 계층, nextest 기준을 다시 계산한다. review·오늘할일 trailing head의 CI 완료를 최종 조건으로 둔다.

## PR #5170 단일 nextest archive와 Native Skia 병렬화

- [PR #5170](https://github.com/edwardkim/rhwp/pull/5170)은 default-feature archive를 preflight 직후
  한 번만 빌드하고 slow 1개와 regular 3개 shard가 같은 archive를 병렬 소비하도록 변경했다.
- code candidate `df4c76a61`의 CI run `32002991473`은 11분 27초에 성공했다. 이전 성공 run의
  14분 48초보다 3분 21초 단축됐고, 필수 check와 CodeQL은 실패·대기 0건이다.
- Native Skia와 archive builder는 preflight 직후 같은 시각에 시작했다. archive 완료 직후 네 shard가
  병렬 실행됐으며 최종 `Build & Test`가 runnable 합계와 모든 lane 결과를 승인했다.
- 32개 generated suite 직접 축소는 열린 PR 충돌을 유발하므로 제외한다. 후속 성능 개선은 기존 테스트
  배정 구조를 유지하면서 workspace compile/link 재사용과 조건부 디스크 정리를 검토한다.
- review·오늘할일 trailing 문서 head의 fast-pass와 `CLEAN` 상태를 확인한 뒤 병합하고 작업 브랜치와
  전용 산출물을 정리한다.
