---
kind: daily-order
status: active
date: 2026-08-29
---

# 오늘 할 일 - 2026년 8월 29일

## #4969 — W10 common shaping

- [x] N2 checkpoint `422a8f7bc` 뒤 최신 `upstream/devel@f6a6bee8f`을 merge commit `c0998c280`으로
  병합하고 cross-impact 98건·전체 integration 8,568건·native-skia·Clippy·Docker WASM·Studio를 재통과했다.
- [x] 재자격화 증적을 checkpoint `167e3b3d2`로 커밋했다. remote push는 별도 승인 전 수행하지 않는다.
- [x] Q2-A~D5의 owner·activation·backend·resource·성능 증거를 종합해 Q2 최종 support classification을
  `bounded-subset`으로 판정했다.
- [x] exact portable old-Hangul의 one-line/one-run horizontal LTR stored-boundary lane과 좁은 NO_LS atomic
  lane만 활성 지원으로 고정했다.
- [x] RTL·Latin default liga 일반화·multi-line/run·variation·vertical·Native Skia blob replay는 Q2 성공
  범위에서 제외하고 structured reject 또는 TextRun/W9/K0 fallback으로 유지했다.
- [x] classification 결과와 checkpoint commit을 승인받아 Q2 종료 상태를 정본에 반영했다.
- [x] Q3 official Happiness Sans VF source·`fvar` axis·standalone shaper와 실제 transaction·backend capability를
  감사했다.
- [x] effective default identity 중복, mutable face cross-instance 누출, Q2 cache/lowerer·CanvasKit/Native Skia의
  variation 단절을 반영한 Q3 수정 수행계획을 작성했다.
- [x] Q3 수정 수행계획을 승인받아 Q3-A canonical instance red 계약의 진입 범위를 확정했다.
- [x] Q3 capability 감사·수정 수행계획을 checkpoint `4b68ce669`로 커밋했다.
- [x] Q3-A canonical identity·mutable face·transaction request/cache red 계약을 작성하고 `qualified-red`로
  판정했다.
- [x] Q3-A `qualified-red` 결과를 승인받았다.
- [x] Q3-A red 계약을 checkpoint `1075f5dce`로 커밋하고 Q3-B 시작 승인을 받았다.
- [x] Q3-B `fvar` default canonicalization, instance-local face·cache와 prepared source owner를 구현했다.
- [x] 공개 5-instance Hangul·Latin, 1/2/8 ownership, native 66건·Node WASM 9건을 검증해 `qualified` 결과를
  작성했다.
- [x] Q3-B `qualified` 결과를 승인받았다.
- [x] Q3-B 결과를 checkpoint `6a156ca98`로 커밋하고 Q3-C 시작 승인을 받았다.
- [x] Q3-C canonical replay identity·producer outline·backend shadow를 구현하고 native·Node WASM·Native Skia·
  CanvasKit 음성 cache 계약·Docker WASM을 검증했다.
- [x] Q3-C `qualified` 결과를 승인받았다.
- [x] Q3-C 결과를 checkpoint `0a248f3b8`로 커밋했다.
- [x] 최신 `upstream/devel@955abb5268c3`을 merge commit `a8ff6a19d`로 통합하고 오늘할일 양측 기록을
  보존적으로 합쳤다.
- [x] merge head에서 Q3 직접 69건·교차 영향 33건·Native Skia·Studio·Docker WASM·Node WASM을 재통과했다.
- [x] 병합 후 재자격화 결과를 승인받았다.
- [x] 재자격화 증적을 checkpoint로 고정했다.
- [ ] remote push·PR·GitHub issue 현행화는 각각 별도 승인을 받는다.

## planet6897 #6304·#6305·#6309·#6314·#6316 통합 검토

- [x] 최신 `upstream/devel` 기준 누적 검토 브랜치에 원 PR head를 `cherry-pick -x`로 적용하고,
  source PR별 계보를 [통합 검토 기록](../pr/archives/pr_6304_6305_6309_6314_6316_planet6897_review_impl.md)에
  보존했다.
- [x] #6304의 명시 local face 목록이 이전 OS 감지 cache로 fallback하지 않도록 메인터너 보정하고 Studio
  focused 테스트 16/16을 통과시켰다.
- [x] #6309의 Square float band focused Rust 회귀 1건과 #6314의 stored-row focused Rust 회귀 3건을
  통과시켰다. HWP 2020 기준 PDF와 native-skia object crop 증적도 보관했다.
- [x] visual sweep SVG raster에 Studio webfont projection 기반 local/CDN 글꼴과 Noto Sans KR fallback을
  주입해, macOS 글꼴 부재로 생기던 두부문자를 제거했다. helper 2/2, Python 43/43, #6309 1페이지
  end-to-end sweep이 성공했다.
- [x] 최신 `upstream/devel@f6a6bee8f`를 충돌 없이 통합하고, #6309 1/1, #6314 3/3, Studio 16/16,
  webfont helper 2/2, Python visual sweep 43/43을 같은 merge-base에서 다시 성공시켰다.
- [ ] upstream 임시 브랜치에서 `devel` 대상 통합 PR을 만들고 최신 head의 required CI를 확인한다.
- [ ] CI 성공 후 각 원 PR의 판정과 메인터너 보정 사유를 contributor에게 정중히 comment하고, 실제 연결된
  issue/PR 상태를 `post_merge.md`에 따라 처리한다. contributor fork branch는 유지한다.
