# 오늘 할일 - 2026년 8월 12일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
| --- | --- | --- | --- |
| [#4655](https://github.com/edwardkim/rhwp/issues/4655) | v0.8.4 신규 배포 채널 제거 | 구현·devel 통합·CI 완료 | v0.8.2 공식 채널만 보존, maintainer 직접 반영 |
| [#4601](https://github.com/edwardkim/rhwp/issues/4601) | v0.8.3 릴리즈 | PR #4612 self-review | code candidate CI 성공, trailing review-only fast-pass 대기 |
| [#4029](https://github.com/edwardkim/rhwp/issues/4029) | cold-cache release CI 복구 | PR #4581 merge·devel CI 성공 | 운영 기록 fast-pass 뒤 main release-grade CI 재검증 |

## Issue #4655 - v0.8.4 신규 배포 채널 제거

- `v0.8.2`의 실제 workflow와 배포 가이드를 기준으로 기존 공식 채널을 고정했다.
- GHCR, PyPI·`@rhwp/node`, Python·Node 바인딩 개발 표면, deb/rpm/MSI·설치 스크립트,
  cargo-binstall·AUR·MCP registry, Scoop/Homebrew/Winget, 설치용 GitHub Action을 제거한다.
- 신규 배포 채널은 전 범위 안전 검증과 사용자 영향 부재를 확인한 뒤 메인테이너가 명시적으로
  채택해야 하며, workflow merge·태그 push·기존 secret을 승인으로 간주하지 않는다.
- 작업지시자가 이번 타스크를 PR 없이 메인테이너 권한으로 처리하도록 명시했다.
- 구현 커밋 `7342dda65`와 사용자 표시 버전 계약 커밋 `5dbe99f4f`를 원격 `devel`에 직접
  반영했다. CI run `31547394146`과 CodeQL run `31547393914`는 모두 성공했고 #4655를 종료했다.

## Issue #4601 - v0.8.3 릴리즈

- 콜레보레이터 작업 종료 뒤 `upstream/devel@572786d02`를 기준선으로 다시 고정했다.
- 네이티브 release-test 5,767건, Native Skia 64건, Docker WASM, Studio·editor,
  Python·Node 바인딩, Chrome·Firefox·VS Code 패키징과 호스트 CDP E2E를 검증했다.
- Python 공식 바인딩에 최신 agent-value 명령 11개가 누락된 문제와 release version 정렬
  도구가 런타임 상수 두 곳을 놓친 문제를 근본 보완했다.
- VS Code 개발 의존성의 `fast-uri` high 취약점을 3.1.5로 패치하고 audit 0건을 확인했다.
- Docker CLI image를 처음부터 빌드해 `rhwp v0.8.3`, capabilities 0.8.3과 비루트
  `rhwp` 사용자 실행을 확인했다.
- 최종 커밋 기준 Firefox AMO source archive는 30MB이고 ZIP 무결성·필수 재현 파일·
  대형 샘플 및 로컬 산출물 제외를 확인했다.
- 로컬 후보 산출물은 `output/4601/release-candidate/`, 상세 결과는
  [Stage 4 보고서](../working/task_m100_4601_stage4.md)에 둔다.
- crates.io는 15,064 files / 1.3GiB compressed로 기존 선행 정비 조건을 충족하지 않으므로
  이중 게이트가 게시를 건너뛰는 상태를 유지한다.
- Stage 4 커밋 후 최신 원격 기준선을 다시 확인한다. 별도 승인 전에는 push·PR·tag·publish를
  수행하지 않는다.
- 작업지시자 승인 뒤 [PR #4612](https://github.com/edwardkim/rhwp/pull/4612)를 `devel`
  대상 Open PR로 생성했다. code candidate `8a58029b9`의 CI·CodeQL·Render Diff·Native
  Skia·바인딩·최종 Build & Test는 모두 성공했다.
- maintainer self-review 방식으로 전환했다. 작성자 본인 reviewer request와 `APPROVE`는
  만들지 않고 [검토 기록](../pr/archives/pr_4612_review.md)을 trailing docs-only commit으로
  포함한 뒤 최신 fast-pass·mergeability를 확인해 `COMMENTED` review를 게시한다.
- 1차 트리야지는 assignee `edwardkim`, milestone `v1.0.0`, labels `documentation`, `ci`,
  `packaging`, `python`, `api`, `dependencies`로 정렬했다. merge와 #4601 종료는 각각
  후속 승인·실제 배포 완료 조건으로 남긴다.

## Issue #4029 - cold-cache release CI 복구

- [PR #4581](https://github.com/edwardkim/rhwp/pull/4581)을 `devel`에 admin merge했다.
- merge commit `96e52a01b`의 CI와 CodeQL이 성공했다. archive builder 3개는 `release-test/30`으로
  5분 34초, 7분 21초, 5분 41초에 각각 완료됐고, Native Skia·worker 4개·최종 `Build & Test`도 통과했다.
- [검토 기록](../pr/archives/pr_4581_review.md)을 운영 기록으로 보존한다.
- 최신 devel을 main에 반영한 뒤 `release/60`으로 cold-cache release-grade 경로를 재검증한다.
## planet6897 표·사다리·TAC 통합 검토

- 원 PR [#4569](https://github.com/edwardkim/rhwp/pull/4569),
  [#4570](https://github.com/edwardkim/rhwp/pull/4570),
  [#4572](https://github.com/edwardkim/rhwp/pull/4572),
  [#4575](https://github.com/edwardkim/rhwp/pull/4575),
  [#4611](https://github.com/edwardkim/rhwp/pull/4611)을 최신 `devel` 위
  `review/planet6897-20260812`에 오래된 순서로 누적했다. #4575의 동일 도구 충돌은 #4572의
  위양성 가드와 stricter node-row 판별을 모두 보존하도록 메인터너 해소했다.
- 공백 TAC 캐리어 저장 vpos 보정은 block 표에도 적용될 수 있던 경계를 inline TAC에만 한정하는
  메인터너 보정 `708eded7b`과 회귀 8건을 추가했다.
- 리베이스 전 누적 통합 후보에서 release-test 5,776 passed / 36 skipped / 7 slow, Native Skia 58+2+4,
  Clippy, fmt, diff check, WASM build와 focused #4514/#4515/provenance 검증을 마쳤다.
- 이후 최신 `upstream/devel@88ffffb7d`으로 리베이스했다. 이 기준선은 이미 병합된 PR만 반영한
  상태이므로 작업지시자 지시에 따라 전체 회귀를 다시 실행하지 않는다.
- [#4514](https://github.com/edwardkim/rhwp/issues/4514) sample은 HWP 2020 MCP 기준 PDF(46쪽)와
  p8-p10 대조를 완료했다. 겹침은 사라졌지만 rhwp 48쪽과 overlay 표 잔여 행 공백은
  [#4568](https://github.com/edwardkim/rhwp/issues/4568)에서 계속 처리한다. #4514는 닫지 않는다.
- 개별 검토 기록과 통합 이행 기록은 `mydocs/pr/archives/`에 포함한다. 통합 PR CI는 확인하되,
  새 릴리스 준비가 끝나고 별도 승인이 있기 전에는 merge·원 PR close·issue close를 하지 않는다.

## planet6897 후속 float·TAC·배분 정렬 통합 검토

- #4667 이후 갱신된 [#4570](https://github.com/edwardkim/rhwp/pull/4570),
  [#4572](https://github.com/edwardkim/rhwp/pull/4572)와 누락됐던
  [#4614](https://github.com/edwardkim/rhwp/pull/4614),
  [#4623](https://github.com/edwardkim/rhwp/pull/4623),
  [#4640](https://github.com/edwardkim/rhwp/pull/4640),
  [#4651](https://github.com/edwardkim/rhwp/pull/4651),
  [#4665](https://github.com/edwardkim/rhwp/pull/4665)를 최신 `devel` 위
  `review/planet6897-20260812-r2`에 오래된 기능 순서로 적용했다.
- #4640의 `layout.rs` 충돌은 #4614/#4623의 사다리 역보정과 동거 float 판별을 함께 남기도록
  해소했다. #4570 전면 이미지 낱장화의 정확히 절반(2/4) 포함 결함은 엄격한 과반 helper와 회귀로
  메인터너 보정했다.
- 현재 통합 head에서 release-test 전체 `5,782 passed / 36 skipped`, Clippy, Native Skia 58+2+4,
  WASM build와 공개 #4665 SVG fixture를 검증했다. 개별 review와 이행 기록은
  `mydocs/pr/archives/`에 보존한다.
- 통합 PR CI가 동일 head에서 성공하고 작업지시자가 승인하기 전에는 merge, 원 PR close, 관련 issue close를
  수행하지 않는다. #4533과 #4599는 캠페인 이슈로 유지한다.

## kevin9327 Gym·테마파크 누적 통합 검토

- 원 PR [#4652](https://github.com/edwardkim/rhwp/pull/4652),
  [#4656](https://github.com/edwardkim/rhwp/pull/4656),
  [#4666](https://github.com/edwardkim/rhwp/pull/4666)을 최신 `devel` 위
  `review/kevin9327-20260812`에 오래된 기능 순서로 누적했다. #4656/#4666에 중복된 #4600 history는
  #4652 적용 뒤 재적용하지 않았다.
- Node 배포 산출물 혼입, 릴리스 gate 바이너리 전달, baseline 생성 뒤 미채점, legacy 제출물 예외 종료,
  stale CI contract 호출을 메인터너 보정으로 각각 고쳤다.
- Gym baseline은 reference 보유 86개 과제를 성공 86·실패 0으로 생성·채점했고, leaderboard 검증 5/5,
  release diff 100개 과제·122개 관측 `stable`, release gate `stable · 0`을 확인했다.
- Rust 소스 변경이 없으므로 작업지시자 지시에 따라 전체 Cargo 회귀는 실행하지 않았다. PR별 검토와
  누적 이행 기록은 `mydocs/pr/archives/`에 보존한다.
- 원격 통합 PR 생성·CI·merge와 원 PR comment/close는 작업지시자 승인 뒤에만 수행한다. `core-cli`의
  legacy 기준자료 공백은 전체 historic score 재현을 주장하지 않는 후속 정비 항목으로 남긴다.

## Issue #4673 - stale run 취소 runner TLS 보정

- [PR #4670](https://github.com/edwardkim/rhwp/pull/4670)의 stale run 취소 job은 GitHub API 목록 조회 중
  `DEPTH_ZERO_SELF_SIGNED_CERT`로 실패했다. Studio bridge 변경과 무관한 GitHub-hosted runner의 Node CA
  신뢰 저장소 문제였으며, 해당 PR의 Build & Test·CodeQL·Render Diff는 성공했다.
- [PR #4674](https://github.com/edwardkim/rhwp/pull/4674)은 `actions/github-script` step에
  `NODE_OPTIONS=--use-system-ca`를 적용한다. TLS 검증 비활성화는 사용하지 않고, contract test가 시스템 CA
  설정 위치와 `NODE_TLS_REJECT_UNAUTHORIZED` 부재를 검증한다.
- code candidate `92dbeda54`의 CI·CodeQL·Native Skia·nextest shard·Build & Test는 모두 성공했다.
  archive review를 포함한 trailing docs push의 `synchronize` event에서 실제 `cancel-stale-runs` 성공을
  확인한 뒤 merge를 판단한다.
