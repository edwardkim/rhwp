# 오늘 할 일 - 2026년 8월 6일

## PR #4084 - #4054 이른 분할 보고서와 델타-PI 측정 도구 계약 보정

- [PR #4084](https://github.com/edwardkim/rhwp/pull/4084)의 contributor 원 head
  `ae7e4db3eab9c6d93a381eb823446be8129098ee`를
  `review/planet6897-4084-20260806`에서 검토했다. contributor history는 변경하지 않았다.
- 최초 `pi_map_hash.py`는 `dump-pages` 실패를 빈 TSV 행으로 남겨 실패 두 번을 지도 미변경으로 오판할 수
  있었고, basename key 때문에 동명 문서를 구분하지 못했다. 메인터너 보정 `607e2fb73`은 실패 시 기존 TSV를
  유지하고 exit 1로 종료하며, 원래 경로 key·페이지 지도 존재·양의 jobs/timeout 계약을 추가했다.
- 회귀 4건, Python 구문·공백, 실제 `k-water-rfp.hwp` 27쪽 PI 지도 생성, 최신
  `upstream/devel` `3f7d87542` merge simulation을 통과했다. 보정 code head의
  [CI 31098025096](https://github.com/edwardkim/rhwp/actions/runs/31098025096?pr=4084)과
  [CodeQL 31098025589](https://github.com/edwardkim/rhwp/actions/runs/31098025589?pr=4084)가 모두 성공했다.
- 최신 `upstream/devel` `3f7d87542`를 정확한 한 부모로 둔 bridge `90c76c10e`를 review 기록보다
  먼저 만들었다. `607e2fb73`과의 자동 3-way tree 일치를 확인했고, 이 뒤의 review-only commit으로
  fast-pass aggregate와 최신 `MERGEABLE/CLEAN` 상태를 확인한 뒤 merge한다.

상세 근거: [PR #4084 검토](../pr/archives/pr_4084_review.md),
[메인터너 보정 실행 기록](../pr/archives/pr_4084_review_impl.md).

## PR #4096 - 글자겹침 `charSz` 테두리 경로 한정

- [PR #4096](https://github.com/edwardkim/rhwp/pull/4096)의 contributor 원 head
  `8128683632154b598aad1e44438fea53b15af6d7`를
  `review/planet6897-4096-20260806`에서 검토했다. contributor history는 변경하지 않았다.
- `char_overlap_size_ratio`를 SVG·WebCanvas·Native Skia가 공유해, 실제 테두리가 없는
  `border_type=0` CharOverlap에는 `charSz` 축소를 적용하지 않고 PUA 결합 숫자의 원형 승격과
  반전 사각형 축소는 보존했다.
- 최신 `devel` `8b85fd64f`와의 merge simulation은 conflict 없이 완료됐다. focused CharOverlap
  test 10건, Native Skia test 58건, fmt, Native Skia CLI build가 모두 통과했다.
- 실제 관세청 HWP 1쪽은 테두리 없는 SVG 조건(`font-size="22.67"`, ellipse 0건)을, K-water HWPX
  13쪽은 반전 사각형 축소(`font-size="18.13"` 3건)를 확인했다.
- 현재 `devel`의 같은 오늘할일 문서와 충돌해 양쪽 기록을 보존하는 기준선 병합을 추가했다. 자동
  merge-tree가 이 문서 충돌을 해소하지 못하므로 review-only fast-pass 대상은 아니며, push한 최신 head의
  full CI와 최신 mergeability를 확인한다. merge는 작업지시자 승인 뒤에만 수행한다.

상세 근거: [PR #4096 검토](../pr/archives/pr_4096_review.md).
## PR #4104 - Native Skia와 기본 테스트 shard 의존성 분리

- [#4103](https://github.com/edwardkim/rhwp/issues/4103)의 수용 기준에 따라 네 기본 테스트 shard의
  `native-skia-tests` `needs`와 실행 조건을 제거했다. 각 shard는 이제 preflight와 자신이 소비하는
  archive만 기다린다.
- `native-skia-tests`는 독립 job으로 유지하고, `Build & Test` aggregate의 Native Skia 결과 검증과
  `needs`는 보존했다. 따라서 Native Skia 실패·취소는 계속 aggregate 실패가 된다.
- workflow 계약 18건, review-only fast-pass 계약 4건, `actionlint`, 공백 검사를 통과했다.
- [CI 31092097921](https://github.com/edwardkim/rhwp/actions/runs/31092097921?pr=4104)과
  [CodeQL 31092097642](https://github.com/edwardkim/rhwp/actions/runs/31092097642?pr=4104)가 모두
  성공했다. Lint 종료 뒤 Native Skia와 archive A/B/slow가 10:13:10~12에 병렬 시작했고, archive
  완료에 맞춰 해당 shard가 시작하며 final aggregate도 성공했다.
- 이 review·오늘할일 trailing commit을 push해 fast-pass aggregate와 최신 mergeability를 확인한 뒤
  merge한다.

상세 근거: [PR #4104 검토](../pr/archives/pr_4104_review.md).

## PR #4087 - build-from-ingest 기반 HWPX 테스트 데이터 생성기

- [PR #4087](https://github.com/edwardkim/rhwp/pull/4087)의 contributor 원 head
  `aa68e4a05fefa5e4ee4ed47c11e13d9dabf32829`를 같은 가시성 브랜치
  `review/kevin9327-4087-20260806`에서 검토했다. 수제 zip 조립 대신 `build-from-ingest`와
  `rhwp info --json`을 연결한 생성기여서 #4044의 비정합 HWPX 문제를 재발시키지 않는다.
- 최초 검토에서 config의 `../escaped` template 이름이 `--output-dir` 밖에 결과를 쓰는 path traversal와,
  비실행 `--rhwp-bin`이 traceback·종료 1로 실패하는 설정 오류 계약 위반을 확인했다.
- 메인터너 보정 `231924b8f`에서 단일 filename template만 허용하고 path 구분자·절대 경로·NUL을
  거부했다. rhwp binary도 실행 가능한 regular file만 허용하도록 했으며, unsafe output·path-like
  config·비실행 binary 회귀를 추가했다.
- 전용 `target/review-pr4087`의 실제 `rhwp`로 생성기 회귀 16건과 기본 템플릿 5종(1·1·2·1·7쪽)의
  독립 `rhwp info --json` 검증을 통과했다. Python 구문·공백, fast-pass workflow 4건, CI 영향 18건,
  classifier 27건도 통과했다.
- 보정 code head의 [CI 31090102442](https://github.com/edwardkim/rhwp/actions/runs/31090102442?pr=4087)와
  [CodeQL 31090101925](https://github.com/edwardkim/rhwp/actions/runs/31090101925?pr=4087)가 모두 성공했다.
  최신 `devel` update merge의 `merge-tree` 일치도 확인했다. 이 archive review·오늘할일 trailing
  commit을 source branch에 push해 #4102 current-base fast-pass aggregate와 최신 mergeability를
  재검증한 뒤 병합한다.

상세 근거: [PR #4087 검토](../pr/archives/pr_4087_review.md),
[메인터너 보정 기록](../pr/archives/pr_4087_review_impl.md).

## PR #4102 - current-base 병합 뒤 review-only fast-pass

- [#4101](https://github.com/edwardkim/rhwp/issues/4101)의 #4073 재실행 사례를 기준으로, green code head 뒤
  current base를 source에 병합하고 문서 기록만 추가한 경우의 preflight 후보 탐색을 보정했다.
- bridge는 current base를 정확히 한 부모로 둔 2-parent merge만 허용하며, `git merge-tree` 결과가 실제 tree와
  같은지 source 비실행 방식으로 검증한다. 불일치·충돌·조회 실패·다중 bridge·일반 merge는 full CI로 남긴다.
- workflow fast-pass 계약 4건, CI 영향 workflow 계약 18건, 분류기 27건, `actionlint`, 공백 검사를 통과했고,
  #4073의 `f10691230`·`d76d4e98` 자동 merge tree가 `49d2511e9` tree와 일치함을 재현했다.
- code head `b38d715f7`의 전체 CI·CodeQL은 성공했다. 이 review·오늘할일 trailing commit을 push한 뒤 same-PR
  candidate 재사용으로 heavy worker가 skip되는 aggregate와 최신 mergeability를 확인하고 merge한다.

상세 근거: [PR #4102 검토](../pr/archives/pr_4102_review.md),
[구현·검토 기록](../pr/archives/pr_4102_review_impl.md).

## PR #4073 - schema-validator `oneOf` 선택 대안 WARNING 보존

- [PR #4073](https://github.com/edwardkim/rhwp/pull/4073)의 contributor 원 head
  `a59f8bc36aff9acabca566e75e54dda874735bec`를 같은 가시성 브랜치
  `review/kevin9327-4073-20260806`에서 검토했다. contributor 원 commit은 수정하지 않았다.
- `oneOf`가 정확히 하나와 일치할 때 선택된 대안의 scratch WARNING을 버려, Rust
  `build-from-ingest`가 거부하는 `text` 블록의 `bold`·`ref` 혼입을 검증기가 무경고 성공으로
  반환하는 결함을 확인했다.
- 메인터너 보정 `f10691230`에서 선택된 대안의 WARNING만 최종 결과에 보존했다. 매치하지 않은
  대안의 진단은 계속 격리해 `oneOf` 0개/2개 이상 ERROR 의미론은 유지했다.
- Python 구문 검사와 schema-validator 회귀 17건을 통과했고, 실제 CLI에서 `bold`와 `ref` 각각
  경고 1건·`valid:true`·종료 0을 확인했다. 보정 head의 전체 GitHub Actions, Build & Test,
  CodeQL도 성공했다.
- 같은 날짜의 archive review와 오늘 기록을 보존하도록 최신 `devel` update merge를 추가했다. 이
  archive review·오늘할일 trailing commit을 source branch에 push한 뒤 최신 head 전체 CI와 mergeability를
  확인하고 병합·후속 정리를 진행한다.

상세 근거: [PR #4073 검토](../pr/archives/pr_4073_review.md),
[메인터너 보정 기록](../pr/archives/pr_4073_review_impl.md).

## PR #4061 - #4055 차트 B1 스파이크 메인터너 보정

- [PR #4061](https://github.com/edwardkim/rhwp/pull/4061)의 contributor 원 head
  `d03425e893bf81caa78e800a4070acea97e2ba9c`를 같은 가시성 브랜치
  `review/johndoekim-4061-20260806`에서 검토했다. contributor 원 commit은 수정하지 않았다.
- 최초 검토에서 한컴 전수 판정 범위와 최종 보고서의 불일치, `mini_cfb` 루트 CLSID 손실의
  무손실 표기 오류, H-A 누락/테스트 수 불일치, 1,229줄 테스트 파일을 확인했다.
- 메인터너 보정 `97c8d9732`에서 10개 변종·대조군을 HWP 2020 MCP로 전수 PDF 변환했다. 모두
  `status=success`, `run_status=0`, `validation=ok`, 1페이지 A4였다. OOXML 변경 6개와
  대조군·레거시 단독 변경 4개는 144 DPI 렌더 해시 두 그룹으로 명확히 분리됐다.
- 프로브를 828줄과 417줄 helper로 분리하고, Stage 2·Stage 4·계획서·최종 보고서에 실제 10개
  판정과 CFB CLSID 보존 한계를 반영했다. focused 프로브 9 passed, ignore 생성기 1 passed,
  fmt, clippy, diff 검사, 최신 `devel` 병합 simulation을 통과했다.
- 보정 code/test commit과 archive review·오늘 기록 commit은 contributor 원격 `task4055`에
  push했다. 같은 날 `devel`의 오늘 기록 추가와 충돌해 양쪽 기록을 보존하는 기준선 병합을
  추가했다. 이 병합 head의 전체 CI와 mergeability를 확인한 뒤 merge 여부를 결정한다.

상세 근거: [PR #4061 검토](../pr/archives/pr_4061_review.md),
[메인터너 보정 기록](../pr/archives/pr_4061_review_impl.md),
[#4055 최종 보고](../report/task_m100_4055_report.md).

## planet6897 누적 통합 검토 - #4041, #4047, #4053, #4057, #4060, #4066, #4067, #4077

- 최신 `upstream/devel` `0b2e1c7e8` 위에 8개 원 PR의 고유 commit만 누적했다. stack으로 중복된
  WMF/EMF 선행 commit은 한 번만 적용했다.
- `typeset.rs`의 #4041/#4077 겹침은 메인터너 보정으로 순수 경계 판정과 회귀 테스트를 추가해,
  저장 꼬리줄 상한과 각주 안전마진 예외가 함께 보존되도록 했다.
- 최신 integration head에서 `cargo test --profile release-test --tests` 467개 test binary가 모두
  성공했다. Stage 1의 focused typeset·이미지·Native Skia·clippy·fmt·WASM 검증도 완료했다.
- #4067 실제 HWP를 HWP 2020 MCP 기준 PDF로 5쪽 변환하고, 첫 페이지의 Square 그림이 표 셀 안에
  남는 것을 compare/overlay asset으로 확인했다. 전체 폰트 fidelity는 별도 과제로 유지한다.
- 개별 검토 문서 8개, 누적 계획, 기준 PDF와 대표 PNG를 통합 PR에 포함한다. 원격 CI 통과 뒤 merge하고
  원 PR은 통합 PR로 superseded 처리한다.

상세 근거: [누적 통합 기록](../pr/archives/pr_planet6897_20260806_review_impl.md),
[Stage 1](../working/task_m100_planet6897_20260806_stage1.md),
[Stage 2](../working/task_m100_planet6897_20260806_stage2.md).

## #4051 - batch-convert 실제 변환 실패·출력 충돌 보정

- [PR #4052](https://github.com/edwardkim/rhwp/pull/4052)의 실제 rhwp export 연결을 검토했다.
- 메인터너 보정으로 동일 상대 경로·stem의 HWP/HWPX를 변환 전 거부하고, 활성 포맷 일부만 실패해도
  문서 전체를 Failed와 exit 1로 집계하도록 수정했다.
- rhwp exit 1만 재시도하고 사용법·feature 부재인 exit 2와 spawn 오류는 즉시 실패하도록 구분했다.
- batch-convert 통합 테스트 18건, 실제 native-skia 없는 all-formats 실패 경로, 출력 충돌 사전 거부,
  fmt, clippy, diff 검사를 완료했다.
- 메인터너 code commit `f36434eb3`은 contributor source `30ff9736d` 위에 원격 반영됐고, CI
  `31023915543`, CodeQL `31023922453`, Render Diff `31023915292`가 모두 통과했다.
- 이 review·오늘할일 trailing commit을 같은 source branch에 push한 뒤 fast-pass aggregate를 확인하고,
  PR 병합과 후속 처리를 진행한다.

상세 근거: [PR #4052 검토](../pr/archives/pr_4052_review.md),
[PR #4052 메인터너 보정 계획](../pr/archives/pr_4052_review_impl.md),
[#4051 Stage 4](../working/task_m100_4051_stage4.md).

## #4037 - 에이전트 자동화 도구킷 출력·실패 계약 보정

- [PR #4037](https://github.com/edwardkim/rhwp/pull/4037)의 contributor 원 head `48b41ca9c` 위에
  메인터너 보정 `6f8ee6d84`, CI 제외 정리 `6049d2919`을 추가했다.
- 기존 `-o` 산출물·보고서·계획 CSV·NDJSON을 새 실행이 삭제하거나 덮어쓰지 않도록 사전 거부하고,
  새 호출이 생성한 경로만 정리하도록 보정했다.
- `bulk_sweep`의 레코드 없는 batch 비정상 종료를 `batchFailures`와 exit 1로 반영하고,
  `archive_search` 보고서에는 최종 exit·batch 종료 코드·stderr를 저장하도록 보정했다.
- 실제 release-test `rhwp`로 도구킷 회귀 27건, CI 영향 분류 27건, workflow 회귀 18건, Python 문법과
  diff 검사를 모두 통과했다. 단순 보조 도구의 회귀는 핵심 CI에서 제외했다.
- code head `6049d291`의 [CI 31068823951](https://github.com/edwardkim/rhwp/actions/runs/31068823951)과
  [CodeQL 31068823815](https://github.com/edwardkim/rhwp/actions/runs/31068823815)가 모두 성공했고,
  PR 상태는 `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`이었다.
- 첫 archive review commit `ca77f8ba9`는 같은 날 `devel`의 오늘할일 추가와만 충돌했다. 양쪽 기록을
  보존한 update-branch merge `df3aefd70` 뒤 첫 preflight는 green candidate를 찾지 못해 full CI로
  전환됐다.
- 기준선 병합 head `67aedc594`의 [CI 31069850597](https://github.com/edwardkim/rhwp/actions/runs/31069850597)과
  [CodeQL 31069850507](https://github.com/edwardkim/rhwp/actions/runs/31069850507)가 모두 성공했다.
- 이 문서-only commit은 `67aedc594`을 candidate로 재사용한 fast-pass aggregate와 최신 `CLEAN` 상태를
  확인한 뒤 병합한다. 병합 comment에는 기존 산출물 삭제와 batch 실패 은닉을 방지하기 위한 메인터너
  보정 사유를 남긴다.

상세 근거: [PR #4037 검토](../pr/archives/pr_4037_review.md),
[PR #4037 메인터너 보정 실행 기록](../pr/archives/pr_4037_review_impl.md).

## #4042 - 중첩 표 담은 셀 p2 조판 붕괴: 폭 재단 + 쪽경계 Top 앵커

- 42065 페이지 2 의 두 결함을 다중열 중첩 표 한정 케이스 가드로 정정해 origin/devel 에 반영했다
  (`20d990b04` 폭 재단, `ddd6ddcc7` Top 앵커, `48f49e002` Stage 2 기록).
- 폭 재단: layout_table 이 표 선언/스트레치 폭으로 조판해 원점(inner_x) 대비 우측이 셀 밖으로 넘쳐
  클립되던 것을, col_area(inner_width−om_left)에 맞춰 col_widths·row_col_x 를 균일 축소해 flush
  시켰다. 게이트는 `col_count>1` 로 한정 — 1×1 중첩 표는 render_normalization 스트레치가 정답
  기하(#2195/#4058 76076, issue_2308)이므로 제외했다.
- Top 앵커: 쪽 경계로 잘리는 다중열 중첩 표 셀(가시 슬라이스 < 전체 콘텐츠)이 valign=Center 로 남아
  첫 표를 셀 중앙(offset ~211px)으로 밀던 것을 Top 으로 강제했다. 1×1 은 `has_multicol_nested`
  게이트로 제외해 76076 회귀를 막았다.
- origin/devel 이 41 커밋 급증(text_measurement 리팩토링·image_resolver·typeset 등)한 위로
  rebase(무충돌) 후 재검증: 42065 Table A Top 앵커·조문대비표 우측 flush·issue_2308 유효,
  `cargo test --profile release-test --tests` 467 binary 전부 통과, fmt 통과. push 승인 후 반영했다.
- 페이지 2 하단 채움(② 큰 행이 안 잘려 비는 fill-to-bottom)은 image diff 로 상단 일치·하단 발산을
  확인하고 **#4069 로 별건 등록**했다(줄 단위 행 분할 + 첫페이지/continuation split 통일 필요).

상세 근거: [Stage 2 구현계획서](../working/task_m100_4042_stage2.md),
[#4069](https://github.com/edwardkim/rhwp/issues/4069).
