# 2026-09-25 오늘할일

## planet6897 PR #7406 메인터너 단독 검토

- 최신 병합 base `c80a8370ab294259557c850c2e54495ecd0e79c0`에서 원 head `f49ebdefb9254d153e20a8cfd1424be398290eb4`를 `-x` 체리픽한 단독 branch `review/planet6897-7406-20260925`를 검토했다. [개별 review](../pr/pr_7406_review.md)에 입력·source SHA·판정을 분리했다.
- 원 HWPX를 한컴 2024 엔진으로 직접 변환한 [140쪽 PDF](../../pdf/issue2006/1790387_prep_final_report-2024.pdf)를 증적으로 추가했다. 설치 KoPub 글꼴을 공급한 Native Visual Sweep은 물리 67/68/69/70/94/108쪽 각각 **90.33392/50.05/41.16299/35.15547/99.55839/99.79788%**로 68~70쪽 gate에 실패했다. 69쪽에서 표가 선행 본문·캡션 위로 올라오는 것을 직접 확인했다.
- 원 PR의 KoPub 집중 2/2는 PASS지만, PrEP 겹침은 검토 base 14건에서 source head 15건으로 늘었다. baseline 14를 임시 복원한 정확한 검사는 FAIL(exit 101)했다. 원 주석의 p68 건수와 겹침 불변 주장을 정정했고, 기준 상한 15는 미해결로 남겼다. **머지 보류**로 기록하며 표 행 컷·높이 예약·최종 배치를 함께 보정하고 새 head로 재검증한다. 원격 PR 생성·approve·merge와 전체 승인 검증은 진행하지 않는다. 로그는 `output/pr-review/planet6897-7406-20260925/`에만 보관한다.

## planet6897 기여 PR 8건 승인 후보 통합

- `upstream/devel` `b3e3d4e2170a43ca449e3d832440a9274e4e8ee4`에서 `review/approved-planet6897-20260925-v2`를 구성했다. 원 기여 head를 `git cherry-pick -x`로 적용하고 #7409에 중복 포함된 #7397 commit은 한 번만 적용했다. 원 PR·source SHA·체리픽 SHA와 메인터너 보정은 각 [개별 review 문서](../pr/archives/pr_7388_review.md)에 기록했다.
- 개별 판정은 **승인** #7392·#7395·#7399·#7401, **메인터너 보정 후 수용 가능** #7388·#7393·#7397·#7409다. [#7388](../pr/archives/pr_7388_review.md), [#7392](../pr/archives/pr_7392_review.md), [#7393](../pr/archives/pr_7393_review.md), [#7395](../pr/archives/pr_7395_review.md), [#7397](../pr/archives/pr_7397_review.md), [#7399](../pr/archives/pr_7399_review.md), [#7401](../pr/archives/pr_7401_review.md), [#7409](../pr/archives/pr_7409_review.md)의 검토·보정 기록 13개를 통합 branch에 커밋했다. 보류 #7366·#7382·#7384·#7385·#7400·#7404·#7406·#7410의 제품 코드는 이번 후보에서 제외했다.
- 메인터너는 저장 높이 0 도형 복원, 빈 표 뒤 본문·쪽 경계 검사, 비균등 14구역 바탕쪽 회귀와 한컴 2024 PDF 증적, 기준 PDF 비교 도구의 문자 순서·내용 배율 판정을 보정했다. `scripts/visual_sweep.py`의 글꼴 공급과 대형 SVG 래스터 처리도 수정했고 `CLAUDE.md`·`CONTRIBUTING.md`에 **기여자**의 렌더링 PR Visual Sweep 제출 의무를 명시했다. 모든 실행 로그는 Git에서 제외한 `output/`에 보관했다.
- 검증 code head `459d5e581eac979e8a3c69a72e71fb7cc3c9ea88`에서 집중 release-test **42/42 PASS**, 전체 **10,229/10,229 PASS·50 skip**, Native Skia lib **3,930 PASS·13 ignored**, Studio **1,770 PASS·2 skip**, fmt·Native/WASM/workspace Clippy·workspace build·manifest base 비교가 통과했다. fresh WASM 빌드와 브라우저의 저장 높이 **0→400→0→400**도 통과했다. Mac 로컬 WASM은 `--no-opt` 대체 빌드이며 Docker 최적화 빌드 결과로 보고하지 않는다.
- [시각 증적](../pr/assets/approved_planet6897_20260925/evidence.json)의 API 14·15·21·49·60·88쪽과 라틴 72·76쪽은 Native/fresh WASM에서 모두 90% gate를 통과했다. 설치된 Mac 사용자 글꼴 690개를 공급해 한글 glyph를 확인했다. 한컴 2024 원본·왕복 PDF의 3–8쪽 픽셀 대조도 차이 0이었다. 남은 범위와 각 PR의 승인 조건은 개별 review에 적었다.
- 오늘할일과 검토 문서를 포함한 최종 head의 Markdown 링크·diff·현재 base와의 merge tree를 검증한 뒤 통합 PR을 생성한다. GitHub CI·mergeability와 실제 merge SHA는 PR 생성 후 확인한다. 원 contributor PR의 comment·close와 이슈 후속 처리, 소유 `output/` 정리는 실제 통합 merge 뒤 수행한다.

## planet6897 도구 PR #7415·#7417 최신 devel 충돌 보정

- 열린 [#7415](../pr/archives/pr_7415_review.md)·[#7417](../pr/archives/pr_7417_review.md)의 최신 원 head를 확인했다. 두 원 PR은 병합된 #7397/#7409 코드를 stack으로 포함해 GitHub에서 `CONFLICTING/DIRTY`다. `upstream/devel` `97073606de2545f88540429e2f7f8312eefa9641` 위의 `review/planet6897-20260925-tools`에 고유 변경만 `-x` 체리픽했다.
- 메인터너는 병합된 XML 속성 순서 보정을 대칭 줄 구성과 결합했고, PUA·옛한글 자모를 통째로 지워 다른 줄 컷을 같게 만드는 문제를 비교 불가 문단 수 공개로 보정했다. 표의 이웃 칸 PDF/SVG 분리와 다른 저장 컷 반례를 검사했다.
- 통합 code head `7cb91df10`: Python 단위 검사 **10/10 PASS**. 추적된 `issue1853` HWPX/PDF 쌍은 rhwp/정본 저장 컷 **308/349 대 308/349**, `hwpspec` HWP/PDF 쌍은 비교 불가 48문단을 제외하고 **219/261 대 224/261**, 정본만 지킨 컷 5였다. 두 원 PR은 **메인터너 보정 후 수용 가능**으로 기록했고 통합 PR의 최신 CI·mergeability는 생성 뒤 확인한다. 도구만 바꿔 renderer Visual Sweep은 비해당이다.
- 별도 14-PR 누적 검토의 집중 회귀는 **66/66 PASS**지만 다수 렌더링 PR의 시각 게이트가 미달이다. #7411 단독 후보는 71쪽 Native 92.78%로 통과했으나 다음 72쪽은 Native/fresh WASM 2.27%이고 `devel`과 PNG가 정확히 같아 기존 쪽 나눔 차이로 확인했다. 이 보류 변경을 도구 PR 후보에 섞지 않는다.
- GitHub comment·push·merge와 소스 PR 후속 처리는 실제 통합 PR 및 최신 CI가 확인된 뒤 수행한다. 실행 로그와 임시 JSON은 `output/pr-review/planet6897-20260925/`에만 보관한다.

## planet6897 PR #7400 메인터너 개별 보정 후보

- 원 [#7400](../pr/archives/pr_7400_review.md)의 고유 commit을 최신 `upstream/devel` `a7458aa39ca4ac8636a52e4c7a607d55973bf3a6`에 `-x`로 적용하고 이전 메인터너의 표 위 여백·HCR Batang 보정을 현재 코드에 재적용했다. 원 기여자 head를 직접 병합하는 판단과 별도 통합 후보를 구분한다.
- code head `177acf40054f2bbb4bbe65cd9f4267d3df308f8f`에서 집중 6/6, 신규 HWPX 코퍼스 84/84, 전체 release-test 10,235/10,235·50 skip, Native Skia 3종, fmt·Clippy·manifest·workspace build가 통과했다. Mac fresh WASM은 `--no-opt` 로컬 대체 빌드다.
- [한컴 2020 PDF와 1쪽 시각 증적](../pr/assets/pr7400_20260925/evidence.json)은 편집 후 Native/fresh WASM 99.9979%·gate passed, 편집 전 Native 100.0%다. 원본 HWPX는 rhwp 합성 편집 입력임을 구분한다. PR 본문에서 정확한 head SHA 고정 대표 review·overlay 이미지를 실제로 표시해야 한다.
- 원격 CI·mergeability 확인과 #7265 및 원 PR의 후속 comment·close, 소유 output 정리는 통합 PR 병합 뒤 처리한다.

## planet6897 PR #7384 메인터너 개별 통합 후보

- 원 [#7384](../pr/archives/pr_7384_review.md)의 head `f18c56f66043dc9a50e6ded67b4750208fae0573`를 최신 `upstream/devel` `aeb9f489e1d5e297c1e98cf1ca8ff84532270aca` 위에 `-x` 체리픽해 code head `ae6b96567c324f558e9d2cf08812865209f784d1`를 만들었다. 제품 코드의 추가 보정은 없고, 원 PR에 빠진 정확한 head의 대표 review·overlay 증거를 별도 통합 후보에 보완했다. 원 기여자 head 자체의 제출 완료로 판단하지 않는다.
- 물리 4쪽 pi=5 표 조각 하단은 1039.0px, 본문 하단은 1046.9px로 Native/WASM render tree에서 일치했다. [한컴 PDF와 Native/fresh WASM 4·5쪽 비교](../pr/assets/pr7384_20260925/evidence.json)는 각 쪽의 2px 내용 실루엣 97.149%/95.62958% 및 97.14679%/95.60362%, 모두 gate passed다. 대표 review·overlay를 직접 확인했다. Mac WASM은 `--no-opt` 로컬 대체 빌드다.
- 집중 회귀 1/1, 전체 release-test **10,236/10,236 PASS·50 skip**, Native Skia 본체 3,930 PASS·13 ignored, 그림 누락 2/2, 직접 PDF 출력 4/4, fmt·Native/WASM/workspace Clippy·workspace build·manifest base 비교가 통과했다. 새 PR 본문에는 정확한 head SHA 고정 review·overlay 이미지를 표시한다.
- #7206의 물리 8쪽 +4.57px, 한컴 12쪽 대비 rhwp 13쪽, 글자 +58은 남으므로 이 이슈는 닫지 않는다. 원격 CI·mergeability 확인과 원 PR·이슈 comment 및 전용 output 정리는 통합 PR 병합 뒤 처리한다.
