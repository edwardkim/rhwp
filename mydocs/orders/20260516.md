# 오늘 할일 - 2026년 5월 16일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **#928** | fix — HWP3 표 셀 내 inline 사각형 텍스트 중복 + 사각형 폰트 + ㉢ 그림 중복 | **완료** | exam_kor.hwp 5p 다이어그램 한컴 정합. table_layout.rs Shape 가드 + shape_layout.rs ratio 가드 + Picture/Shape 가드 시그널 정합 (inline_shape_position 기반). 사용자 시각 ★. |

## 작업 메모

### Task #928 처리 요약 (5/16)

- **회귀 3건** (이슈 본문 1건 + Stage 4 시각 검증 추가 발견 2건) 모두 해결
  1. Shape 분기 `will_render_inline` 가드 누락 — 텍스트 (`(가) ⇨ ⇨ (나)`) 2회 emit
  2. 인라인 사각형 폰트 축소 (Task #874 #3 ratio 로직 범위 과대)
  3. Picture/Shape 가드 boundary 조건 (`abs_pos == line_chars` 미통과) — ㉢ 그림 중복
- **가드 시그널 정합**: `tac_controls + line_chars` 기반 → `tree.get_inline_shape_position(...).is_some()` 기반. Equation/Table 분기와 동일 패턴 확보.
- **변경 파일**: `src/renderer/layout/table_layout.rs`, `src/renderer/layout/shape_layout.rs` (HWP3 파서 무변경, CLAUDE.md 룰 정합)
- **검증**: `cargo test --release` 1275+ 전체 통과 (svg_snapshot 8/8, hwp-img-001 회귀 차단), 한컴 2022 PDF + 작업지시자 시각 검증 완료
- **worktree**: `rhwp.worktrees/task928`, 브랜치 `local/task928`

### Stage 진행
1. Stage 1 (8c2899e3): 정밀 재현 + 텍스트 중복 ROOT CAUSE
2. Stage 2: 코드 trace — Shape 분기 가드 누락 확정
3. Stage 3 (738d63fd): Shape 분기 가드 추가 (Fix 1)
4. Stage 4: 시각 검증 — 잔존 회귀 2건 발견
5. Stage 5: 잔존 회귀 fix (Fix 2 + Fix 3)
6. Stage 6: 최종 보고서 + 오늘할일

### 다음 작업 (사용자 결정 대기)
- `local/devel` 머지 (별도 승인 필요)
- 이슈 #928 close (별도 승인 필요)
