# 오늘 할일 (2026-05-16)

## 진행 중

(없음)

## 완료

### Task #929 — [Bug] hwp3-sample19.hwp HWP3 파서 파싱 실패 — ✅ 완료

- 마일스톤: v1.0.0 (M100)
- 브랜치: `local/task929`
- 워크트리: `/Users/tsjang/Cloud/Devel/rhwp.worktrees/task929`
- 원인: `src/parser/hwp3/mod.rs` 의 char loop `9 =>` 분기가 HWP3 spec §10.5 표 39 (탭 = 8 bytes / 4 hchar) 를 미준수 → cursor 6 bytes 일찍 진행 → 탭 내부 hunit 값이 ch=17 (각주) 로 잘못 해석 → 재귀 paragraph_list → garbage → EOF.
- 수정: spec 정합 처리 (추가 6 bytes read + i += 3).
- 검증: issue_929 회귀 가드 2 pass + 전체 cargo test FAILED 0 + 모든 hwp3 dump 정상.
- 단계: 사전(ece18fec) → Stage 1(11718e80) → Stage 2(a372069b) → Stage 3(e2de8ad8) → Stage 4 생략 → Stage 5 (최종 보고서).
- 보고서: [mydocs/report/task_m100_929_report.md](../report/task_m100_929_report.md)
- 머지/푸시 대기 중 (작업지시자 승인 필요).

## 후속 과제 (별 이슈 등록 후보)

- `src/wasm_api/tests.rs:11467` 의 unused Result 51 호출 — `cargo clippy --all-targets -- -D warnings` 차단 (local/devel 기존 결함).
- HWP3 ↔ HWPX `ir-diff` 의 `char_offsets`/`char_shapes` 차이 (IR 표현 차).
- HWP3 char loop 의 다른 컨트롤 spec 정합도 점검 (ch=9 와 동일 패턴 결함 가능성).
