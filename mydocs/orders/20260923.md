# 2026-09-23 오늘할일

## PR #7348 — typeset 책임 분리와 기여자 조판 규칙 관리

- [PR #7348](https://github.com/edwardkim/rhwp/pull/7348)은 작업지시자의 제출 승인으로
  `task_m100_7280`에서 `devel` 대상으로 생성했다. 관련 Issue는 #7280이다.
- 고정 devel `1966af77f`를 흡수한 제품의 전체 회귀 10,137 PASS / 실패 0 / 기존 제외 50,
  Native Skia 3종과 Native/fresh Docker WASM 전후 대조가 통과했다. 첫 제출 head `b77a0d692`의
  필수 lint·workspace build·고정 base 정책 검사도 모두 재실행해 통과했다.
- `c08b4fc3`의 CI(Archive A–D, lint, Native Skia), Render Diff, CodeQL, Impact Policy가 통과했다.
- [작성자 self-review](../pr/archives/pr_7348_review.md)를 완료했다. 검토 범위에서 새 제품 결함이나
  devel 변경 반영 누락을 발견하지 않았다. 제품 코드는 기존 검증본과 동일하므로 이미 통과한
  검증을 재사용한다. 중복 시작한 focused 재빌드는 중단했고 추가 테스트는 실행하지 않는다.
- 후속 절차 승인으로 진단 라벨 잘림을 [#7349](https://github.com/edwardkim/rhwp/issues/7349)에
  분리하고 self-review 판정을 **승인**으로 정리했다. 원본 증적 PNG와 제품은 변경하지 않았다.
- review/오늘할일/asset 안내의 문서-only 후속 commit을 같은 PR에 반영한다. 녹색 candidate는
  `c08b4fc3`이며 실제 CI 재사용 여부와 후속 head required check를 push 후 확인한다.
- 남은 절차는 후속 head CI 확인과 작업지시자 merge 승인이다.
- merge·GitHub approve·이슈 종료·댓글·다른 worktree 정리는 이번 승인 범위가 아니다.
