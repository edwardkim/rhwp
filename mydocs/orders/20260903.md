# 2026-09-03 오늘할일

## 진행 중

- #6628 Gym 정상화를 최신 `upstream/devel@900b56edc` 위에서 재개했다.
- 기존 `gym/README.md`와 개별 `gym/docs/*.md`에는 인간 메인테이너의 전수 실행을 한 흐름으로
  묶은 운영 정본과 `gym/**` 범위 AI 에이전트 지침이 없음을 확인했다.
- 수동 운영 매뉴얼과 역할 분리형 에이전트 지침을 작성했고, 문서 진입점·도구 규약 현행화와
  링크·metadata·구조 감사·oracle 계약 검증을 완료했다. #6628의 잔여 Stage 5·6은 계속 진행한다.
- JSON 증적의 인간 판독용 시각화는 후속 이슈 #6669로 분리했다. 시각화가 JSON 정본을
  대체하거나 제품 릴리스 게이트가 되지 않는 범위를 이슈 본문에 고정했다.
- #6628에서 분리한 제품 결함 #6641과 증적 시각화 #6669를 GitHub sub-issue로 연결하고
  parent/child 양방향 API 결과를 확인했다. 후속 이슈는 부모 작업군과 함께 관리해 추적
  분산을 줄인다.
- #6641 PR #6673 병합·CI 성공을 확인하고 #6628 브랜치를 최신
  `upstream/devel@edeaeb289`과 충돌 없이 동기화했다. #6641에서 이미 실행한 exact runner·제품
  후보 전수 증적과 현재 실행 의미 동일성을 대사했으며, Stage 4는 우회 없이 완료 판정했다.
- #6628 핵심 정상화 구현의 Stage 6 제출 감사를 진행 중이다. 이 구현을 먼저 devel에 통합하되
  부모 이슈는 닫지 않고, 후속 #6669를 최신 devel의 독립 브랜치에서 완료한 뒤 부모 최종
  게이트를 정산한다.
- #6628 핵심 구현 PR #6677을 Open으로 생성했다. 첫 code candidate의 Full CI가 실행되는 동안
  self-review에서 trajectory timeout 분류가 카탈로그 밖이라 `unexpected`로 접히는 불일치를
  발견해 로컬 보정했다. 최종 예외 행과 타입-카탈로그 동기화 회귀를 추가하고 focused·exact Gym
  계약을 다시 통과했으며, 새 code head push 전 승인 게이트를 유지한다.
- 승인된 정정 head `3eb2e0d5a`를 PR #6677에 push하고 본문을 현행화했다. 새 후보의 Full CI는
  성공 33개, 실패·대기 0개로 완료됐으며 작성자 self-review를
  `mydocs/pr/archives/pr_6677_review.md`에 `승인`으로 기록했다. review·오늘할일만 추가하는 trailing
  commit의 원격 push와 review-only 재검증은 별도 승인 게이트로 남긴다.
- 핵심 PR #6677 병합 뒤 마지막 sub-issue #6669를 독립 브랜치에서 구현해 PR #6686을 생성했다.
  code candidate `c5efd5240`은 GitHub check 성공 29개, skip 4개, 실패·대기 0개이며 작성자
  self-review 판정은 `승인`이다.
- self-review 시점 최신 `upstream/devel@bd72886c0`의 PR #6591 변경과 경로 중첩이 없고 exact
  merge-tree가 성공했다. review·오늘할일·부모 #6628 보고서만 추가하는 후행 commit의 원격 push와
  review-only 재검증은 별도 승인 게이트로 남긴다.

## 완료

- jeong-sik 원 PR #6642, #6650, #6652의 최신 head를 `upstream/devel` 위에 provenance-preserving cherry-pick했다.
- 전체 integration nextest, Rust manifest/unit-tier gate, Clippy, native Skia CLI, WASM 웹 패키지 빌드를 실행했다.
- 실제 HWP와 저장소 정본 PDF의 대상 쪽을 비교한 시각 증적 및 PR별 검토 기록을 trailing commit으로 남겼다.

## 제외

- #6647은 기여자가 CLOSED 처리했고 동등한 figure-space 변경이 이미 `devel`에 있으므로 통합 대상에서 제외했다.

## 다음 단계

- 이 trailing commit을 포함한 통합 PR을 명시적 승인 뒤 생성한다.
- 원 PR #6642, #6650, #6652의 close/comment는 통합 PR 병합 및 devel CI 성공 후 `post_merge.md`에 따라 한 번만 처리한다.

## #6641 필드 편집 후 소유 문단 재조판

- `task_m100_6641`에 최신 `upstream/devel@900b56edc`를 정상 merge한 `7f1174f1d`를 검증 후보로 고정했다.
- 기존 field focused 56건과 새 devel layout 4건을 합친 60/60, Rust 필수 lint, unit-tier,
  전체 integration 8,973/8,973이 통과했다.
- exact candidate 바이너리로 BO05·BO15 2/2, Gym positive 1,035/1,035,
  discrimination false-pass 0, trajectory 239/239 load-bearing을 재확인했다.
- Gym 검증 전용 detached worktree와 재생성 가능한 임시 제출물 약 344MB는 검증 뒤 제거했다.
- 이 현행화 기록은 같은 task branch의 후행 문서 commit에 포함한다. 이후 별도 승인으로
  원격 push·PR 절차에 진입한다.
- PR #6673을 `devel` 대상으로 생성했고 최초 head `362e912a6f`의 CI·CodeQL·Proptest·Adapter
  inter-diff가 모두 통과했다. 최신 base merge tree도 head tree와 일치했다.
- 작성자 self-review는 `mydocs/pr/archives/pr_6673_review.md`에 `승인`으로 기록했다. 이 기록만
  추가하는 후행 head의 review-only checks와 mergeability 확인, 사용자 merge 승인은 남아 있다.

## PR #6591 근소 초과 TAC 표 본문 폭 보정 검토

- 외부 기여자 davindev의 PR #6591 source head `2050166436301f7dfed5a796d5742601f78b85d7`을
  최신 `upstream/devel` 위에서 merge simulation했고 충돌 없이 적용됐다.
- renderer normalization lint, manifest, full integration nextest 8,974/8,974, native·WASM Clippy,
  WASM web package를 통과했다. PR CI·CodeQL·Render Diff·Native Skia·Adapter·Proptest도 성공했고,
  GitHub WASM Build는 정책상 skip이라 로컬 WASM package 성공으로 보완했다.
- `BlogForm_BookReview.hwp`는 Hancom 2020 기준 PDF와 직접 비교해 top-level TAC 표가 본문 bbox
  안에 맞음을 확인했다. issue2559 baseline 증가 항목은 같은 방식으로 21쪽을 검토해 실제 표 경계
  충돌이 없음을 확인했다. 기준 PDF와 stable PNG 증적은 `pdf/` 및
  `mydocs/pr/assets/pr_6591_nearfit_tac_table_20260903/`에 포함했다.
- reviewer `jangster77`를 지정했고 판정은 `승인`이었다. PR #6591은 일반 merge commit
  `bd72886c02d301ff796b6b5c55a452a870cf317a`으로 병합됐고, merge SHA의 CI·CodeQL·Adapter·Proptest·Close
  Issues가 모두 success다.
- 상세 판정과 hash·명령·시각 증적 범위는 `mydocs/pr/archives/pr_6591_review.md`에 보존한다. 이 archive
  review, 오늘할일, 기준 PDF, stable PNG만 담는 옵션 B 문서 PR을 별도로 생성하며, 그 병합과 devel CI
  성공 뒤에만 원 contributor PR comment 및 #6590 후속 상태를 `post_merge.md`에 따라 한 번 처리한다.

## jeong-sik 그림 위치 보정 통합 검토

- 원 PR #6659(`c1a5396df78e14eaa4836a9bb26c202409042cc1`), #6661(`847810448e3569fef1e28466ec776361682445e1`),
  #6663(`241335219626a946c0c25805ed069509bdedae18`), #6664(`ff9b24a75e1fbcc25731643ffd7fdf38b5de6e4b`)에
  reviewer `jangster77`가 사전 지정된 것을 확인하고, `upstream/devel@eb2ea3add` 위에
  provenance-preserving cherry-pick했다.
- #6655 보정의 가로 오프셋이 일반 표 stack까지 번지는 문제는 synthetic 재분류 줄에 구현 태그를 붙이고
  해당 stack에만 오프셋을 적용하는 메인터너 보정 `c89a7bf56`으로 범위를 한정했다.
- Rust 필수 format·native/WASM/workspace Clippy와 manifest/unit-tier, focused #6653/#6655/#6656,
  전체 integration nextest 8,977/8,977, Native Skia, 직접 WASM package build를 통과했다.
- `hwpctl_ParameterSetID_Item_v1.2.hwp`, `exam_math.hwp`,
  `issue2004_cell_image_stack.hwpx`, `hwpx_sample2.hwp`의 대상 페이지를 저장소 기준 PDF와
  비교했다. 대표 합성 PNG와 지표·사람 판정은
  `mydocs/pr/archives/pr_6659_6664_jeong_sik_visual_sweep.md` 및
  `mydocs/pr/assets/pr_6659_6664_jeong_sik_integration_20260903/`에 보존했다.
- PR별 archive review는 #6659, #6661, #6663, #6664 모두 `메인터너 보정 됨 수용 가능`으로
  기록했다. #6663은 working 문서 전용이므로 별도 시각 fixture 검증 비대상이다.
- 통합 PR #6691은 #6653, #6655, #6656을 종료 대상으로 연결하고 진행 현황 원장 #6662는
  참조만 한다. 원 PR comment/close와 이슈 후속 처리는 통합 PR 병합 및 devel CI 성공 뒤
  `post_merge.md`에 따라 한 번만 수행한다.

## #6695 PDF 오라클 단일 경로·Git LFS 폐지

- `pdf/`, `pdf-2020/`, `pdf-large/`의 실제 파일·byte·Git/LFS 상태와 활성 소비자를 조사했다.
- 같은 목적의 열린 이슈가 없고, 세 경로 편입을 다룬 종료 이슈 #5345와 목적이 다름을 확인한 뒤
  #6695를 등록해 `edwardkim` 담당, `v1.0.0`, `documentation`·`ci`로 트리야지했다.
- 최신 `upstream/devel@2b2646f40`에 로컬 `devel`을 fast-forward하고
  `task_m100_6695_pdf_oracle_consolidation` 브랜치를 만들었다.
- 작업트리에 pointer만 남은 `pdf-large/hwpx/issue_1133.pdf`의 실제 110,523-byte PDF가 로컬 LFS
  object store에 있고 선언 SHA-256과 일치함을 확인했다. 네트워크 재다운로드 없는 복원이 가능하다.
- 공개 이력을 재작성하지 않고 현재 PDF를 `pdf/**` 일반 Git blob으로 통합하는 수행계획을 작성했다.
  계획과 50 MiB 파일 상한 승인 전에는 자산 이동·LFS rule 변경을 시작하지 않는다.
- 메인테이너가 수행계획과 Stage 1 진입을 승인했다. 19개 전환 대상의 old/new 경로·SHA-256을
  동결하고, 17개 LFS pointer의 OID·선언 크기·실제 object·PDF magic을 전건 검산했다.
- 복원 불가능 object와 hash 불일치 충돌은 0건이다. 기존 `pdf/`와 byte-identical한 2건은 목적지를
  보존하고 source만 제거하며, 신규 17건을 더해 최종 `pdf/**` 1,178개가 되는 전환 원장을
  `mydocs/working/task_m100_6695_stage1.md`에 기록했다.
- Stage 1 결과와 Stage 2 진입 승인을 받아 19개 오라클을 `pdf/**`로 단일화했다. LFS pointer였던
  17개는 로컬 실제 object와 원장 SHA-256을 대조해 일반 Git blob으로 stage했고, 동일 중복 2개는
  기존 목적지를 보존했다.
- `.gitattributes`의 PDF LFS rule을 제거하고 `pdf/README.md`·`CONTRIBUTING.md`에 단일 경로,
  50 MiB 미만, 비-LFS 정책을 명시했다. 작업트리·Git index pointer, LFS attribute, 크기·magic,
  폐기 경로를 검사하는 정책 검사기와 7개 단위 테스트를 추가했다.
- Stage 3 승인 뒤 workflow·CI classifier·Oracle Public·LLM verifier와 현행 문서를 `pdf/**`
  단일 root로 맞췄다. 파일명에 버전이 없는 오라클 110개는 디렉터리에서 버전을 추론하지 않고
  `unknown`으로 보존했다.
- 최신 `upstream/devel@b6b9384ed`의 16개 선행 commit을 일반 merge했다. 충돌은 이 일일 기록
  하나였으며 #6695와 humdrum00001010 통합 기록을 모두 보존했다.
- LFS object store가 없는 독립 clone에서 PDF 1,178개·1,079,000,023 bytes와 Stage 1 원장 19개
  SHA-256을 전건 검증했다. CI 정책, Oracle 139건, LLM 122,400행, 문서 링크와 전체
  native/WASM/workspace Rust gate도 병합 후 다시 통과했다.

## humdrum00001010 green source PR 통합 검토

- 적용 경로: `collaborator_external_pr`, `intake_and_review`, `local_validation`, `visual_fixture_evidence`, `multi_pr_update_branch`를 기준으로 최신 `upstream/devel@867d282304c7830750241a2add4122efd39539e7` 위에 `review/humdrum00001010-green-20260903`을 만들었다.
- CI green source만 provenance-preserving `-x`로 누적했다. #6666 head `7408d6adb33bdab974bf52e5969e72b48f9d81b7`의 9개 commit과 #6668 head `295d1c29e69f5e4088bbbe25c12ece15997e0a47`의 3개 commit을 적용했다. #6685는 Draft이며 CI failure가 있어 대상에서 명시적으로 제외했다.
- #6666의 새 bounds test가 최신 devel의 #6655 horizontal-offset 계약보다 이전 `82.4` x를 고정해 통합 검증에서만 실패했다. maintainer correction `17c79013b0be7d52be75abac1a8fcafdc00f2878`은 HWP/HWPX page 5~8 기대값을 현행 계약 `103.5`, `110.3`, `103.1`, `101.5`로 바꾼 test-only 보정이며 runtime source, fixture, golden PDF를 바꾸지 않았다.
- 로컬 검증: #6666 focused 2 passed, #6668 focused 3 passed, Studio `npm test` 1373 passed/1 skipped, `npm run build` 성공, Rust format/native-WASM-workspace Clippy/workspace build/manifest/unit-tier gate 성공, full integration `cargo nextest run --locked --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 10 --no-fail-fast` 8,986 passed/46 skipped/실패 0.
- 시각 증적: #6666은 기준 PDF와 HWP/HWPX page 4~8을 비교해 각 5 page complete, 후보 0건을 확인했다. #6668은 `samples/issue-617/exam-kor` page 5의 layer와 explicit legacy raster absolute error `0`을 확인했다. stable asset과 사람 검토 범위는 `mydocs/pr/archives/pr_6666_6668_humdrum00001010_visual_sweep.md` 및 `mydocs/pr/assets/pr_6666_6668_humdrum00001010_20260903/`에 보존한다.
- PR 생성 뒤 source review archive의 최종 판정은 #6666 `메인터너 보정 후 수용 가능`, #6668 `승인`으로 유지한다. 통합 PR의 최종 CI와 merge SHA devel CI가 모두 성공하기 전에는 원 PR/issue comment, close, merge를 하지 않는다. 성공 뒤에는 source review의 `Merge 후 contributor PR comment 계획`에 적은 stable raw asset 링크를 UTF-8 body-file comment에 직접 넣고 API 재조회 후 `post_merge.md`에 따라 한 번만 처리한다.
