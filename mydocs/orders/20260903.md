# 2026-09-03 오늘할일

## 완료

- jeong-sik 원 PR #6642, #6650, #6652의 최신 head를 `upstream/devel` 위에 provenance-preserving cherry-pick했다.
- 전체 integration nextest, Rust manifest/unit-tier gate, Clippy, native Skia CLI, WASM 웹 패키지 빌드를 실행했다.
- 실제 HWP와 저장소 정본 PDF의 대상 쪽을 비교한 시각 증적 및 PR별 검토 기록을 trailing commit으로 남겼다.

## 제외

- #6647은 기여자가 CLOSED 처리했고 동등한 figure-space 변경이 이미 `devel`에 있으므로 통합 대상에서 제외했다.

## 다음 단계

- 이 trailing commit을 포함한 통합 PR을 명시적 승인 뒤 생성한다.
- 원 PR #6642, #6650, #6652의 close/comment는 통합 PR 병합 및 devel CI 성공 후 `post_merge.md`에 따라 한 번만 처리한다.

## #6641 필드 편집 후 소유 문단 재조판

- `task_m100_6641`에 최신 `upstream/devel@900b56edc`를 정상 merge한 `7f1174f1d`를 검증 후보로 고정했다.
- 기존 field focused 56건과 새 devel layout 4건을 합친 60/60, Rust 필수 lint, unit-tier,
  전체 integration 8,973/8,973이 통과했다.
- exact candidate 바이너리로 BO05·BO15 2/2, Gym positive 1,035/1,035,
  discrimination false-pass 0, trajectory 239/239 load-bearing을 재확인했다.
- Gym 검증 전용 detached worktree와 재생성 가능한 임시 제출물 약 344MB는 검증 뒤 제거했다.
- 이 현행화 기록은 같은 task branch의 후행 문서 commit에 포함한다. 이후 별도 승인으로
  원격 push·PR 절차에 진입한다.
