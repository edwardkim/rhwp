---
kind: daily-order
status: active
date: 2026-08-21
---

# 2026-08-21 오늘할 일

## #4962 W3 실제 renderer 기반 font metric coverage 재계측

- 상위 로드맵 [#4960](https://github.com/edwardkim/rhwp/issues/4960)의 선행 작업인 W0·W1
  [#4939](https://github.com/edwardkim/rhwp/issues/4939)와 W2
  [#4961](https://github.com/edwardkim/rhwp/issues/4961)이 `devel`에 반영된 사실을 확인했다.
- 작업 기준은 `upstream/devel@fb434269eea237cc12053914560a2dbaf16270bf`이며, 로컬 `devel`,
  `origin/devel`, `upstream/devel`은 착수 시점에 일치했다. 작업 브랜치는
  `task_m100_4962`다.
- 기존 로컬 POC에는 전체·HWP·HWPX aggregate와 생성 source가 남아 있다. 실제 사용 font, 언어 slot,
  bold·italic, 장평·자간, 커닝, 문맥, 정렬, stored LineSeg 존재 여부, 문서·문단·run·문자 수와 고정
  frame 위험 통계는 새로 만들지 않고 W3 기준선으로 재사용한다.
- 기존 `metric mapped 91.16%`는 `layout_metric_face_name()`을 원시 font 이름에 직접 적용한 face-level
  수치다. 기존 aggregate에는 code point, `altType`, `substFont`, renderer 정규화 계보, 문자별 hit/miss,
  overlay·surrogate relation이 없어 W3의 7개 분류를 역산할 수 없다.
- 따라서 W3의 전수 작업 후보는 기존 계측 전체를 반복하는 것이 아니라, 기존 usage key와 합계에
  문자별 renderer decision만 보강하는 조건부 delta pass다. fresh layout과 backend snapshot은 별도
  cohort로 두며, 전수 적용 필요성과 비용은 pilot 결과를 보고 다시 승인받는다.
- W2 trace는 단일 page, 기본 1,024자·최대 4,096자이고 offset이 없다. raw trace를 page별로 저장하는
  방식 대신 기존 POC walker와 W2의 공통 decision을 결합한 streaming aggregate 경로를 검토한다.
- 로컬 private corpus의 10,000개 HWP/HWPX는 읽기 전용 입력이다.
  원문·본문·식별 파일 목록·절대 경로·raw trace는 저장소에 게시하지 않고 aggregate·비식별 결과와
  결정론적 aggregate hash만 남긴다.
- 메인테이너가 [수행계획서](../plans/task_m100_4962.md)를 승인했다. Stage 1에서는 기존 자산 동결,
  분류·분모·privacy 계약과 RED test까지만 수행한다. 별도 승인 전에는 private corpus 전수 실행을
  시작하지 않으며, W3 결과 승인 전에는 같은 이슈의 W4 조판 위험 순위 단계로 넘어가지 않는다.
- [Stage 1 기록](../working/task_m100_4962_stage1.md)을 완료했다. 기존 POC를 재생성하지 않고 비식별
  projection과 HWP+HWPX 가산성을 검증했으며, W1의 1,352개 candidate identity는 의미 변화 없이
  20개 boundary의 digest만 drift한 것으로 판정했다. 7개 분류·독립 분모·privacy·hash 계약 test는
  10/10 통과했다. Stage 2는 메인테이너 승인 대기다.

## Review-only 후보 실행 재사용

- [#5834](https://github.com/edwardkim/rhwp/issues/5834)의 #5832 재현을 수정했다. `af9ea69`처럼 review 문서와
  신규 PDF 증적을 담았지만 Full CI가 성공한 head도 Adapter inter-diff·Proptest roundtrip의 후보로 다시
  시도한다. 현재 head의 진행 중 run이나 없는 run은 더 이전 후보를 확인하고, 완료 실패·identity 불일치는
  fail-closed로 worker를 실행한다.
- [#5835](https://github.com/edwardkim/rhwp/pull/5835)의 code candidate `7ed4296`에서 workflow contract
  171건, Python compile, `cargo fmt --all -- --check`, `git diff --check`를 완료했다. Rust·renderer 변경은
  없으므로 전체 Cargo 회귀는 적용 대상이 아니다.
- workflow 변경 PR이므로 review·오늘할일 trailing commit도 fast-pass로 취급하지 않는다. 최신 head의 Full CI,
  CodeQL, Adapter inter-diff, Proptest roundtrip과 mergeability를 확인한 뒤 작업지시자 승인으로 merge한다.

## CI 통과 외부 PR 통합 검토

- 최신 `upstream/devel@fb434269e` 위 `review/green-ci-20260821`에서 CI 성공·non-draft·mergeable 조건을
  확인한 #5786, #5796, #5805, #5806, #5810을 오래된 번호 순으로 누적 적용했다. code candidate는
  `05fd35449`이며, source head와 적용 SHA, 개별 판정은 archive review 문서에 남겼다.
- #5781은 #5786의 #5780 해결과 중복되므로 직접 적용하지 않는다. #5794는 source CI 실패로 제외했다.
- #5806의 nested `hp:ole` dispatch 누락은 groupLevel 보존 계약을 완성하는 메인터너 보정으로
  `270d28ffd`에 반영했다. CI가 관리하는 generated suite/manifest drift는 수정하지 않는다.
- 로컬 full nextest **8,059 passed, 5 slow, 39 skipped**, native-Skia, fmt, clippy, WASM build,
  Studio unit/build/E2E를 완료했다. #5786의 HWP 2020 PDF p1 visual sweep은 candidate 0, pixel match
  98.5481%, proxy 98.50499%이며 PDF와 PNG asset을 보존했다.
- code candidate의 GitHub Build & Test, archive shard, Lint, Native Skia, Render Diff, Proptest,
  Adapter inter-diff, CodeQL Rust/JavaScript/Python이 모두 성공했다. 이 trailing 문서 commit의
  review-only fast-pass와 최신 mergeability를 확인한 뒤 통합 PR을 merge한다.
- merge 뒤에는 devel 포함과 PDF/asset 경로를 검증한 후 source PR #5781/#5786/#5796/#5805/#5806/#5810,
  관련 issue의 실제 close 상태를 확인하고 근거 comment를 남긴다. #3315는 provenance 개선만 포함하므로
  계속 열린 상태로 유지한다. `post_merge.md`에 따라 이번 integration branch만 정리하며
  `target/pr-review`와 다른 작업 worktree는 보존한다.

## CI 통과 PR #5812-#5815 통합 검토

- 최신 `upstream/devel@7df17a0ca` 위 `review/green-ci-20260821-r2`에서 #5812, #5813, #5814,
  #5815의 CI 성공·non-draft·`CLEAN` source head를 오래된 번호 순으로 누적 적용했다. code candidate는
  `af9ea69e5`이며, 원 PR별 적용 SHA와 판정은 `mydocs/pr/archives/pr_5812_review.md`부터
  `pr_5815_review.md`까지에 보존했다.
- GitHub code candidate CI는 Build & Test, Lint, Native Skia, archive builder/shard, front package,
  CodeQL Rust/JavaScript/Python, Canvas visual diff, Proptest roundtrip, Adapter inter-diff를 모두
  성공으로 완료했다. `WASM Build`와 front unit gate는 변경 범위 정책에 따른 skip이다.
- 로컬에서는 전체 nextest **8,068 passed, 39 skipped**, focused Rust cases, Studio unit/build, WASM build,
  native-Skia, native/WASM clippy 및 format을 완료했다. Hancom 2020 기준 PDF와 visual sweep PNG는
  `pdf/pr_5813`, `pdf/pr_5814`, `pdf/pr_5815`, `mydocs/pr/assets/`에 보존했다.
- 이 trailing 오늘할일 commit은 review-only 허용 범위만 포함한다. 최신 head의 preflight와 aggregate가
  fast-pass로 성공하고 `MERGEABLE/CLEAN`을 재확인한 뒤 #5832를 merge한다.
- merge 뒤에는 `post_merge.md`에 따라 devel 포함을 확인하고, 원 PR #5812/#5813/#5814/#5815와 관련
  issue의 close 상태 및 검토 결과 comment를 처리한다. 이번 integration branch만 정리하며
  `target/pr-review`와 다른 작업 worktree는 보존한다.
