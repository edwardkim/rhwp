---
kind: daily-order
status: active
date: 2026-08-26
---

# 오늘 할일 - 2026년 8월 26일

## Open PR CI-complete 통합 체리픽 검토

- [x] 최신 `upstream/devel@ee7e8a6ed0c4784bea882a30dc94de49c42f6bad` 기준으로
  `review/open-ci-complete-20260826` 브랜치를 만들었다.
- [x] draft PR(#5953, #6059)은 제외하고, CI 완료된 non-draft open PR #6019, #6056, #6068, #6070,
  #6071, #6074를 누적 적용했다.
- [x] #6071의 중간 upstream/devel merge commit은 통합 절차에 따라 제외했다.
- [x] #6019는 review `CHANGES_REQUESTED` 상태라 보호 문서 autosave debounce·상태바 복원·비활성화
  알림 누수를 메인터너 보정으로 처리했다.
- [x] #6056은 CI 실패와 review `CHANGES_REQUESTED` 상태라 기존 #2792 계약 갱신, dry-run/실행 순회
  정합, 글상자 안 표 direct search, Studio 타입 누락을 메인터너 보정으로 처리했다.
- [x] #6068은 원 PR 최신 head의 후속 commit으로 Lint/Archive B 실패가 해소됐음을 확인했다.
- [x] #6070, #6071은 최신 원 PR CI 녹색과 검토 문서 기준으로 수용 가능하다고 판단했다.
- [x] #6074는 최신 원 PR CI 녹색과 #6030 focused 회귀·IR sweep baseline 재검증 기준으로 수용
  가능하다고 판단했다.
- [x] 개별 review 문서 `pr_6019_review.md`, `pr_6056_review.md`, `pr_6068_review.md`,
  `pr_6070_review.md`, 기존 `pr_6071_review.md`, `pr_6074_review.md`와 통합 구현 기록을 준비했다.
- [x] focused 검증: #2792 suite 10 pass, replace occurrence 4 pass, autosave 14 pass, Studio build,
  Cargo test compile, fmt 통과.
- [x] 전체 Rust nextest 1차에서 #4138 쪽수 핀 2개가 196 기대/197 실제로 실패했다. 한글 2024
  오라클 197쪽으로 돌아온 정합 개선이므로 메인터너 보정으로 테스트 기대값을 갱신했다.
- [x] 보정 후 focused #4138 재검증 2 pass, 전체 Rust nextest 2차 8,356 pass, 43 skip,
  208.954s.
- [x] #6074 추가 후 focused #6030 회귀 1 pass, IR sweep baseline 1 pass 및 정렬 diff 차이 없음.
- [x] #6074 포함 최종 전체 Rust nextest 8,364 pass, 43 skip, 208.190s.
- [ ] 최신 `upstream/devel` 동기화·rebase 후 통합 PR을 생성한다.
- [ ] PR CI 완료 후 merge, 원 PR/issue comment와 close, branch/worktree 정리를 `post_merge.md`에
  따라 수행한다.

## Issue #4967 - W8 rank 1 font face 교정 qualification

- [x] 전날 승인된 W8-R1-Q1 결과를 `9fc8110d3` local stage commit으로 고정하고 Q2에 진입했다.
- [x] 현행 generated `MBatang` entry와 exact `MT.TTF hmtx`를 source에서 직접 대사하고 Q1의 current
  transform을 재생했다.
- [x] 공개 fixture 1,556건, 장평·자간·justification 13축, fixed-frame 6축에서 current→virtual relation과
  virtual→exact advance·첫 crossing delta가 모두 0임을 확인했다.
- [x] 전체 layout-bearing domain의 base advance 동치를 증명해 Q0의 private 22문서 재parse가 불필요함을
  확인했다. 10k 전수·Hyper-V·제품 mutation은 0이다.
- [x] layout 이득이 없고 historical generated entry의 exact source 계보도 unknown이므로 rank 1을
  `no-change`로 판정했다. W8-R1-Q3에는 진입하지 않는다.
- [x] W8-R1-Q2 결과를 `e70e35ec3` local stage commit으로 고정하고 rank 1 최종 disposition 절차를 시작했다.
- [x] W4 miss가 runtime 실제 경계라 lineage 정정 대상이 아니지만, Q2에서 layout delta가 0이라 제품
  correction 이득도 없음을 분리해 rank 1 최종 상태를 `no-change`로 확정했다.
- [x] qualified 전용 product-correction 자식 이슈·registry operation·acceptance matrix는 만들지 않고,
  Q3·Q4도 미진입으로 종결했다. #4967은 rank 7과 evidence-reopen lane 때문에 OPEN을 유지한다.
- [x] 최종 audit에서 Q0 cohort의 bold 38,090자를 발견해 style-domain 누락 가능성을 재검토했다. 현행
  `boldFallback`은 regular metric advance를 그대로 쓰며 layout 폭을 바꾸지 않음을 source·계약 테스트로
  고정해 `no-change` 결론을 유지했다.
- [x] W8-R1-Q5 최종 보고서를 승인받아 local stage commit으로 고정했다.
- [x] 51커밋 전진한 `upstream/devel@ee7e8a6ed`를 merge commit `e10dd2258`로 통합하고, 변경된
  `text_measurement` 입력 때문에 네이티브·Docker WASM과 Q1·Q2를 재계측했다.
- [x] Q1 native/WASM parity와 `layout-name` 경계, Q2 advance delta 0·`no-change` 판정이 유지됐으며
  Node 8/8·Python 12/12 계약 테스트가 통과했다. canonical 증적과 보고서를 최신 입력 해시로 갱신했다.
- [x] PR 생성 직전 24커밋 전진한 `upstream/devel@6240d255b`를 merge commit `1252c5e5b`로 통합하고,
  오늘할일 add/add 충돌은 원격 통합 검토 절과 #4967 절을 모두 보존해 해결했다.
- [x] 최신 renderer 변경을 포함해 네이티브·Docker WASM과 Q1·Q2를 다시 계측했다. 각 형식 1,556건의
  byte parity와 두 advance delta 0, 최종 `no-change` 판정이 유지됐다.
- [x] code candidate `b83d9d08e`를 PR #6081로 제출했고 Full CI·CodeQL·Proptest·Adapter inter-diff가
  모두 성공했다.
- [x] self-review 직전 최신 `upstream/devel@35c270f47`을 확인했다. 전진한 2커밋은 시각 검증 절차 문서
  3개뿐이며, code candidate와의 `merge-tree`는 충돌 없이
  `55c6b3d48cbf2da2dace6cd5ee8ca48063ef334f`를 생성했다.
- [x] 최신 시각 검증 필수 조건을 적용했다. PR은 HWP trace fixture를 추가하지만 제품
  renderer·layout·typeset·paint 경로와 사용자-visible 개선 주장을 포함하지 않으므로 직접 visual sweep
  필수 조합이 아니다.
- [x] PR #6081 self-review 문서와 `mydocs/` 한정 single-parent trailing 상태 정정을 준비했다.
- [x] trailing 기록 commit·push 뒤 review-only fast-pass required aggregate와 최신
  `MERGEABLE/CLEAN`을 확인했다.
- [x] PR #6081을 normal merge commit 방식으로 병합하고 merge commit의 CI·CodeQL·Proptest·Adapter
  inter-diff 성공, 이슈·PR 후속 comment와 local/remote task branch 정리를 완료했다.

## Issue #4967 - W8 rank 7 font face 교정 qualification

- [x] PR #6081 merge commit `70ebacc4c`와 최신 `upstream/devel`이 일치하고 작업트리가 깨끗함을 확인했다.
- [x] 최신 기준에서 `task_m100_4967_v3` 브랜치를 만들었다.
- [x] W4 rank 7 aggregate, W5 exact/subst/missing ladder, exact KoPubWorld Dotum Light TTF,
  W7.5 registry·projection의 기존 증적을 교차 확인했다.
- [x] 10k·Hyper-V 전수 재실행을 금지하고 기존 journal의 bounded 재사용, exact TTF와 CDN OTF·WOFF2의
  metric identity 분리, 후보 생존 시에만 private cohort·시각 검증에 진입하는 수행계획을 작성했다.
- [x] 수행계획 승인 뒤 Stage W8-R7-Q0을 시작했다.
- [x] 기존 10,000-entry W3 journal을 원문 재parse 없이 1회 streaming해 rank 7 cohort 5문서
  (HWP 3, HWPX 2)를 재선정하고 W4 위험 63,732자와 전 항목이 일치함을 확인했다.
- [x] target 63,858자 중 table-cell 52,149자, compressed 63,082자, ratio 95·spacing -9 51,226자,
  bold 4,468자라는 실제 편집 축을 고정했다. 위험 문자는 전부 stored lane이다.
- [x] exact TTF의 hash·localized/English/PostScript name·cmap·hmtx·fsType을 재확인하고, 현행에는
  supply rule 두 개만 있으며 Rust layout-name·layout-metric rule은 0임을 확인했다.
- [x] private 원장 mode `0600`과 tracked aggregate baseline mode `0644`를 분리하고 계약 테스트 9건을
  통과했다. 10k·Hyper-V·제품 mutation은 0이다.
- [x] Stage W8-R7-Q0 결과를 승인받아 commit `331eb2366`으로 고정하고 Q1에 진입했다.
- [x] W5 rank 7 HWPX를 봉인 hash와 byte-exact하게 복원하고, HWP5 반복 변환 hash·IR·1쪽 동치를
  확인했다.
- [x] 현재 HEAD의 release native와 Docker WASM을 새로 빌드해 HWPX·HWP5 각 1,556건의 형식별
  canonical trace byte parity를 확인했다.
- [x] HWPX substitution metadata는 HWP5에 보존되지 않지만 두 형식의 source+layoutMetric projection,
  actual layout run과 fixed geometry가 byte-equivalent임을 확인했다.
- [x] current metric entry가 전건 `null`이고 substitution 차이는 paint 후보에만 영향을 주므로 첫
  divergence를 `layout-metric` 하나로 확정했다.
- [x] Node 13건, Python 28건, Rust Font Decision Trace 8건과 release native·Docker WASM 빌드를 통과했다.
- [x] Stage W8-R7-Q1 결과를 commit `3a6d97b5d`로 고정하고 Q2 exact TTF·CDN artifact metric 제한
  비교에 진입했다.
- [x] exact TTF와 pinned CDN OTF·WOFF2가 font identity는 아니지만 공통 cmap 25,973자와 fixture 53자의
  advance mismatch가 0임을 확인했다.
- [x] current transform 1,556건을 mismatch 0으로 재생했고 exact 후보는 전체 advance를 40,744 HWPUNIT
  줄였다. fixed-frame 6축의 crossing 앞당김·신규 발생은 0이다.
- [x] Q0 style domain 63,858자의 ratio·spacing 축을 대사하고 bold 4,468자는 regular metric + synthetic
  bold advance 불변식을 source에서 확인했다. 실제 bold cohort 동적 판정은 Q3 gate로 남겼다.
- [x] private corpus·10k·Hyper-V·제품 mutation 없이 Q2를 `qualified-for-q3`, target decision plane을
  `layout-metric` 하나로 판정했다.
- [x] Stage W8-R7-Q2 결과를 commit `884e71be3`으로 고정하고 Q3의 동결된 5문서 same-snapshot
  qualification에 진입했다.
- [x] source usage 63,858자와 render observation 74,969자를 별도 회계로 유지하고 exact metric
  74,132자, transform replay mismatch 0을 확인했다.
- [x] 개선 line도 있었지만 HWPX table-cell admitted stored-row에서 current 0 → candidate 0.707px 신규
  overflow, +162 HWPUNIT의 동일 modelled boundary signature가 51회 관찰됐다.
- [x] bold modelled regression은 0이지만 cache 55,461자와 nested style 63,465자는 unmodelled로
  보존해 전체 evidence complete를 주장하지 않았다.
- [x] 결정적 modelled regression이 있으므로 rank 7 일괄 exact layout-metric 후보를 `no-change`로
  회수하고 W8-R7-Q4에는 진입하지 않는다.
- [x] private detail mode `0600`, 공개 projection privacy scan, 계약 테스트 10건과 private/public 반복
  byte-exact를 확인했다. 10k·Hyper-V·제품 mutation은 0이다.
- [x] Stage W8-R7-Q3 결과 승인 뒤 Q5 최종 disposition과 #4967 tracker 상태를 감사했다.
- [x] Stage W8-R7-Q3 결과를 commit `b6728a31f`로 고정하고 Q5 최종 disposition 감사에 진입했다.
- [x] rank 7을 `no-change`로 최종 확정하고 Q4·product-correction 자식 이슈·registry operation·제품
  mutation을 만들지 않기로 판정했다.
- [x] rank 1·7·8은 모두 `no-change`, 나머지 14개는 외부 evidence 변화 전까지 terminal이라 현재 W8
  수행 queue가 비었음을 확인했다.
- [x] GitHub read-only 감사에서 #4967 OPEN·sub-issue 0개, #4960 W8 checkbox 미완료를 확인했다.
- [x] 병합 뒤 #4967→#4960 sub-issue 연결, W8 checkbox 완료, 최종 comment·reopen 조건, #4967 completed
  close 순서의 후속안을 작성했다. GitHub mutation은 0이다.
- [ ] Q5 결과 승인 뒤 local stage commit과 제출 전 최신 base 통합·전체 검증 절차로 진행한다.
