---
kind: daily-order
status: active
date: 2026-08-19
---

## #5525 한글 2024 조판 호환 플래그

- 원 PR source head `0a51e845741b8f9108dc43bb31cac589566386f7`를 기준으로 검토 archive와 오늘할일을 trailing docs-only commit으로 준비한다.
- PR #5525의 원 기능 commit과 contributor history는 rewrite하지 않는다. source branch `feat/5524-hangul2024-compat`에 fast-forward push한다.
- 로컬 검증은 focused nextest, CLI 실샘플 기본값·`--compat 2022`·`--compat 2024`, 잘못된 옵션 거부, fmt, manifest, unit-tier, diff check를 통과했다.
- GitHub code candidate의 Full CI·CodeQL·Native Skia·Canvas visual diff는 통과했다. Chromium 다운로드 HTTP 403 재실행 실패는 환경 오류로 분류했다.
- 검토 결과 차단 결함은 없었다. `dump-pages --compat`의 help·capabilities·JSON 모드 필드·관련 매뉴얼과 CLI 계약 테스트는 메인터너 후속 보정으로 기록했다.
- [PR #5525 검토 기록](../pr/archives/pr_5525_review.md)과 이 오늘할일을 같은 trailing docs-only commit에 포함해 remote push한다.
- push 후 최신 PR head SHA와 required CI를 모니터링하고, 모두 완료되면 작업지시자 승인 절차에 따라 PR #5525 merge 및 #5524 후속 issue 상태·branch·검토 기록을 정리한다.

## #5398 CharShape 기본 장평 OWPML 기본값 정합

- 원 PR 기능 head `75c2af163fcbcf155a3273e61dca19aeab354b3c`의 `CharShape::default()` 장평을 OWPML 기본값 `100`으로 정합화했다.
- 최신 `devel` 병합 보정과 [PR #5398 검토 기록](../pr/archives/pr_5398_review.md)을 포함한 code/trailing head의 GitHub Full CI·CodeQL·Native Skia·Canvas visual diff는 모두 통과했다.
- 이 항목만 trailing 문서 commit으로 반영한다. push 후 최신 head의 CI·mergeable 상태를 확인하고, 통과하면 PR #5398 merge 및 #4161 상태·comment·로컬 branch 정리를 수행한다.
