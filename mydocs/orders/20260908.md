# 오늘 할일 - 2026년 9월 8일

## #6852 일반 사각형 실선 복원 — PR #6858

- 메인테이너가 최종 보고 및 이슈 본문 정정·push·PR 생성을 승인했다.
- [PR #6858](https://github.com/edwardkim/rhwp/pull/6858)을 devel 대상으로 생성했다.
  assignee edwardkim, milestone v1.0.0, bug/rust/rendering/layout/hwp5/hwpx 라벨을 적용했다.
- 검증 후보 `393401e5a`에서 전체 nextest 9,224건, 필수 lint·Native Skia 3종·Docker WASM을 통과했다.
  신규 HWPX의 기존 문단 ID 정규화는 수정 전후 대조 후 원장 네 행만 등록했다.
- 최초 제출 `aa3cc0bfd`는 검증 후보 이후 문서만 추가한 head다. 5쪽 메인테이너 시각 판정 통과와
  새 WASM의 1·5·7쪽 SVG 동일성을 기록했다. IR 개선은 #6856으로 분리했다.
- 제출 HEAD `7887e5c66`의 Full CI·CodeQL 및 관련 checks 성공을 확인했다.
  최신 devel `7138fe784`와 merge-tree clean, GitHub MERGEABLE / CLEAN이었다.
- [Self-review](../pr/archives/pr_6858_review.md)를 승인 판정으로 완료했다.
  집중 회귀 재실행 6건 통과, 대표 5쪽 PNG를 생성·직접 확인하여 리뷰 기록과 로컬 커밋으로 남긴다.
  남은 순서는 승인된 기록 push → 새 HEAD checks 확인 → 별도 승인된 merge commit 병합·close다.
  이번 self-review에서 원격 push·병합·이슈 종료는 수행하지 않았다.

## #6856 소유 구조 기반 사각형·글상자 식별 — PR #6894

- [PR #6894](https://github.com/edwardkim/rhwp/pull/6894), candidate `98a2d403d`.
- 최신 devel `a7de17ff8` 통합 검증에서 전체 nextest 9,266개, lint·Native Skia·Docker WASM을 통과했다.
  A4의 사각형 1개·글상자 2개 식별과 한컴 확인 손상 파일 거부도 유지됐다.
- [Self-review](../pr/archives/pr_6894_review.md)에 코드·시각·검증 근거를 남긴다.
  메인테이너가 병합과 close를 승인했다. 코드 후보의 CI·언어별 CodeQL·Render Diff·Adapter·Proptest는
  성공했고 종합 CodeQL은 NEUTRAL이다. 문서-only trailing push 후 최신 checks를 확인한다.
- merge commit 병합 후 #6856 초기 본문을 승인된 재착수 범위와 정합시켜 종료한다.
  A 선 억제 정책과 전체 문서 시각 동일성까지 해결했다고 해석하지 않는다.

## #6812 원본 1페이지 수용 후보 — PR #6850

- [PR #6850](https://github.com/edwardkim/rhwp/pull/6850)을 devel 대상으로 생성했다.
  assignee edwardkim, milestone v1.0.0 및 관련 labels를 적용했다.
- #6847·#6848 최신 devel을 통합한 `380f83830`에서 집중 92건, 전체 nextest 9,218건,
  필수 lint·Native Skia 3종·Docker WASM을 통과했다. Studio 추가 검사도 1,493건 통과했다.
- 새 WASM의 원본 1페이지 SVG는 메인테이너가 승인한 `edf083614` 출력과 바이트 단위 동일하다.
  셀 확장 및 전체 11쪽 시각 검증 완료로 확대하지 않는다.
- `b08b765a8`의 CI·CodeQL·Render Diff·adapter inter-diff·Proptest·CI Impact Policy 성공을
  확인하고 [최종 self-review](../pr/archives/pr_6850_review.md)를 승인 판정으로 완료했다.
  검토 시점 MERGEABLE/CLEAN이며 병합을 막을 새 결함을 발견하지 않았다.
- 최종 기록은 로컬 문서 commit으로 남긴다. 남은 순서는 승인된 문서 push → 최신 head
  checks 확인 → 별도 승인된 merge commit 병합·#6812 close다. 병합·이슈 종료는 미실행이다.

## #6876 Rust CodeQL PR 통계 쿼리 축소 - PR #6877

- [PR #6877](https://github.com/edwardkim/rhwp/pull/6877)의 코드 후보 `04c71852c`에서 Full CI,
  Rust/JavaScript/Python CodeQL, Adapter, Proptest가 성공했다. 로컬 workflow 계약 테스트 16개도 통과했다.
- 사용자 지정 [PR #6867](https://github.com/edwardkim/rhwp/pull/6867)과 비교해 Rust job은
  16분 49초에서 14분 38초로 2분 11초(약 13%), 분석 단계는 14분 33초에서 12분 48초로 줄었다.
  보안 쿼리 16개와 추출 진단 목록은 동일하고 내부 통계 3개만 제외됐다.
- 공통 기반과 CLI/query pack/threads는 같지만 source와 checkout 시간이 달라 전체 차이를
  필터 효과로 단정하지 않는다. 동일 소스 반복 A/B 실험은 미실행이다.
- 사용자 승인 후 merge commit `9947ad8633215de8966a844b881c150008cd00f5`으로 병합했고
  로컬 devel을 동기화했다. devel CI/CodeQL/Adapter/Proptest는 exact-green PR 결과를 재사용해 성공했으며
  heavy worker skip 및 duration refresh 성공까지 확인했다.
- 사용자 지시한 옵션 2로 [self-review](../pr/archives/pr_6877_review.md)와 이 기록을 별도 문서 PR에 포함한다.
  원 PR에 trailing 기록을 추가하지 않는다. #6876은 closing reference가 없어 OPEN을 유지한다.
- 남은 작업은 문서 PR의 최신 CI와 병합 조건 확인, 승인 범위의 후속 comment 및 작업 전용 local branch 정리다.

## planet6897 6개 PR 체리픽 통합 및 메인터너 보정

- #6849·#6851·#6853·#6857·#6859·#6867의 기능 커밋 8개를 출처 보존 체리픽했다.
  브랜치는 `review/planet6897-ci-green-20260908`, 기준 devel은 `91147aec332651309029faac521d19106305c6f5`,
  체리픽 완료 head는 `d66f2a6a5aaccc8deae4d77877835ce1a5fdeb86`다. 최종 검증에는 그 뒤의 미커밋 보정도 포함한다.
- [#6849 리뷰](../pr/archives/pr_6849_review.md), [#6851 리뷰](../pr/archives/pr_6851_review.md),
  [#6853 리뷰](../pr/archives/pr_6853_review.md), [#6857 리뷰](../pr/archives/pr_6857_review.md),
  [#6859 리뷰](../pr/archives/pr_6859_review.md), [#6867 리뷰](../pr/archives/pr_6867_review.md)를 작성했다.
  통합 판정은 메인터너 보정 완료, 수용 가능이며 각 리뷰에 명시한 변경 범위로 한정한다.
- #6776은 중첩 빈 문단 높이와 TAC 표 뒤 간격을 반영해 그래프가 18쪽이 아닌 19쪽에 배치되도록 보정했다.
  중간 보정에서 발생한 #1939 왕복 렌더 실패는 HWP5 원본과 HWP5 계보 HWPX의 공통 조판 조건으로 해결했다.
- 최종 전체 회귀를 8스레드·no-fail-fast로 끝까지 실행해 **9,241 통과, 실패 0, 46 건너뜀**, exit 0을 확인했다.
  실행 시간은 388.355초다. 이번 PR 준비 때문에 이미 통과한 전체 회귀를 중복 실행하지 않는다.
- 최종 보정 CLI를 다시 빌드하고 새 한컴 기준 PDF의 18·19·20쪽과 직접 대조한 PNG를 재산출했다.
  18쪽에는 그래프가 없고 19쪽 그래프·20쪽 큰 그림은 본문 상단에 온전히 배치된다.
  [공통 시각 증적](../pr/archives/pr_6849_6867_planet6897_visual_sweep.md)에 바이너리·입력·PDF 해시와 재현 명령을 기록했다.
- 글꼴 대체·일부 세부 간격과 #6859의 전체 쪽수 차이는 남아 있으므로 문서 전체의 시각 동일성을 주장하지 않는다.
  새 기준 PDF 7개와 코멘트에서 사용할 최종 PNG만 포함하며 임시 SVG·JSON·중간 PNG·로그는 추가하지 않는다.
- 사용자가 오늘할일을 포함한 통합 PR 생성과 최종 lint 재실행을 승인했다. fmt, native/WASM/workspace Clippy,
  workspace build, suite manifest 검사가 모두 exit 0으로 통과했다. 보정은 `64f33cfc1c96d893c7e081b987349994dbff2717`에
  커밋했으며 review·오늘할일·증적을 별도 커밋해 같은 upstream 임시 head로 push한다.
  통합 PR에 owner를 자동 reviewer로 지정하지 않는다. 최신 PR CI·병합·원 PR/이슈 comment·close는 아직 수행하지 않았다.

## Issue #6891 / PR #6893: Visual Sweep 로컬 폰트 보존

- [x] SVG의 로컬 폰트 선언 삭제로 발생한 한글 본문 깨짐을 #6891로 등록하고 보정.
- [x] 선언·안전한 대체 순서 보존, 같은 family 웹폰트 충돌 방지, 단일 family descriptor 보호.
- [x] 단위 테스트 6개 및 실제 Chrome 1배·2배 캡처 테스트 통과. JavaScript 구문·공백 검사 통과.
- [x] 동일 SVG의 물리 7쪽을 재산출하고 기존 PNG·한컴 PDF와 직접 대조. 최종 수정 전후 PNG만 보관.
- [x] [PR #6893](https://github.com/edwardkim/rhwp/pull/6893) 생성 및 [검토 기록](../pr/archives/pr_6893_review.md)을 같은 PR의 trailing commit으로 준비.
- [ ] 최종 head CI 확인, 머지 및 `post_merge.md` 후속 처리. 현재 CI·머지 완료로 간주하지 않음.

검증 범위는 7쪽 본문의 글리프 깨짐이다. 전체 46쪽·Studio 픽셀 동등성·Rust 전체 회귀 테스트·WASM 빌드는 수행하지 않았다.
