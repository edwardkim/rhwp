# 오늘 할일 - 2026년 5월 4일

## M100 — v1.0.0 조판 엔진 (PR 처리)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #551 Task #544** | 21_언어_기출 passage 글상자 우측 시프트 정정 (옵션 A2 cherry-pick, closes #544) | **완료 (핀셋 cherry-pick 머지 + push)** | 작업지시자 보고 "21_언어_기출_편집가능본.hwp 1/2/4/5/7/8/11/13/14 페이지 글상자 우측 시프트". 이슈 #544 (closed) 재오픈 후 PR #551 잔존 cherry-pick. 당초 검토 옵션 A3 (`1934161f` Task #552 + `05beb208` Task #544 v2 + `84d1d4b2` Task #544 v3) 채택했으나 B1 진단 결과 본 devel 의 Task #479 미적용 (pre-#479 trailing-ls 항상 가산 모델) 확인 → #552/v3 cherry-pick 시 #479-style 분기 신설로 광범위 회귀 위험. **옵션 A2 (`05beb208` 단독, #547+#544(1) 통합) 로 변경 채택**. 본질: `paragraph_layout.rs` (1) `box_x = col_area.x` / `box_w = col_area.width` (margin 미적용, paragraph border outline = col_area 전체) + (2) `inner_pad` 분기 제거 (visible-stroke + bs=0 인 경우 `margin_left = box_margin_left + inner_pad_left` 이중 적용 → `margin_left = box_margin_left` 단일 적용). 측정 (페이지 4 [7~9]): 박스 left 128.51→117.17 (PDF 117.0), width 402.5→425.17 (PDF 425.1), 본문 첫 글자 '평' x 153.12→128.51 (PDF 128.5). 9 박스 모두 정합. 검증: cargo test --lib 1120 / test_544+test_547 +2 GREEN / clippy 0 신규 / 5 샘플 58 페이지 광범위 sweep 38 differ (의도된 paragraph border 정합 변경, visible-stroke paragraph text -11.33 px 좌측 시프트 의도) / 회귀 검출 가능 영역 (비-border paragraph text) 0 변경. 작업지시자 시각 판정 통과. cherry-pick `457d5f33` (Task #544 v2 Stage 2 from @planet6897 `05beb208`). devel merge `f6039f32` push 완료, 이슈 #544 close. 처리 보고서: `mydocs/pr/archives/pr_551_v3_544_a2_report.md`. 검토 문서: `mydocs/pr/archives/pr_551_review_v3_544_a2.md`. 잔존 미반영: #552 / v3 (#479 미적용 모델로 발현 안 함, 별도 사이클 결정 대기), #548 Phase C (별개 본질 [푸코] 페이지). |

## 작업 메모

- 본 cherry-pick 은 PR #551 의 세 번째 핀셋 (Task #525 / Task #528 다음). PR #551 잔존 ~50 task 중 Task #544 만 우선 처리.
- B1 진단의 핵심 발견: 본 devel paragraph_layout.rs 의 trailing-ls 모델이 컨트리뷰터 fork (#479 적용) 와 다름. 이슈 #544 본문 측정값 (rhwp box top y=224.4) 은 컨트리뷰터 fork 기준 → 본 devel 에서는 box top y=233.97 ≈ PDF 233.8 이미 정합 ((2) y 시프트 발현 안 함). 사용자 보고 "오른쪽으로 밀려있음" 은 (1) box_x/w 단독.
- PR #551 잔존 처리 사이클의 분리 단위: Task #525 (1차) / Task #528 (2차) / Task #544 (3차, 본 사이클). 다음 사이클 후보: Task #547 (이미 v2 에 통합됨 → 별도 처리 불필요), Task #548 (별개 본질), Task #552 / v3 (#479 모델 도입 결정 시 일괄), 기타 Task #517~#523 등.

## 메모리 정합

- `feedback_check_open_prs_first` — 사용자 보고 (글상자 시프트) 즉시 PR #551 잔존 + 이슈 #544 매핑
- `feedback_pdf_not_authoritative` — PDF 보조 ref. B1 진단으로 본 devel measurement 직접 수행 (이슈 본문 측정값 대신).
- `feedback_essential_fix_regression_risk` — 옵션 A3 → A2 변경 사례. trailing-ls 모델 전환 위험 인지 후 최소 fix 채택.
- `feedback_rule_not_heuristic` — paragraph border outline = col_area 산식 단일 룰 (분기 없음).
- `feedback_visual_regression_grows` — 작업지시자 직접 시각 판정 게이트.
- `feedback_local_task_branches_origin_backup` — `devel-backup` / `origin/local/task544_v[1-3]` 보존 유지.

## M100 — exam_science 7번 표 ㉠/㉡ baseline (Task #566)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #566** | exam_science.hwp 7번 문제 표(4×7) 행 0 ㉠/㉡ baseline 위쪽 시프트 | **완료 (옵션 A — 코드 변경 없이 종결)** | 작업지시자 승인. **Stage 1 핵심 finding**: 96 dpi 픽셀 측정 결과 시프트 양 약 1.0~1.5 px (≈75~110 HU, ≈0.4 mm 종이상). ㉠/㉡ 만의 결함이 아니라 표 전 셀 텍스트(직선형/굽은형/HOF/CF₄ 등) 동일 적용. 폰트 fallback (한컴윤고딕 ↔ HY신명조→Batang) 의 어센트 차이로 충분히 설명 가능, ㉠/㉡ 의 원형 글리프 특성상 시각 도드라짐만 강한 케이스. **가설 검증**: (1) baseline 산식 (bl=978HU) 확정 — `paragraph_layout.rs:835` (2) CharShape 세로 시프트 기각 (char_offsets=[0,...,0]) (3) valign/padding 기각 (aim=false 정상 산식) (4) 글리프 메트릭 부분 — 시각 도드라짐만 (5) SVG transform/scale 분기 기각 (셀/본문 동일 형식). **옵션 B (산식 정정 — `(bl+lh)/2` 등)** 미채택 사유: 광범위 표 셀 회귀 리스크 vs 시각 개선 미미. **CharShape/LineSeg 실측**: cell[1] (㉠) cs_id=117 / cell[8] (직선형) cs_id=10 모두 ratios=[95×7] char_offsets=[0×7] base_size=1150 — 동일 메트릭. 산출물: `mydocs/plans/task_m100_566.md` + `mydocs/working/task_m100_566_stage1.md` + `mydocs/report/task_m100_566_report.md` + `examples/dump_cell_lineseg.rs` (진단 도구). 후속: 만약 한컴 PDF baseline 정합이 우선순위가 되면 옵션 B 별도 타스크 등록 권고. |

## 메모리 정합

- Task #544 v2/v3 직후의 baseline 측정 — sub-pixel 변위는 폰트 fallback 으로 흡수되는 정상 범위
