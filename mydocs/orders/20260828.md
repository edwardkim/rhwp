---
kind: daily-order
status: active
date: 2026-08-28
---

# 오늘 할일 - 2026년 8월 28일

## CI 운영

- [ ] [#6257](https://github.com/edwardkim/rhwp/pull/6257) squash merge 뒤 trusted CodeQL 재사용 보완
  - 로컬 CI policy 및 trusted reuse 계약 70건, Python workflow 계약 166건 통과.
  - 최신 PR head의 GitHub Actions 통과 뒤 merge한다.
  - merge 뒤 devel push에서 동일 PR의 성공 run이 재사용되고 CodeQL worker가 skip 되는지 확인한다.

## #4969 — W10 common shaping

- [x] Q2-B 결과를 qualified로 판정하고 native·Node WASM parity mismatch 0과 제품 소비자 0을 기록했다.
- [x] Q2-C 진입 승인을 받았다. 이 승인은 제품 mutation이 아니라 activation matrix·rollback 경계 확정까지다.
- [x] 현행 composer·W9 line-break owner를 재감사하고 complex-required horizontal LTR activation matrix를 작성했다.
- [x] Q2-C 단독 line boundary 활성은 Q2-D 전까지 emitted run owner와 불일치함을 확인해 dormant transaction으로
  순서를 정정했다.
- [x] Q2-C dormant transaction 변경 범위와 보호 불변식을 보고하고 구현 승인을 받았다.
- [x] Q2-C0 segmentation·eligibility, Q2-C1 cluster-aware width owner, Q2-C2 bounded final-line transaction을
  제품 소비자 0으로 구현했다.
- [x] native·Node WASM focused 8건에서 parity mismatch 0, later-segment 실패의 문단 전체 rollback,
  `productPublished=false`를 확인했다.
- [x] Q2-C 결과 승인 뒤 Q2-D emitted-run `Arc` sidecar와 line-boundary publication의 원자적 activation 계획을
  별도로 승인받았다.
- [x] Q0~Q2-C 승인 결과를 checkpoint `452b6b741`로 커밋하고 원격 push는 보류했다.
- [x] Q2-D owner를 재감사해 `TextRunNode` public field 대신 `ComposedParagraph` internal `Arc`와
  `PageRenderTree` bounded `NodeId` sidecar table을 권고했다.
- [x] 동일-boundary activation과 NO_LS boundary-changing activation을 분리한 Q2-D 수정 수행계획을 작성했다.
- [x] Q2-D 수정 수행계획을 승인받아 D0 sidecar ABI·bounded table을 제품 소비자 0으로 구현했다.
- [x] native·Node WASM focused 각 4건에서 `Arc` 공유, JSON 무변화, typed fail-closed와 4,096개 상한을
  확인했다.
- [x] D0 결과 승인 뒤 D1 composition owner 인계를 시작했다.
- [x] D1 composition `Arc` handoff, D2 emitted-run mapping, D3 common GlyphRun lowerer를 각각 승인·검증했다.
- [x] D4-A condensed replay projection·exact source certificate, D4-B one-line/one-run atomic activation,
  D4-C actual CanvasKit draw·cross-backend·성능 판정을 완료했다.
- [x] D4는 `qualified-bounded`로 마쳤다. 최초 strict lane은 유지하되 456,688B portable font resource 재사용을
  입증하기 전에는 multi-line/run으로 확대하지 않는다.
- [x] 최신 `upstream/devel@5645e1f5bb`를 merge하고 양쪽 오늘할일 기록을 보존했으며 Rust·Studio·WASM·CI
  정책 회귀를 다시 통과했다.
- [x] [PR #6270](https://github.com/edwardkim/rhwp/pull/6270)을 `devel` 대상으로 생성했다. PR 생성 뒤
  `jangster77`의 singleton shaping test registry 보정 `a271733ba`를 authorship 그대로 반영하고 로컬 검증했다.
- [x] `a271733ba`의 CI·CodeQL·Render Diff·Adapter inter-diff·Proptest roundtrip이 exact head에서 모두
  성공했고 PR은 `MERGEABLE / CLEAN`으로 전환됐다.
- [ ] self-review trailing 기록을 push하고 최신 head의 fast-pass·mergeability를 확인한다.
- [ ] 대형 PR review cycle과 메인테이너 승인 뒤 정상 merge한다. #4969는 D5가 남아 있어 자동 close하지 않는다.

## PR 검토

- [ ] [#6245/#6247/#6248/#6249/#6250/#6254/#6259 통합 검토](https://github.com/edwardkim/rhwp/pulls)
  - draft가 아니고 CI 실패·진행 check가 없는 open PR만 선별했다.
  - #5953, #6059는 draft라 제외했고, #6073/#6246/#6252는 `Build & Test` 실패라 제외했다.
  - #6083은 실패 check는 없지만 `DIRTY`와 기존 메인터너 재작업 요청이 있어 보류 유지.
  - 통합 브랜치 `review/open-ci-green-20260828`에서 체리픽 완료.
  - #6245는 메인터너 보정으로 후속 문단 사다리 흡수 판정을 바로 뒤 실제 lineSeg로 좁혔다.
  - 로컬 검증 완료: fmt, unit tier, suite manifest, focused #6167/#6173/#6179/#6194, OOB 관련 lib
    tests, clippy, 전체 nextest 8,477 pass / 43 skipped, WASM build, Native Skia 3종.
