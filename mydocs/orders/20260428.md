# 오늘 할일 - 2026년 4월 28일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #398 | 표 페이지 분할 시 rowspan>1 셀이 분할 단위 산정에서 누락 (2025년 기부·답례품 보고서 양식 1쪽 하단) | 완료 | `MeasuredTable.row_block_start/end` + `compute_row_blocks` + `row_block_for`/`snap_to_block_boundary` 추가. `pagination/engine.rs::split_table_rows` + `typeset.rs::paginate_table` (실제 SVG 경로) 양쪽에 블록 정책 적용. 본 샘플 1쪽 표 분할 사라지고 2쪽 시작 (PDF/한글과 동일). 1023 lib + 6 svg_snapshot + 50 통합 = 1073 통과, release 빌드 성공 |

## 버그

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #402 | 동일 문단 내 두 번째 line의 inline 그림이 첫 line과 겹침 (2025년 기부·답례품 보고서 양식 7쪽 파이차트) | 완료 | `layout.rs::layout_shape_item`: 선행 TAC 컨트롤이 있으면 `para_start_y`를 진행된 `y_offset`으로 갱신 → 그림 y가 표 아래로 이동. `typeset.rs::typeset_table_paragraph` (실제 SVG 경로): 선행 TAC 그림의 line_seg lh를 `current_height`에 누적 + 페이지 초과 시 분할 → 7쪽 표만, 8쪽에 파이차트. 1023 테스트 통과, 10개 샘플 LAYOUT_OVERFLOW 카운트 회귀 없음 |
| #404 | 헤딩 문단이 후속 콘텐츠와 다른 페이지로 분리됨 (2025년 기부·답례품 보고서 양식 9쪽 pi=83 "(7) 다수 기부자 현황") | 완료 | `typeset.rs::typeset_section` 메인 루프에 vpos 기반 heading-orphan trigger 추가. 5개 조건 AND (current fits + vpos overflow > 1mm + next substantial > 30px + next doesn't fit + single column non-wrap) 로 false positive 차단. page_top_vpos 는 current_items 첫 item para_index 로 즉시 계산. 1073 테스트 통과, 10개 샘플 LAYOUT_OVERFLOW 회귀 없음 + 타겟 -15 / kps-ai -1 개선 |
| #409 | 21페이지 2x1 표 위치 오류 (TopAndBottom Picture 다음 문단 vpos 보정 차트 높이 이중 반영) | 진행 중 | 2025년 기부·답례품 실적 지자체 보고서_양식 21페이지: 차트 아래 빈 표가 페이지 하단으로 밀려 잘림 + 후속 10x5 표 521px overflow. `prev_has_overlay_shape` 가드를 Control::Picture (non-TAC) + TopAndBottom + vert=Para 케이스로 확장 |
