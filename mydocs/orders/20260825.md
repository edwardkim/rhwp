---
kind: daily-order
status: active
date: 2026-08-25
---

# 2026-08-25 오늘할 일

## PR #6017 - Square host 빈 문단 저장 사다리 줄 예약 검토

- [x] #6017을 최신 `upstream/devel` 위 로컬 review branch `review/planet6897-6017-20260825`에 병합해
  충돌 없는 상태를 확인했다.
- [x] 신규 #5809 회귀 1/1, 기존 #2069 OLE enter/backspace 반증 10/10, manifest prepare/check,
  `cargo fmt --all -- --check`, `git diff --check`를 통과했다.
- [x] `rhwp info --json` 결과 `lastSavedWith=null`을 확인하고, PR 본문의 한글 2022 실측 근거와 MCP
  산출 PDF `Creator: Hwp 2022`를 기준으로 HWP 2024 MCP engine `2020` 기준 PDF를 생성했다.
- [x] visual sweep p1을 수행해 flagged 0, pixel match `89.98028%`를 확인하고 review PNG·summary·PDF를
  증적으로 보존했다.
- [x] `mydocs/pr/archives/pr_6017_review.md`에 수용 가능, 메인터너 보정 불필요 판단을 기록했다.
- [ ] 통합 PR을 생성하고 CI 완료 후 merge, 원 PR comment/close, `upstream/devel` 포함 확인 및 branch
  정리를 `post_merge.md`에 따라 수행한다.

## PR #6018 - visual_sweep 한글 라벨 폰트 fallback 보완

- [x] #6016에서 `mydocs/pr/assets/pr_6014_issue5712_p1_review.png`의 한글 metric 라벨이 tofu로 보이는
  현상을 제품 renderer가 아니라 visual sweep 도구 라벨 폰트 선택 문제로 분리했다.
- [x] `RHWP_VISUAL_SWEEP_LABEL_FONT`, fontconfig `fc-match`, Linux/macOS/Windows CJK 폰트 경로 후보를
  순서대로 적용하고, 모든 TrueType 후보가 실패할 때만 Pillow 기본 폰트로 fallback하도록 보완했다.
- [x] PR review workflow와 visual fixture evidence 문서에 대표 review PNG의 한글 glyph·metric text·overlay
  legend 판독성을 직접 확인하는 gate를 추가했다.
- [x] `python3 -m unittest scripts.tests.test_visual_sweep` 43 tests OK, `py_compile`, Markdown 링크 검사,
  `cargo fmt --all -- --check`, `git diff --check`를 통과했다.
- [x] self-review 문서 `mydocs/pr/archives/pr_6018_review.md`와 오늘할일 trailing commit을 준비했다.
- [ ] 최신 head의 GitHub CI와 `MERGEABLE/CLEAN`을 확인한다.
- [ ] 자동 merge 승인에 따라 #6018을 merge하고 #6016 close 확인, PR/issue comment, merge SHA의
  `upstream/devel` 포함 및 branch/worktree 정리를 `post_merge.md`에 따라 수행한다.

## PR #6049 - font rule lifecycle·evidence delta 계약

- [x] #5955의 봉인 schema 1.0을 보존하고 830개 rule을 lifecycle schema 2.0으로 의미 변화 없이 이행했다.
- [x] append-only change set, evidence lineage, active-only projection과 W2 offline lifecycle audit을 구현했다.
- [x] release·Native Skia·Studio·Docker WASM과 native/WASM 공개 SVG 173쪽 제품 0-delta 검증을 완료했다.
- [x] 첫 Full CI 뒤 self-review에서 누락된 relation·metric·supply semantic guard와 validator totality를
  발견해 PR을 Draft로 전환하고 `b1afde931`에서 정정했다.
- [x] corrected code head의 GitHub Actions가 24 success/3 policy skip, 실패·대기 0으로 완료됐다.
- [x] self-review 결과를 `mydocs/pr/archives/pr_6049_review.md`에 기록했다.
- [ ] PR 본문의 정정 전 93/93 수치와 semantic guard 설명을 현행화하고 review-only trailing commit을 push한다.
- [ ] 최신 trailing head의 fast-pass·required aggregate와 `MERGEABLE/CLEAN`을 확인한 뒤 Ready 전환과 정상
  merge 승인을 받는다.

## Issue #4967 - W8 rank 8 font face 교정 qualification

- [x] 최신 `upstream/devel@242c104bd`에서 `task_m100_4967` 작업 브랜치를 만들었다.
- [x] W4·W5·W7.5 정본과 보존된 W3 journal을 대사해 rank 8 실사용 cohort가 6건(HWP 3, HWPX 3)이며
  위험 43,374자·stored LineSeg lane임을 전수 재계측 없이 확인했다.
- [x] 현행 v2 registry에는 rank 8의 Canvas2D·CanvasKit supply 규칙만 있고 Rust layout metric은
  heuristic임을 확인했다.
- [x] `mydocs/plans/task_m100_4967.md`에 qualification-only 수행계획과 재계측 금지선을 작성했다.
- [x] 메인테이너가 수행계획을 승인했고, Stage W8-Q0 projector·공개 baseline·working report와
  5개 계약 테스트를 완료했다. 10k·Hyper-V 재실행은 0이다.
- [x] 승인된 Stage W8-Q0 local commit을 완료했다.
- [x] W8-Q1 공개 fixture를 W5와 동일한 SHA-256으로 재현하고 current native/Docker WASM 1,556-record
  trace의 canonical byte exact, heuristic 1,556건, 실제 fixed-frame 6축 geometry 기준선을 고정했다.
- [x] 승인된 W8-Q1 결과를 local stage commit으로 고정했다.
- [x] W8-Q2에서 exact TTF와 pinned CDN OTF·WOFF2의 source identity를 분리하고, 공통 cmap 25,970개의
  advance mismatch 0과 current transform 1,556건 replay mismatch 0을 확인했다.
- [x] actual fixed-frame 6축의 exact `hmtx` projection에서 crossing 지연 3·제거 1·불변 2,
  앞당김·신규 0을 확인해 `layout-metric`만 `qualified-for-q3`로 판정했다.
- [x] 승인된 W8-Q2 결과를 `bc25152cc` local stage commit으로 고정했다.
- [x] W8-Q3에서 Q0의 private 6문서만 bounded 비교했다. source usage 43,432자와 페이지 render observation
  44,117자를 별도 회계로 유지하고 current transform replay mismatch 0을 확인했다.
- [x] 모델링 가능한 문서는 개선 3·악화 1이었고, 2문서 4,397자는 query snapshot 간 run set 차이 때문에
  frame 미조인으로 `unmodelled`임을 확인했다. 제품 변경 없이 Q3를 `blocked`로 판정했다.
- [x] Q3 `blocked` 결과와 보강 계획을 승인받아 `90768be62`로 고정했다. 독립 query join을 폐기하고
  같은 page tree snapshot에서 trace·TextLine frame/context·stored-row disposition을 함께 내는 W8-Q3R
  읽기 전용 query 계약을 시작했다.
- [ ] W8-Q3R query의 단일 tree build·기존 trace 불변·전건 line/run join과 fail-closed cache disposition을
  공개 fixture에서 검증한다.
- [ ] Q0의 동일 private 6문서만 제한 재판정하고 Q4 진입 가능성을 다시 승인받는다.
