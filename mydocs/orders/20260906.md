# 2026-09-06

## #6634 Release 이후 package publish 자동 기동 정상화와 PR #6799

- [수행계획](../plans/task_m100_6634.md)에 따라 `release.published` 간접 이벤트를 제거하고 Release Binary
  성공 뒤 같은 commit의 reusable Publish All Packages를 직접 호출하도록 구현했다. exact tag source guard,
  npm core/editor·VS Code Marketplace·Open VSX의 독립 멱등 재시도와 구조화 완료 evidence를 함께 추가했다.
- latest `devel@ff1ce007b428547da74e0d6b7e9a196592c60ff6`을 포함한 executable head
  `559edb06826e7b8bfa2348d5951d78cf18d066e9`에서 Release Binary와 Publish All Packages verify-only run이
  성공했다. native 5종, WASM, VSIX와 evidence를 만들고 Release·외부 publish 4개를 모두 skip했다.
- 최초 dogfood에서 reusable REST job 접두사가 YAML job ID가 아닌 caller 표시명인 것을 발견했다. #6689
  promotion policy를 실제 이름으로 정정하고 회귀를 추가했으며, 최종 collector는 pagination 완결·waiver
  0건, offline verifier는 오류 0건·`ok=true`로 두 run을 수락했다.
- 공개 네 채널은 실행 전후 기존 `0.8.6 already-present`였고 `test` tag·Release는 생성되지 않았다.
  [최종 보고서](../report/task_m100_6634_report.md)는 production stable-tag 경계를 다음 release canary로
  분리하고, 그 전에는 #6634를 닫지 않는 조건을 기록한다.
- [PR #6799](https://github.com/edwardkim/rhwp/pull/6799)는 23 files, `+3148/-94`, 12 commits다. assignee
  `edwardkim`, labels `bug`·`ci`·`packaging`, milestone `v1.0.0`으로 트리야지했다. current candidate
  `82d92923573c6cbcf4e879a989ec9a4d989103a6`의 Full CI, CodeQL 3언어, Adapter inter-diff와 Proptest가
  모두 성공했고 `MERGEABLE`·`CLEAN`이다.
- [self-review](../pr/archives/pr_6799_review.md)의 판정은 **승인**이다. 자기 PR이므로 reviewer와 GitHub
  approve event는 만들지 않는다. review-only trailing commit을 push한 뒤 최신 head Actions·devel 정합을
  다시 확인하고 별도 merge 승인을 받는다.
- `main@e8800c8def63449808a4092798442652ed460552`에는 devel에 없는 v0.8.6 release merge와 hotfix 두 commit이
  남아 있다. 다음 release 전에 main→devel 동기화와 새 exact devel promotion 증적이 필요하다.

## #6712까지 렌더링 보정과 PR #6813

- [PR #6813](https://github.com/edwardkim/rhwp/pull/6813)을 `devel` 대상으로 생성했다.
  작성자 `jangster77`의 self PR이므로 reviewer와 GitHub approve event는 만들지 않았다.
- #6712의 표 셀 Square 그림 흐름/분할/말미 소실 및 비례폭 한글 제목 겹침을 보정했다.
  확인한 한국어/중국어 원본 모두 3쪽에서 기준 PDF와 같은 2쪽으로 줄었고 제목 실제 ink 간격도 양수다.
  #6708 inline 그림 기준선, #6714 음수 표 offset 보정은 Closes 대상으로 포함했다.
  #6699는 잔여 dx=-1.41px로 부분 보정만 Ref 처리하며 #6662와 함께 닫지 않는다.
- 코드 `6dcd46608`에서 전체 nextest 9,096 passed / 46 skipped, 세 Clippy lane,
  Native Skia 공식 3종, WASM host `--no-opt` 및 실제 Chromium 렌더를 통과했다.
  Docker 최적화 경로는 미실행이며 동일한 검증으로 주장하지 않는다.
- [최종 분석/검증](../working/task_m100_6662_stage22_pr_readiness.md),
  [self-review](../pr/archives/pr_6813_review.md), [실행 계획](../pr/archives/pr_6813_review_impl.md)에
  완료 범위, 실제 수치와 한계, merge 후 이슈/PR 이미지 코멘트 계획을 기록했다.
- 대표 PNG 6장과 원본 HWP/기준 PDF는 보존했고 실행 로그·중간 PNG/SVG/JSON은 커밋하지 않았다.
  초기 head `964ef10a6`의 Full CI, CodeQL, Render Diff, Adapter, Proptest가 모두 성공했다.
  오늘할일 신규 파일의 add/add 충돌만 mydocs bridge에서 양쪽 기록을 보존해 해소하고,
  이 문서와 review를 같은 PR에 trailing push한다.
  최신 head CI와 별도 merge 승인이 남아 있으며 아직 merge/issue close하지 않았다.
