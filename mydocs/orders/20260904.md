# 2026-09-04

## #6717 Frontend package gates install timeout stabilization

- PR #6715의 exact-head full CI에서 네 package의 직렬 `npm ci`가 두 번 연속 20분 job 상한을
  잠식한 원인을 [#6717](https://github.com/edwardkim/rhwp/issues/6717)로 등록했다.
- 제품 source·lockfile은 건드리지 않고 install의 자동 audit 호출 제거, npm cache 우선 사용,
  30분 hang 상한과 workflow 계약 테스트를 한 O3 운영 변경으로 처리한다.
- [PR #6720](https://github.com/edwardkim/rhwp/pull/6720)의 code candidate
  `8702ac8fcc392f8320824b555d252c62c05b6f2e`는 Full CI와 CodeQL을 통과했다. Frontend package
  job은 6분 52초, install은 14초였고 후속 브라우저·build gate도 전부 실행됐다.
- self-review에서 범위 누출이나 blocker는 발견되지 않아 [승인 판정](../pr/archives/pr_6720_review.md)을
  남겼다. review-only trailing head는 trusted controller의 A.1 증명과 최신 required check를 다시
  확인한 뒤 별도 merge 승인을 받는다.
- #6717을 `devel`에 먼저 통합한 뒤 PR #6715를 최신 base와 정합시켜 exact-head CI를 재검증한다.

## #6728 studio 그림 이동 E2E 회귀 계약 검토

- [PR #6728](https://github.com/edwardkim/rhwp/pull/6728)의 reviewer `jangster77`을 사전 배정했다. code candidate `572b66372e558d4208d3875e71f131191ce617a4`는 studio E2E 하나만 추가하며 제품 renderer, layout, WASM, sample, fixture를 바꾸지 않는다.
- source head에서 `node e2e/run-with-vite.mjs -- node e2e/issue-6202-picture-move-reflow.test.mjs --mode=headless`를 직접 실행했다. 그림 y는 `476.2 -> 548.2`, 그림이 비켜간 자리의 글자 띠 screenshot은 `49456B -> 46692B`로 달라졌고 두 회귀 assertion이 통과했다.
- code candidate의 CI, CodeQL, Render Diff, Adapter inter-diff, Proptest와 trusted CI impact policy가 성공했다. 직접 package script가 없는 수동 E2E라는 사실은 기존 suite 운용과 같아 단독 머지 차단 사유로 삼지 않았다.
- [검토 기록](../pr/archives/pr_6728_review.md)은 `승인`으로 판정했다. 다만 #6202 원본 `156483689...` fixture의 저장-재열기와 모든 placement 조합을 검증한 것은 아니며, PR 본문에 closing keyword가 없으므로 Issue #6202는 OPEN으로 유지한다.
- 이 기록은 source branch의 review-only trailing commit이다. 최신 head의 fast-pass aggregate와 mergeability를 재확인한 뒤 작업지시자 merge 승인을 받으며, merge 뒤 devel CI가 성공하기 전에는 contributor comment나 issue close를 하지 않는다.

## #6711 mydocs monthly archive governance

- [#6711](https://github.com/edwardkim/rhwp/issues/6711)을 등록하고 메인테이너에게 할당했다.
- `orders`, `plans`, `pr`, `report`, `working`의 이전 달 생성 문서를 각 `archives/`로 옮기는
  월별 거버넌스와 첫 2026년 8월 이전 문서 정리를 하나의 이슈에서 수행한다.
- root/archive 경로와 이슈·PR 상태를 분리하고, Git 최초 도입 시각·충돌 fail-closed·내부 링크
  원자 갱신·신규 오류 0건을 보호 불변식으로 삼는다.
- GitHub PR file API 3,000개 제한을 넘지 않도록 governance와 네 폴더, `working`을 최신
  `upstream/devel` 기준의 순차 PR로 처리한다.
- Stage 1에서 월별 archive 정본과 이동 기준선을 고정하고, Stage 2-A에서 `orders/plans`의
  cutoff 이전 후보 773개를 771 rename·동일본 중복 2건 제거·divergent 충돌 2건 보존으로 처리했다.
- [PR #6713](https://github.com/edwardkim/rhwp/pull/6713)을 `devel` 대상으로 생성했다. code
  candidate `968a35305b9b1b4ad209e64d97655df411754bf4`의 self-review에서 예상 밖 삭제·범위 누출과
  신규 링크·metadata 오류는 발견하지 않았으며 [review 기록](../pr/archives/pr_6713_review.md)에
  승인 판정과 trailing head 조건을 남겼다.
- 이 review와 오늘 기록만 추가한 후행 head의 GitHub Actions·최신 `devel` 정합·mergeability를
  확인한 뒤 별도 merge 승인을 받는다. #6711은 후속 `pr/report`, `working`, 전수 감사가 남아 있어
  close하지 않는다.
- PR #6713은 merge commit `009e30fe1f6812b046862589783c68f890b4d363`으로 `devel`에 병합됐고,
  merge SHA의 CI·CodeQL·Adapter·Proptest와 이슈 자동 종료 점검 workflow가 모두 성공했다. #6711은
  후속 batch가 남아 OPEN으로 유지했다.
- 최신 `upstream/devel`에서 Stage 2-B continuation branch를 만들고 `pr/report`의 cutoff 이전 후보
  832개를 830 rename·동일본 중복 2건 제거·divergent 충돌 2건 보존으로 처리했다. 기존 유효 링크
  6,623개는 모두 보존됐고 정규화된 historical broken-link 집합 2,544건에도 증감이 없다.
- [PR #6715](https://github.com/edwardkim/rhwp/pull/6715)을 `devel` 대상으로 생성했다. candidate
  `15d4a8f25a8fb7b081af7179a863393cd6e2764b`의 Full CI는 29 success·3 skipped·실패 0으로
  완료됐으며, REST 10페이지의 905개 파일과 로컬 diff가 일치했다.
- [self-review](../pr/archives/pr_6715_review.md)는 이동 손실·충돌·링크·canonical·최신 base를 다시
  검증해 `승인`으로 판정했다. 이 review와 오늘 기록만 추가한 trailing head의 Actions와 mergeability를
  확인한 뒤 별도 merge 승인을 받으며, #6711은 Stage 3·4가 남아 OPEN으로 유지한다.
- [PR #6726](https://github.com/edwardkim/rhwp/pull/6726)은 Stage 3-A로 `working` cutoff 이전
  후보 1,119건을 1,111 rename과 byte-identical root 중복 8건 제거로 처리했다. PR files API
  1,258건과 로컬 diff가 일치하며 보수적 경로 수는 2,369개다.
- 첫 head의 Gym 경로 실패와 첫 정정 head의 분할 조립 Rust 경로 실패를 실제 소비자 경로 정정과
  월별 archive 거버넌스 보완으로 해결했다. 최종 candidate `e3b0b8833f5a04cbe66b0542048309b715752813`은
  Full CI·CodeQL·Proptest·Adapter·Gym·Skill router에서 32 success·5 policy skip·실패 0이다.
- [self-review](../pr/archives/pr_6726_review.md)는 두 실패 head를 승인 대상에서 제외하고 최종
  candidate만 `승인`으로 판정했다. review와 오늘 기록만 추가한 trailing head의 Actions와
  mergeability를 확인한 뒤 별도 merge 승인을 받는다. #6711은 Stage 3-B와 Stage 4가 남아 OPEN이다.
- [PR #6730](https://github.com/edwardkim/rhwp/pull/6730)은 Stage 3-B로 `working` cutoff 이전
  후보 1,120건을 모두 archive로 이동했다. 목적지 충돌·동일본 중복 제거·divergent 충돌은 0건이며,
  107개 상대 링크·incoming source·활성 font evidence 소비자를 함께 현행화했다.
- code candidate `e44032cb9d16154c1c8b50aa1f1c184d9b720b86`은 Full CI·언어별 CodeQL·Proptest·Adapter와
  trusted Impact Policy를 통과했다. GitHub REST 1,227개 파일과 로컬 diff가 일치하고, 내부 링크
  9,224개와 유효 8,671개·historical broken 553개 집합을 그대로 보존했다.
- GHAS 종합 CodeQL이 기존 alert #186을 신규 경고로 잘못 귀속한 원인은 암호 로드 반환 객체에서
  폰트 runtime snapshot SHA-256까지 이어진 과도한 data-flow였다. 실제 `DocumentInfo`에는 암호가
  없으므로 근거와 재발 방지를 [#6731](https://github.com/edwardkim/rhwp/issues/6731)로 분리하고
  alert를 `false positive`로 분류해 exact-head CodeQL check를 정상화했다.
- [self-review](../pr/archives/pr_6730_review.md)는 이동 손실·링크·활성 소비자·merge tree와 CodeQL
  판정을 재검토해 candidate를 `승인`으로 판정했다. 이 review와 오늘 기록만 추가한 trailing head의
  Actions와 mergeability를 확인한 뒤 별도 merge 승인을 받으며, #6711은 Stage 4가 남아 OPEN이다.

## planet6897 layout batch integration review

- 최신 `upstream/devel` 위 local branch `review/planet6897-layout-batch-20260903`에서 planet6897의 PR #6675, #6676, #6678, #6680, #6682, #6688, #6692를 provenance-preserving `-x` cherry-pick으로 누적했다.
- reviewer `jangster77`을 각 source PR에 사전 지정했다. source PR의 comment/close는 integration merge와 실제 `devel` CI 성공 뒤에만 수행한다.
- #6680 conflict는 현재 `devel`의 `TAG_IMPLEMENTATION_PROPERTY` 기반 canonical inline offset 구현을 유지하고 contributor regression test/evidence를 통합해 보정했다. duplicate helper만 문서화한 source follow-up은 대상 소멸로 skip했다.
- all-features build를 막던 `vello 0.10`과 `vello_svg`의 `vello 0.9` Scene type 충돌은 `Cargo.toml`/`Cargo.lock`의 0.9 alignment로 최소 보정했다.
- 검증 완료: `node scripts/rust-test-suite-manifest.mjs --prepare`, `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --profile release-test --tests` (78 success summaries), `wasm-pack build --release --target web`.
- 2020 Hancom PDF와 Chrome webfont 144 DPI visual sweep stable evidence, PR별 review record는 [pr_6675_6692_planet6897_visual_sweep.md](../pr/archives/pr_6675_6692_planet6897_visual_sweep.md) 및 PR별 archive record에 남겼다. #6692는 43 physical PDF pages/86 logical rhwp pages의 N-up mapping이라 1:1 overlay를 pass/fail 기준으로 사용하지 않는다.
- 다음 단계: 이 날짜 정정 trailing commit을 원격 PR로 올린 뒤 latest-head CI와 mergeability를 확인한다. source PR의 comment/close는 integration merge와 실제 `devel` CI 성공 뒤에만 수행한다.

## Green-CI external PR batch review

- 열린 PR을 다시 분류해 요청대로 #6713과 draft PR, 재조회 시 `CONFLICTING/DIRTY`였던 #6637을 제외했다. #6683, #6690, #6698, #6703, #6705, #6709, #6710만 최신 source head의 CI/CodeQL/Render Diff/Adapter/Proptest가 성공 또는 정책상 skip인 것을 확인했다.
- 각 source PR에는 local integration 전에 reviewer `jangster77`을 지정했다. 최신 `upstream/devel` `009e30fe1`에서 `review/green-ci-batch-20260904-full`을 만들고 13개 source commit을 원래 순서의 `-x` cherry-pick으로 누적했다.
- #6202와 #5057의 external-corpus 회귀 테스트가 fixture 발견 뒤 API/ZIP 오류를 성공으로 삼키는 것을 검토에서 발견했다. 보정 commit `872f3d4c5`은 fixture 부재의 정책 skip은 유지하고, 그 뒤의 오류를 명시 실패로 바꾼다.
- PR별 archive review와 batch implementation record를 남겼다. 통합 head Rust lint/test 및 private corpus 기반 시각 검증은 아직 실행하지 않았고, source PNG는 통합 head 증적이 아니므로 approval 또는 source PR close/comment를 수행하지 않았다.
- #6202와 #5057의 private corpus HWP는 각각 `samples/issue6202/`와 `samples/issue5057/`의 정식 fixture로 등록했다. 테스트에서 환경 변수와 Windows 개인 경로 탐색을 제거하고 repository fixture 읽기를 필수화했으며, SHA-256 manifest와 raw-byte `.gitattributes` 규칙을 함께 추가했다.
- 메인터너 보정 뒤 `IR field sweep`, #1623 focused regression, 전체 `cargo test --profile release-test --tests`가 성공했다. source PR #6703, #6709, #6710의 판정은 원 head가 아니라 각각 `547277a1a`, `bc7baa359` 보정을 포함한 통합 후보 기준의 **메인터너 보정 후 수용 가능**이다.
- 기준 PDF는 `pdf/issue5941-1480000-201900698-2020.pdf`, `pdf/issue6202-156483689-2020.pdf`, `pdf/issue5057-21484591-2020.pdf`에 보관한다. #6703은 PR 본문·공개 fixture·회귀 계약으로 판단하며 rhwp 202페이지와 한컴 참고 PDF 205페이지의 차이를 blocker로 사용하지 않는다.

## #6729 planet6897 green source PR 통합

- [PR #6729](https://github.com/edwardkim/rhwp/pull/6729)는 #6716, #6719, #6721의 원본 head를
  provenance-preserving cherry-pick으로 최신 `devel` 위에 통합했다. #6697과 #6718의 private
  fixture 탐색은 공개 sample·manifest로 교체했고, native HWP5 zero-vpos reset은 저장
  line-segment 계약으로 한정했다.
- code/trailing-review head `17b68f2258c48f78c5bcaff4f99d98487c089050`의 실제 PR CI는
  CI [33869089172](https://github.com/edwardkim/rhwp/actions/runs/33869089172),
  CodeQL [33869089138](https://github.com/edwardkim/rhwp/actions/runs/33869089138),
  Render Diff [33869088883](https://github.com/edwardkim/rhwp/actions/runs/33869088883),
  Adapter inter-diff [33869089108](https://github.com/edwardkim/rhwp/actions/runs/33869089108),
  Proptest [33869089069](https://github.com/edwardkim/rhwp/actions/runs/33869089069),
  CI Impact Policy [33870364121](https://github.com/edwardkim/rhwp/actions/runs/33870364121)가
  모두 성공했다. `WASM Build`, frontend unit gate, nextest duration refresh는 해당 policy의
  expected skip이었다.
- 원 PR별 판정·메인터너 보정·자산 provenance는
  [#6716 검토](../pr/archives/pr_6716_review.md),
  [#6719 검토](../pr/archives/pr_6719_review.md),
  [#6721 검토](../pr/archives/pr_6721_review.md)와
  [통합 시각 증적](../pr/archives/pr_6716_6719_6721_planet6897_visual_sweep.md)에 남겼다.
  `export-png`/`export-svg`와 `rhwp-studio`의 known discrepancy 때문에 source/후보 PNG와
  PDF는 국소 증적 범위만 보존하며 최종 visual oracle로 과장하지 않는다.
- 이 오늘 기록은 #6729에 추가하는 trailing documentation commit이다. 새 head의 required CI와
  mergeability가 다시 성공한 뒤 일반 merge commit으로 병합하고, 그 뒤 devel CI 성공 시에만
  #6550 및 원 PR #6716, #6719, #6721의 comment/close를 `post_merge.md` 절차로 한 번 처리한다.

## planet6897 #6702, #6732 공개 fixture 통합 검토

- 최신 `upstream/devel` `d1831146587b1ac2346f9ed1216a64c2943a02f9`에서
  `review/planet6897-open-batch-20260904`를 만들고, reviewer `jangster77`을 원 PR #6702와
  #6732에 먼저 지정했다. 두 source head의 CI, CodeQL, Render Diff, Adapter inter-diff, Proptest,
  CI Impact Policy는 모두 success 또는 policy skip이었다.
- #6702의 원 code/report `e984794`와 test/evidence `a64d1e`를 `-x` cherry-pick한 뒤, private
  환경 변수와 Windows corpus를 찾지 못하면 성공하던 regression test를
  `samples/issue6697/80550-agricultural-machinery-act-amendment.hwpx`의 mandatory
  `include_bytes!` fixture로 보정했다. 이제 source CI와 통합 CI 모두 실제 공개 fixture를 읽어야 한다.
- #6732 `37d3dc8`은 saved-tail이 body bottom seat에 실제로 걸릴 때만 재배치를 막도록 조건을 좁힌다.
  공개 `issue5941` fixture에서 기준 native binary 305 pages와 후보 native binary 304 pages를 확인했다.
- native-skia 후보 PNG는 #6702 target caption 존재와 #6732 마지막 logical page 비공백이라는 국소 범위만
  확인했다. font/export 차이 때문에 Studio 동등성 또는 full-page pixel fidelity로 과장하지 않으며,
  파일·해시·한계는 [통합 시각 증적](../pr/archives/pr_6702_6732_planet6897_visual_sweep.md)에 남겼다.
- PR별 판정은 [#6702 검토](../pr/archives/pr_6702_review.md)의 **메인터너 보정 됨 수용 가능**과
  [#6732 검토](../pr/archives/pr_6732_review.md)의 **승인**이다. source PR comment/close, fork branch
  정리, integration PR 생성은 아직 수행하지 않는다. 작업지시자의 통합 PR 생성 승인 뒤에만 temporary
  upstream branch push와 PR 생성을 진행하고, merge 뒤 실제 devel CI 성공 후에만 `post_merge.md`에 따라
  source PR을 후속 처리한다.
