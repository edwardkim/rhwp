---
kind: daily-order
status: active
date: 2026-08-18
---

## #5201 2차 CI 보정

- 새 merge-ref의 정규 테스트 3개 샤드 실패를 조사해, CI 생성 산출물이 아닌 `edit` 하위명령 자기서술과 지식지도 §2-2 계약 불일치로 확정했다.
- `src/main.rs`: 공개 하위명령 중복을 첫 선언 기준으로 제거하고, USAGE를 실제 디스패치 88개와 동기화했다.
- `mydocs/manual/agent_knowledge_map.md`: `form-value` 봉투 5개 필드와 수식 `script`를 추가하고 §2-2 헤딩을 329개로 갱신했다.
- 로컬 통과: fmt, build, `capabilities_subcommands_contract` 4/4, `knowledge_map_field_dictionary_contract` 2/2, diff check. `tests/generated/regression_suite_*` 및 `tests/suites/manifest.json`은 사용자 지시대로 수정·stage하지 않는다.
- 보정 커밋을 만든 뒤 push하고 새 PR head의 CI가 모두 통과할 때까지 재감시한다.
- 추적 대상 `tests/suites/unit-test-tiers.json`은 소스 줄 번호 기준선 1건만 재계산했으며 검사를 다시 통과했다.

# 2026-08-18 오늘할 일

## kevin9327 누적 PR 통합 검토

- 대상: #4886, #5002, #5003, #5004, #5005, #5006, #5007, #5050, #5055, #5062, #5070, #5074, #5076, #5077, #5078, #5079, #5084, #5086, #5105, #5113, #5120, #5127, #5134, #5137, #5143, #5148, #5155, #5156, #5158
- 기준: `upstream/devel@ba097d6bf9f2`, 검토 branch `review/kevin9327-20260817`
- 결과: non-draft 원 PR head를 확인하고 누적 기능을 최신 devel 기준으로 통합했다. #5003은 중첩 표 검색 코드와 계약이 이미 기준선에 있어 빈 cherry-pick으로 건너뛰었다.
- #5158은 오래된 누적 branch의 기존 기능 되돌림을 피하기 위해 `set-section-def` 고유 기능과 후속 계약·문서 보정만 수동 병합했다.
- `tests/generated/regression_suite_*` 및 `tests/suites/manifest.json`은 생성 산출물 정책에 따라 통합하지 않았다.
- 각 PR별 archive review는 `mydocs/pr/archives/pr_<N>_review.md`에 작성했다.
- 로컬 누적 검증은 이미 완료된 결과를 사용한다. 이번 문서 단계에서 전체 Cargo 회귀를 다시 실행하지 않는다.
- `git diff --check`는 통과했다. fmt gate의 기존 포맷 차이와 생성 suite 참조 문제는 원격 CI/정책 확인 대상으로 기록한다.
- 원격 push, 통합 PR 생성, review comment, merge, 원 PR close는 별도 승인 후 수행한다.

## #5198 후속 통합 보정 (#5201)

- 기준: `upstream/devel@efbd8da6a84786dbdad8274c0ced49669e5f3e45`, 후속 PR [#5201](https://github.com/edwardkim/rhwp/pull/5201).
- 최신 CI 실패를 재현해 MCP capabilities의 양식 값 설정 등록 누락 2건과 도구 중복 등록 3건을 메인터너 보정했다.
- 로컬 release-test 계약 suite `029`·`031`·`032`와 fmt, Clippy, unit-tier, 자동 에이전트 문서, diff 검사를 통과했다.
- `tests/generated/regression_suite_*`와 `tests/suites/manifest.json`은 CI 생성 산출물로 남기며 커밋하지 않는다. 보정 commit push 뒤 최신 PR head CI가 통과하면 merge 및 후속 처리를 진행한다.
