# 오늘 할일 - 2026년 5월 13일

## M100 — v1.0.0 조판 엔진 (PR 처리 — 5/13 사이클)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #819 (Ref #260)** | feat — Ctrl+Backspace 단어 삭제 + Cmd+Backspace 줄 시작 삭제 | **완료 (옵션 B — 메인테이너 직접 구현 + PR close)** | 컨트리뷰터 @oksure (Hyunwoo Park). PR 제출(5/11) 이후 Alt+Backspace 단어 삭제가 이미 devel에 흡수 → 충돌(CONFLICTING). 신규 가치 2건(Ctrl+Backspace 단어 삭제 + Cmd+Backspace 줄 시작 삭제)을 메인테이너가 devel의 `moveToWordBoundary` API 활용하여 `handleCtrlKey()` switch에 직접 구현. tsc 에러 0 / cargo test 1232 passed / 작업지시자 시각 판정 ★ 통과. commit `151da003` / devel merge `e8cb19cb` / WASM 빌드 4,411,190 bytes. PR #819 close + 코멘트 (신규 가치 아이디어 참조 명시). 검토 보고서: `mydocs/pr/pr_819_review.md`. |
| **Task #704 (M100)** | 통합재정통계 페이지 분할 0.84 px borderline — `tests/issue_703.rs` `#[ignore]` 2 건 cleanup | **완료 (cleanup-only — 본질 정정은 Task #676 에서 이미 진입)** | Task #676 (PR #679, 커밋 `52ae6558`) 의 trailing empty paragraph 가드 (`typeset.rs:1182-1199` — `LAYOUT_DRIFT_SAFETY_PX` 영역 내 흡수, 이슈 본문 "옵션 B" 와 동일 시멘틱) 가 통합재정통계 2010.11/2011.10 케이스 본질 정정. 잔존하던 `tests/issue_703.rs` 의 `#[ignore = "Issue #704 ..."]` 2 건 강제 실행 시 GREEN 검증 → cleanup. 소스 코드 무변경. cargo test --release 1231 passed / 1 fail (`wasm_api::tests::test_empty_save_analysis` 기존 flaky, 단독 PASS, 본 task 무관). commit `2839a7c2` (Stage 1-2) + Stage 3 보고서 커밋 예정. 보고서: `mydocs/report/task_m100_704_report.md`. 후속: devel HEAD 의 lib test 54 clippy warnings 별도 이슈 등록 권장. |
