# 2026-09-05

## #6725 한글 클립보드 HWPJSON native paste 검토

- [PR #6725](https://github.com/edwardkim/rhwp/pull/6725)의 code candidate
  `00efe0e00538aab312336b5dc20e119f0cb4f004`를 검토했다. 한글 CF_HTML의 HWPJSON을 HWPX
  조각으로 변환해 기존 외부 문서 삽입 경로를 사용하고, HTML paste 및 저장 조판 없는 문단의
  layout 회귀를 함께 정정한다.
- 기여자 head 검토에서 발견한 unsupported control/그림 원본 오류의 무음 content 유실은
  메인터너 commit `d64c94cc...`로 fail-closed 처리했다. native 변환이 실패하면 스튜디오는 종전
  HTML paste 경로로 fallback하며, 빈 XML 또는 0-byte 그림으로 성공한 것처럼 삽입하지 않는다.
- field ID 수집은 remapper와 같은 중첩 control 범위를 따르도록 보정했다. CI가 금지하는 새
  source-side `cfg(test)` module은 제거해 test-tier 기준선을 유지했으며, production 보정과
  HWPJSON integration 회귀는 남겼다.
- public sample은 `samples/hwpjson/korea-gmn-198518529-physical-ai-strategy.hwpx`다. 공개
  korea.kr 다운로드 원본의 SHA-256은
  `639c0c64030b98228046fc9562a61d7fb5d6334ad95d8a92b0058b13078e3e74`이며 provenance는
  sample README와 [검토 기록](../pr/archives/pr_6725_review.md)에 남겼다.
- exact candidate의 Full CI, Rust/Python/JavaScript CodeQL, Canvas visual diff, Adapter
  inter-diff, Proptest, CI Impact Policy가 모두 성공했다. `WASM Build`, Frontend unit,
  nextest duration refresh는 policy상 expected skip으로만 기록한다.
- 최종 판정은 **메인터너 보정 됨, 수용 가능**이다. 이 review와 오늘 기록은 PR #6725에 추가하는
  documentation-only trailing commit이며, 새 head CI와 `MERGEABLE`/`CLEAN` 확인 및 별도 merge
  승인 전에는 병합이나 issue close를 수행하지 않는다.
## #6711 월별 archive 전수 감사와 최종 보고

- 최신 `upstream/devel@693c4b6b3edd1317934d6648449edcf47b0689e3`에서 Stage 4 전수 감사를
  수행했다. 기준선의 cutoff 이전 후보 3,844개는 Git rename 3,832개와 SHA-256 동일본 제거
  12개로 모두 귀결됐고, 기준선 9월 문서 72개와 상이 충돌 4개의 양쪽 보존을 확인했다.
- [최종 보고서](../report/task_m100_6711_report.md)는 cutoff 이전 root 잔여 0개, 예상 archive
  목적지 누락 0개, canonical 링크 오류 0개, historical broken 553건 유지, 기존 metadata 오류
  16건·신규 0건을 기록하며 메인테이너 승인을 받아 `final`로 전환됐다.
- [PR #6744](https://github.com/edwardkim/rhwp/pull/6744)의 code candidate
  `a3f2ab3e26939672126bc86d7705a14f74584868`은 계획서와 최종 보고서 2개만 변경한다. exact-head
  CI·CodeQL·Adapter·Proptest·Impact Policy가 성공했고 `MERGEABLE`·`CLEAN`이다.
- [self-review](../pr/archives/pr_6744_review.md)는 실제 2개 파일, 전수 감사 수식, 최신 base merge
  tree와 Actions를 재검토해 `승인`으로 판정했다. 자기 PR이므로 reviewer와 GitHub approve event는
  만들지 않는다.
- review·오늘할일과 보고서의 당월 root 수 설명만 추가한 trailing head를 push한 뒤 Actions와 최신
  `devel` 정합을 다시 확인하고 별도 merge 승인을 받는다. #6711은 merge SHA의 post-merge 검증이
  성공할 때까지 OPEN으로 유지한다.
- CodeQL alert #186은 메인테이너가 입력한 `used in tests`를 유지하며, 후속 #6731과 분리한다.

## #6731 CodeQL alert #186 근거화·재발 방지

- 최신 `upstream/devel@9bf5bcfd061b491c89be1ea28ef1fff8a892b6d1`에서 `task_m100_6731`
  브랜치를 만들었다.
- 최초 분석 `1659063897`(CodeQL 2.26.3)과 현재 분석 `1725324233`(2.26.4)이 동일한 네 flow를
  재현한다. 함수 이름 휴리스틱이 암호가 없는 `DocumentInfo` 반환값을 password source로 잡고,
  브라우저와 Node 계측의 공유 폰트 모듈·cache를 하나의 실행 세계로 연결한 결과다.
- 메인테이너의 최종 분류 `dismissed` / `used in tests`를 유지한다. query 제외·KDF 교체·광범위
  sanitizer 대신 password open command와 metadata query를 분리하는 최소 CQRS 경계를 우선 검증한다.
- [수행계획](../plans/task_m100_6731.md)이 승인됐다. Stage 1에서 SARIF 기준선을 보존하고 Stage 2
  RED 보호 계약을 작성한 뒤 구현 진입 판정을 받는다.
- Stage 1은 최초·현재 SARIF 네 flow를 정규화 증적으로 보존해 커밋 `ee64c2f48`로 고정했다.
- Stage 2 RED 계약은 단위 7건 중 기존·무암호 경계 6건이 통과하고, 아직 command/query가 분리되지
  않았다는 목표 계약 1건만 실패했다. 세 암호 포맷의 브라우저 기준선은 모두 통과했다.
- Stage 3에서 password open facade를 `void` command로 바꾸고 성공 뒤 `getDocumentInfo()` query를
  분리했다. 보호 계약 7건, Studio 전체 단위 1,376건, production build와 세 암호 포맷 E2E가
  통과했다.
- #4430 content-loss E2E는 변경 직접 호출부와 다수 저장 경계를 통과한 뒤, 8월 30일
  `2ca9aa90f7`에서 추가된 보호 Save As 기본 암호 계승과 갱신되지 않은 평문 사본 test가 충돌해
  후반 timeout을 재현했다. #6731 구현 회귀와 분리했으며 Stage 4 전에 최소 test 정정 범위를
  확정한다.
- Stage 4에서 #4430 평문 사본 시나리오가 `암호 없이 저장`을 선택하도록 test 한 곳만 현행화했다.
  #4430 전체 E2E, Studio 단위 1,376건, production build와 세 암호 포맷 E2E가 모두 성공했다.
- `upstream/devel`은 계획 기준선 `9bf5bcfd061b491c89be1ea28ef1fff8a892b6d1` 그대로다. 동일 파일을
  변경하는 PR #6725·#6637은 open이므로 push·merge 직전 다시 확인한다. alert #186의 final gate는
  remote push 승인 뒤 PR exact-head CodeQL로 수행한다.
- PR 생성 직전 재점검에서 공통 원자적 helper가 여전히 `DocumentInfo`를 만들어 버리는 내부 반환 경계를
  발견했다. helper도 `void` command로 바꾸고 평문 public API만 성공 뒤 metadata를 query하도록 보정해,
  중복 query와 CodeQL 잔여 flow 가능성을 PR 생성 전에 제거한다.
- PR #6748의 code candidate `751b4c5fc`와 base `9bf5bcfd`로 만든 exact merge ref를 CodeQL이 실제
  JavaScript/TypeScript 87개 rule로 분석했다. analysis `1727960739`의 결과와 merge ref open alert는
  모두 0건이며 CI·Render Diff·Proptest·Adapter도 성공했다.
- self-review에서 계획했던 운영 매뉴얼과 최종 보고서가 PR에 아직 없다는 blocker를 발견했다. Stage 5에서
  alert 귀속 시점 분리·`used in tests` 증적·post-merge 재확인 절차와 최종 결과를 보완하며, 메인테이너
  결과 승인 전까지 보고서를 `active`로 유지한다.
- 메인테이너가 Stage 5 결과를 승인해 [최종 보고서](../report/task_m100_6731_report.md)를 `final`로
  전환하고 [PR #6748 self-review](../pr/archives/pr_6748_review.md)를 `승인`으로 판정했다. 자기 PR이므로
  reviewer·GitHub approve event는 만들지 않는다.
- PR 생성 시 #6731의 assignee `edwardkim`, label `bug`·`ci`, milestone `v1.0.0`이 승계되지 않은 것을
  발견했다. trailing push·merge 전에 명시 승인으로 metadata를 정상화하고 API로 재조회한다.

## CI 그린 PR 9건 통합 검토와 보류 해소

- `review/ci-green-batch-20260905`에서 #6736, #6743, #6745, #6746, #6747, #6750, #6751, #6752, #6755를 검토했다. 작업지시로 #6637은 체리픽·검토 대상에서 제외했다.
- 커밋 후보 `8204bdb29999fdbf96c3227fa25ee860211fd3b1`의 전체 Rust 회귀 9,043개와 Native Skia 3종, host/WASM/전체 clippy, workspace build, host WASM build가 성공했다.
- 실제 브라우저에서 #6751 undo 후 셀 블록 복원 누락을 발견했다. 일반 SnapshotCommand의 선택 전달·보관을 메인터너 보정하고 회귀 2개를 추가했다. 보정 후 Studio 1,399개 통과/1 skip, TypeScript 통과, Delete→undo→redo·빈 셀 재삭제·Escape 후 입력이 성공했다.
- #6736의 IME는 실제 재조판 경계에서 CDP 입력으로 확인했다. #6745/#6746/#6755는 해당 페이지의 한컴 PDF 비교·OVL·현재 출력과 원 기여자 A/B 출처를 구분해 보존했다. 새 PDF 3건도 `pdf/`에 포함했다.
- 판정은 **승인 6건**, **메인터너 보정 됨, 수용 가능 3건(#6746/#6751/#6755)**, **현재 로컬 머지 보류 0건**이다. [개별 리뷰와 공통 증적](../pr/archives/ci_green_batch_20260905_visual_sweep.md)을 갱신했다.
- 재검토에서 남아 있던 overflow-cell 기준선 강화 누락을 보정했다. 두 기존 문서 71쪽·3쪽을 전부 렌더해 32개·0개를 확인하고, 허용값 48→32 및 87개 행 삭제를 반영했다.
- 보정 후 Rust 전체 회귀 9,043 통과/46 skip/실패 0(241.513초, exit 0), overflow-cell 16개 파티션 통과 및 새 dump와 강화한 기준선의 정확한 일치를 확인했다. 로그는 커밋 대상에서 제외하고 개별 리뷰와 공통 증적에 결과만 기록했다.
- 추가 TS 보정과 문서·증적은 아직 커밋 전이며, 통합 PR 생성·push·merge는 승인 대기다. 원 PR·이슈 댓글/close와 branch 정리는 실행하지 않았다.

## CI 그린 PR #6759~#6768 중 5건 통합 검토

- 기준 devel `2c144b180dd776aa450c499778510199ae6cdf89` 위의 `review/ci-green-6759-6768-20260905`에 #6759·#6760·#6762·#6763·#6768의 원본 출처를 보존한 여섯 커밋을 누적 적용했다. 체리픽 HEAD는 `d87b3037e5aeb6b662904b0182c361d5a2929108`이고 메인터너 보정은 `902a208b515e83024502f004a2adaf84c33f18de`로 커밋했고, 검토 기록은 같은 branch의 후속 문서 커밋으로 반영한다.
- [#6759](../pr/archives/pr_6759_review.md)와 [#6763](../pr/archives/pr_6763_review.md)는 승인, [#6760](../pr/archives/pr_6760_review.md)·[#6762](../pr/archives/pr_6762_review.md)·[#6768](../pr/archives/pr_6768_review.md)은 메인터너 보정 됨, 수용 가능으로 판정했다.
- #6756·#6764 원본을 정식 sample로 등록하고, 비공개 경로가 없으면 검사 없이 통과하던 테스트를 필수 sample 읽기로 보정했다. #6753의 lazy 기준 정규화 순서를 수정했고 #6764의 표 존재·첫 23개 source row·용지 경계 검증을 추가했다.
- 최초 전체 회귀의 새 sample baseline 실패 두 건은 기준 devel A/B 측정으로 원인을 확인했다. #6764의 text-overlap은 23→23, off-canvas는 6→5여서 새 경로에만 각각 23·5를 등록했으며 기존 fixture 기준선은 높이지 않았다.
- 보정 후 전체 Rust 회귀는 **9,049 통과, 0 실패, 46 skip**, Native Skia library는 4,112 통과·13 ignored, placeholder 2건·direct PDF 4건 통과였다. Studio는 1,403 통과·1 skip였고 TypeScript, native·WASM·workspace Clippy, fmt, workspace·WASM 빌드와 suite 정책 검사가 통과했다.
- #6763은 CDP로 실제 속성 폼과 설정 버튼을 거쳐 높이 `1→1→283→283`, 너비 `17716` 보존과 재열기 `62.50 / 1.00` mm를 확인했다. 임시 검증 스크립트의 포커스·숫자 선택 문제를 보정했으며 제품 소스는 추가 수정하지 않았다.
- [시각 증적](../pr/archives/pr_6759_6768_visual_sweep.md)에 native SVG/Chrome raster와 한컴 PDF의 실제 비교 범위 및 Studio CDP 결과를 기록했다. 코멘트용 최종 PNG 9개, 신규 기준 PDF 2개를 남기고 기존 #6753 PDF를 재사용했다. 임시 PNG·SVG·분석 JSON·로그는 커밋하지 않는다.
- #6764는 큰 표 조각 초과 축만 수용했다. 제목 문단 초과·쪽 배분 차이가 남아 issue는 종료하지 않는다. 추가 renderer Studio canvas 자동화의 미완료 상태를 native 시각 대조 성공과 혼동하지 않았다.
- 이번 PR 준비에서는 이미 통과한 테스트·lint·빌드를 재실행하지 않고 위 실제 결과를 재사용했다. 새 통합 PR의 required CI는 별도로 확인해야 한다.
- 개별 review·공통 검증 기록·오늘할일·본문 초안까지 준비했다. 작업지시자의 승인으로 메인터너 보정 commit을 완료했고 검토·증적 문서 커밋과 upstream push·통합 PR 생성을 진행한다. merge와 원 PR/issue 댓글·close·branch 정리는 아직 실행하지 않았다.
