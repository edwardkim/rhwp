# 오늘 할 일 - 2026년 8월 7일

## #536 - Canvas2D 한컴 사각 안 숫자 PUA parity

- [PR #4122](https://github.com/edwardkim/rhwp/pull/4122)의 17쪽 pagination 수정 위에
  `task_m100_536_canvas2d_boxed_pua` stacked branch를 만들었다. 자식 PR의 base는
  `task_m100_4069`이며, #4122 merge 뒤 `devel`로 retarget한다.
- 작업지시자가 `issue2007_nested_cell_pagination_42065.hwp`의 정상 한컴 PDF를 교체했다.
  새 정답지 SHA-256은 `9b0390f856bb9ad43337679babf6677209b7c7ab678b6616fcc6d6d5551ff1c4`다.
- 대상 `U+F02B1`은 `CharOverlap`이 아니라 raw `TextRun`이다. CanvasKit의 결정적 사각형+숫자
  합성 경로와 달리 기본 Canvas2D가 브라우저 글꼴에 raw PUA를 맡긴 것이 근인이다.
- Canvas2D에 `U+F02B1..U+F02C4` bounded vector fallback을 추가하고 실제 HWP 물리 2쪽 E2E를
  고정했다. 17쪽·raw PUA·사각 외곽·내부 숫자 잉크가 통과했고 표준 WASM build도 성공했다.
- 작업지시자 승인 뒤 release library 3,286 passed, 전체 `release-test --tests`, Native Skia
  58+2+4, fmt·diff·clippy·doc test, TypeScript, Studio 763 tests와 최종 WASM/E2E를 통과했다.
  [stacked Draft PR #4139](https://github.com/edwardkim/rhwp/pull/4139)를 생성했고 author
  assignee, milestone `v1.0.0`, `bug`·`rust`·`javascript`·`rhwp-studio`·`rendering`·`test`
  labels로 1차 트리야지를 완료했다. 작업지시자 최종 시각 판정도 통과했으며, 현재 상태는 #4122
  선행 merge와 #4139의 `devel` retarget·CI 대기다.

상세 근거: [#536 Stage 1](../working/task_m100_536_stage1.md),
[#4139 검토](../pr/archives/pr_4139_review.md).

## kevin9327 누적 PR 검토 - #4105, #4106, #4108, #4114, #4116

- 초기 `upstream/devel` `9f564bbe` 위에 다섯 PR의 원 commit을 순서대로 누적 cherry-pick했고 충돌은 없었다.
- #4106의 착수 잠금은 단순 `착수` 단어 인용을 잠금으로 오인할 수 있어, `착수합니다 — <범위>` 형식만
  인정하도록 메인터너 보정과 Python 회귀 2건을 추가했다.
- #4105의 protocol, #4108의 R94와 기계 집계, #4114의 R7, #4116의 R74·R79을 누적 구현 상태와
  정렬했다. README 생성 결과는 `완료 31 · 실측 9 · 문서 7 · 이슈 4 · 가설 49 = 100`이다.
- CI 워크플로 변경은 작업지시자 지시에 따라 별도 revert했다. Python·문서 보정 뒤 Cargo 전체 회귀는
  실행하지 않았으며, 실행한 focused Python/Rust 계약, fmt, clippy, actionlint는 모두 통과했다.
- 최초 #4140 node-binding CI는 capabilities 34개 선언과 Node 31개 생성 타입의 drift, 세 wrapper 누락을
  발견했다. 메인터너 보정으로 `export-plan-schema`·`export-agent-manifest`·`explain` wrapper와 argv
  회귀를 추가하고 생성 타입을 재생성했다. 실제 Node 17 files·445 tests, typecheck, build, gen:check를
  통과했다.
- #4140과 보정 head는 원격에 push했다. 이 파일의 #536 기록과 add/add 충돌을 보존하도록 최신
  `upstream/devel` `a5698255e`를 merge했고, 새 head의 CI·CodeQL 성공을 확인한 뒤 merge한다.

상세 근거: [누적 구현·검토 기록](../pr/archives/pr_kevin9327_20260807_review_impl.md),
[PR #4105](../pr/archives/pr_4105_review.md), [PR #4106](../pr/archives/pr_4106_review.md),
[PR #4108](../pr/archives/pr_4108_review.md), [PR #4114](../pr/archives/pr_4114_review.md),
[PR #4116](../pr/archives/pr_4116_review.md).
