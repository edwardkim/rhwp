# 오늘 할일 - 2026년 5월 8일

## M100 — v1.0.0 조판 엔진 (Task #595 후속 sweep 결함 정정 사이클)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **Task #685** | rhwp-studio 그리드 모드 (zoom ≤ 0.5) click 좌표 단일 컬럼 가정 14곳 분기 일괄 어긋남 정정 | **완료 (Stage 1+2+3, 부분 정정 — 작업지시자 scope 결정)** | Task #595 후속 sweep 으로 발견된 한컴 호환 결함. 분기 기준점: `da7461d` (Merge local/devel: Task #595 본 작업 + 후속 sweep, Issue #685/#686 등록 포함). **본질 결함**: `virtual-scroll.ts` 의 그리드 모드 (`pageLefts[i] = marginLeft + col*(pw+gap)`) 가 `input-handler-mouse.ts` 14곳 모두 단일 컬럼 가정 공식 `(scrollContent.clientWidth - pageDisplayWidth) / 2` 만 사용해 그리드 모드 click 좌표 ±수백 px 어긋남. **정정 (3 단계)**: (Stage 1) `virtual-scroll.ts` 에 `getPageLeftResolved(pageIdx, containerWidth)` 헬퍼 신규 (+12 LOC) — 그리드 모드 `pageLefts[i]` / 단일 컬럼 `(containerWidth - pageWidth) / 2` fallback. `input-handler.ts:2572-2586 formBboxToOverlayRect` 의 verbose sentinel 패턴 1곳도 헬퍼 호출로 단순화 (-3/+1 LOC, 동치 refactor). 기존 `getPageLeft` 4 호출자 (`canvas-view.ts`/`field-marker-renderer.ts`/`caret-renderer.ts`) 무회귀 보존. (Stage 2) `input-handler-mouse.ts` 14곳 (라인 23/129/176/279/296/357/431/**475**/**811**/**889**/931/1146/1196/1243) 헬퍼 일괄 치환 (+14/-14 LOC, 1:1 표현식 치환). `pw`/`pageDisplayWidth` 변수 + 페이지 인덱스 변수 (`pi`/`pageIdx`/`picBbox.pageIndex`) 모두 보존. 6 회 Edit (replace_all 3 + 컨텍스트 단건 3, prefix 분리). (Stage 3) `e2e/grid-mode-click-coord.test.mjs` assert 강화 (+54 LOC) — `dumpGridState` 헬퍼 동치성 + `probeClickAtPage` last-col only strict assert (non-last col 은 후속 결함 #689 로 SKIP 안내). 추가 probe: zoom=0.25 last col + zoom=1.0 baseline. 진단노트 `mydocs/troubleshootings/grid_mode_click_coord.md` 끝부분에 "정정 완료 — 부분 정정 (Task #685)" + #689 안내 +20 LOC. **검증**: typecheck 무에러 / vite build 정상 / `body-outside-click-fallback.test.mjs --mode=headless` 무회귀 / `grid-mode-click-coord.test.mjs --mode=headless` **PASS=11/FAIL=0/SKIP=2** (의도된 non-last col). last-col 정합 자동 회귀: zoom=1.0 page 0, zoom=0.5 page 1, zoom=0.25 page 4 모두 helperResolved max delta=0.00px + cursor.rectPageIdx 정합. **시각 검증 (작업지시자 직접, hwpctl_action_table_v11.hwp 그리드 모드, 2026-05-08)**: non-last col 페이지(1, 2, 4, 5 등) 어긋남 = #689 시각 재현 / last col 페이지(3, 6, 9 등) 정상 = #685 정정 효과 확인. **Stage 3 발견사항 → Issue #689 분리 등록**: `virtual-scroll.ts:133-140 getPageAtY(docY)` 가 Y 좌표만 보고 row 의 last page idx 만 반환 → non-last col click 어긋남 (Task #685 의 pageLeft 정정과 별개의 결함 영역). **작업지시자 결정 (scope 엄격 준수)**: Task #685 본문 명시 범위 (pageLeft 공식 14곳) 그대로 유지 + #689 분리 등록 + 즉시 #689 시작. 정정 방향: `getPageAtPoint(docX, docY)` 헬퍼 + 14곳 일괄 치환. 커밋 `769f534` (Stage 1) + `d982d50` (Stage 2) + `7fdf01d` (Stage 3). 수행계획서: `mydocs/plans/task_m100_685.md`. 구현계획서: `mydocs/plans/task_m100_685_impl.md`. 단계 보고서: `mydocs/working/task_m100_685_stage{1,2,3}.md`. 최종 결과 보고서: `mydocs/report/task_m100_685_report.md`. **`feedback_process_must_follow` 메모리 권위 영역 강화** — Stage 3 추가 결함 발견 시 scope 확장 충동 억제 + 작업지시자 결정 후 #689 분리 등록 패턴 정합. **`feedback_hancom_compat_specific_over_general` 정합** — 그리드 모드 한컴 호환 결함 (한컴 정상 / RHWP 어긋남) 영역 본질 정정. **회귀 차단 가드 영구 보존** — e2e assert 강화로 last-col 정합 자동 회귀 차단. **부분 정정 명시 패턴** — 진단노트 + 결과보고서 + orders 모두 "Task #685 = 부분 정정, #689 후속" 명시로 후속 영역 추적 가능성 확보. |
| **Issue #689** | rhwp-studio 그리드 모드 `getPageAtY` X 좌표 무시 — non-last col 페이지 click 어긋남 정정 | **완료 (Stage 1+2+3, Task #685 누락분 10곳 동반 정정 — 작업지시자 시각 검증 통과)** | Task #685 Stage 3 e2e assert 강화로 노출된 후속 결함. M100 (v1.0.0), 우선순위 High (UX-blocking). 분기 기준점: `7651d91` (Merge local/devel: Task #685). **본질 결함**: `virtual-scroll.ts:133-140 getPageAtY(docY)` Y-only loop 가 그리드 모드의 row-shared `pageOffsets[i]` 환경에서 항상 last page idx 만 반환 → non-last col click 시 row 의 last col 페이지로 cursor 처리. **정정 (3 단계)**: (Stage 1) `virtual-scroll.ts` 에 `getPageAtPoint(docX, docY)` 헬퍼 신규 (+33 LOC) — 단일 컬럼은 `getPageAtY` 동치, 그리드는 row(Y) 안에서 X 로 col 결정, gap 영역은 가장 가까운 페이지 fallback. coordinate-system.ts:18 의 `getPageAtY` 는 dead code (`documentToPage` 호출자 0건) 확인 → 미수정. (Stage 2) 마우스 컨텍스트 18곳 `getPageAtY` → `getPageAtPoint` 치환 (input-handler-mouse 12곳 + input-handler.ts 4곳 + table 1곳 + picture 1곳, 5 회 `replace_all`). 동시에 Task #685 sweep 누락 buggy pageLeft **10곳 동반 정정** — input-handler.ts 4곳 + table 3곳 (L400/74/111) + picture 1곳 + connector 2곳 (L85/152). 추가 4곳 (table L74/L111 + connector L85/L152) 은 `getPageAtY` 호출 없이 (state 또는 함수 매개변수에서 pageIdx 획득) buggy pageLeft 패턴 사용 — sweep 중 추가 발견 → 작업지시자 추가 승인 (2026-05-08) 으로 동반 정정. (Stage 3) `e2e/grid-mode-click-coord.test.mjs` non-last col SKIP 분기 제거 → 모든 col strict assert 일원화. zoom=0.25 last-col only probe → for 루프로 col 0~4 5건 모두 probe. 진단노트 `mydocs/troubleshootings/grid_mode_click_coord.md` 끝부분에 "완전 정정 완료 (Task #685 + #689 결합)" +18 LOC. **검증**: typecheck 무에러 / vite build 정상 / `body-outside-click-fallback.test.mjs --mode=headless` 무회귀 / `grid-mode-click-coord.test.mjs --mode=headless` **PASS=21/FAIL=0/SKIP=0** (이전 SKIP 2건 모두 PASS 로 전환). 모든 col rectPageIdx 정합: zoom=0.5 page 0 (1→0), page 2 (3→2), zoom=0.25 col 0~4 모두 정합. 단일 컬럼 zoom=1.0 무회귀. **시각 검증 (작업지시자 직접, hwpctl_action_table_v11.hwp 그리드 모드, 2026-05-08)**: 이전 단계 시점에 보고된 "1, 2 페이지 클릭 안 됨" 결함 정합화 — "오 정상적으로 잘 된다." 평가. **코드 품질 검토 (작업지시자 review, 2026-05-08)**: `getPageLeftResolved(pageIdx, containerWidth)` API 의 `containerWidth` 인자 leaky abstraction 검토 — Option A (현재, stateless click 시점 clientWidth 사용) vs Option B (`setPageDimensions` 시점 단일 컬럼도 실좌표 저장) 비교. **결정**: Option A 유지 — A 는 stateless 라 robust (race condition 없음, ResizeObserver 누락 케이스 영향 없음, 미래 코드 변경 시 fragile 안 함), B 는 stateful 이라 setPageDimensions 호출에 의존. verbose trade-off 보다 robustness 우선. 커밋 `a22cbf0` (Stage 1) + `8369ebd` (Stage 2) + `5db03a8` (Stage 3). 수행계획서: `mydocs/plans/task_m100_689.md`. 구현계획서: `mydocs/plans/task_m100_689_impl.md`. 단계 보고서: `mydocs/working/task_m100_689_stage{1,2,3}.md`. 최종 결과 보고서: `mydocs/report/task_m100_689_report.md`. **Task #685 + #689 결합으로 그리드 모드 click 한컴 호환 완성** — 추가 후속 이슈 없음. **`feedback_process_must_follow` 메모리 권위 영역 강화** — Stage 2 sweep 중 추가 buggy pageLeft 4곳 발견 시 즉시 작업지시자 승인 받아 scope 확장 패턴 정합. **DRY 정합** — `getPageLeftResolved` 재사용으로 신규 좌표 계산 코드 0줄 (#685 도입분 활용). **stateless API 설계 정합** — robustness 우선 결정으로 race condition 없는 click-time 측정값 사용 패턴 권위 영역 확보. **회귀 차단 가드 영구 보존** — e2e PASS=21 strict assert 모든 col 정합 자동 회귀화 + 진단노트 권위 기록 + Task #685 와 #689 결합 효과 명시. |

## 작업 메모

### Task #685 본질
- **`feedback_process_must_follow` 메모리 권위 영역의 권위 케이스** — Stage 3 e2e assert 강화 시 추가 결함 (`getPageAtY` X 무시) 발견. scope 확장 충동 억제 + 작업지시자 결정 받아 #689 로 분리 등록.
- **하이퍼-워터폴 절차 정합** — 수행계획서 → 구현계획서 → Stage 1/2/3 → 최종보고서 모두 작업지시자 승인 게이트 거침. 단계별 보고서 (`_stage{N}.md`) + 단계별 커밋.
- **DRY 정합** — `getPageLeftResolved` 헬퍼 도입 시 기존 `input-handler.ts:2579` 의 verbose sentinel 패턴 (이미 sentinel-aware 직접 풀어 씀) 도 같은 헬퍼로 정리하여 총 15곳 통일. 작업지시자 결정 (Stage 0 게이트).
- **회귀 위험 영역 좁힘** — `canvas-view.ts` / `field-marker-renderer.ts` / `caret-renderer.ts` 4 호출자 미수정 (이미 그리드 인프라 정상 처리 중, 헬퍼 통일 욕심 시 회귀 위험).
- **HWP IR 표준 직접 사용** — `virtualScroll.pageLefts[i]` (이미 그리드 인프라가 채우는 정합 데이터) 그대로 적용. 신규 좌표 계산 코드 0줄.
- **시각 검증 일관성** — 작업지시자가 `hwpctl_action_table_v11.hwp` 그리드 모드에서 직접 시각 확인 → last col 정합 + non-last col 어긋남 = #689 시각 재현. 자동 e2e (PASS=11/SKIP=2) 와 시각 결과 정합.

### Task #685 vs Issue #689 영역 분리 정합
| 결함 영역 | Task #685 (완료) | Issue #689 (시작 예정) |
|----------|-----------------|------------------------|
| pageLeft 공식 (단일 컬럼 가정) | ✅ 정정 (`getPageLeftResolved`) | n/a |
| 페이지 인덱스 결정 (Y-only) | n/a | 🔄 정정 예정 (`getPageAtPoint`) |
| 영향 범위 | last col 정합 | non-last col 정합 |
| 검증 | last-col e2e assert | (#689 e2e 강화 예정) |

### #689 즉시 시작 절차
1. `local/task685` → `local/devel` 머지 (작업지시자 권한, `--no-ff`, "Merge local/devel: Task #685 ... (closes #685, partial — #689 후속)")
2. 이슈 #685 close (부분 정정 명시 코멘트 권장)
3. `local/devel` 에서 `local/task689` 분기
4. #689 수행계획서 작성 → 승인 게이트
## M100 — v1.0.0 조판 엔진 (PR 처리 — 5/7 사이클 영역의 연속 영역)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #684 (#449)** | rhwpDev 디버깅 툴킷 결함 수정 — paragraph 식별 + 다중 매치 (#449 후속) | **완료 (cherry-pick 머지 + push, PR #684/Issue #449 close, Issue #692 신규)** | 컨트리뷰터 @oksure (Hyunwoo Park, oksure@gmail.com) — 8번째 사이클 PR. v0.7.10 후 처리 PR (5/8). 옵션 A — 2 commits cherry-pick (`44d532e` + `9792fee`, author oksure + Co-Authored-By Claude Opus 4.6 보존) + 본 환경 정정 보강 2 commits (`f73177a` main.ts 중복 import 정정 + `a1ea189` search() 무한 루프 가드 옵션 B). **결함 #1 (paragraph 식별 영역)**: `containerKey()` 영역 도입 — body vs cell[p,c,i] 분리. **결함 #2 (다중 매치 영역)**: `search(text)` 영역 SearchResult[] 배열 반환. **본 환경 정정 보강 (작업지시자 직접 영역 무한 루프 위험 발견)**: SearchResult 인터페이스에 `wrapped?: boolean` 추가 + wrap-around 가드 + MAX_MATCHES=10000 한계 + 후진 가드 영역 4 가드. 검증: cargo test --lib 1157 / clippy 0 / build --release / Docker WASM 4,572,439 bytes / rhwp-studio npm run build 통과. **메인테이너 Chrome 웹 콘솔 검증**: ✅ findNearest(618) 통과 — `closest paraIdx=0 (cell[p0,c2,i0], distance=618) "※ "` (결함 #1 정정의 container 정보 정합) / ⚠️ search(text) 미정합 → 후속 Issue #692 등록 영역 분리. devel push `b051dd0..a1ea189`. PR #684 close + Issue #449 수동 close + Issue #692 신규 등록. 처리 보고서: `mydocs/pr/archives/pr_684_report.md`. 검토 보고서: `mydocs/pr/archives/pr_684_review.md`. **`feedback_visual_judgment_authority` 권위 사례 강화 영역 — PR #602 + 본 PR 영역의 메인테이너 게이트웨이 방식의 권위 사례 영역 + 본 환경 정정 보강 영역의 무한 루프 위험 발견 영역 (작업지시자 직접 영역 사용 영역) + 후속 영역 분리 영역 (search → Issue #692) + Co-Authored-By Claude 패턴 정합 영역 모두 정합.** |
| **PR #638 Task #634** | 한컴 호환 쪽번호 표시 동작 검증 + 회귀 방지 테스트 | **close (본질 결함 발견 → 후속 PR 재제출 권고)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 10번째 사이클 PR. PR 본문 영역의 src 변경 0 + 회귀 방지 통합 테스트 8건. **메인테이너 검토 영역에서 본질 결함 3건 발견**: (1) `pagination/engine.rs:516-531` 영역의 `page_hides` 수집이 본문 paragraph 만 대상, 셀 안 PageHide 무시 (page 2 쪽번호 결함의 본질). (2) `layout.rs:404-407` 영역의 `hide_border` + `hide_fill` 가드 부재 (6 필드 중 2 미적용). (3) `main.rs:1897-1920` 영역의 dump 셀 안 PageHide 분기 부재. **본 환경 직접 측정**: aift.hwp page 2 셀[167] p[3] PageHide 6 필드 모두 true 영역. 작업지시자가 [감추기] 다이얼로그 영역으로 직접 확인 영역. **PR #641 의 cover-style 휴리스틱 영역도 우회 (workaround) 영역으로 분석** → 컨트리뷰터에게 정석 처리 (셀 안 PageHide 수집 + hide_border/hide_fill 가드 + dump 분기) 영역 안내 + 후속 PR 재제출 권고 영역. devel 반영 부재. PR #638 close + 한글 댓글 등록. 처리 보고서: `mydocs/pr/archives/pr_638_review.md` (close 영역 영역 영역 review 영역만). **`feedback_visual_judgment_authority` 권위 사례 강화** — 결정적 검증만으로 부족, 메인테이너 시각 판정 + 본질 결함 발견 영역의 권위 사례 영역 누적 영역. |
| **PR #649 Task #517 (재적용)** | Layout 리팩터링 Phase 1 디버그 인프라 누락 회귀 정정 — closes #647 | **close (PR #650 영역에서 두 commit 함께 cherry-pick 영역으로 통합 처리, devel 반영 부재)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 13번째 사이클 PR. 단일 commit `ffb32ff7` (원 commit `9c16a1b4` cherry-pick + `(cherry picked from commit 9c16a1b4)` 영역 author 정합). +528/-0 LOC, 7 files. 본질: 묶음 머지 `a7e43f9 (Task #517/#518/#519/#520/#521/#523/#528)` 영역에서 `local/devel` 까지만 머지되고 `devel` 으로 승격 누락 — PR #620 (Task #519, Issue #618) 동일 패턴 선례. 본 환경 cherry-pick simulation 충돌 0건. 결정적 검증 1165 lib + clippy clean. **env-var 미설정 / 설정 영역의 SVG md5 동일** (`842c9513bbbb833c5ba1ad27bac52694`) — 기본 동작 무영향 확증, 시각 판정 면제 합리. **PR #650 영역의 의존성 안내**: PR #650 (Task #518 Phase 2 본질 정정) 이 본 PR `ffb32ff7` + `e8dd3f0f` 두 commit 포함. 작업지시자 결정 — "이미 다음 PR 이 도착해있으니 이번 PR 은 close 로 간단하게 처리". PR #649 close + 한국어 댓글 등록. Issue #647 OPEN 유지 (PR #650 머지 시 close 영역 정합). 처리 보고서: `mydocs/pr/archives/pr_649_close_report.md`. 검토 보고서: `mydocs/pr/archives/pr_649_review.md`. **`feedback_close_issue_verify_merged` 권위 사례 강화** — 묶음 머지 누락 영역의 본질 정합 + close 영역의 devel 머지 영역의 후속 PR 영역 통합 처리 영역 정합. **`feedback_visual_regression_grows` 권위 사례 — 면제 영역의 합리화** — env-var-checked 본질 영역의 SVG md5 동일성 영역 직접 확증 영역으로 시각 판정 게이트 면제 영역 정합 (런타임 동작 무영향 영역의 합리화 영역). |
| **PR #645 Task #595** | hit_test_header_footer 영역이 본문 침범 정정 — closes #595 | **완료 (옵션 B — 5 commits 단계별 보존 머지, merge commit `11f3bf13`, 별도 이슈 #685/#686 컨트리뷰터 자체 등록)** | 컨트리뷰터 @johndoekim (johndoe, amok0316@gmail.com) — **첫 번째 사이클 PR**. `exam_math.hwp` 2페이지+ 수식 더블클릭 시 머리말 편집 모드 잘못 진입 결함. 본질: `hit_test_header_footer_native` 가 `build_page_tree` 의 Header bbox (자식 단 구분선 line 까지 `expand_bbox_to_children` 으로 확장된 영역, 본문 80% 침범) 사용 → 본문 영역을 머리말로 잘못 인식. 정정: `find_page` + `layout.header_area`/`layout.footer_area` 직접 hit 판정 (단일 함수 +37/-24 LOC). `expand_bbox_to_children` 의도 (#42 머리말 표 셀 안 Picture 클리핑 방지) 보존. 부수 효과: mousedown 마다 호출되던 트리 빌드 비용 제거. 본 환경 cherry-pick simulation 충돌 0건. 결정적 검증 1165 lib + 5 issue_595 ALL PASS. 광범위 sweep 169 fixture / 1788 페이지: 본문 hit:false **1788/0** ✅, 머리말 hit:true **1334/0** ✅, 꼬리말 hit:true 1361/16 (Issue #686 별도 영역). WASM 빌드 4,573,671 bytes. **작업지시자 시각 판정 ★ 통과**: exam_math.hwp page 1+ 수식 더블클릭 → 수식 편집기 정상 진입 확인. 컨트리뷰터의 TDD Stage 1~3 + 후속 영역 절차 정합 (수행/구현 계획서 + 단계별 보고서 + 최종 보고서 + 회귀 차단 가드 영구 보존). 별도 이슈 분리 등록: **Issue #685** (rhwp-studio: zoom ≤ 0.5 그리드 모드 click 좌표 단일 컬럼 가정, 14곳 분기 일괄 어긋남, 한컴 호환 결함 framing) + **Issue #686** (rhwp-studio: master page 글상자 더블클릭 시 첫 페이지로 점프, `hwpctl_Action_Table__v1.1.hwp` 16p, 한컴 mismatch). 회귀 차단 가드: `tests/issue_595.rs` (5 케이스) + `examples/inspect_595_regression.rs` (169 fixture / 1788 페이지 sweep) + `rhwp-studio/e2e/issue-595.test.mjs` (1365×1018 사용자 환경 모사). 검토 보고서: `mydocs/pr/archives/pr_645_review.md`. 처리 보고서: `mydocs/pr/archives/pr_645_report.md`. **`feedback_visual_judgment_authority` 권위 사례 강화** — 결정적 검증 + 광범위 sweep 1788/0 외에 작업지시자 시각 판정 게이트 통과 정합. **`feedback_v076_regression_origin` 권위 사례** — 컨트리뷰터 환경 (macOS + Chrome) + 작업지시자 환경 (Linux + WSL2) 두 영역 모두 시각 판정 통과로 환경 차이 회귀 가능성 차단. **`feedback_assign_issue_before_work` 권위 사례 강화** — Issue #595 assignee 부재가 외부 기여자에게 "오픈 타스크" 로 인식된 영역 (첫 번째 컨트리뷰터 케이스). |
| **PR #644 Task #643** | 페이지 분할 드리프트 정정 (5축 정합) — closes #643 | **완료 (옵션 B — 5 commits 단계별 보존 머지, merge commit `42bb7946`, Issue #716 별도 등록)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 12번째 사이클 PR. 5축 누적 드리프트 분해: (1) pagination fit 마지막 줄 lh-only, (2) LAYOUT_DRIFT_SAFETY_PX 10→4px, (3) VPOS_CORR backward 1.0→8.0px, (4) VPOS_CORR end_y sb_N 사전 차감, (5) Task #404 vpos_end `line_segs.last()` 직접 사용. 핵심 통찰: HWP `vpos_(N+1) - vpos_N = lh_total + ls_total + sa_N + sb_(N+1)` vs Layout `sb_N + lh_total + ls_total` → `sb_N ≠ sb_(N+1)` 시 누적 드리프트. 본 환경 cherry-pick simulation 충돌 0건. 결정적 검증 1221 테스트 ALL PASS (issue_643 신규 + task554 sweep 12/12 + svg_snapshot 7/7 + issue_598 4/4). 페이지 수 정합 회복: `2022년 국립국어원 업무계획.hwp` 40→35 페이지. WASM 빌드 완료 (4,572,438 bytes). **메인테이너 시각 검증에서 별건 결함 발견**: `samples/20250130-hongbo.hwp` page 1 마지막 줄 LAYOUT_OVERFLOW_DRAW (line=2 y=1048.2 overflow=20.1px). devel 과 PR-merged devel 의 page 1 SVG md5 동일 (`ab0de6c2e9ecc015402e7acfa091fa02`) → **PR #644 도입 회귀 아님**, devel 기존 결함. 결함 위치: `src/renderer/layout/paragraph_layout.rs:875` Task #332 Stage 4b 잔존 영역 (Stage 5 미수행). 작업지시자 옵션 A 결정 — PR #644 머지 유지 + Issue #716 별도 등록 + 추후 처리. 검토 보고서: `mydocs/pr/archives/pr_644_review.md`. 처리 보고서: `mydocs/pr/archives/pr_644_report.md`. 디버그 오버레이: `output/debug/pr644-page1-debug.svg`. **`feedback_v076_regression_origin` 권위 사례 강화** — 작업지시자 시각 판정 게이트가 컨트리뷰터 결정적 검증 통과 후에도 별건 결함 검출하는 본질 작동 확인 영역. **`feedback_visual_regression_grows` 권위 사례 강화** — task554 sweep 12/12 통과 + 페이지 수 정합에도 hongbo 결함은 시각 판정에서만 검출. |
| **PR #641 Task #639** | 한컴 호환 — cover-style 페이지 자동 쪽번호 미표시 (closes #639) | **close (PR #711 본질 정정으로 supersede, devel 반영 부재)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 11번째 사이클 PR. PR 본문 영역의 cover-style 룰 (items=1 + Table + tac=false) 영역 + 174 샘플 영역 중 영향 페이지 2 (전체의 0.06%) 영역만 영역 매칭. **PR #638 메인테이너 검토 영역의 본질 결함 영역과의 관계 영역 분석**: 본 PR 의 cover-style 룰 영역은 본 환경의 셀 안 PageHide 무시 결함 영역의 우회 (workaround) 영역. PR #641 영역의 분석 영역에서 H2 가설 (셀 내부 PageHide) 기각 — "두 페이지 모두 PageHide page_num=true 컨트롤 없음 (검증됨)" 영역 — 이 영역이 본 환경 결함 영역으로 인한 잘못된 측정 결과 영역 (PR #711 Stage 0 영역에서 정량 확인). **작업지시자 결정**: "이번 PR은 #711 에서 다시 코드 리뷰하겠으며, CLOSE 합니다. devel 반영은 하지 않겠습니다." → 본 PR close + PR #711 (Task #705) 본질 정정 영역으로 재검토 영역. **PR #711 영역의 본질 정정 영역 (메인테이너 권고 3 영역 정확 정합)**: (1) engine.rs + typeset.rs 두 경로 셀 안 PageHide 수집, (2) layout.rs hide_border/hide_fill 가드, (3) main.rs dump PageHide 분기. **Stage 0 198 샘플 sweep 영역 셀 안 PageHide 13건 / 6 샘플 발견** — PR #640 H2 측정 누락 영역의 정량 확인 영역. 영향 샘플 6 영역 (aift / 2022 국립국어원 / KTX / kps-ai / tac-img-02 hwp+hwpx) — PR #641 의 174 샘플 중 2 페이지 영역 영역 영역. KTX TOC 영역의 부당한 page number footer 영역 정합 정정 (골든 SVG 갱신). cherry-pick 영역 영역 영역 부재 (devel 반영 부재). PR #641 close + 한글 댓글 등록 ([#issuecomment-4403285953](https://github.com/edwardkim/rhwp/pull/641#issuecomment-4403285953)) — supersede 안내 + 가치 영역 보존 영역 안내 영역. close report 영역 + review 영역 archives 이동. 처리 보고서: `mydocs/pr/archives/pr_641_close_report.md`. 검토 보고서: `mydocs/pr/archives/pr_641_review.md`. **`feedback_rule_not_heuristic` 권위 사례 강화** — 휴리스틱 룰 영역 → 본질 정정 영역 영역 영역 패턴. **`feedback_visual_judgment_authority` 권위 사례 강화 누적** — PR #602 + PR #638 + PR #641 영역의 메인테이너 게이트웨이 방식 영역 누적 영역. 컨트리뷰터의 11 사이클 PR 누적 영역의 가치 영역 보존 + 본질 정정 영역 재제출 영역 (PR #711) 정합. |

## 작업 메모

### 본 사이클 (5/7~5/8) 패턴 누적
- PR #638 (Task #634) — 본질 결함 발견의 close
- PR #641 (Task #639) — 우회 접근의 close (PR #711 supersede)
- PR #644 (Task #643) — 5축 드리프트 정정 머지 (옵션 B) + 별건 결함 Issue #716 분리
- PR #645 (Task #595) — hit_test_header_footer 영역 침범 정정 머지 (옵션 B) + 별도 이슈 #685/#686 컨트리뷰터 자체 분리 등록 (첫 번째 컨트리뷰터)
- PR #649 (Task #517 재적용) — close (PR #650 영역 통합 처리), env-var-checked 본질 영역의 시각 판정 면제 영역 정합

→ 메인테이너 게이트웨이 방식의 권위 사례 강화 누적.

### 다음 영역 — PR #711 처리

PR #711 (Task #705) — 본질 정정 영역의 정합 영역 정합:
- 옵션 A 권장 — 7 commits 단계별 보존 (TDD 5 단계 + 골든 SVG 갱신)
- 시각 판정 — 6 샘플 영향 (KTX TOC 골든 SVG 갱신 정합 정합)

### 후속 이슈
- Issue #692 (5/8 신규) — `rhwpDev.search(text)` 다중 매치 동작 불완전. PR #684 후속.
- Issue #716 (5/8 신규) — `samples/20250130-hongbo.hwp` page 1 LAYOUT_OVERFLOW_DRAW. **본질 위치는 `layout.rs:1403-1555` VPOS_CORR 진입 조건 영역** (작업지시자 진단 정정: pi=2 Table 다음 pi=3 진입 시 VPOS_CORR 호출 누락 → +130px 시프트가 pi=15 까지 누적). devel 과 PR-merged devel 의 page 1 SVG md5 동일 → PR #644 도입 회귀 아닌 devel 기존 결함. PR #644 시각 검증에서 발견.
- Issue #685 / #686 (5/8 컨트리뷰터 자체 등록) — PR #645 영역 외 별도 발견 (그리드 모드 좌표 + master page 글상자 한컴 mismatch). 본 PR 영역에 포함 시도 안 함, 깔끔한 분리.
