# 2026-09-21 오늘할일

## PR #7317 — 저장 줄 전진의 측정·조판 정합

- [PR #7317](https://github.com/edwardkim/rhwp/pull/7317), `jangster77` collaborator self PR,
  관련 [#6656](https://github.com/edwardkim/rhwp/issues/6656).
- `LayoutEngine`과 `HeightMeasurer`의 저장 전진을 공통 `stored_line_flow_height`로 정합했다.
  `TypesetEngine`의 page-budget은 #1139·overflow 회귀를 피하도록 기존 줄 상자 예약을 유지한다. 실제 HWP 문단 0.7의 수정 전
  `28.8px` 대 `21.333px` 실패를 회귀 계약으로 고정했다.
- [검토 기록](../pr/archives/pr_7317_review.md): **승인**. Rust 품질 게이트·workspace build·
  `regression_suite_014` 210개, #1139·overflow 회귀 4개, Native/fresh WASM 3쪽 Visual Sweep을
  완료했다. 구조 후보는 양쪽 0건이고 대표 review·standalone overlay 4장을 보관한다.
- code head CI는 성공했다. trailing head의 GitHub Actions 성공, `MERGEABLE`/`CLEAN` 재확인과
  작업지시자의 merge 승인이 남았다.
  병합 후 #6656 close와 Visual Sweep 증적 PR comment를 실제 merge SHA·CI URL로 게시한다.

## PR #7311 — Top 앵커 비끝 표 조각의 쪽 프레임 복원

- [PR #7311](https://github.com/edwardkim/rhwp/pull/7311), `planet6897` 외부 contributor PR.
  관련 [#6923](https://github.com/edwardkim/rhwp/issues/6923)의 부분 개선이며 이 기록은 이슈 상태를 바꾸지 않는다.
- 원 code candidate `d1d2118f`의 Top 앵커 비끝 조각 프레임 확장에 Center 반례의 실제 입력·PDF가
  없던 보류 사유를, 메인터너 보정 `cb107e299`으로 해소했다. 실제 20쪽 Center HWP와 한컴 2020 PDF,
  p19에서 쪽 프레임을 주장하지 않는 음성 대조군을 같은 PR head에 추가했다.
- [검토 기록](../pr/archives/pr_7311_review.md): **메인터너 보정 후 수용 가능**. #6923 focused 3개와
  #7095 6개, Rust/WASM 품질 게이트, fresh Native/WASM Visual Sweep(Top p1·p2, Center p19)을
  확인했다. 구조 flag는 모두 0건이며 review·standalone overlay 12장을 최종 증적으로 보관한다.
- 보정 head의 [GitHub CI](https://github.com/edwardkim/rhwp/actions/runs/35577504780)는 성공했다. 이 review·
  오늘할일·증적은 review-only trailing commit으로 로컬 준비 상태다. push 승인 뒤 최신 head의 fast-pass CI,
  reviewer 지정, `MERGEABLE`/`CLEAN` 및 작업지시자의 merge 승인을 확인한다. 완료·push·merge·comment·
  issue close는 아직 수행하지 않았다.

## PR #7289 — 레거시 OLE 수식 편집 지원

- [PR #7289](https://github.com/edwardkim/rhwp/pull/7289), `jangster77` collaborator self PR,
  관련 [#7105](https://github.com/edwardkim/rhwp/issues/7105).
- `hwpeq5X` 레거시 OLE 수식을 편집할 때 같은 control slot을 native equation으로 전환하고
  편집·HWP 저장·재열기를 지원한다. Firefox service worker는 viewer 탭 생성 완료를 기다린다.
- 최초 CI가 검출한 `promoteOleEquation` mutation audit 동사 누락을 `0f1fa2f72`에서 보정했고,
  같은 routing guard와 #7105 Studio 검사를 다시 통과했다.
- [검토 기록](../pr/archives/pr_7289_review.md): **승인**. Windows Core 회귀와 Rust 품질 게이트,
  Studio·Firefox unit/package 검증 및 Windows Firefox 153.0.4·macOS Firefox 155.0.1의 실제
  11쪽 HWP 열기를 확인했다. 수식 렌더·조판 출력 변경이 아니므로 PDF Visual Sweep은 비해당이다.
- 검증 입력은 기존 커밋 파일 `samples/issue5725/2921145_equation_ole.hwpx`와
  `tests/fixtures/issue_7105/transistor-mosfet.hwp`를 사용했으며 중복 사본을 추가하지 않았다.
- 최신 trailing head의 CI와 mergeability를 다시 확인한 뒤 작업지시자의 merge 승인에 따라 병합·#7105
  close·후속 처리를 진행한다. 완료·병합·이슈 close는 아직 수행하지 않았다.

## PR #7309 — Dependabot 의존성 8건 통합

- [PR #7309](https://github.com/edwardkim/rhwp/pull/7309), `jangster77` collaborator self-review 통합 PR.
  Dependabot 원 PR [#7301](https://github.com/edwardkim/rhwp/pull/7301)부터
  [#7308](https://github.com/edwardkim/rhwp/pull/7308)까지 8건을 최신 `upstream/devel` 위에
  `git cherry-pick -x`로 적용했다.
- `rhwp-studio`에서 #7306과 #7307의 manifest·lockfile 충돌은 `@types/chrome 0.3.0`과
  `puppeteer-core 25.11.0`을 함께 유지하고 `npm ci --ignore-scripts`로 결합 결과를 확인해 해소했다.
- [검토 기록](../pr/archives/pr_7309_review.md)과
  [실행 기록](../pr/archives/pr_7309_review_impl.md): **승인**. package별 npm 설치, Chrome
  extension smoke/download, Studio build/test(1,761 passed, 2 skipped), VS Code compile,
  actionlint 및 Windows PowerShell 격리 worktree의 `CARGO_BUILD_JOBS=8 cargo check --workspace --locked`를
  통과했다.
- code candidate `6f709298c`의 [Full CI](https://github.com/edwardkim/rhwp/actions/runs/35564126524)는
  성공했다. 현재 review·implementation·오늘할일 trailing head는 code/fixture를 바꾸지 않으므로
  review-only CI 결과와 최신 `MERGEABLE`/`CLEAN`을 확인한 뒤 작업지시자의 merge 승인에 따라 병합한다.
- renderer·layout·pagination·paint·Canvas/WASM API·HWP/HWPX fixture·기준 PDF는 바뀌지 않아
  Visual Sweep과 한컴 PDF 비교는 비해당이다. 원 Dependabot PR close/comment와 post-merge duration
  refresh는 병합 뒤에만 수행한다.

## PR #7310 — planet6897 조판·정본 원장 10건 통합

- [PR #7310](https://github.com/edwardkim/rhwp/pull/7310), `jangster77` collaborator 통합 PR. 원 PR [#7286](https://github.com/edwardkim/rhwp/pull/7286), [#7290](https://github.com/edwardkim/rhwp/pull/7290), [#7291](https://github.com/edwardkim/rhwp/pull/7291), [#7294](https://github.com/edwardkim/rhwp/pull/7294), [#7295](https://github.com/edwardkim/rhwp/pull/7295), [#7296](https://github.com/edwardkim/rhwp/pull/7296), [#7297](https://github.com/edwardkim/rhwp/pull/7297), [#7298](https://github.com/edwardkim/rhwp/pull/7298), [#7299](https://github.com/edwardkim/rhwp/pull/7299), [#7300](https://github.com/edwardkim/rhwp/pull/7300)을 최신 `upstream/devel` 위에 `git cherry-pick -x`로 누적했다. #7290에 포함된 #7286 선행 commit은 중복 적용하지 않았다.
- code candidate [`4e4ac77d9`](https://github.com/edwardkim/rhwp/commit/4e4ac77d92790ce2f6ee69c965411b27c946552e)의 [Build & Test CI](https://github.com/edwardkim/rhwp/actions/runs/35570500455), CodeQL, Canvas visual diff, Native Skia, archive A–D와 [CI Impact Policy](https://github.com/edwardkim/rhwp/actions/runs/35571860540)가 모두 성공했다. 사전 승인 전에는 이 문서·10개 개별 review·선별한 Native/WASM review/overlay 14개를 commit 또는 push하지 않는다.
- Windows `release-test`: 본문 바닥 넘침 원장 16/16 PASS(69.85초), 텍스트 겹침 원장 16/16 PASS(72.01초), 정답지 쪽수 원장 16/16 PASS(12.71초). macOS fresh Native/WASM Visual Sweep은 hwpctl p17/64/94, KTX p2, 시장구조조사 p4/5, 신명 중명조 기준 p4에서 구조 이상 0쪽을 확인했다.
- 개별 review의 CI URL·head·판정은 확정했다. 사용자 사전승인 후 이 기록과 증적만 trailing commit으로 push하고, 그 다음 head의 CI와 mergeability를 다시 확인해 merge·후속 처리를 진행한다.

## PR #7313 — HWP3 변환 HFT 한글 face ASCII 반각 전진

- [PR #7313](https://github.com/edwardkim/rhwp/pull/7313), `planet6897` 외부 contributor PR,
  관련 [#7051](https://github.com/edwardkim/rhwp/issues/7051).
- HWP3 변환본의 HFT 한글 전용 face가 ASCII를 치환 TTF의 비례폭으로 측정해 저장 LineSeg를 넘던
  문제를 `em/2` 전진으로 고쳤다. 영문 HFT와 일반 HWP5 legacy HFT 이름은 반례로 제외한다.
- 메인터너 보정 `7c51a07d3`은 미추적 PDF를 새로 추가하지 않고 기존 추적 분할본의 원본·분할 provenance와
  489쪽→분할본 189쪽 매핑을 고정했다.
- [검토 기록](../pr/archives/pr_7313_review.md): **메인터너 보정 후 수용 가능**. #7051 focused 3개,
  Native/WASM 품질 게이트와 fresh Native/WASM 489쪽 Visual Sweep을 확인했다. 구조 flag는 0이고
  문제 런은 554.9px로 저장 폭 566.9px 안에 든다. review·overlay 4장을 증적으로 보관한다.
- code candidate `7c51a07d3`의 [Full CI](https://github.com/edwardkim/rhwp/actions/runs/35584051980)는
  성공했다. 이 review·오늘할일·증적 trailing head의 fast-pass CI와 `MERGEABLE`/`CLEAN`을 다시 확인한 뒤
  작업지시자 지시에 따라 병합과 post-merge 후속 처리를 진행한다. 병합·이슈 close·comment는 아직 수행하지 않았다.

## 통합 PR 준비 — #7315 Studio 영문화와 #7316 저장 되감김 쪽 경계

- 외부 contributor PR [#7315](https://github.com/edwardkim/rhwp/pull/7315)와
  [#7316](https://github.com/edwardkim/rhwp/pull/7316)을 최신 `upstream/devel`
  `be33c9359` 위 통합 검토 브랜치에 `git cherry-pick -x`로 누적했다. 적용 commit은 각각
  `6570f2935`, `878ba0d80`이며 원 contributor commit은 rewrite하지 않았다.
- [#7315 검토](../pr/archives/pr_7315_review.md): **승인**. Studio test 1,761 passed,
  build와 영문·한국어 catalog key 양방향 대조를 완료했다. renderer 출력 변경이 없어 PDF Visual
  Sweep은 비해당이다.
- [#7316 검토](../pr/archives/pr_7316_review.md): **메인터너 보정 후 수용 가능**. 기준 PDF
  provenance 오류를 제품 코드와 분리한 `4c663976b`으로 실제 추적 HWP·한컴 2020 PDF로 정정했고,
  focused 회귀 4개와 Native/fresh WASM 13·14쪽 Visual Sweep을 완료했다. review와 standalone
  overlay 8장은 `mydocs/pr/assets/pr7316_review/`에 보관한다.
- 이 review·오늘할일 trailing 기록은 로컬 통합 브랜치에만 준비됐다. 원격 push·통합 PR 생성·comment·
  close·merge는 아직 하지 않았다. PR 생성 뒤 최신 trailing head의 required CI,
  `MERGEABLE`/`CLEAN`을 확인하고 작업지시자의 merge 지시에 따라 병합 및 contributor·issue 후속
  처리를 진행한다.
