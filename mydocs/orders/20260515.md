# 오늘 할일 - 2026년 5월 15일

## M100 — v1.0.0 조판 엔진 (Task #902 v2)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **#902** | **WMF renderer 근본 개선** | **완료 (PR 대기)** | v1 → v2 scope 재정의. Stage 3~8 완료. 1412 passed / 0 failed. PR 생성 직전 작업지시자 명시 승인. |
| #895 | HWPX 변환본 lineseg vpos page break reset 누락 | won't-fix (이슈 OPEN) | 다중 fix 시도 실패, 작업지시자 지시로 중단 (5/14). |

## 작업 메모

### Task #902 v2 처리 요약

**원래 issue**: "WMF 텍스트가 한컴 viewer 와 비교 시 크기/간격 미세 차이"
**ROOT CAUSE 발견**: EXTTEXTOUT 의 DX 배열은 MBCS byte index — Korean wide char 마다 grapheme index 접근 시 매 둘째 entry (=0) 잘못 사용

**scope 확장 (v1 → v2)**: 단일 dx fix 불가, WMF renderer 의 fundamental 갭 다수 발견:
1. DX byte-aware indexing (Stage 3 — ROOT CAUSE fix)
2. META_SETVIEWPORTEXT/ORG 미구현 (Stage 4)
3. EXTTEXTOUT options flags (ETO_OPAQUE/PDY) 미처리 (Stage 5)
4. OFFSET WINDOW/VIEWPORT, SCALEVIEWPORTEXT 미구현 (Stage 6)
5. 폰트 substitution garbled fallback + fallback chain 정합 (Stage 7)

**검증** (Stage 8):
- cargo test --release --all-targets: 1412 passed / 0 failed
- golden SVG (8 tests): 통과
- sample14/16/17/18 SVG export: 회귀 없음
- rsvg-convert PNG 셀프 검증: 정상

**잔존 한계** (follow-up):
- 굴림체 ↔ open 한국어 폰트 미세 metric 차이 → 폰트 임베딩 후속 task
- 사용 빈도 낮은 WMF records (Region/Palette 등) → 발견 시 별도 task
- sample19 parser 실패 → 별도 issue (parser 영역, WMF 무관)

### 진행 방식

작업지시자 지시 (5/15):
- 매 stage 자동승인 (PR 직전까지)
- 시각 검증은 rsvg-convert 셀프 (작업지시자 시각 비교 요청 없음)

### 다음 step

- 작업지시자 명시 승인 후 `gh pr create` (base: `upstream/devel`, head: `local/task902`)
- PR merge 후 issue #902 close
