# 오늘 할일 - 2026년 5월 15일

## M100 — v1.0.0 조판 엔진 (Task #902 v2)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **#902** | **WMF renderer 근본 개선 + LibreOffice emfio 포팅 baseline** | **완료 (PR 대기)** | v1 → v2 scope 재정의 + raster Player 확장 (Stage 1~16). 1412 passed / 0 failed. PR 생성 직전 작업지시자 명시 승인. |
| #895 | HWPX 변환본 lineseg vpos page break reset 누락 | won't-fix (이슈 OPEN) | 다중 fix 시도 실패, 작업지시자 지시로 중단 (5/14). |

## 작업 메모

### Task #902 v2 + 확장 처리 요약

**원래 issue**: "WMF 텍스트가 한컴 viewer 와 비교 시 크기/간격 미세 차이"

**Stage 11 결정적 발견**: sample16 (4.7MB) vs sample18 (211KB) WMF 구조 차이
- sample16: POLYPOLYGON 10,476 + 임베디드 비트맵 177 + flood fill 168 — 복잡 합성
- sample18: 단순 폴리곤 + 텍스트 — 정상 표시 (우리 약점 미사용)

**Stage 1~16 처리**:
1. DX byte-aware indexing (Stage 3 — ROOT CAUSE fix)
2. META_SETVIEWPORTEXT/ORG (Stage 4) + MM_ANISOTROPIC ratio
3. EXTTEXTOUT options flags (Stage 5)
4. OFFSET WINDOW/VIEWPORT, SCALE (Stage 6)
5. 폰트 fallback chain (Stage 7)
6. B2 raster (WMF SVG → resvg PNG) (Stage 10)
7. POLYPOLYGON fill-rule fix (Stage 11)
8. **LibreOffice emfio 포팅 baseline** (Stage 12~15):
   - `src/wmf/converter/raster/` 신규 모듈 (MPL 2.0 attribution)
   - tiny-skia + fontdue + 시스템 폰트 cache
   - polygon/poly_polygon/rectangle/ellipse/line drawing
   - LO DrawText 알고리즘 포팅 (byte-aware DX, alpha blit)

**검증**:
- cargo test --release --all-targets: 1412 passed / 0 failed
- golden SVG (8 tests): 통과
- sample14/16/17/18 SVG export: 회귀 없음
- RasterPlayer 시각: sample16 92KB PNG, sample18 518KB PNG

**dispatcher**:
- SVG 경로 (resvg) default — 안정, 전 records 지원
- RasterPlayer opt-in API — `rasterize_wmf_direct_pub`

**잔존 한계 (follow-up)**:
- RasterPlayer 의 bitmap records 미구현 → image crate decode 필요
- arc/pie/chord, font weight/italic 미적용
- 굴림체 정확 metric → 폰트 임베딩 (라이센스 고려)
- sample19 HWP3 parser 실패 → 별도 issue

### 진행 방식

작업지시자 지시 (5/15):
- 매 stage 자동승인 (PR 직전까지)
- 시각 검증은 rsvg-convert 셀프 (작업지시자 시각 비교 요청 없음)

### 다음 step

- 작업지시자 명시 승인 후 `gh pr create` (base: `upstream/devel`, head: `local/task902`)
- PR merge 후 issue #902 close
