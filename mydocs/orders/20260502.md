# 오늘 할일 - 2026년 5월 2일

## M100 — 그림 회전·뒤집기(flip) 렌더링 정합

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #519 | 그림(Picture) `horz_flip / vert_flip / rotation_angle` 속성이 SVG 렌더링에 적용되지 않음 — exam_eng p4 28번 curl 카드 데코 누락 | **완료** | 6개 `ImageNode` 생성 지점에 `transform: extract_shape_transform(&pic.shape_attr)` 추가 (`picture_footnote.rs / paragraph_layout.rs / table_cell_content.rs / layout.rs`). 기존 헬퍼 재사용, 신규 코드 0줄. 150 파일 분포 조사상 영향 1건(exam_eng.hwp). cargo test --lib 1103 + svg_snapshot 6 통과. exam_eng 8페이지 중 p4만 4 lines 변경 (`<g transform>` 래퍼). 28번 curl 좌상단 표시 ★ PDF 일치 |
| #521 | 그림(BehindText) vertical extent 가 paragraph height 미반영 — exam_eng p2 18번 답안지 ① ~ ⑤ 가 박스 하단에 너무 가까움 (PDF 대비 ~20px 위로 올라감) | 진행 중 (Stage 1) | pi=104 = 그림(BehindText offset=0/h=67.8mm) + 그림(BehindText offset=58.1/h=18.5mm) + 표(tac=true h=76.2mm) 복합. line_height = 22207 HU 만 기준이라 그림의 vertical extent (76.6mm) 미반영, 다음 문단이 ~1395 HU (18.6 px) 위로 올라감 |

## 회귀 정정

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #509** | PUA (Private Use Area) 글머리표 글리프 회귀 — KTX/synam-001/kps-ai 등 14 샘플의 글머리표가 ☰ (폰트 폴백 결함) 또는 잘못된 글리프로 출력 | **완료 (Stage 6/6)** | **본질**: PR #251 (외부 컨트리뷰터, 2026-04 영역) 의 일반 텍스트 PUA 변환 도입 후 매핑 표 정확성 영역 회귀. **진단 영역**: Stage 1 — 광범위 PUA 사용 영역 정합 (14 샘플 / 18 codepoint / 111 회). Stage 2 — `mydocs/manual/hwp/Help/extracted/hwpbase/hncpuaconverter.htm` 정독, KS X 1026-1:2007 표준 정합 (별도 task #512 등록). Stage 3~4 — 4개 옵션 (A/B/C/F) 점검, **Option F 확정** (PR #251 draw_text 영역 보존 + 매핑 표 한컴 PDF 정답지 정확화). **작업지시자 통찰**: *"한컴의 경우 PUA 는 폰트지정과 상관없이 처리해야 할 것 같습니다. 지정된 폰트에 없는 경우 현재 문제가 되는 것으로 판단됩니다."* → rhwp 도 한컴과 동일하게 PUA 자체 변환 발행. **검증 도구**: `gen-pua` 서브명령 추가 — 18 종 PUA 코드포인트 영역 검증용 HWP 생성. samples/pua-test.hwp + 한컴 2022 출력 PDF 캡처 → 정답지 영역 정합. **정정 매핑 (2개)**: U+0F0A0 ▪ U+25AA → · U+00B7 (Middle dot, synam-001 9회), U+0F0E8 ➤ U+27A4 → ➔ U+2794 (Heavy wide-headed arrow, kps-ai 4회). **신규 매핑 (10개)**: Supplementary PUA-A (U+F02B0~F02FF) — U+F02B1~B9 → ① ~ ⑨ (mel-001/kps-ai), U+F02EF → · (KTX). **검증 게이트**: cargo build (lib) + pua_mapping 5/5 + svg_snapshot 6/6 회귀 0 + WASM 4.18MB 빌드 + rhwp-studio 배포. **작업지시자 시각 검증** — KTX p10 + samples/pua-test.hwp 정상 출력 ★. **별도 결함 (본 task 너머)**: Supplementary PUA-A 의 SVG 출력 누락 — 함수 매핑 정확하나 SVG 렌더링 영역에서 누락 (UTF-16 surrogate pair 처리 결함 추정), 별도 task 분리 권장 |

## 외부 PR 처리

| PR | 제목 / 작성자 | 상태 | 비고 |
|----|--------------|------|------|
| **#507** | CASES+EQALIGN 중첩 토폴로지 분수 분실 정정 (#505 / #175 후속) — @cskwork (Agentic-Worker) | **수정 요청 (open)** | 코드 변경 (헬퍼 추출 + DRY) 과 회귀 테스트 9건 (인라인 fixture 기반) 자체는 정합. 그러나 본질이 시각 결함 정정 (`12r²` squashing → `(1/2) x²`) 이고 `samples/` 에 `미적분 기출문제_03.미분계수와 도함수1-1.hwp` 부재. 메모리 `feedback_v076_regression_origin` / `feedback_pdf_not_authoritative` / `reference_authoritative_hancom` 정합으로 작업지시자 직접 시각 판정 게이트 미충족 → 한컴 2010+2020 직접 판정 필요. PR comment 등록 — `samples/` 추가 요청 (라이선스 절차 불필요). 문서: `mydocs/pr/pr_507_review.md` |
| **#510** | Task #508: PageLayerTree image brightness/contrast JSON 필드 추가 — @postmelee (Taegyu Lee), alhangeul-macos downstream | **머지 완료 (cherry-pick)** | 데이터 contract 보강 — `PaintOp::Image` JSON 에 `brightness`/`contrast` 4 줄 추가, schemaVersion 유지 (additive). 소스 1 파일 (+6/-2) + 테스트 assertion 4 줄. 단일 commit `50a9f63` cherry-pick → `548df60` (footer + `closes #508` 보강). **검증**: paint::json 4/4 + lib 1110/1110 + clippy 0 + issue_418/501/svg_snapshot 회귀 0 + **before/after SVG byte-identical (380,804 bytes)** ★. 작업지시자 시각 판정 승인. 문서: `mydocs/pr/pr_510_review.md` + `pr_510_review_impl.md` + `pr_510_report.md` |

## 작업 메모

- Task #509 정합 — `feedback_pdf_not_authoritative` 영역에 한컴 2022 편집기 영역의 직접 시각 캡처는 정답지 영역으로 활용 가능 (단, 작업지시자가 직접 캡처한 영역만)
- `gen-pua` 도구 — 향후 PUA 회귀 검증 영역의 결정적 영역 도구로 보존 (samples/pua-test.hwp 영역 영구 보존 권장)
- PR #510 처리 — core SVG byte-identical 결과는 contract 보강 PR 의 정합 검증 패턴으로 보존 가치. 향후 유사 contract-only 변경 시 동일 검증 적용

## planet6897 작성 issue 처리 (6건)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #465 | exam_kor 16p 우측 "홀수형" 뱃지 누락 | **재현 불가 / close** | 현재 + 신고 시점 commit (c71fa49) 모두 4페이지 모두 PDF 와 동일 위치에 정상 렌더링. 결함 원천: master 가 아닌 sec1 pi=0 의 InFrontOfText 사각형 글상자 (section-level decoration). 이슈 본문 master cell[0] 가설 부정확. `mydocs/report/task_m100_465_report.md` |
| #467 | 확장 바탕쪽 다른 apply_to 조합 미검증 | 보류 / close | Stage 1 조사: `samples/exam_science.hwp` 에 다른 apply_to 조합 샘플 (master[3] Even ext + master[4] Both ext overlap) 발견. 페이지 4 좌측 "4(화1)" PDF 표시 / SVG 미표시 결함 발견하나 본 task 범위와 직접 연관 모호. 보류 사유: 조사 task / master 처리 회귀 위험 / PDF 비교 신뢰도 낮음 / 한컴 환경 검증 불가. `mydocs/report/task_m100_467_report.md` |
| #479 | 페이지 12 layout drift trailing line_spacing 누적 | **해결 / close** | devel HEAD 에 fa73785 + ef1ff58 + 73c6a95 commit 으로 fix + 광범위 검증 완료. 페이지 12 200px drift 해결, 한컴 2020 페이지 수 일치 (3 샘플), 단위 1078+골든 6 통과. close 만 누락된 상태였음 |
| #491 | exam_science p1 2번 답안지 위치 미세 drift | 보류 / close | HWP IR 분석: pi=9 (3x5 표) vpos=47860 → y=787.4px (HWP 인코딩 위치 일치, 정상 동작). PDF/SVG 페이지 크기 차이로 직접 좌표 비교 불가. #489/#490 정정 후 자연 해소 가능성. 추후 구체 측정값 또는 PDF 좌표 정규화 후 재평가 |
| #496 | exam_science p2 12번 본문 줄간격 압축 (인라인 수식 다수) | **Phase 2 로 본질 (B) 해결** | 본질 (B) ls[2..] break 미사용은 Phase 2 (#518) 에서 정정. 잔존 본질 (A) baseline 정렬 / (C) 인라인 vs 블록 정책은 Phase 3/4. 2026-04-30 보류 결정 후 본 세션 Phase 2 로 신고 핵심 증상 해소. close 유지 |
| #500 | exam_science p2 박스 안 사각형 잘못된 줄 위치 | **완료 (신규 fix) / close** | `table_layout.rs` Shape 분기에 Picture 분기와 동일한 target_line/리셋 로직 적용. shape_area.y / layout_cell_shape 의 para_y 인자를 tac_img_y 로 변경. exam_science p2 ㉠ 사각형 ls[0]→ls[1] 정상 위치, PDF 정합. 단위 1103 + 7 샘플 170 페이지 (169 byte 동일 / 1 의도된 정정) 회귀 0건 |

## Layout 리팩터링 (Phase 0~2)

`#467 / #491 / #496` 보류 사유 "layout 리팩터링 시 종합 해결" 의 구체화. 작업지시자 지시 "layout 리팩터링 진행".

| Phase | 내용 | 상태 | 비고 |
|-------|------|------|------|
| 0 | 분석 + 로드맵 | **완료** | `mydocs/tech/layout_refactor_roadmap.md`. 모듈 12,509 LOC, 보류 이슈 3건 본질 분리 (#467 master 매핑 / #491 PDF 좌표 정규화 / #496 layout_inline_table_paragraph 한계), #496 = 복합 본질 (A baseline 정렬 / B ls[2..] break / C 인라인 vs 블록 정책) |
| 1 (#517) | 디버그 인프라 + 회귀 검증 도구 | **완료 / close** | `RHWP_LAYOUT_DEBUG=1` env logging (paragraph_layout.rs) + `scripts/svg_regression_diff.sh` (build/diff 두 모드, 7 샘플 170 페이지 자동화). `mydocs/manual/svg_regression_diff.md`. 단위 1103 + 회귀 170/170 동일 (회귀 0) |
| 2 (#518) | line_break_char_idx 다중화 | **완료 / close** | Option<usize> → Vec<usize> 일반화 (ls[1..] 모두 사용). ctrl_gap 기반 algorithm → 직접 char_offsets 룩업 (paragraph 전체 controls 합 over-subtraction 버그 정정). exam_science p2 pi=61 본문 1줄 column 폭 초과 → 3줄 정상 wrap. 7 샘플 170 페이지 same=167 / diff=3 (exam_science 정확도 정정), 다른 6 샘플 byte 동일. 단위 1103. **#496 본질 (B) 해결** |
| 3 | 다중행 인라인 표 baseline 정렬 정책 | **미시작** | 회귀 위험 큼 — 모든 인라인 표 영향. PDF 직접 비교 가능한 한컴 환경 검증 가능 시점에 진행 권고 |
| 4 | 인라인 vs 블록 정책 도입 | **미시작** | 회귀 위험 매우 큼. HWP 스펙 + 한컴 동작 분석 선결 |

## 본 세션 요약

- planet6897 작성 6건 모두 close (재현 불가 1, 보류 3, 해결 1, 신규 fix 1)
- Layout 리팩터링 Phase 0/1/2 완료 — devel 푸시 9회
- 신규 issue 등록 + close 2건 (#517 Phase 1, #518 Phase 2)
- 코드 변경: `src/renderer/layout/paragraph_layout.rs` (Phase 1 logging + Phase 2 break 다중화) + `src/renderer/layout/table_layout.rs` (#500 fix)
- 인프라 추가: `scripts/svg_regression_diff.sh` + `mydocs/manual/svg_regression_diff.md`
- 모든 변경 단위 1103 + 광범위 회귀 검증 통과
- Task #519 추가 — 그림 flip/rotation 속성 SVG 적용 (6 ImageNode 지점, exam_eng p4 28번 curl 정합)

## 잔여 이슈 (Phase 3/4 보류)

| 결함 | 본질 | Phase |
|------|------|-------|
| #496 본문 baseline 정렬 (수직) | A | 3 |
| #496 인라인 vs 블록 정책 | C | 4 |
| #467 다른 apply_to 조합 (master 매핑) | (별개 영역) | 별도 |
| #491 PDF 좌표 정규화 | (인프라) | 별도 |

향후 한컴 환경 직접 검증 가능 시점에 Phase 3/4 진행 권고.
