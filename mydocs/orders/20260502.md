# 오늘 할일 - 2026년 5월 2일

## M100 — 그림 회전·뒤집기(flip) 렌더링 정합

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #519 | 그림(Picture) `horz_flip / vert_flip / rotation_angle` 속성이 SVG 렌더링에 적용되지 않음 — exam_eng p4 28번 curl 카드 데코 누락 | 진행 중 | `picture_footnote.rs:107-117 / :316-326` `ImageNode` 생성 시 `transform` 필드 누락. PDF는 박스 좌상단 curl, rhwp SVG 는 원본 그대로 우하단 (180° flip 미적용) |
