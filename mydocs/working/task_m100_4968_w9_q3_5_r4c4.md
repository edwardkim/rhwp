# Task M100 #4968 — Stage W9-Q3-5R4C-4 최종 검증·오라클 교차검증 결과

- 작성일: 2026-08-27 KST
- 작업 브랜치: `task_m100_4968`
- 선행 커밋: `f5f11e33c` R4C-3 common fresh line-break consumption
- 통합 기준: `upstream/devel@381785208d6c`, merge `828d0a8ab`
- 상태: **검증·보고 완료, 메인테이너 결과 승인 대기**
- remote push·PR·comment: 수행하지 않음

## 1. 결론

R4C-4에서 fresh 본문뿐 아니라 표 셀과 글상자도 같은 exact kerning measurement를 소비하는지 공개 제품
경로로 다시 검증했다. 이 검증이 R4C-3의 실제 누락을 잡았다. 표 셀은 K0/K1 줄 경계가 달랐지만, 저장
`LineSeg`가 없는 가로 글상자는 `compose_paragraph`의 45자 fallback을 그대로 사용해 exact source를 등록해도
K0/K1 경계가 같았다.

가로 글상자의 NO_LS 문단만 본문·표 셀과 같은 fresh frame reflow로 보냈다. 유효한 stored `LineSeg`가 있는
글상자는 기존 feature-detection 경로를 그대로 유지한다. 공개 small exact face의 bounded width ladder에서
표 셀·글상자 모두 K0/K1 경계 차이와 K1 반복 결정성을 확인했다.

한컴 PDF 오라클과 실제 샘플을 교차검증한 결과, 대상 글상자 내부 그림의 가로 위치·크기는 사실상 같고 앞 문단
기준 상대 간격도 유지됐다. 다만 이 샘플의 글상자 텍스트는 공백 5자뿐이고 보이는 내용은 inline image 2개라서,
오라클은 **실문서 글상자 배치 비회귀**를 검증하지만 새 문자 kerning 줄 경계를 직접 판정하지는 않는다. 그
경계는 공개 synthetic integration test가 담당한다.

## 2. R4C-3 누락과 정정

### 2.1 실패가 먼저 증명한 것

기존 `tests/cases/issue_4968_kerning_capability_provider.rs`에 새 source를 만들지 않고 다음 공개 경로 검사를
추가했다.

1. blank document에 kerning request가 켜진 `AV` 반복 문단을 만든다.
2. 같은 문단을 1×1 표 셀과 가로 글상자에 각각 넣는다.
3. 7,000..13,000 HWPUNIT의 100-unit width ladder에서 K0와 exact-source K1 줄 경계를 비교한다.
4. 차이가 드러난 첫 폭에서 K1을 다시 실행해 결정성을 확인한다.

초기 실행에서 표 셀은 통과하고 글상자만 실패했다. 따라서 “네 소비 경로가 모두 연결됐다”는 R4C-3 판정을
R4C-4에서 그대로 재사용하지 않고 실제 container별 공개 출력으로 재검증한 것이 유효했다.

### 2.2 원인

`LayoutEngine::layout_shape_control`의 가로 글상자 경로는 `Paragraph.line_segs.is_empty()`여도
`compose_paragraph` 결과를 직접 사용했다. 이 fallback은 본문 폭이나 `KerningMeasurementContext`를 소비하지
않는 45자 고정 줄이므로 exact registry 등록이 글상자 줄 경계까지 도달하지 않았다.

### 2.3 정정 경계

- stored `LineSeg`가 있으면 기존 composed paragraph를 그대로 사용한다.
- NO_LS 가로 글상자만 실제 inner width와 paragraph margin으로 `ParagraphBox`를 만든다.
- `recompose_stored_lines_in_frame(..., StoredRowMissPolicy::Reflow)`를 사용해 본문·표와 같은 fresh
  measurement context를 소비한다.
- inner width가 0이거나 재조판이 실패하면 기존 composed 결과로 fail-closed한다.
- 세로 글상자와 backend glyph replay는 이번 절편에서 바꾸지 않는다.

## 3. 한컴 PDF 오라클 교차검증

### 3.1 증거 계보

- source:
  `samples/issue5820/156560092_ecard_meeting_press.hwpx`
- source SHA-256:
  `dce69e86c147f5d53da567fae39418f24ab13157e1406360a8105f3cca666faa`
- reference PDF:
  `pdf/pr_6088_6144/hancom2020/pr_6088_6144_issue5820_ecard_meeting_press_156560092_ecard_meeting_press-2020.pdf`
- PDF SHA-256:
  `f4f395675f301ae57bcc7bdf918a5aae5cb8b052ff063e68bbadb0406145825b`
- PDF metadata: Creator `Hwp 2022 0.0.0.0`, Producer `Hancom PDF 1.3.0.550`,
  2026-08-26 20:09:53 KST, A4 2쪽
- 기존 ledger 환경: Hancom 2020 MCP reference PDF
- source last-saved 단서: Hancom Office 2018 `10.0.0.11529`

Creator 문자열, ledger 실행 환경, source 저장 버전이 서로 같지 않으므로 하나로 뭉개지 않고 각각 기록한다.

### 3.2 구조·텍스트 비교

`tools/fidelity_compare/fidelity_compare.py`의 text-only 2쪽 전건 결과는 다음과 같다.

- PDF/SVG/tree page count: 모두 2쪽
- 처리 완결성: 2/2
- layout candidate: 0건
- table text overlap·boundary·clip candidate: 0건
- glyph-risk candidate: 0건
- 기존 page-owner 차이: PDF 1쪽의 31자가 rhwp에서는 2쪽으로 이동
  - `개선하기위하여전자카드를바탕으로한건설현장출입관리시스템구축에`

31자 이동은 이번 글상자 정정이 만든 것이 아니라 앞선 본문 문단이 rhwp에서 한 줄 더 접히는 기존 전체 페이지
fidelity 잔여다. 글상자 자체 결함과 분리한다.

### 3.3 시각 sweep과 글상자 판정

96 DPI 2쪽 결과는 이전 PR integration 증적과 정확히 같았다.

- pixel match: `96.77153%`
- 내용 픽셀 중심 자동 일치율 보조값: `6.30452%`
- flagged page count: 0
- compare:
  `/tmp/rhwp-4968-r4c4-oracle.GAjTP4/sweep96/issue-4968-r4c4-textbox-96/compare/compare_002.png`
- overlay:
  `/tmp/rhwp-4968-r4c4-oracle.GAjTP4/sweep96/issue-4968-r4c4-textbox-96/overlay/overlay_002.png`
- review:
  `/tmp/rhwp-4968-r4c4-oracle.GAjTP4/sweep96/issue-4968-r4c4-textbox-96/review/review_002.png`

144 DPI에서 글상자 inline image group의 bounding box는 rhwp `(758,592,306×61)`, 오라클
`(757,547,308×59)`였다. x와 크기는 거의 같고 y의 +45px 차이는 글상자 앞 본문이 rhwp에서 한 줄 더 접힌
page-flow 차이를 그대로 따른다. 앞 문단과 글상자 사이의 상대 간격 차이는 약 4~5px다.

샘플 dump의 글상자 문단은 `text="     "`, `line_segs=0`, `inline controls=2`다. 따라서 사람 판정 후보는
“글상자 내부 object placement 비회귀, 앞 본문 page-flow 잔여는 별도”다. 자동 보조값은 사람 판정 정확도가
아니다.

코멘트: 내용 픽셀 중심 자동 일치율 보조값 = 약 6.30%.
높을수록 좋음: 기준 PDF와 rhwp PNG가 더 비슷함
낮을수록 나쁨/검토 필요: 잉크 위치나 형태 차이가 큼
단, 사람 판정 정확도가 아니라 내용 픽셀 중심 자동 일치율 보조값입니다

## 4. 검증 결과

### 4.1 source·manifest

- `cargo fmt --all`: 통과
- `cargo fmt --all -- --check`: 통과
- `git diff --check`: 통과
- `cargo check --locked --all-targets`: 통과
- `cargo clippy --locked --all-targets -- -D warnings`: 통과
- #4968 review target: 24/24 통과
- generated manifest prepare/check: 통과
  - 963 sources / 4,372 static attrs
  - 32 generated suites + 10 exception targets
  - 42/48 targets, nextest 최소 6,559 cases

review worktree에서만 generated suite·manifest와 Cargo target sync를 사용했다. root `Cargo.toml`, generated
suite, manifest는 제출 변경에 포함하지 않는다.

### 4.2 전체 native gate

최종 source head에서 `cargo nextest run --locked --cargo-profile release-test --target-dir
target/pr-review --tests --no-fail-fast`를 실행했다.

- 8,423/8,423 통과
- 43 skip, 10 slow
- 실행 시간: 964.842초
- 환경 경고: 저장소 권고 nextest `0.9.140`, 실제 `0.9.137`

첫 전건 실행에서 유일하게 실패한 `issue_2724_passthrough_invalidation_guard`는
`register_exact_font_source_native`를 미분류 문서 mutator로 탐지했다. 이 함수는 exact registry,
kerning measurement context, layout cache만 바꾸고 문서 IR·직렬화 대상은 바꾸지 않는다. 기존 `EXEMPT`에
`SessionState`와 근거를 추가한 뒤 집중 검사와 위 전건을 다시 실행해 통과했다.

### 4.3 native-skia gate

- `cargo test ... --features native-skia --lib`: 통과
- `issue_2225_missing_picture_placeholder`: 2/2 통과
- `render_p37_direct_pdf_export`: 4/4 통과

### 4.4 Docker WASM·크기

사용자 지정 Docker 경로 `docker compose --env-file .env.docker run --rm wasm`은 376.31초에 통과했다.

- 작업본 `pkg/rhwp_bg.wasm`: 9,585,119 bytes
- 작업본 SHA-256: `87c3ab34efcdec46c345de276751bc4eff9619cdd85d2c58054eb02c5fb2a31b`
- 같은 `HEAD 828d0a8ab` 기준선: 9,584,687 bytes
- 기준선 SHA-256: `3644bf709b04b4d7a7ae7872cf620a6a027fa097fe063c98f211ee8e35123807`
- R4C-4 순증분: **432 bytes, +0.004507%**

기준선은 별도 detached worktree와 별도 Docker volume에서 같은 compose·toolchain·wasm-opt 절차로 만들었다.
측정 후 임시 worktree와 전용 Docker volume을 제거했다.

### 4.5 K0 native/WASM identity

Docker WASM `pkg`와 최종 native release binary로 `kerning_q1_baseline`을 실행했다.

- baseline script unit test: 3/3 통과
- selected K0 run: 9
- normalized layer tree: 동일
- SVG: byte-for-byte 동일
- canonical SHA-256:
  `20f1c2b6932d7cc0ec77582058c8da13ccb5ca7fda60237b3b29ae48f6b08e34`
- 증적 JSON:
  `/tmp/rhwp-4968-r4c4-oracle.GAjTP4/kerning_q1_baseline.json`

### 4.6 WASM integration test 토폴로지 한계

`wasm-pack test --node --release . --test issue_4968_kerning_capability_provider`는 선택 타깃만 빌드하지 않고
Cargo에 등록된 모든 test와 binary를 `--tests`로 컴파일했다. 그 결과 다음 기존 네이티브 전용 의존·test가
WASM compile을 막았다.

- 전역 dev-dependency `proptest`의 fork/timeout → `getrandom 0.3`, `wait-timeout`
- `src/service/mod.rs` 파일 경로 단위 test의 native-only `open_path`
- 64-bit `usize`를 전제한 WMF test
- generated regression suite의 native-only PDF module
- CLI test binary의 native-only external-image·font API

원인을 확인하기 위한 임시 정정은 모두 되돌렸고 제출 diff에 남기지 않았다. 저장소 전체의 선택 가능한 WASM
integration harness가 별도 과제다. 이번 절편은 실제 Docker 배포 WASM build와 native/WASM K0 runtime identity를
통과했으며, target-neutral measurement test의 WASM 직접 실행은 이 기반 한계 때문에 미충족으로 정직하게
남긴다.

## 5. 보호 불변식 판정

1. K0 line break·layer tree·SVG identity: 충족.
2. exact slot/style/language/inline-control 경계에서 pair 교차 적용 0: 충족.
3. body·table-cell·text-box fresh path가 같은 measurement를 소비: 충족.
4. edit reflow와 fresh pagination의 generation/context 공유: 충족.
5. segment/boundary budget 실패 시 문단 전체 K0 rollback: 충족.
6. stored `LineSeg` feature detection 보존: 충족.
7. source bytes·private path·family 이름 비노출: 충족.
8. backend glyph replay 미변경: 충족, R4D 범위 유지.
9. target-neutral integration test의 WASM 직접 실행: **기반 토폴로지로 미충족**, 별도 후속 필요.

## 6. 승인 경계와 다음 단계

R4C의 제품 목적과 K0 보호 불변식은 충족했다. 오라클은 실문서 글상자 object placement 비회귀를 지지하고,
공개 integration은 visible text의 표 셀·글상자 K1 줄 경계를 고정한다. 다만 WASM integration harness 부재를
R4C 완료와 섞어 숨기지 않는다.

메인테이너가 이 결과와 제한을 승인한 뒤에만 R4C-4 변경·보고서를 단계 커밋한다. 다음 R4D에서는
`TextRunNode`에 결정 positions를 보존하고 backend replay를 연결하며, 승인 전에는 진행하지 않는다.
