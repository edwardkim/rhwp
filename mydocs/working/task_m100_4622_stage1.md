# Task #4622 Stage 1 — TAC 거대 줄 host_seg 사영으로 표 위 텍스트 겹침 해소

Issue: #4622 (#4599 캠페인 Phase 3, 사이클 3)
Branch: `fix/4599-tac-giant-line-host-seg` (base: devel 572786d02)

## 무엇을

treat_as_char(TopAndBottom 블록) 표를 품는 문단의 음수 줄간격(Fixed) 흐름 리셋
(Task #9 경로)이 `line_segs.get(control_index)` 인덱스 폴백으로 **표 줄이 아닌 seg**
를 집어 advance=0 으로 흐름을 문단 시작에 되돌리던 결함을 고쳤다. #4531 이 native
프로파일 한정으로 도입한 앵커 사영(`control_line_seg_index`)을 hwpx 저장 사다리로
확장하되, 3중 증거 게이트로 좁혔다:

1. 전 세그 저장-태그 (종전 hwpx 제외 근거였던 계산-lineseg 회귀(56734607)를 태그로 배제)
2. 표 앞 컨트롤이 전부 비가시 (test_521 이중 가산 방지 계약 유지)
3. 사영된 seg 의 lh ≥ 표 선언 높이 (그 seg 가 실제 표 줄이라는 기하 증거)

## 왜 (실측 근거 — 36392662 p1, #4599 쪽머리 pi01 군집 최대 편차 287.4px)

- pi7 = [공백 seg0(lh=1300, ls=-1300), 표 줄 seg1(lh=13377 = 표 12380 + 여백)] +
  6×6 TAC 표. 종전 host_seg=seg0 → 음수-ls 리셋 advance = (1300-1300)=0 → 흐름이
  문단 시작으로 → 후속 '나. 근무지원 신청현황' 절이 표 위에 **77px 겹침**
  (`LAYOUT_TABLE_OVERLAP` 자체 진단 실측).
- 한글 2022 캐시 PDF·저장 사다리 모두 '나' 절을 표 아래 627.7 에 배치.
- 사영 후 host_seg=seg1 → advance = 13377-1300 = 161.0px → 흐름 603.7 (사다리
  603.7 과 일치), 겹침 소멸.

## 반증 격리 이력

1. 1차(무조건 사영): 156556059 p3 pi40 — TAC 앞 **가시 Shape** 문단에서 test_521
   계약(host_seg=None)을 우회해 ls +10.4px 재가산 → body 전체 하방(쪽번호 앵커 실측).
   또한 156753918·36458608 동형 → **표 앞 비가시 한정** 게이트 추가로 3건 전부 해소.
2. 기하 증거(lh ≥ 표 높이) 게이트 — 무증거 사영의 여지 차단.

## 검증 실측 (단독 브랜치, base 572786d02)

### #4599 backlog 242 재판정 (캐시 PDF)

- 개선 1: 36392662 **287.4 → 34.5** (겹침 해소; 잔여 34.5 는 이중 +17.3 가산의 별개
  하위 기전 — 후속 사이클 대상)
- **불변 242 · 악화 0** (+2px 초과 이동 0)

### hwpx 3418 스윕 (baseline 클린 572786d02 worktree 빌드)

- 판정 이동 0 · worst +2px 악화 0 · 개선 2 (대상 −150.0, 2958257 −4.8)
- 전수 Table-diff 변동 3: 대상(의도) · 156652085(−1.1px 미세) · 36424327(±7px
  사다리-충실 방향, 겹침 없음 확인 — 무캐시 검증 후보)

### 저장소 게이트

- cargo fmt --check: 통과 · clippy: 경고 0 · cargo test --release 전체: exit 0 ·
  wasm-pack build --target web: 성공

### 참고 — 사이클 2 와의 결합 검증

PR #4614 브랜치에 본 수정을 얹은 결합 빌드로도 242 재판정(개선에 상호 간섭 없음)과
3418 스윕을 수행했다(`_oracle_pdf_2022/` 의 `sweep3418_c2c3_integration.tsv`).
