# #2148 3-sum 오라클 — Windows 실측 결과 (2026-08-13)

[러너북](task2148_3sum_runbook.md)의 A~D 를 Windows+한컴에서 실행한 결과. 러너북의 좌표
가정과 오라클 구현 양쪽에서 오류가 나와 `tools/hangul_row_heights.py` 를 재설계한 뒤 측정했다.

## 0. 실행 환경

| 항목 | 값 |
|---|---|
| 한컴 | Office 2022 (`HOffice120`, COM `Version=[12,0,0,535]`) |
| COM 결정 | `HWPFrame.HwpObject`(및 `.1`/`.2`)가 전부 같은 CLSID `{2291CF00-…}`. HKCU 등록(2022)이 HKLM 등록(2020)을 이긴다 — 버전별 ProgID 분기는 없다 |
| pyhwpx | 1.7.2, `FilePathCheckerModule` 등록됨 |
| rhwp | `v0.8.4`, `local/task2148-3sum-prep` (= `origin/devel` + 이 브랜치 1커밋), release 빌드 15m30s |
| 격리 | 별도 worktree. `D:` 가 exFAT 이라 `safe.directory` 등록 필요 |

**버전 주의**: 러너북의 대조 기준선은 B 가 한컴 2024 PDF, D 가 한컴 2020 PDF 다. 이번 COM
실측은 2022 이므로 D 의 "PDF 괘선 = COM" 검증은 버전을 가로지른다.

## 1. 결과 요약

| | 문서 | pi | 복원 | 표 총합 차(rhwp−한글) | 이탈 행 |
|---|---|---:|---|---:|---|
| A | 36404953_gyeoljae.hwpx | 0 | 6/6 행, 11/11 셀 | **−0.02px** | 0/6 |
| B | 76076_regulatory_analysis.hwpx | 전수 | 85개 표, 414행 | **+163.8px** (중앙 +0.02) | 42/414 = 10.1% |
| C | 21761835_jeonjik_exemption_table.hwp | 4 | 78/78 행, 296/296 셀 | −711.76px — **무효** | 10/78 |
| D | sample1-repro.hwp 표64 | 763 | 75/75 행, 399/399 셀 | **+8.68px** | 3/75 |

쪽수: A 1쪽, B **82쪽**(= 한컴 2024 PDF 82 ✓), C 6쪽.

## 2. A — #2148 원 재현: 완전 일치

`표1 [구역0:문단0]` 6행×4열. 행별 |diff| 최대 0.02px, 총합 −0.02px.
리눅스 선측정 rhwp 값 `[32.2, 79.3, 17.1, 31.7, 31.7, 30.0]` 과도 일치.
**이 문서 계열은 클램프 없이 종결**해도 되는 것으로 실측 확인.

## 3. D — 방법 검증: 성립, 단 이탈 3행

`표64 [구역0:문단763]` 75행×10열, 셀 399개. 방문 셀 399 = `rhwp info` 셀수 399 (교차 검증).

- 러너북이 지목한 중앙 행 피치: **rhwp 32.12px ↔ 한글 COM 32.13px** (0.01px 차).
  한컴 2020 PDF 괘선 32.0px 과는 0.13px 차 — PDF 괘선 추출이 COM 실측을 0.4% 안에서
  재현한다. **"PDF 괘선 = COM 실측" 은 성립**하며, 이후 전수 대조를 PDF 로 대체할 수 있다.
  (다만 2022 COM ↔ 2020 PDF 라 0.13px 이 버전차인지 추출 오차인지는 미분리.)
- 72/75 행이 ±0.01px 로 일치. 어긋난 3행:

| row | 한글 px | rhwp px | diff |
|---:|---:|---:|---:|
| 7 | 21.20 | 32.12 | +10.92 |
| 35 | 47.96 | 32.12 | −15.84 |
| 62 | 18.07 | 32.12 | +14.05 |

**세 행 모두 rhwp 가 32.12 = 이 표의 최빈 행높이로 찍는다.** 한글은 21.20/47.96/18.07 로
제각각인데 rhwp 만 표준 피치로 정규화한다. 행높이 팽창이 아니라 **행높이 균질화**가
이 표의 오차원이다.

## 4. B — 코호트 전수: 중앙은 정답, 소수 표가 한쪽으로 샌다

`rhwp info` 표 113개 중 TABLE_DRIFT 를 내는 표 94개, 그 중 85개 측정
(9개는 래퍼/중첩 표 pi 충돌로 제외 — 아래 6절).

- 표 총합차가 0.5px 이내: **58/85**. 중앙값 **+0.02px**.
- |차|>1px: 27개. 방향이 **한쪽으로 쏠린다** — rhwp 가 큰 표 23개 vs 한글이 큰 표 4개.
- 문서 전체 누적 **+163.8px**.

즉 "rhwp 가 표를 전반적으로 키운다" 가 아니라, **대부분은 정확하고 특정 형태의 표에서만
일정량씩 크게 잡는다**. 형태별로 값이 반복되는 것이 결정적이다:

| 형태 | 표 수 | 차 | 성격 |
|---|---:|---:|---|
| 1×1 | 13 | **모두 정확히 +3.8px** (한글 17.4 → rhwp 21.1) | 상수 결함 |
| 5×2 | 4 | **모두 정확히 +6.4px** (이탈 2/5행) | 상수 결함 |
| 7×2 | 4 | +24.0, +24.0, +36.5, +53.9 (이탈 1/7행) | 한 행에 몰림 |
| 31×17 | 4 | −43.1, −14.8, −8.3, −7.6 | 유일하게 한글이 큼 |

1×1 13개만으로 49.4px, 5×2 4개로 25.6px — 누적 +163.8px 의 46% 가 이 두 상수 결함이다.
**클램프 재설계보다 이 두 형태의 최소 행높이 산정을 먼저 보는 것이 비용 대비 효과가 크다.**

전체 이탈 표 27개 목록은 `output/task2148/B_76076_all.txt` (gitignore 대상 — 재생성:
`python tools/hangul_row_heights.py samples/issue1891/76076_regulatory_analysis.hwpx
--exe target/release/rhwp.exe --all`).

### 러너북 B 목표는 측정 불가

러너북이 지목한 `표4 [구역0:문단10]` 5행×7열은 **TABLE_DRIFT 를 아예 내지 않는다.**
최상위 표 19개(pi 0, 5, 10, 60, 150, 154, 221, 311, 315, 358, 394, 486, 490, 566, 656,
660, 726, 817, 821)가 같은 상태다. 이 진단으로는 관측할 수 없는 표가 있다는 뜻이며,
전수 대조를 TABLE_DRIFT 에만 의존하면 이 19개는 영구 사각지대다.

## 5. C — 이 방법으로는 측정 불가

`표1 [구역0:문단4]` 78행×5열, 셀 296개(78×5=390 → 병합 94개). 방문 셀 296 = info 296.
78행 전부 "복원" 되지만 **합계가 한글 5582.10 vs rhwp 4870.33 으로 −711.76px 벌어진다** —
복원값이 표를 한 번씩 덮지 못한다는 증거다.

원인은 복원 휴리스틱의 전제 붕괴다. 행높이를 "그 행에서 시작하는 셀들의 최소 높이" 로
잡는데, 이는 그 행에 rowspan=1 셀이 하나라도 있어야 성립한다. C 의 row 11 은 시작 셀이
전부 세로 병합이라 최소값이 671.36px 이고, 이는 rhwp row 11–17 의 합(671.3)과 같다.
따라서 C 의 큰 diff 10건 중 다수는 실측 차이가 아니라 **병합 잔재**다.

이슈 본문의 "한글 ≈24.3px vs 재합성 37.76px" 는 이번 실행으로 확인하지 못했다.
**후속**: rowspan 을 COM 에서 직접 읽거나(셀 ParameterSet), 셀별 (시작행, 높이) 집합에서
행높이를 연립으로 푸는 복원기가 필요하다. 합계 일치가 그 검증 게이트다.

## 6. 좌표계 — 러너북 정정

`--table-index`(한글 HeadCtrl 순번)와 `--pi`(rhwp 문단 인덱스)는 다르며, **HeadCtrl 순번은
문서 순서와 어긋난다**(중첩 표가 끼어든다). 실측으로 확정한 규칙:

> **한글 tbl 앵커 `GetAnchorPos(0)` 의 `List==0` 일 때 그 `Para` 값 == rhwp TABLE_DRIFT 의 `pi`.**

76076 상위 60개 표 기준 36 OK / 4 MISMATCH / 20 미해당(중첩 또는 drift 없음)으로 검증.
이 규칙을 도구에 넣어 `--pi` 하나로 양쪽을 같은 좌표에서 고르게 했다. `--table-index` 는
레거시 경로로만 남긴다.

MISMATCH 4건(pi 36/197/202/370)은 전부 **1×1 래퍼 표 안에 중첩 표**가 든 형태다.
한글은 바깥 표 1행을 보고, rhwp 는 같은 pi 로 안쪽 표(3/3/2/3행)를 보고한다 —
**중첩 표에 바깥 문단 pi 를 붙여 pi 가 충돌한다.** 도구는 info 행수 ≠ drift 행수인 표를
건너뛴다(B 에서 9개).

러너북 좌표 정정:

| | 러너북 | 실제 |
|---|---|---|
| A | `--table-index 0 --pi 0` | 맞음 |
| B | `--table-index 3 --pi 10` | **pi 10 은 존재하지 않음** — `--all` 로 대체 |
| C | `--table-index 0 --pi 0` | **`--pi 4`** (문서에 표 1개, 문단4) |
| D | `--table-index 63 --pi 763` | pi 763 맞음 |

## 7. 오라클 결함 3건 (수선함)

1. **표 진입 실패** — `SetPosBySet`+`FindCtrl` 은 표 앵커와 같은 문단에 누름틀(`%clk`)이나
   그림(`gso`)이 있으면 표가 아니라 그쪽을 잡는다. 36404953 은 `tbl@(0,0,16)` 옆에
   `%clk@(0,0,24)` 가 있어 FindCtrl 이 몇 번을 호출해도 `%clk` 만 반환하고 A 가 통째로
   실패했다. pyhwpx 의 `get_into_nth_table` 도 같은 경로라 함께 깨진다.
   → `hwp.select_ctrl(ctrl)` 로 컨트롤 객체를 직접 선택.
2. **침묵 폴백** — `--pi` 가 아무것도 못 맞히면 조용히 옛 `cut_rows=[...]` 표기의 **첫 줄**로
   폴백했다. 현행 바이너리에도 `cut_rows` 가 13개 남아 있어서, B 의 `--pi 10`(없는 pi)이
   **전혀 다른 표(pi 13, 31행)** 의 값으로 그럴듯한 대조표를 찍었다. n/a 보다 나쁜 실패다.
   → `--pi` 가 없으면 사용 가능한 pi 목록과 함께 죽는다.
3. **병합 셀을 행으로 셈** — `TableLowerCell` 순회는 세로 병합 셀을 한 행으로 세어
   병합 구간 합을 한 행 값으로 낸다(C: 실제 78행인데 31스텝, D: 75행인데 10스텝).
   → `goto_addr` 로 전 셀을 훑어 행별 최소 높이를 취한다. 방문 셀 수가 `rhwp info` 의
   셀 개수와 일치하는지, 복원 합이 rhwp 합과 맞는지가 검증 게이트다(A·D 통과, C 실패).

부수: 스윕 중 한컴이 죽는 경우(`RPC 서버를 사용할 수 없습니다`)가 있어 부분 결과를 살려
반환하도록 했다. 직전 실행의 COM 서버 잔재가 원인이면 `taskkill /F /IM Hwp.exe` 후 재시도로 풀린다.

## 8. #2148 후속 판단 재료

- **코호트 재계측 vs 클램프 재설계** → 실측은 **재계측 쪽**을 지지한다. B 85개 표 중 58개가
  0.5px 이내이고 중앙값이 +0.02px 다. 전면 클램프는 정상인 다수를 건드린다.
- 대신 **형태별 상수 결함 2건**(1×1 +3.8px, 5×2 +6.4px)이 누적의 46% 다. 좁고 확실한 표적.
- D 는 팽창이 아니라 **균질화**(3행을 최빈 피치 32.12 로 찍음)가 오차원임을 보여준다.
  #4568 의 "행 피치는 일치, 쪽수 +2 는 꼬리 조각 되감기 축" 관측과 모순되지 않는다.
- 측정 신뢰도 경계: 복원 휴리스틱은 rowspan=1 셀이 없는 행에서 **한글 쪽을 부풀린다**
  (= 차를 음수로 민다). B 에서 관측된 쏠림은 양수(+163.8px)이므로 이 편향과 **반대 방향**이다
  — 즉 B 의 결론은 보수적이다. 반대로 31×17 표 4개의 음수 차는 이 편향으로 설명될 수 있으니
  단독 근거로 쓰면 안 된다.
