# Task M100 #4969 W10-Q4-D1 — exact-source-bound dormant owner 결과

- **상위 계획**: [`task_m100_4969_w10_q4_d.md`](../plans/task_m100_4969_w10_q4_d.md)
- **D0 checkpoint**: `87d7a13a2`
- **기계 판독 증적**:
  [`w10_q4_d1_exact_source_owner.json`](../tech/investigations/issue-4969/w10_q4_d1_exact_source_owner.json)
- **실행일**: 2026-08-30 KST
- **판정**: `qualified-dormant-certified`
- **메인테이너 결과 승인**: 2026-08-30 KST
- **제품 출력 변경**: 0

## 결론

Q4-D1은 exact font registry의 불변 snapshot을 소유하고 `(charShapeId, languageIndex)` slot의 source
identity를 shape 직전에 재검증하는 `VerticalShapingContext`를 제품 module에 등록했다. context가 만드는
결과는 source handle·registry generation·동일 `Arc<[u8]>`·units-per-em을 인증한 dormant transaction이며,
layout·sidecar·paint·backend caller는 여전히 0이고 `product_published=false`다.

D0의 다섯 red 가운데 module registration과 exact-source-bound owner 두 항목만 green으로 전환됐다.
남은 D2 atomic layout commit, D3 vertical publication, D4 CanvasKit feature detection 세 항목은 의도한 red로
유지된다. 따라서 이 결과는 제품 activation이 아니라 다음 절편이 사용할 인증된 입력 권위만 연 것이다.

## 1. 구현 계약

1. `renderer::shaping_vertical`은 제품 module에 등록되지만 `#[allow(dead_code)]` 상태다.
2. `VerticalShapingContext`는 생성 시 exact registry를 소유해 이후 외부 registry 변경을 관찰하지 않는다.
3. `prepare_dormant`는 legacy fallback geometry를 먼저 검증하고, exact slot handle과 source를 다시 대사한 뒤
   Q4-C transaction을 만든다.
4. source certificate는 registry generation, exact slot, SHA-256·길이·face index handle, 동일 source bytes
   `Arc`, units-per-em만 소유한다.
5. Debug에는 raw text·font bytes·경로를 남기지 않는다. source 부재·identity mismatch·malformed face와
   Q4-C intent/run/variation/limit 실패는 typed reason과 원래 fallback geometry를 반환한다.

## 2. 검증 결과

| gate | 결과 |
| --- | --- |
| Q4 계약 + D1 behavior, red 제외 | 30 passed / 0 failed |
| D1 exact source·typed rejection behavior | 2 passed / 0 failed |
| D0 red 전환 상태 | 2 passed / 3 expected failed |
| 격리 wasm32 + Node D1 behavior | 1 passed / 0 failed |
| 표준 Docker WASM release build | 통과, `pkg/rhwp_bg.wasm` 9,810,922 bytes |
| integration source 정책 | 19 passed / 0 failed |
| focused Clippy `-D warnings` | 통과 |
| `cargo fmt --all -- --check` | 통과 |

표준 `docker compose --env-file .env.docker run --rm wasm`은 성공했다. 전용 wasm image에는 Node 실행 파일이
없어 같은 컨테이너의 `wasm-pack test --node`는 runner 시작 전에 중단됐고, 제품 빌드 실패로 판정하지 않았다.
이미 준비된 호스트 Node·wasm-pack·wasm32 target에서 동일 격리 하네스를 실행해 exact-source context behavior
1/1을 확인했다. 임시 하네스는 확인 뒤 휴지통으로 이동했으며 추적 산출물은 남기지 않았다.

WASM 바이트 수는 이번 빌드 산출물의 절대값이며, 동일 base·toolchain의 사전 측정값이 아니므로 크기 delta로
해석하지 않는다. `pkg/`에는 추적 diff가 없다.

## 3. 보호 불변식 대사

- 제품 layout·sidecar·paint·backend caller: 0
- product publication과 `TextRun` 교체: 0
- D2·D3·D4 source contract: 3건 모두 red 유지
- 외부 registry 변경의 D1 snapshot 유입: 0
- rejection 뒤 fallback geometry mutation: 0
- Debug raw text·font bytes·path 노출: 0
- 새 integration `.rs`, generated suite·manifest, Cargo marker: 0
- private corpus·Hyper-V·GitHub mutation: 0

## 다음 승인 경계

메인테이너가 `qualified-dormant-certified` 결과와 checkpoint 진행을 승인했다. 최종 format·JSON·링크·
diff 검사를 재실행한 이 commit을 D1 checkpoint으로 고정한다. 최신 `upstream/devel@7592e9c99`는 제품
source와 겹치지 않지만 작업 기록 문서가 겹치므로, D2 착수 전에 checkpoint 위로 병합·재검증한다.
Q4-D2 atomic table-cell layout commit은 동기화와 별도 착수 승인 전에는 시작하지 않는다.
