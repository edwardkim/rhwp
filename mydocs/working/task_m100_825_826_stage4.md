# Task #825/#826 Stage 4 (회귀 검증) 보고서

**브랜치**: `local/task825_826`
**선행**: Stage 3 (3a + 3b + 3c) 완료
**목표**: 두 결함 정정의 회귀 영향 객관 검증

## 자동 검증

### cargo test (release)

```
$ cargo test --release
... (모든 test 슈트 실행)
passed=1358 failed=0
```

신규 테스트 5건 (issue_825 × 3 + issue_826 × 4 = 7건 추가, sample11 fixture sanity 포함) 통합 1358 통과.

### cargo clippy

```
$ cargo clippy --release -- -D warnings
Finished `release` profile [optimized] target(s) in 7.51s
```

lib + 우리 변경 부분 clippy clean.

### TypeScript

```
$ cd rhwp-studio && npx tsc --noEmit
(clean)
```

## SVG 회귀 검증

baseline (`local/devel` HEAD `ffdcb1a`) vs patched (`local/task825_826` HEAD `41550b3`) — 6개 sample 전체 page 비교.

### 결과 표

| Sample | Pages | Diff | 비고 |
|---|---:|---:|---|
| hwp3-sample.hwp | 16 | 0 | bit-identical |
| hwp3-sample4.hwp | 36 | 0 | bit-identical |
| hwp3-sample5.hwp | 64 | 0 | bit-identical |
| hwp3-sample10.hwp | 763 | 272 | **PUA 변환 (의도)** |
| hwp3-sample11.hwp | 152 | 151 | **PUA 변환 (의도)** |
| pic-in-head-02.hwp | 7 | 0 | bit-identical |
| **합계** | **1038** | **423** | (의도 변화 100%) |

### Diff 본질 — 의도된 PUA 변환

샘플 diff (sample11 page 2):
```diff
-<text ...>󰠏</text>   ← baseline: U+F080F (한컴 PUA, 글리프 부재)
+<text ...>━</text>   ← patched : U+2501 (HEAVY HORIZONTAL, 표준 Unicode)
```

PUA 문자 정확 1:1 변환 입증:

| Sample | Baseline U+F080F | Patched U+F080F | Patched U+2501 (━) |
|---|---:|---:|---:|
| hwp3-sample10 | 24,695 | **0** | **24,695** |
| hwp3-sample11 | 27,376 | **0** | **27,376** |
| **합계** | **52,071** | **0** | **52,071** |

→ 모든 PUA 문자가 표준 Unicode 로 정확히 변환되었으며, 다른 곳에 영향 zero.

**해석**: 423 page 의 diff 는 모두 Stage 2 PUA substitution 의 의도 효과. 회귀 zero.

### Stage 3 영향

Stage 3 (Render chain + WASM API + UI) 의 변경은:
- ImageNode 메타 필드 추가 (`header_footer_ref`)
- 신규 WASM API (`getHeaderFooterPictureProperties`)
- TypeScript ref 확장 + dialog dispatch

→ 모두 메타데이터 / 신규 경로 추가로 기존 SVG 출력에 영향 없음. 위 6 sample × 1038 page 회귀 결과로 입증.

## WASM 재빌드

```
$ wasm-pack build --target web --out-dir pkg --release
Finished `release` profile [optimized] target(s) in 18.56s
[INFO]: Optimizing wasm binaries with `wasm-opt`...
[INFO]: ✨   Done in 25.51s
```

## 다음 단계

Stage 5 (시각 검증) — rhwp-studio dev 서버 실행 + 작업지시자 시각 판정 요청:
- 머리말/꼬리말 가로선 패턴 한컴오피스 2022 정합 (#826)
- 머리말 그림 우클릭 → "개체 속성" dialog 정상 표시 + 속성 조회 (#825 view-only)
- U+F0827 매핑 (U+25A0 ■) 시각 정합 확인 (사용 위치 발견 시)
