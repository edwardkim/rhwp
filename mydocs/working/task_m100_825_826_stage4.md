# Task #825/#826 Stage 4 (회귀 검증) 보고서

**브랜치**: `local/task825_826`
**선행**: Stage 3 (3a + 3b + 3c + 3d) + Task #824 merge 완료
**목표**: 통합 PR 전 객관 회귀 검증

## 자동 검증

### cargo test (release)

```
$ cargo test --release
... (모든 test 슈트 실행)
passed=1360 failed=0
```

신규 테스트 9건 (issue_824 × 2 + issue_825 × 3 + issue_826 × 4) 통합 1360 통과.

### cargo clippy

```
$ cargo clippy --release -- -D warnings
Finished `release` profile [optimized] target(s) in 7.68s
```

본 검증 중 clippy lint 1건 발견 — `set_picture_properties_native` 의 불필요한 `return` (Stage 3d 리팩토링 잔존). early-return → if/else 표현식으로 정리 commit 추가.

### TypeScript

```
$ cd rhwp-studio && npx tsc --noEmit
(clean)
```

## SVG 회귀 검증

baseline (`local/devel` HEAD `ffdcb1a`) vs patched (`local/task825_826` HEAD) — 6 sample 전체 page 비교.

### 결과 표

| Sample | Pages (patched) | Pages (baseline) | Diff | 비고 |
|---|---:|---:|---:|---|
| hwp3-sample.hwp | 16 | 16 | 0 | bit-identical |
| hwp3-sample4.hwp | 36 | 36 | 0 | bit-identical |
| hwp3-sample5.hwp | 64 | 64 | 0 | bit-identical |
| hwp3-sample10.hwp | 763 | 763 | **272** | PUA 변환 (의도) |
| hwp3-sample11.hwp | 152 | 0 (fixture) | n/a | 신규 fixture (Task #824) |
| pic-in-head-02.hwp | 7 | 7 | 0 | bit-identical |
| **합계 (비교 가능)** | **886** | **886** | **272** | (의도 변화 100%) |

### Diff 본질 — 의도된 PUA 변환

sample10 의 272 페이지 diff 는 모두 Stage 2 PUA substitution (U+F080F → U+2501) 의도 효과. 회귀 zero.

> sample11 은 본 task 에서 신규 fixture 로 추가되어 baseline 부재 (정상).

## WASM 재빌드

```
$ wasm-pack build --target web --out-dir pkg --release
✨ Done in 26.07s
```

## 작업지시자 시각 검증 결과 (Stage 5 진행 중 누적)

진행 중 발견된 결함과 처리:

| 결함 | 처리 |
|---|---|
| 머리말 그림 우클릭 → "개체 속성" 메뉴 부재 | Stage 3c 보강 (commit 36f9d86) — 머리말 모드 picture click 감지 |
| 본문 모드 머리말 그림 클릭 시 selection 부재 | Stage 3c 추가 보강 (4b20082) — 모든 enterPictureObjectSelectionDirect 호출에 headerFooter 전파 |
| dialog 클릭 후 "문단 인덱스 4294967295 범위 초과" 에러 | Stage 3a 보강 (c97d971) — TAC 인라인 picture 의 ImageNode 후처리 (header_footer_ref 부여 + para_index 정규화) |
| dialog 표시되나 "E$$00001.jpg + 문서에 포함 미체크" | Task #824 merge — pic_type 분기 (`pic_type == 0` 만 external_path) |
| picture 회전/대칭 버튼 미동작 | **별도 이슈 #831 신규 등록** (본 task scope 외, 모든 picture 공통 결함) |

## PR #827 close

Task #824 의 4개 commit 이 본 브랜치에 merge 완료되어 PR #827 (Task #824 단독) 은 close. 통합 PR 에서 `closes #824 + #825 + #826` 일괄 처리 예정.

## 다음 단계

Stage 5:
1. 작업지시자 최종 시각 검증 통과 확인
2. 최종 보고서 + orders 갱신
3. 통합 PR 생성 (`closes #824, #825, #826`)
