# Task #259 Stage 4 완료 보고서 — 타 HY 계열 스모크 검증

- 일자: 2026-04-23
- 브랜치: `local/task259`
- 작업: Stage 2 alias 추가 이후 HY 계열 7종 각각을 사용하는 대표 샘플에 대해 렌더링 정상 동작 확인

## 샘플 분포 스캔

`rhwp info` 로 `samples/*.hwp{,x}` 143개 중 HY 폰트 사용 파일 33개 식별:

| HY 폰트 | 사용 샘플 수 | 대표 샘플 |
|---------|-----------|---------|
| HY중고딕 | 4 | `20250130-hongbo.hwp`, `text-align.hwp` |
| HY신명조 | 4 | `2010-01-06.hwp`, `통합재정통계(2010.11월).hwp`, `aift.hwp` |
| HY견고딕 | 13 | `hwpspec.hwp`, `kps-ai.hwp`, `k-water-rfp.hwp`, `multi-table-001/002.hwp` 등 |
| HY견명조 | 3 | `footnote-01.hwp`, `endnote-01.hwp`, `tac-img-02.hwp{,x}` |
| HY헤드라인M | 30+ | 거의 모든 샘플에 등장 |
| HY그래픽 | 0 | **샘플 부재** |
| HY궁서 / HY궁서B | 0 | **샘플 부재** |

## 스모크 테스트

HY 폰트 5종(중고딕·신명조·견고딕·견명조·헤드라인M) 각 대표 샘플 1개에 대해 `rhwp export-svg -p 0` 실행:

| HY 폰트 | 샘플 | 결과 | `<text>` 요소 수 |
|---------|------|------|-----------------|
| HY중고딕 | `20250130-hongbo.hwp` | ✅ 정상 | 531 |
| HY신명조 | `2010-01-06.hwp` | ✅ 정상 | 583 |
| HY견고딕 | `hwpspec.hwp` | ✅ 정상 | 92 |
| HY견명조 | `footnote-01.hwp` | ✅ 정상 | 390 |
| HY헤드라인M | `biz_plan.hwp` | ✅ 정상 | 45 |

- panic·오류 메시지 없음
- 모든 샘플에서 `<text>` 요소 수 비정상 없음 (0 아님, 렌더 결손 없음)

산출물: `output/svg/task259-smoke/{HY폰트}/*.svg` (커밋 제외 — `.gitignore`)

## 샘플 부재 폰트 (HY그래픽, HY궁서)

현재 `samples/` 에 HY그래픽 · HY궁서 를 사용하는 파일이 없다. 본 타스크에서는:

- alias 매핑은 **방어적으로** 추가 (Stage 1 에서 공개 폰트 메타 + 한컴 관례로 확정)
- Stage 2 단위 테스트로 `resolve_metric_alias` · `find_metric` 반환값 정합성 검증 (7종 전수)
- 실제 렌더 회귀는 샘플 확보 후 **후속 이슈**로 이관

신규 샘플 생성은 본 타스크 범위 외 — 별도 이슈 등록 후 샘플 수집 필요.

## 검증 결론

| 검증 항목 | 결과 |
|-----------|------|
| HY 5종 (샘플 존재) 스모크 렌더 | ✅ 정상 |
| HY 2종 (샘플 부재) alias · DB 매핑 | ✅ Stage 2 단위 테스트로 검증 완료 |
| 기존 SVG 스냅샷 회귀 | ✅ 그린 (`cargo test --test svg_snapshot`) |

## 산출물

- `output/svg/task259-smoke/` — 스모크 SVG 출력 (로컬, 커밋 제외)
- 본 보고서

## 후속 이슈 후보

- **HY그래픽 / HY궁서 샘플 수집 및 렌더 회귀 테스트** — 본 타스크에서 해당 폰트가 실제 문서에서 어떻게 렌더되는지 시각 확인이 필요한 경우 후속 이슈에서 다룬다.

## 승인 요청

Stage 4 완료 승인 및 Stage 5 (문서화 + 메모리 등록) 착수 승인을 요청드립니다.
