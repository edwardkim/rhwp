# Task #826 Stage 2 (GREEN) 보고서

**브랜치**: `local/task825_826`
**선행**: Stage 1 (RED) 완료 — `issue_826` 2 FAIL + 2 회귀 가드 PASS
**목표**: 최소 변경으로 RED 테스트 PASS 전환

## 수정 내용

[src/renderer/layout/paragraph_layout.rs:3052-3066](../../src/renderer/layout/paragraph_layout.rs#L3052-L3066)

기존 0xF00D0~0xF09FF 범위 PUA match 에 두 케이스 추가:

```diff
     if (0xF00D0..=0xF09FF).contains(&code) {
         return match code {
             // 책괄호 (한국어 도서 제목) — 용비어천가, 석보상절, 월인천강지곡 등
             0xF0854 => '\u{300A}', // 《 LEFT DOUBLE ANGLE BRACKET
             0xF0855 => '\u{300B}', // 》 RIGHT DOUBLE ANGLE BRACKET
             // 예시 마커 — `(F00DA 단풍 철 : 철 성분)` 패턴 — 한컴 PDF 시각 검증 필요
             0xF00DA => '\u{25B8}', // ▸ BLACK SMALL TRIANGLE (잠정, 시각 판정 후 정정)
+            // [Task #826] HWP3 한컴 PUA 그래픽 라인 (PR #753 후속 — johab.rs:65,67).
+            // 한컴 함초롬 폰트는 PUA glyph 보유, rhwp-studio 번들 폰트 (오픈 라이선스)
+            // 부재 → render-time substitution. 측정/렌더링 양쪽 자동 적용.
+            // sample11.hwp 머리말/꼬리말 가로선 패턴 (각 85+ 회) 시각 정합.
+            0xF080F => '\u{2501}', // ━ BOX DRAWINGS HEAVY HORIZONTAL (한컴 — 굵은 가로선)
+            0xF0827 => '\u{25A0}', // ■ BLACK SQUARE (한컴 — 잠정, 시각 판정 후 조정)
             _ => ch,
         };
     }
```

핵심 변경: 1 hunk, +6/-0 (코멘트 4 + match arm 2). 별도 모듈/함수 신설 없음.

## 검증 결과

```
$ cargo test --test issue_826
running 4 tests
test issue_826_non_pua_passthrough ... ok
test issue_826_other_pua_existing_unchanged ... ok
test issue_826_pua_f080f_substitution ... ok
test issue_826_pua_f0827_substitution ... ok

test result: ok. 4 passed; 0 failed; 0 ignored; 0 measured
```

| 테스트 | Stage 1 | Stage 2 | 의미 |
|---|---|---|---|
| `issue_826_pua_f080f_substitution` | ❌ FAIL | ✅ PASS | RED → GREEN ✓ |
| `issue_826_pua_f0827_substitution` | ❌ FAIL | ✅ PASS | RED → GREEN ✓ |
| `issue_826_non_pua_passthrough` | ✅ PASS | ✅ PASS | 회귀 가드 유지 |
| `issue_826_other_pua_existing_unchanged` | ✅ PASS | ✅ PASS | 기존 PUA 정합 유지 |

## 적용 범위 (자동)

`map_pua_bullet_char` 의 호출처 (Stage 1 조사):
- `web_canvas.rs:1597` (Canvas draw_text) — 화면 렌더링 적용
- `paragraph_layout.rs:223` (텍스트 layout) — 측정 + 줄 분리 적용
- `paragraph_layout.rs:2933` (bullet) — 글머리표 (본 task 직접 영향 없음)

→ 측정/렌더링 양쪽 자동 적용. WASM 재빌드 후 rhwp-studio 시각 정합 예상.

## 다음 단계

Stage 3 (GREEN #825) — WASM API 에 optional header/footer path 파라미터 추가.
