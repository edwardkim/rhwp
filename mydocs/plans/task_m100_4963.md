# 수행계획 — Task M100 #4963 W5 Oracle Profile·controlled ladder

- **이슈**: [#4963](https://github.com/edwardkim/rhwp/issues/4963)
- **상위 추적**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행 작업**: [#4962](https://github.com/edwardkim/rhwp/issues/4962) W4 조판 위험 순위
- **브랜치**: `task_m100_4963`
- **작성일**: 2026-08-22 KST
- **통합 기준**: `upstream/devel@4bd9c5d60efe85d80e935ff76ed5441c17d56699`
- **원격 상태**: #4963 OPEN, 담당자 `edwardkim`, milestone `v1.0.0`, 연결 PR 없음
- **절차 상태**: Stage W5-2 fixture·분석기·source inventory 완료, Stage W5-3 승인 대기

## 1. 결정과 목표

W5의 목적은 W4 상위 후보에 임의 fallback을 추가하는 것이 아니다. 같은 입력을 한컴의 정확한 폰트가
있는 상태부터 관련 폰트가 모두 없는 상태까지 통제해서 출력하고, **폰트 선택·글립·advance·줄·페이지 중
어디에서 최초 조판 차이가 생기는지** 재현 가능한 Oracle Profile로 고정하는 것이다.

이번 작업은 다음 질문에 답한다.

1. 문서가 요구한 exact face가 설치되었을 때 한컴은 실제로 어떤 font/glyph/advance를 쓰는가?
2. exact face가 없을 때 문서의 `substFont`, 공식 successor, 한컴 자체 missing-font 선택은 각각 어떤
   결과를 만드는가?
3. 서로 다른 관계를 이름 유사성이나 metric 근사치로 합치지 않고 증명할 수 있는가?
4. 다음 font incident에서 동일한 ladder를 다시 쓸 수 있는가?

### 범위 안

- W4 A·B band 17개 face의 readiness와 조사 상태를 명시한다.
- Oracle Profile schema, 관계 enum, 상태 enum, deterministic fixture와 비교기를 만든다.
- 이미 존재하는 W3·W4·한컴 2022 계측은 hash가 일치하면 재사용한다.
- 승인된 통제 환경에서 exact·missing ladder를 canary부터 단계적으로 실행한다.
- 완료할 수 없는 face도 `unknown` 또는 `blocked` 근거를 남겨 분모에서 숨기지 않는다.

### 범위 밖

- 제품 font metric DB, fallback 순서, paint/backend 정책 변경
- 이름이 비슷하다는 이유만으로 exact·alias·successor 관계 강제
- 351개 전체 face 전수 조사
- 비재배포 font bytes·subset PDF·private corpus 식별 정보 공개
- HWP/HWPX 또는 한컴 빌드 번호만으로 정책을 분기하는 구현

## 2. 동결 입력과 재계측 금지선

W5 입력은 W4 공개 ranking을 정본으로 사용한다.

| 입력 | SHA-256 | W5 용도 |
| --- | --- | --- |
| `mydocs/report/assets/task_m100_4962/font_typesetting_risk_rank.json` | `6947e9e8a6c67a60a54b04dc6f1abf75e3cc66d9096a978d301ba2c10bb4ee3a` | 17개 action queue와 후보별 질문 |
| W4 canonical output | `95e7a41d1ed92a60cb66e1705b038c3e9086829b3c8aee48af57e8c2da111a68` | ranking 의미 동일성 확인 |
| `tools/task2430/EVIDENCE.md` | `975e827841449182bc7154ba945835b5776e6f078db9bb1bae51f1359bd0dd5e` | 기존 한컴 2022 HFT 계측 환경·결과 |
| `tools/task2430/measured/preflight_report.tsv` | `d82042076d15cfcfd4520f1582d4a5c2586b46d7321b8158d6c12d8126f4abed` | exact face/FontType readback |

W4는 351개 face를 순위화했고 A 6개, B 11개를 W5로 넘겼다. 이 17개는 위험 문자
1,562,076자, base risk mass 7,015,182로 전체의 810,374 ppm을 차지한다. W5는 이 자료를 다시
10k corpus에서 전수 산출하지 않는다. 다음 중 하나가 실제로 달라질 때만 해당 입력을 제한적으로
재생성한다.

- source commit 또는 ranking contract hash
- 대상 입력·font bytes hash
- 한컴/PDF producer·OS·export route처럼 Oracle 결과를 바꿀 환경 identity
- 비교기 schema 또는 canonicalization 규칙

## 3. Stage W5-0 준비도 census

### 3.1 17개 action queue

| 순서 | document face | 착수 상태 | W5 최초 행동 |
| ---: | --- | --- | --- |
| 1 | 문체부 바탕체 | `ready-local-sfnt` | exact name/hash 고정 후 canary |
| 2 | -윤명조120 | `source-unavailable` | 출처·라이선스 증거 탐색, 관계 추정 금지 |
| 3 | -윤고딕110 | `source-unavailable` | 동일 |
| 4 | 산돌명조 L | `source-unavailable` | 동일 |
| 5 | -윤고딕310 | `source-unavailable` | 동일 |
| 6 | -윤고딕120 | `source-unavailable` | 동일 |
| 7 | KoPubWorld돋움체 Light | `ready-local-sfnt` | 공식 공급물·name table·bytes 검증 완료 |
| 8 | KoPubWorld바탕체 Light | `ready-local-sfnt` | 공식 공급물·name table·bytes 검증 완료 |
| 9 | 맑은 고딕 | `ready-local-sfnt` | exact name/hash 고정 후 canary |
| 10 | 한양신명조 | `ready-existing-hft-evidence` | 2022 readback 재사용, mutable canary 별도 |
| 11 | -윤명조320 | `source-unavailable` | 출처·라이선스 증거 탐색, 관계 추정 금지 |
| 12 | -윤명조130 | `source-unavailable` | 동일 |
| 13 | 휴먼명조 | `ready-local-sfnt` | 기존 2022 readback과 SFNT를 교차검증 |
| 14 | -윤고딕320 | `source-unavailable` | 출처·라이선스 증거 탐색, 관계 추정 금지 |
| 15 | -윤고딕130 | `source-unavailable` | 동일 |
| 16 | 한컴 윤고딕 230 | `ready-local-sfnt` | exact name/hash 고정 후 canary |
| 17 | 산돌고딕 M | `source-unavailable` | 출처·라이선스 증거 탐색, 관계 추정 금지 |

W5-2 census는 exact SFNT 6개, 기존 HFT readback만 있는 face 1개, source-unavailable 윤·산돌 계열
10개다. `휴먼명조`는 SFNT와 2022 HFT readback을 모두 가져 두 경로를 잇는 교차검증 후보다.

### 3.2 확인된 local-only source

다음 font bytes는 저장소 밖 `/home/edward/mygithub/ttfs`를 기준으로 관리한다. Windows system path는
발견 위치일 뿐 정본 보관 위치로 사용하지 않는다.

| face | PostScript name | SHA-256 |
| --- | --- | --- |
| 문체부 바탕체 | `MBatang` | `d10509215d923fef07c1f2dffe8ebf55cbca706476559a861dff6f7cf969ff44` |
| KoPubWorld돋움체 Light | `KoPubWorldDotumLight` | `069494cce21a4222c88e537f256b6f46fee209375aba769f82431b2d382bc84f` |
| KoPubWorld바탕체 Light | `KoPubWorldBatangLight` | `e3ee21a86b6a6728c567a95aaebd8883480f27ce4f230207b0d7266b5cb3fb18` |
| 맑은 고딕 | `MalgunGothic` | `7a183cf1c6c56b9609fcc16eda8b5229fbc11758a21e669ec00343239b02192f` |
| 휴먼명조 | `휴먼명조` | `d033df71fa41de48905407f7d39aca74ebd9ae87298c5420513acaa187fbefc6` |
| 한컴 윤고딕 230 | `HaanYGodic23` | `cdf746176aba807cbf9b168c2d749be6270f44f4b80568b9e18b05d1ba53a200` |

기존 `KoPub 바탕/돋움 Light`와 W5 후보 `KoPubWorld 바탕/돋움 Light`는 이름과 세대가 다르다. 기존
KoPub bytes를 exact 또는 official successor로 자동 승격하지 않는다. KoPubWorld는 공식 공급 경로와
실제 name table/hash를 확인했으며 저장소 밖 local-only font root에만 보관한다.

`한양신명조`는 한컴 2022에서 `FontType=2` exact HFT readback이 이미 측정됐다. 그러나 현재 호스트에서
발견되는 한컴 2010은 acceptance oracle로 사용하지 않는다. 최종 ladder의 1차 Oracle은 한컴 2020 또는
2022의 정확한 build·PDF producer·환경 manifest를 기록한 통제 환경이어야 한다.

## 4. Oracle Profile 계약

각 실행은 하나의 profile JSON이며, 다음 identity와 observation을 함께 가져야 한다.

### 4.1 identity

- issue, candidate rank, exact document face, ladder state, question id
- input HWP/HWPX SHA-256, fixture contract/version, generator source commit
- OS, locale, Hancom full version/build, PDF producer, export route
- 설치 font의 name table·face index·SHA-256과 전체 ambient font manifest digest
- 실행 시작·종료 시각, process reset/reboot 여부, 반복 번호

### 4.2 observation

- requested face, Hancom readback face와 `FontType`
- PDF subset/base font name, font type, encoding/CID 정보
- `pdfObservedAdvance`: PDF text/glyph positioning에서 관찰한 advance
- `hmtxAdvance`: 확인된 SFNT face의 `hmtx` advance와 units-per-em
- 대표 glyph outline의 canonical digest
- line count·line boundary·page count·대표 anchor 위치
- 최초 divergence plane과 최초 다른 character/line/page
- relation evidence와 직접 anchor

`hmtxAdvance`와 `pdfObservedAdvance`는 같은 값으로 간주하지 않는다. PDF CID width, Type3 glyph,
Hancom HFT처럼 원본 SFNT `hmtx`를 검증할 수 없는 경우 `hmtxAdvance`를 만들어내지 않고 상태와 사유를
기록한다.

### 4.3 필드 상태

모든 필수 관찰 필드는 값과 함께 다음 상태 중 하나를 갖는다. 설명 없는 `null`은 허용하지 않는다.

- `observed`: 도구와 원본 evidence로 실제 관찰
- `unavailable`: 현재 source/tool로 관찰 불가
- `not-applicable`: 해당 format/profile에는 개념이 적용되지 않음
- `blocked`: 안전·라이선스·immutable bundle 등 명시적 장애가 있음

### 4.4 relation enum

- `identity-exact`
- `identity-alias`
- `official-successor`
- `document-substitution`
- `metric-surrogate`
- `hancom-missing-font`
- `unknown`

각 relation은 별도 evidence anchor가 있어야 한다. `official-successor`는 제작·배포 주체의 직접 문서나
bytes/name-table 계보 없이 지정하지 않는다. `metric-surrogate`는 폭 유사성일 뿐 identity나 공식
successor가 아니다.

## 5. controlled ladder

같은 input bytes와 fixture를 다음 순서로 실행한다.

1. `exact-installed`: exact font가 설치된 기준 profile
2. `exact-removed`: exact font만 제거하고 일반 ambient 환경은 유지
3. `document-subst-font-only`: 관련 후보군에서는 문서의 `substFont`만 남김
4. `curated-official-successor-only`: 직접 증명된 공식 successor만 남김
5. `all-related-fonts-missing`: exact·subst·successor·known alias를 모두 제거

여기서 `only`는 Windows 필수 font까지 제거한다는 뜻이 아니다. **대상 face와 증명된 관련 face set**만
통제하고, 나머지 ambient font manifest는 모든 profile에서 동일하게 유지한다. 각 전환 뒤 한컴 process를
완전히 종료하고 font cache 상태를 기록한다. 필요하면 snapshot을 복원해 순서 효과를 제거한다.

bundled HFT처럼 안전하게 제거할 수 없는 font는 `blocked-immutable-bundle`로 남긴다. exact가 실제로
남아 있는 상태를 missing profile이라고 이름 붙이는 것은 금지한다.

## 6. deterministic fixture

private corpus 문서를 Oracle 입력으로 공개하지 않는다. W4가 드러낸 HWP 편집 습관을 재현하는 공개
synthetic fixture를 face별로 같은 template에서 생성한다.

- 한글, 라틴, 숫자, 문장부호, 전각 공백과 대표 혼합 문자열
- 장평 100/90/80, 자간 0/-5/-10, kerning on/off
- 본문, 고정 폭 표 셀, 글상자, 머리말/꼬리말 문맥
- 동일 문자열 반복과 line/page boundary anchor
- stored LineSeg lane과 fresh candidate lane을 구분하되 LineSeg 유효성을 미리 가정하지 않음
- 생성 후 canonical input hash와 semantic manifest를 기록

모든 ladder state에서 input bytes는 동일해야 한다. font 이름이나 문서 내용을 state마다 다시 생성하면
비교 실행으로 인정하지 않는다.

## 7. 단계별 실행

### Stage W5-1 — 계약·schema RED

산출물 후보:

- `mydocs/tech/investigations/issue-4963/oracle_profile.schema.json`
- `mydocs/tech/investigations/issue-4963/oracle_profile_contract.json`
- schema·관계·상태 negative fixture와 단위 테스트
- `mydocs/working/task_m100_4963_w5_stage1.md`

검증:

- 필수 identity/observation 누락 거부
- 설명 없는 `null`, relation anchor 없는 successor, PDF width를 hmtx로 둔 profile 거부
- 기존 W4 queue/hash가 달라지면 fail-closed

**게이트**: Stage W5-1 구현은 본 수행계획 승인 후 시작한다.

### Stage W5-2 — fixture·분석기·source inventory

**상태: 완료 (2026-08-22)**

- deterministic HWPX fixture와 manifest 생성기
- SFNT name/face index/`hmtx`/outline/fsType inventory
- PDF font/subset/CID width·glyph 위치·line/page observation 분석기
- KoPubWorld 공식 공급 경로 재확인과 local-only bytes 검증
- 17개 candidate readiness ledger

검증:

- 두 번 생성한 fixture와 manifest byte exact
- 같은 PDF를 반복 분석한 canonical JSON byte exact
- corrupted font/PDF, oversize, path escape, symlink negative control
- font bytes와 절대 경로가 tracked diff에 들어가지 않는지 확인

**게이트**: 외부 font 다운로드·복제는 Stage W5-2 승인 후 수행한다. 저장소에는 bytes를 넣지 않는다.

### Stage W5-3 — 무변경 evidence import·read-only canary

- 기존 한컴 2022 `한양신명조`·`휴먼명조` HFT evidence를 hash 고정해 profile로 투영
- 현재 설치 상태를 바꾸지 않는 exact-installed 관찰만 제한 실행
- 고정된 remote HWP2020 환경은 그 환경에서 실제 관찰 가능한 state 하나로만 기록
- 기존 PDF와 새 PDF를 서로 다른 환경이면 golden pair로 비교하지 않음

이 단계는 설치·제거를 하지 않으며, 현재 한컴 2010 결과는 secondary/historical evidence로만 분리한다.

**게이트**: remote 변환·파일 업로드가 필요하면 별도 승인을 받는다.

### Stage W5-4 — disposable 환경 controlled canary

한컴 2020/2022가 있는 복구 가능한 Windows VM/snapshot에서 2~3개 대표 face로 5-state ladder를 먼저
실행한다.

- `문체부 바탕체`: W4 rank 1, exact SFNT가 확인된 공공 후보
- `휴먼명조`: exact SFNT와 기존 HFT readback을 잇는 교차검증 후보
- `KoPubWorld돋움체 Light` 또는 `KoPubWorld바탕체 Light`: 공식 bytes 검증 성공 때만 포함

각 상태 전후 설치 목록/hash를 기록하고 snapshot 복원 뒤 재실행한다. first divergence가 안정적으로
재현되지 않으면 17개 확대를 중단하고 계약·환경부터 교정한다.

**강제 정지 게이트**: VM/snapshot·복구 절차·대상 font set을 메인테이너가 승인하기 전에는 어떤 host
font도 설치·제거하지 않는다.

### Stage W5-5 — 17개 queue 확대·공개 projection

- canary를 통과한 동일 계약으로 17개 후보를 순서대로 실행
- source-unavailable·immutable HFT는 증거 있는 `unknown`/`blocked`로 완료 상태를 기록
- profile 간 selection/glyph/advance/line/page 최초 divergence를 집계
- W1 Font Rule Ledger와 incident response 문서에 재사용 가능한 evidence anchor 연결
- 최종 보고서와 다음 제품 변경 이슈 후보를 분리 제안

W5 완료는 17개 모두 5-state 수치를 억지로 채우는 것이 아니다. 각 후보가 `observed`, `unavailable`,
`not-applicable`, `blocked` 중 검증된 disposition을 갖고, 관찰되지 않은 값을 제품 정책의 근거로 쓰지
않는 상태다.

## 8. 보호 불변식

1. **입력 동일성**: ladder 사이에서 HWP/HWPX bytes를 바꾸지 않는다.
2. **환경 동일성**: 대상 related font set 외 ambient manifest가 다르면 같은 ladder로 비교하지 않는다.
3. **관계 분리**: exact, alias, successor, subst, surrogate, Hancom missing을 합치지 않는다.
4. **advance 분리**: source SFNT `hmtx`와 PDF 관찰 advance를 분리한다.
5. **최초 차이 보존**: 최종 페이지 차이만 보지 않고 selection→glyph→advance→line→page 계보를 남긴다.
6. **feature detection**: HWPX/한컴 version 분기가 아니라 해당 문단·font evidence의 실제 유효성을 본다.
7. **fail-closed**: source·관계·환경이 확인되지 않으면 `unknown`이지 자동 fallback 성공이 아니다.
8. **제품 무변경**: W5 evidence만으로 metric DB/fallback/paint를 바로 고치지 않는다.
9. **복구 가능성**: system font mutation은 disposable snapshot과 복원 검증 없이는 하지 않는다.
10. **비공개 경계**: font bytes·private corpus·문서 식별자는 tracked/public 산출물에 넣지 않는다.

## 9. 보안·저작권·자원 한계

- local font inventory 원문은 권한 `0600`의 local-only 산출물로 둔다.
- 공개 profile에는 승인된 family/full/PostScript name, digest, license/embedding 상태만 남긴다.
- OS/2 `fsType`과 배포 조건을 확인하지 않은 subset PDF나 font bytes는 커밋하지 않는다.
- 입력·출력은 지정 root 아래 regular file만 허용하고 path traversal·symlink를 거부한다.
- PDF page·object·glyph·파일 크기 상한, child-process timeout, 임시 폴더 정리를 둔다.
- malformed PDF/font 한 건이 batch 전체를 무한 대기시키지 않도록 per-item 실패로 격리한다.
- private corpus의 path, filename, document hash, 원문, 개별 결과를 공개하지 않는다.

## 10. 검증 게이트

단계별 관련 테스트 외에 제출 전 다음을 수행한다.

- schema·contract·negative fixture 테스트
- deterministic fixture/profile canonical hash 재현
- PDF parser 두 경로가 가능한 관찰을 상호 대조
- relation evidence fail-closed와 비공개 경계 검사
- `node scripts/check_markdown_links.mjs` 또는 canonical link-check 절차의 해당 범위
- `cargo fmt --all`
- `cargo fmt --all -- --check`
- 변경 범위에 해당하는 local validation gate

실제 렌더링 산출물이 생기는 Stage W5-3 이후에는 side-by-side PDF/SVG 또는 동등한 근거를 남기고,
시각 판정은 메인테이너 승인으로 닫는다.

## 11. 완료·후속 조건

W5 완료 조건은 다음과 같다.

- 17개 후보 모두 readiness와 최종 disposition이 있다.
- 실행된 profile은 input/environment/relation/observation 계보와 hash를 가진다.
- 실행할 수 없는 profile도 blocker와 재개 조건이 있다.
- 공개 산출물에 font bytes·private corpus 식별 정보가 없다.
- 제품 변경은 실측 evidence별 별도 issue로 분리되며 #4963에서 함께 구현하지 않는다.
- 최종 보고서 승인 뒤 #4960의 W5 상태와 후속 작업을 갱신한다.

커밋, 원격 push, PR 생성, self-review, merge, 이슈 close는 각각 프로젝트 승인 절차를 따른다.
