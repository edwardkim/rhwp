# Task #544 v2 수행계획서

## 제목

Task #544 / #547 / #548 회귀 정정 — passage 글상자 우측 시프트 + 박스 안 본문 inset 이중 적용 + 셀 내부 inline TAC Shape margin 누락

## 배경

`mydocs/orders/20260503.md` 말미 "잔존 (별도 이슈 후보)" 로 기록됨. 작업지시자도 인지했던 회귀.

merge `a7e43f99` (Task #517~528 — Layout Phase 0~2) 가 `paragraph_layout.rs` 충돌 해결 중 다음 정정을 함께 revert.

| 원 commit | Task | 회귀 영역 | 현 상태 |
|-----------|------|----------|---------|
| `7ba2ecbe` | #544 Stage 2 (1) box_x/w | passage 글상자가 `box_margin_left` 만큼 우측 시프트 + 우측 잘림 | revert |
| `7ba2ecbe` | #544 Stage 2 (2) y 보정 | 박스 top trailing-ls 누락 | revert (단, Task #552 가 다른 메커니즘으로 부분 해결) |
| `7ba2ecbe` | #544 Stage 2 (3) #540 floor skip 제거 | — | **현 코드에 이미 제거됨** (충돌 해결 중 (3) 만 살아남) |
| `b3586723` | #547 Stage 2 inner_pad_left 분기 제거 | 박스 안 본문 텍스트 좌측 inset 이중 적용 | revert |
| `9576f364` | #548 Stage 2 effective_margin_left_line | 셀 내부 inline TAC Shape (Picture/Shape) margin_left 누락 | revert |

### 작업지시자 보고 증상

> 「[1~3]다음 글을 읽고 물음에 답하시오.」 글상자가 오른쪽으로 밀려있음

→ Task #544 (1) revert 의 직접 결과 — `paragraph_layout.rs:2687-2691` 에서 `box_x = col_area.x + box_margin_left`, `box_w = col_area.width - box_margin_left - box_margin_right` 로 박스가 좁아진 채 우측으로 시프트.

## 현재 devel 코드 진단

`src/renderer/layout/paragraph_layout.rs` (commit `1934161f` Task #552 후):

| Line | 현 상태 | 목표 (#544/#547 fix) |
|------|--------|---------------------|
| 690~716 | `inner_pad_left/right` 분기 + `has_visible_stroke` + `bs_left_px/right_px` | 단일 룰 `margin_left = box_margin_left`, `margin_right = box_margin_right` |
| 786 | `bg_y_start = y_start` | `bg_y_start = y_start + paragraph_border_y_correction_px.get()` (Task #552 와 양립 검증 필요) |
| 2687-2691 | `(col_area.x + box_margin_left, col_area.width - box_margin_left - box_margin_right)` 등 | `(col_area.x, col_area.width)` / override 케이스 `(ox, ow)` |

`src/renderer/layout.rs`:

- LayoutEngine struct 에 `paragraph_border_y_correction_px: Cell<f32>` 추가 (Task #544 (2))
- vpos correction 가드 skip 케이스에서 다음 paragraph 가 border 가지면 trailing-ls 만큼 set (line 1492 부근)

`src/renderer/layout/table_layout.rs`:

- `effective_margin_left_line` 헬퍼 + inline_x 산출 3 분기 (초기 / Picture target_line reset / Shape target_line reset) Left/Justify 케이스에 line_margin 적용 (Task #548)

### Task #552 와의 중첩 검증 (Stage 1 진단 사전)

Task #552 `next_para_starts_visible_border` 가 trailing-ls 보존을 통해 박스 top 위치를 보정 → Task #544 (2) `paragraph_border_y_correction_px` 와 **유사 효과 영역 중첩 가능성**. Stage 1 에서:

1. Task #552 fix 만으로 #544 의 test_544 RED 통과 여부 확인
2. 통과 시 → Task #544 (2) 재적용 **불필요** (Task #552 가 본질 흡수)
3. 미통과 시 → 재적용하되 이중 보정 회피 가드 추가 (`next_para_starts_visible_border` true 시 correction 미적용 등)

## 수정 방향

### Phase A — Task #547 / #544 (1) 수동 재적용 (paragraph_layout.rs)

직접적 회귀 (작업지시자 보고 증상). 영향: paragraph border 박스 좌표 + 본문 텍스트 inset.

- `paragraph_layout.rs:690~716`: inner_pad 분기 제거, `margin_left = box_margin_left`
- `paragraph_layout.rs:2687-2691`: `(col_area.x, col_area.width)` / override 케이스 `(ox, ow)`

### Phase B — Task #544 (2) y 보정 (Task #552 와 양립 검증 후 결정)

Task #552 fix 만으로 trailing-ls 회귀가 해소되어 있다면 skip. 미해소 시 재적용 + 이중 보정 회피.

### Phase C — Task #548 셀 내부 inline TAC Shape (table_layout.rs)

별도 파일 / 별도 영역. 회귀 검증.

### Phase D — TDD 통합 테스트 복원

`tests/integration_tests.rs` 에 `test_544_passage_box_coords_match_pdf_p4` / `test_547_passage_box_text_inset_p7_9` / `test_548_cell_inline_tac_shape_margin_p8` 복원 (원 commit 에서 추출).

## 검증 계획

### 단위 테스트
- cargo test --lib (현 baseline 1119) → 1119 + 신규 3건 = 1122 모두 GREEN
- test_544 / test_547 / test_548 RED → GREEN

### 광범위 회귀 (svg_snapshot 6 fixture)
- 21_언어_기출_편집가능본 (passage 박스 핵심)
- exam_kor / exam_eng / exam_math / exam_science / exam_social
- aift / treatise sample / hwpspec / 2010-01-06
- byte-identical 비율 측정. 박스 좌표 변화는 의도된 정정.

### 시각 판정
- 21_언어_기출 페이지 2 [1~3] / [4~6] / 페이지 4 [7~9] / 페이지 8 셀 5 [푸코]
- PDF 한컴 2010 비교
- 1차 SVG → 2차 rhwp-studio web Canvas

### 회귀 가드
- exam_science 2 페이지 (Task #525) 무회귀
- Task #525/#530/#505/#418/#501/#546 통합 테스트 무회귀
- Task #552 test_552_passage_box_top_gap_p2_4_6 GREEN 유지

## 단계 구성 (4 단계)

1. **Stage 1**: 사전 진단 + Task #552 와 #544 (2) 양립 검증 + TDD RED 테스트 3건 추가
2. **Stage 2**: Phase A (#547 + #544 (1)) 재적용 → cargo test
3. **Stage 3**: Phase B (#544 (2) — 필요 시) + Phase C (#548) 재적용 → 광범위 회귀
4. **Stage 4**: 최종 보고서 + orders/20260503.md 갱신

## 참조

- 원 commit: `7ba2ecbe` (#544 S2), `5fa10e9f` (#544 S3), `b3586723` (#547 S2), `ebe7edb7` (#547 S3), `9576f364` (#548 S2), `af854870` (#548 S3)
- revert 경위 merge: `a7e43f99` (Task #517~528 conflict 해결)
- 관련 task: #540 Stage 4 (#544 가 본질 해결로 일부 제거), #552 (#479 회귀 정정 — 본 task 와 영역 겹침)
- 샘플: `samples/21_언어_기출_편집가능본.hwp`, PDF 한컴 2010
- 코드:
  - `src/renderer/layout/paragraph_layout.rs:690~716, 786, 2687-2691`
  - `src/renderer/layout.rs` LayoutEngine struct (Cell 추가)
  - `src/renderer/layout/table_layout.rs` (effective_margin_left_line)

## 운영 메모

- 본 task 는 작업지시자 결정으로 **이슈 등록 없이** 진행
- 파일명 규칙: 후속 수정 `_v2` (CLAUDE.md 명시)
- 브랜치: `local/task544_v2` (or 기존 `local/devel` 직접) — Stage 1 시작 전 결정
