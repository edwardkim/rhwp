# 수행 계획 — Task M100 #6040

- **재개일**: 2026-09-07 KST
- **현재 브랜치**: `codex/issue-6040-integration-20260910` (원 실험 worktree 보존)
- **재개 기준**: `upstream/devel@56706247f4950286117496c41f5b2c4b1cdbddc5`
- **현재 단계**: 2026-09-10 Stage 4.3을 제외한 통합 후보의 비가시 surface 예산 보정 `931306732`에 대해 사용자 정성 수용 통과. [메모리 게이트](../working/task_m100_6040_memory_gate.md)의 동일 시나리오 2회와 전체 1,643 pass/2 skip, build 통과. 다음은 제출 범위·최종 보고서/PR 초안 정리이며 원격 제출·종료는 별도 승인이다. 전체 native/GPU peak 검증을 주장하지 않는다.
- **현재 결과**: [B1 비교 후보](../working/task_m100_6040_post_stack_stage3_2b.md) `14a306d6c`.
  이후 실제 핀치에서 첫 frame 전 구 큐 실행을 발견해 2026-09-09 dispatch guard와 이미지 취소
  재시도 복구를 보정했다. 전체 1,555 pass/1 skip, 12개 버튼 smoke 통과. 실제 폭 변경의 전역
  갱신 예외와 통제 핀치 비교가 남아 B 전체 수용은 미완료다. staging/새 저화질 preview는 미구현이다.
- **추가 보정 결과**: [읽기 화질 보호](../working/task_m100_6040_post_stack_stage3_2b_reading_quality.md).
  고배율 비편집 visible도 클릭 없이 raw DPR로 복원한다. 전체 1,555 pass/1 skip와 실제 200/211%
  클릭 전후 확인을 마쳤다. 화질 복원 비용 증가를 기록했으며 사용자 체감 수용은 대기한다.
- **승인**: 작업지시자는 Stage 2가 이전 기준선보다 악화되지 않았음을 확인했고, 연속 핀치 계측 →
  기존 화면을 유지하는 페이지별 교체 → 오래된 배율 작업 취소 순서로 진행을 요청했다.
  Stage 3.1 결과 승인 후 제품 교체 경로를 구현한다. push·PR 생성은 별도 승인이다.

## 현재 유효한 재개 계획

2026-09-10 후속 [메모리 게이트](../working/task_m100_6040_memory_gate.md)의 **실패를 보정했다**.
before/after 각 2회에서 500% 최고 소유 surface 관찰값이 268,450,552→402,101,695 pixel로
증가했다. 기존 offscreen Canvas의 retained-transition이 신규 prefetch admission을 우회하는
경로를 확인했다. 사용자 승인 후 회귀부터 보정했고 제품 head는 `931306732`다.
500% 관찰값은 268,450,552로 돌아왔으며 보이는 쪽의 화질은 낮추지 않았다.
이후 클릭 없는 읽기 화질과 줌 직후 스크롤·재확대의 기준선 대비 무회귀 요청에 사용자가
“문제없어”로 응답했다. 해당 범위의 정성 수용을 기록하고 제출 준비로 넘긴다.
이 응답을 원격 제출·merge·이슈 종료 승인으로 해석하지 않는다.

2026-09-10 **로컬 통합 검증 중**: Stage 4.3은 제외하고 Stage 4.2 채택 후보를 최신
`devel@37bd46a72` 위의 `codex/issue-6040-integration-20260910`에 통합했다.
[통합 검증 기록](../working/task_m100_6040_integration_20260910.md)을 현재 진행 상태로 따른다.
전체 1,635 pass/2 skip이며, 종료 조건 보고서의 Firefox·peak 메모리 게이트는 유지한다.
이하 Stage 3/4 기록은 해당 시점의 이력이다. 기존 실험 worktree는 보존한다.

### Stage 4.3 — 동일 physical surface 확정의 제한 실험 계획

2026-09-10 사용자 `진행해줘`에 따라 조사·계획을 작성했다. #6040 본문·댓글 4건을 재확인했고
제품 소스는 바꾸지 않았다. 현재 재현은 같은 key의 354↔500%도 재렌더 1회이며, 300→500%는
scale 자체가 달라 별도다. 여백 꺾쇠가 bitmap에 배율 의존 두께로 그려지고 DOM 그림/callback도
배율을 사용하므로 조건 삭제만으로 최적화하지 않는다.

1. **계약 선행**: 실제 descriptor/dispatch와 guide 수식 재현, paint 순서·완료·소유권 조사.
   이번 단계 완료. [진단 스크립트와 결과](../working/task_m100_6040_post_stack_clamp_reuse_investigation.md)를 남긴다.
2. **승인 후 제한 실험**: Canvas2D의 완료된 active canvas-only surface에서 배율 의존 guide를
   작은 DOM/SVG로 분리해 bitmap을 건드리지 않는 재확정 후보를 검증한다. 첫 진입 raster,
   old contract surface와 미완료/DOM 그림/strict/HF/CanvasKit/LRU fallback을 명시한다.
3. **검증·판정**: 같은 source 조건 A/B, 354↔500과 scale 변경 대조군, 첫 진입·warm·미완료
   재입력을 나눈다. 화질/이미지/좌표/메모리와 실제 Firefox 지연을 확인하고 실익이 없으면 보류한다.
4. **후속 승인**: 제한 실험 결과 뒤 제품 유지·추가 보정·별도 구조 이관 여부를 결정한다.

전체 bitmap 복제, 큰 guide Canvas, 화질 하향, cold metadata 처리, Worker/타일은 포함하지 않는다.
이 계획으로 새 페이지가 처음 나타나는 축소 멈춤까지 해결했다고 약속하지 않는다. 상세 파일 경계와
중단 기준은 [Stage 4.3 구현 계획](task_m100_6040_impl.md#stage-43--고배율-동일-surface-확정의-제한-실험)에 있다.

### 이전 Stage 4.2 사용자 확인

2026-09-10 Stage 4.2 사용자 확인: 클릭 없는 읽기 화질은 통과했다. 300–500%에서 선명해지기
전 재확대와 새 쪽 첫 렌더를 동반한 축소의 멈춤은 잔존한다. 재방문은 나아 보인다는 정성
관찰이며 [판정 기록](../working/task_m100_6040_post_stack_stage4_2.md#사용자-확인-후-판정--2026-09-10)을
남긴다. 버튼 수치만으로 전체 수용하지 않는다. 다음 후보는 동일 physical surface의 구 zoom
확정에 필요한 재래스터를 안전하게 생략할 수 있는지 조사·계획하는 것이며, 아직 구현하지 않는다.

### Stage 4.2 — 줌 visible 렌더 뒤 화면 밖 작업의 시작 경계

- 2026-09-10 계획 `850005bce` 검토 뒤 사용자 `진행해줘`로 **Stage 4.2 구현·검증** 승인을
  받았다. 제품·테스트를 보정하고 집중 78 pass, 전체 1,625 pass/1 skip, build를 통과했다.
  [보고서](../working/task_m100_6040_post_stack_stage4_2.md)에 탐색 27개와 최종 18개 줌 trace,
  스크롤·관찰 비용·품질 상태를 분리했다. 다음 단계 전 Firefox 사용자 확인을 받는다.
- #6040 최신 본문과 댓글 4건을 다시 읽었다. assignee는 `postmelee`, 이슈는 OPEN이다.
  줌 수용 경보를 다루는 보정이며 일반 스크롤의 prefetch/LRU 재설계나 #6821 눈금자 보정으로
  넓히지 않는다. 원격 본문·댓글·PR 변경은 이 단계에 포함하지 않는다.

**목표**: visible 렌더 뒤 화면 밖의 무거운 작업이 연달아 실행되어 첫 frame 기회를 늦추는
경로를 줄인다. 읽는 쪽의 최종 품질을 먼저 보여줄 기회를 만들되, CPU 총량 감소·GPU upload
완료·실제 paint 완료를 이 스케줄 변경만으로 보장하지 않는다.

1. **실행 순서 재현**: frame/timer를 따로 진행하는 host에서 visible 마지막 slice 직후
   0ms retained task가 다음 frame보다 먼저 실행될 수 있음을 재현한다. 테스트가 증명하는 것은
   dispatch 순서이며 브라우저 실제 표시가 아니다.
2. **최소 후보**: zoom-settled가 예약한 visible 작업을 실제 실행한 경우에만, 큐가 빈 뒤
   다음 rAF 경계를 거쳐 화면 밖 deferred 작업을 예약한다. rAF 안에서 무거운 raster를
   직접 실행하지 않는다. 그 이후의 한 task 한 쪽·선택 prefetch idle 정책은 유지한다.
3. **회귀 검사**: 취소·새 zoom·실제 scroll·strict 편집·reset·실패 중 대기가 살아나지 않는지,
   exact hit만 있을 때는 불필요하게 기다리지 않는지 확인한다. 일반 scroll/scroll-settled와
   초기·resize의 정책은 기본값으로 기존 동작을 보존한다.
4. **측정**: 동일 환경의 `76e8496a4` 전후 비교로 이번 변경만 분리하고, 통합 기준선
   `56706247f` 대비 기존 +20% 경보도 별도로 재평가한다. 자동 100→34→50→100% 각 3회
   (첫 회 예열, 본 2회 순서 교대), warm 왕복과 관찰 on/off를 기록한다. 앞 단계 결과를
   새 head의 실측으로 재사용하지 않는다.
5. **사용자 확인·판정**: 자동 gate 뒤 Firefox에서 빠른 확대 재입력과 역방향 핀치, 확대 후
   다른 쪽으로 스크롤·클릭 없이 읽기를 확인한다. 같은 환경에서 고배율 clamp 경계도 비교한다.
   이 단계 보고서와 commit 뒤 남은 통합 매트릭스를 이어갈지 승인받는다.

**판정 기준**:

- 화질 하향·새 placeholder·surface 예산 증액 없이 진행한다. 최종 visible 화질은 기존
  raw 요청과 안전 clamp 계약을 유지한다. 모든 고배율에서 effective DPR 2를 요구하는 것은 아니다.
- known-work 미완료/오류/최종 품질 부족/strict 지연/취소된 작업 재실행은 차단한다.
- visibleStable만 좋아지고 retainedComplete 또는 warm 스크롤이 악화되는 교환을 숨기지 않는다.
  이들 시간의 중앙값 +20% 이상은 기존과 같은 재조사 경보이며, 작은 표본으로 통계적 무회귀를
  확정하지 않는다. main raster 횟수가 스케줄링 변경만으로 늘어나면 원인을 조사한다.
- frame 기회는 실제 compositor 표시가 아니다. 계측의 ready/complete 정의·threshold를
  바꾸어 통과시키지 않으며, 관찰 off와 사용자 Firefox 확인을 별도로 남긴다.
- 대기 동안 구 surface가 유지되는 시간도 길어질 수 있다. actual/target 예약·최종 pixels를
  확인하고 peak RSS/GPU 미측정 상태는 계속 미완료로 둔다. 고배율 clamp 추가 raster의 위험도
  이번 스케줄 보정으로 해결됐다고 간주하지 않는다.
- 백그라운드에서 rAF가 멈출 수 있다. 이 후보는 숨긴 탭의 speculative/retained 작업을
  강제 완료하지 않고 복귀 후 재개·취소 안전성을 검사한다. background 완료가 필수인 경로가
  발견되면 임의 timer 우회를 넣지 말고 설계 범위를 다시 검토한다.

상세 상태·파일 경계는 [구현 계획 Stage 4.2](task_m100_6040_impl.md#stage-42--줌-정착-후-frame-기회-양보-후보)를 따른다.

2026-09-09 [예산 내부 정밀 계측](../working/task_m100_6040_post_stack_budget_detail.md): 사용자
Firefox cold에서 관찰한 584/587ms 예산 갱신을 좁히기 위해 DEV opt-in만 추가했다. 인앱 동일
34%·네 열 smoke의 562.4/564.4ms 중 layer count 조회가 562.2/563.7ms였으며, WASM overlay
조회가 지배적이다. 이는 기준선에도 있는 cold 준비 비용이다. 제품 보정이나 B 전체 수용은 하지
않았으며 순수 planner·LRU를 원인으로 단정했던 가설과 구분한다. 상세 검증·한계는 보고서를 따른다.

2026-09-09 [A/B 준비 보고](../working/task_m100_6040_post_stack_ab_setup.md): 기준선 `56706247f`는
4201, 후보 `0345107c5`는 4202에서 실행한다. 같은 DEV adapter로 cold 자동 기록·warm 수동 기록·
JSON 저장을 제공한다. 파일 hash 대조·20 focused·전체 1,558 pass/1 skip·TypeScript·인앱 기능
smoke를 통과했다. Firefox 수동 표본과 계측 on/off 대조는 아직 없으며 B 완료·성능 판정은 대기한다.

2026-09-09 작업지시자는 회귀 판정과 B의 남은 검증을 #6040에서 먼저 수행하고, 가시 영역/타일
렌더링 구조 실험은 별도 이슈로 분리하는 순서에 동의했다. 새 이슈의 구현은 별도 수행·구현 계획과
Stage 1부터 시작하며 B의 완료로 간주하지 않는다. 현재 후보가 기준선보다 악화됐다면 해당 회귀는
#6040의 차단 조건으로 남긴다. 아직 새 원격 이슈를 생성하지 않았다.

추가 수용 시나리오는 **문서 첫 화면 표시 → 빠르게 4페이지 배치로 축소 → 완성 대기 없이 스크롤**이다.
`exam_kor.hwp`를 우선 재현 대상으로 삼되 실제 배치 모드·최종 배율을 기록하고, 사용자가 말한
4페이지 배치를 임의로 고정 4열과 동일시하지 않는다. 새로 연 문서의 cold 첫 진입, 정착 대기 후
동일 구간 warm 왕복을 구분해 기준선과 후보를 비교한다. 문서·폰트 준비, visible 큐 대기,
쪽별 raster, 브라우저 복사·업로드를 분리하며 빈 visible 영역의 최초 표시/최종 화질 도달 시간과
입력 지연·rAF 간격을 함께 기록한다. 기존 고배율 Firefox 표본을 이 시나리오의 원인 증명으로
재사용하지 않는다. 우선 현재 fast path/페이지별 soft budget/취소·우선순위를 조사하며,
측정 없이 Worker·타일·저화질 선렌더·DPR 하향이나 타이머 교체를 구현하지 않는다.
코드 점검 결과와 블로그 적용 한계는 [조사 보고서 5절](../working/task_m100_6040_post_stack_stage3_2b_firefox_upload.md#5-첫-진입-다중-페이지-스크롤--추가-조사-경계)에 남긴다.

2026-09-09 고배율 재핀치의 [Firefox 업로드 조사](../working/task_m100_6040_post_stack_stage3_2b_firefox_upload.md)를
추가했다. native profile의 약 985/681ms texture upload와 문서 main의 Canvas 복사를 확인했다.
실제 descriptor 실행에서 clamp가 같은 354→500%의 줌 정착은 이미 raster 예약 0건이었다.
읽기 화질은 유지하며 근거 없는 작은 제품 수정을 넣지 않았다. 가시 영역 고해상도 렌더링은
기존 타일링 제외 경계를 넓히므로 **별도 상세 설계 승인 전 제안**이며 아직 구현하지 않는다.

2026-09-09 사용자 지시로 읽기·편집 화질 보호를 B 수용 조건에 추가했다. 스크롤/줌 정착 뒤
visible 쪽은 클릭 없이 raw DPR로 복원한다. 64M 초과 시 1.5/fallback을 허용했던 기존 정책은
이 목표와 충돌하므로 폐기한다. 화면 밖 비용 절감은 유지하되 visible 비용 증가를 명시하고
실문서로 확인한다. 구현·focused/전체 테스트·브라우저 검증 후 사용자 판정을 받는다.

이 절과 [구현 계획의 재개 절](task_m100_6040_impl.md#현재-유효한-재개-설계)이 아래 과거 계획보다
우선한다. 과거 Stage 2·3의 Canvas 전용 좌표 구현을 복원하지 않는다.

### 완료·잔여·제외 범위

| 구분 | 범위 |
| --- | --- |
| 완료 — #6458 | 폭 기반 자동 열 선택, 실제 페이지 수 cap·중앙 정렬, live 열 히스테리시스, resize overlay·눈금자 정합성 |
| 완료 — #6467 / #6637 | surface 예산, visibility 인덱스, page LRU·scheduler, 스크롤 중 기존 화질 유지·정착 승격 |
| 이미 존재 | animation 중 기존 bitmap의 CSS preview와 같은 VirtualScroll을 사용하는 좌표 소비자 |
| 남은 조사·개선 | 매 zoom event의 전체 페이지 geometry 갱신, 열 전환 비용, settled 시 전체 Canvas/layer/cache 해제 |
| 재도입하지 않음 | #6521 저배율 LOD, #6454에서 도입 근거가 부족했던 범용 frame snapshot, 별도 Canvas preview 좌표계 |
| 별도 조사 — [#6821](https://github.com/edwardkim/rhwp/issues/6821) | 가로 스크롤 눈금자 미세 지연과 세로 재현 여부. #6040의 선행 필수 작업이 아니며 기존 지연을 악화시키지 않는 검증은 유지 |

세 PR은 이미 `devel`에 병합됐다. 이후 제출은 이 기준 위의 **별도 일반 PR**이며 기존 native stack을
다시 사용하지 않는다. #6040은 잔여 수용 조건이 있어 열린 상태를 유지한다.

### 불변 계약

- 기존 pointer/중앙 앵커 선택과 감도를 바꾸지 않는다. 편집 커서 쪽으로 강제 줌하지 않는다.
- Canvas·눈금자·캐럿·selection·hit-test는 매 프레임 같은 authoritative geometry를 사용한다.
- #6041/#6042의 DPR, 예산, scroll/cache 정책을 바꾸지 않는다. 재사용 시 수명·취소 계약도 보존한다.
- topology가 유지되는 줌의 정확성부터 확인한다. 자동 열 전환의 앵커는 실제 scroll clamp를 고려하며,
  경계에 막힌 앵커를 강제로 맞추거나 사용자 쪽을 늘 중앙에 고정하지 않는다.
- 성능 이득이 입증되지 않은 리팩터링은 도입하지 않는다.

### 새 단계와 승인 경계

1. **Stage 1.3 — post-stack 기준선**
   - 이슈 본문의 해결/미해결 항목을 정정하고 재개 알림을 게시한다.
   - 현재 코드·이슈 #6454의 조사 결론·기존 줌 테스트를 읽는다.
   - 제품 정책을 변경하지 않는 DEV 관찰로 layout/preview/release/raster 호출과 시간 경계를 기록한다.
   - 4쪽 실문서, `exam_kor.hwp`, `kps-ai.hwp`, 실제 4-layer `KTX.hwp`의 줌 왕복을 확인한다.
     시간 측정과 DOM/스크린샷 확인을 별도 반복으로 분리하고, 정확한 source/WASM/환경을 기록한다.
   - 사용자에게 기준선 서버와 트랙패드·슬라이더·눈금자·클릭 확인 절차를 제공한다.
   - 보고: `mydocs/working/task_m100_6040_stage1_3.md`. 결과 승인·commit 후에만 새 Stage 2로 간다.
2. **새 Stage 2 — geometry/topology의 제한적 비용 보정**
   - 2026-09-07 기준선 승인 후 첫 후보는 animation event당 CSS preview 2회를 1회로 줄이는 것이다.
     `recalcLayout()`의 preview는 유지하고, 그 뒤 scroll 앵커 복원 이후의 동일 호출만 제거한다.
     preview가 scroll 좌표를 읽지 않는 현재 계약을 실제 CanvasView 테스트로 확인한다.
   - 전체 dimensions/layout은 event당 1회를 유지한다. Stage 1.3의 layout 시간만으로 전체 인덱스
     재설계를 정당화하지 않으며, #6821의 눈금자 스케줄링 변경도 포함하지 않는다.
   - Stage 1.3에서 확인된 hotspot만 후보로 삼는다. 안정된 topology의 불필요한 전체 재구축과
     후보/commit 중복을 줄일 수 있는지 먼저 검토한다.
   - 제스처 전체의 열 동결을 미리 확정하지 않는다. 기존 화면 이동을 바꾸는 후보는 별도 설명·승인한다.
   - 공유 authoritative VirtualScroll 계약을 유지할 수 없거나 성능 근거가 약하면 해당 후보를 보류한다.
   - 결과: [새 Stage 2 보고서](../working/task_m100_6040_post_stack_stage2.md). preview 2→1회,
     85 focused·Studio 1,502 pass/1 skip·12개 실문서 전환 통과. 시간 개선은 아직 주장하지 않는다.
3. **새 Stage 3 — 줌 정착 surface 수명과 점진 교체**
   - **3.1 계측 선행**: 핀치 도중과 뗀 직후의 끊김을 분리한다. 기존 패널은 wheel마다 trace를
     시작하므로, 수동 시작/종료하는 bounded 연속 입력 기록을 DEV에 추가한다. 입력 시각, zoom 상태,
     release/raster 경계, rAF 간격을 같은 시간축에 남기고 실제 트랙패드 표본을 요청한다.
   - 입력 간 짧은 정착이 있는지 측정하기 전에는 debounce 시간이나 gesture 종료 규칙을 바꾸지 않는다.
     프로그램 버튼/합성 wheel 검증은 도구 smoke일 뿐 실제 핀치 성능 증명이 아니다.
   - [실제 느린 핀치 결과](../working/task_m100_6040_post_stack_stage3_1.md): 77쪽에서 두 물리
     핀치·354입력에 정착 58회 / main raster 267회. 입력 수렴과 quiet 상태 분리를 상세 설계
     후보로 제안한다. 제품 변경·고정 debounce 값·성능 개선율은 아직 확정하지 않았다.
   - 2026-09-08 빠른·역방향 표본도 수집했다. 역방향은 줌 131건/후속 일반 scroll 38건을 분리했고,
     정착 5회 중 34.47%의 8쪽 동기 처리는 141.5ms였다. 첫 확대는 이전 main raster 종료 577.4ms
     뒤여서 취소 경합을 증명하지 않는다. 추가 수동 녹화를 A 착수 조건으로 삼지 않고 강제 재진입
     테스트를 수행한다. 각 표본의 viewport·입력 차이로 제품 개선율을 계산하지 않는다.
   - **3.2 화면 유지·교체**, **3.3 최신 요청 우선·취소**는 3.1 결과를 검토하고 파일별 설계 승인 후
     구현한다. 제품 처리는 아래 수명 계약을 지키며 각 결과를 별도 보고·commit한다.
   - 2026-09-07 결과 commit `30d777a0e` 뒤의 다음 작업 지시에 따라
     [Stage 3.2 상세 설계](task_m100_6040_impl.md#새-stage-32-상세-설계--반복-정착-억제를-먼저-분리)를 작성했다.
     **3.2A 반복 정착 억제 → 결과 승인 → 3.2B 페이지 교체**로 나눈다. A의 초기 quiet 120ms는
     80/160ms와 비교할 실험 후보이지 확정 제품 상수가 아니다. B의 메모리 부족 시 fallback은 별도 선택이다.
     추가 실제 입력 수집 중에는 서버 source/WASM을 바꾸지 않는다.
   - 줌 정착만으로 모든 활성 Canvas/layer를 지우는 경로를 필요한 페이지 단위 갱신으로 줄인다.
   - 기존 scheduler/LRU와 통합하되 취소·renderer/document epoch, pool/DOM 단일 소유권, 교체 중
     임시 surface 예산을 검증한다. 단순히 `releaseAllRenderedPages()` 호출만 삭제하지 않는다.
   - 현재 visible 우선, 성공한 최신 렌더만 반영하고 실패·취소 때 blank/stale 결과를 노출하지 않는다.
   - 저해상도 선렌더·별도 thumbnail cache는 이번 순서에서 보류한다. 빈 공간 개선을 위해 문서 전체를
     미리 그리거나 기존 cache/DPR 예산을 늘리지 않는다. 아직 렌더하지 않은 페이지의 즉시 표시는
     기존 화면 보존만으로 보장하지 않는다.
4. **새 Stage 4 — 동일 조건 A/B와 제출 판단**
   - 코드·WASM·브라우저·viewport·DPR·fixture를 고정한 before/after 반복 및 사용자 검증.
   - 자동/한 쪽/두 쪽/맞쪽/여러 쪽, 연속 줌·resize·스크롤, Canvas2D/CanvasKit을 검증한다.
   - Studio 전체 test, TypeScript, build와 변경 범위 gate를 수행한다.
   - 유리한 결과뿐 아니라 실패·timeout·회귀·미측정 범위를 보고한다. push·PR은 별도 승인 후 수행한다.

### 측정과 수용 판단

- **결정적 지표**: zoom event, 전체 dimensions/layout 갱신, 전체 release, page raster 호출 수,
  queue·cache·pool/DOM 수명. animation과 settled를 구분한다.
- **환경 의존 지표**: input→preview/visible stable/retained complete, rAF p50/p95/max, long task.
  중첩 wrapper 시간은 합산하지 않으며 관찰 오버헤드와 compositor 측정 부재를 명시한다.
- 새 구현은 기존 baseline보다 좌우·상하 jump, 눈금자/캐럿 지연, 화질·스크롤 회귀가 없어야 한다.
  topology 유지 구간과 scroll clamp 없는 앵커 오차는 각각 1/2 CSS px 기준으로 확인한다.
- 시간 개선의 채택 기준은 동일 조건 반복에서 정한다. 진단용 `--no-opt`와 배포판의 수치를 섞지 않는다.
  기준선만 측정한 단계에서 개선율이나 범용 성능 향상을 주장하지 않는다.

---

## 과거 계획 원문 — 2026-08-30~09-01 (이력, 현재 실행 지시 아님)

아래 branch/stack/승인 순서는 당시 기록이다. 폐기한 Stage 2·3 목표와 위험 완화 문구도 현재 설계로
해석하지 않는다. 현재 작업의 범위와 단계는 위 재개 계획을 따른다.

- **이슈**: [#6040](https://github.com/edwardkim/rhwp/issues/6040)
- **브랜치**: `codex/issue-6040-zoom-topology`
- **PR 기준 commit**: `upstream/devel` `b9d408f0d`
- **최초 구현 기준**: `upstream/devel` `2deb3dd61`
- **작성일**: 2026-08-30 KST
- **절차 상태**: Stage 1·1.1 유지, Stage 1.2 live 자동 열 commit 보정, Stage 2·3 폐기

## 절차 상태와 native stack 경계

- 2026-08-30 작업지시자가 #6040·#6041·#6042를 GitHub native stacked PR로 진행하도록 요청했다.
- #6040 본문에 자동 모드의 실제 점유 묶음 중앙 정렬과 50% gate로 2열을 건너뛰는 현상을 추가하고,
  갱신 알림 댓글을 게시했다. GitHub 직접 첨부만 남은 네 이미지는 본문의 명시적 placeholder로 보존했다.
- 세 이슈의 열린 선점 PR이 없음을 확인하고 GitHub 계정 `postmelee`를 모두 담당자로 지정했다.
- 다른 작업이 있는 원본 worktree는 건드리지 않고 최신 `upstream/devel@2deb3dd61`에서 독립 worktree와
  stack 하단 branch를 만들었다.
- 이 문서는 코드 변경 전 계획이다. 구현은 작업지시자가 수행 계획과 구현 계획을 승인한 뒤 시작한다.
- 2026-08-30 작업지시자가 수행 계획과 구현 계획을 승인하고 Stage 1 진행을 지시했다.
- 2026-08-30 Stage 1 자동 열·실제 점유 중앙 정렬 구현과 focused·Studio 전체 검증을 완료하고 결과
  승인을 기다린다.
- 2026-08-30 작업지시자가 Stage 1 결과를 승인하고 Stage 2 진행을 지시했다.
- 2026-08-30 Stage 2·3 구현 뒤 줌 중 고정된 `VirtualScroll`과 Canvas 전용 CSS preview가 서로 다른
  좌표계를 만들었다. 좌우·상하 jump와 눈금자 지연이 같은 원인에서 파생됐고, 캐럿·선택·hit-test에도
  같은 위험이 있음을 기준 commit부터 재분석했다.
- 2026-08-30 작업지시자 동의 아래 전체 상태를
  `codex/issue-6040-zoom-topology-backup-20260830@d97307cab`에 보존하고 작업 branch를 Stage 1
  `63fa3d0cf`로 재구성했다. Stage 2·3과 후속 회귀 수정은 현재 branch에 포함하지 않는다.
- 2026-08-30 `CanvasView`·`Ruler`·`ViewportManager`·caret/input 경로가 기준 `2deb3dd61`과 동일함을
  확인했다. focused 69건, 전체 Studio 1,246건, TypeScript·build와 실제 Chromium의 눈금자·캐럿·자동
  2→3열 전환을 통과했다. 성능 경로는 단일 공유 좌표 모델의 새 계획 승인 전까지 구현하지 않는다.
- 2026-08-30 작업지시자가 Stage 1 기준 재구성과 기존 줌 재검증 결과를 승인했다. 재구성 문서를 별도
  commit으로 고정한 뒤, 기존 줌 경로를 변경하지 않는 Stage 1.1 눈금자 끝 라벨 처리만 진행한다.
- 2026-08-30 작업지시자가 Stage 1.1을 직접 검증하고 이 구현으로 확정했다. Stage 1과 Stage 1.1만
  #6040의 구현 결과로 유지하며, 폐기한 Stage 2·3 설계는 재개하지 않는다.
- 2026-08-30 PR 준비에서 승인 head를
  `codex/issue-6040-pre-submit-20260830@1d597e81b`에 보존하고 최신
  `upstream/devel@7592e9c99` 위로 네 commit을 재배치했다. range-diff에서 source·test patch는
  동일했고, 오늘할일 충돌은 최신 기록과 M100 섹션을 모두 보존했다.
- rebased code head `049683ee9`에서 TypeScript, Studio 1,278건, production build 243 modules,
  `cargo fmt --all -- --check`와 실제 Chromium 자동 60% 2열·50% 3열·155% 눈금자를 통과했다.
  GitHub `viewerPermission=WRITE`이며 동일 head의 열린 PR은 없다.
- stack trunk는 `devel`이다. #6040은 bottom, #6041은 #6040을 직접 base로 하는 middle, #6042는
  #6041을 직접 base로 하는 top PR이다.
- 각 이슈는 독립 계획 승인과 Stage 결과 승인·commit을 완료한 뒤 다음 branch를 만든다. 세 layer의
  검증이 끝나기 전에는 remote push와 `gh stack submit`을 실행하지 않는다.
- 2026-09-01 maintainer review에서 순수 helper의 `committedColumns`가 실제 `VirtualScroll` 연속 호출에
  전달되지 않는 점을 확인했다. 최신 `upstream/devel@0d1540931`을 merge commit `a19020085`로 통합하고,
  Stage 1.2에서 live commit 소유·reset과 resize 뒤 캐럿/선택 오버레이 재투영을 보정했다.
- Stage 1.2는 기존 settled `releaseAllRenderedPages() → updateVisiblePages()` 경로를 유지한다. PR #6438의
  progressive Canvas 교체에서 지적된 pool/DOM 이중 소유권과 장시간 동기 렌더를 포함하지 않는다.
- 첫 push 직후 `devel`에 #6570 눈금자 resize/paint 변경이 병합돼 PR이 `DIRTY`가 됐다. 최신
  `upstream/devel@b9d408f0d`을 merge commit `db1a15fb1`로 다시 통합하고 오늘 작업 기록 양쪽을 보존했다.
  새 Ruler 실제 동작 harness에는 Stage 1.1의 라벨 경계 판정이 쓰는 `measureText()` fake만 보완했다.

## 목표

현재 branch의 승인된 실행 범위는 아래 1·2·6번이다. 3~5번은 폐기한 Stage 2·3 설계의 목표로서,
눈금자·캐럿·선택·hit-test까지 같은 preview geometry를 소비하는 새 계획을 승인하기 전에는 재개하지 않는다.

1. 자동 모드의 열 후보를 표시 페이지 폭·page gap·편집 영역 폭으로 계산하고 실제 페이지 수로 제한한다.
2. 빈 열이 아니라 실제 점유 페이지 묶음을 편집 영역 가운데에 놓는다.
3. 줌 제스처 중에는 commit된 행·열 토폴로지를 고정하고 기존 화면을 preview로 사용한다.
4. 제스처 정착 시 최종 후보를 한 번 commit하고 기준 페이지의 정규화 앵커를 보존한다.
5. 정착만을 이유로 활성 Canvas를 일괄 제거하지 않고 새 렌더가 준비된 페이지부터 교체한다.
6. 자동 외의 고정 쪽 배치와 기존 그리드 클릭·현재 쪽·행 이동·가로 팬 계약을 보존한다.

## 범위

### 포함

- 자동 열 순수 후보 계산: `viewportWidth`, 표시된 최대 페이지 폭, page gap, page count
- 자동 열 commit 정책과 경계 흔들림 방지
- 실제 점유 열 수 기준 중앙 정렬; 마지막 미완성 행의 기존 시작 열 규칙은 유지
- 줌 제스처 snapshot, preview, 최종 topology commit, 기준 페이지/페이지 내부 앵커 복원
- 줌 정착 시 visible/retained active Canvas의 점진적 교체
- 줌·resize가 동일 후보 계산을 사용하는 회귀 테스트와 진단 계측

### 제외

- `effectiveDpr`, render scale tier, 총 surface pixel/byte 예산: #6041
- 행 인덱스, visibility snapshot 공유, page LRU, eviction, prefetch scheduler: #6042
- 쪽 모양 UI·상태 모델: #6039에서 완료된 계약을 소비만 한다.
- 마지막 미완성 행의 한글 2024 정렬 규칙 변경: 추가 실측 전에는 현재 열 시작 규칙을 유지한다.
- print/PDF/SVG/high-quality export 출력

## 단계와 산출물

1. **Stage 1 — 자동 열·실제 점유 중앙 정렬의 순수 계약**
   - 50% 전역 gate를 제거하고 열 후보를 폭과 page count로 계산한다.
   - 실제 페이지보다 많은 빈 열이 그리드 폭에 포함되지 않게 한다.
   - fixed arrangement와 가로 쪽 이동이 영향받지 않는 focused test를 추가한다.
   - 완료 기록: `mydocs/working/task_m100_6040_stage1.md`
2. **Stage 1.1 — 수평 눈금자 끝 라벨 경계 처리**
   - Stage 1 재구성의 수동 검증에서 확인한 기존 표시 결함만 다룬다.
   - 종이 끝 경계선과 tick은 유지하고, 가운데 정렬된 숫자의 실제 폭이 종이 범위를 넘으면 라벨만 숨긴다.
   - 줌·`VirtualScroll`·Canvas 배치 경로는 변경하지 않는다.
   - 완료 기록: `mydocs/working/task_m100_6040_stage1_1.md`
3. **Stage 1.2 — live 자동 열 commit과 overlay 재투영**
   - `VirtualScroll`이 이전 auto 열 commit을 소유하고 연속 zoom/resize 후보에 전달한다.
   - horizontal·고정 배치·문서 교체에서 이전 auto commit을 지운다.
   - resize 레이아웃 확정 뒤 캐럿·선택 overlay를 같은 authoritative 좌표에 다시 놓는다.
   - 기존 settled Canvas 반환·재할당 경로는 유지하고 actual CanvasView/CanvasPool test로 단일 소유권을
     검증한다.
   - 완료 기록: `mydocs/working/task_m100_6040_stage1_2.md`
4. **Stage 2 — 줌 preview와 topology commit 분리**
   - **폐기**: `VirtualScroll`을 고정하고 Canvas만 별도 CSS 좌표로 움직이는 구현은 기존 화면 요소의
     단일 좌표 계약을 깨므로 현재 branch에서 제거했다.
   - 제스처 시작 snapshot과 정착 commit 상태를 CanvasView에 도입한다.
   - 애니메이션 프레임에서는 전체 `VirtualScroll` 재계산과 최종 품질 render를 하지 않는다.
   - 최종 frame에서 레이아웃을 한 번 계산하고 기준 페이지의 정규화 앵커를 복원한다.
   - 완료 기록: `mydocs/working/task_m100_6040_stage2.md`
5. **Stage 3 — 활성 Canvas 점진적 교체와 계측**
   - **폐기**: Stage 2 좌표 모델에 의존한 구현을 현재 branch에서 제거했다.
   - 줌 정착 시 `releaseAllRenderedPages()`를 호출하는 경로를 페이지 단위 교체로 바꾼다.
   - visible 우선으로 새 결과를 준비하고 성공한 페이지만 교체하며 실패·취소 시 기존 결과를 유지한다.
   - #6042의 LRU·scheduler를 선행 구현하지 않고 현재 active working set 안에서만 동작한다.
   - 완료 기록: `mydocs/working/task_m100_6040_stage3.md`
6. **Stage 4 — 통합 회귀·실제 브라우저 검증**
   - **종료**: 폐기된 Stage 2·3의 통합 검증 대신 Stage 1 기준 기존 줌 동작을 재검증했다.
   - 자동 1/2/3쪽 이상, 51%→50%, 27%·17%, 줌 왕복·resize·고정 배치 매트릭스를 검증한다.
   - TypeScript·Studio 전체 test·build와 실제 Chrome 계측을 수행한다.
   - 최종 보고서: `mydocs/report/task_m100_6040_report.md`

각 Stage는 focused 검증과 working 보고 뒤 작업지시자의 결과 승인을 받고 commit한 다음에만 다음
Stage로 이동한다.

## 검증 계획

- `node --test`로 자동 열·중앙 정렬·CanvasView 줌 preview/commit·기존 앵커 회귀 focused test 실행
- `(cd rhwp-studio && npx tsc --noEmit)`
- `npm --prefix rhwp-studio test`
- `npm --prefix rhwp-studio run build`
- 실제 Chrome에서 A4 3쪽·6쪽 문서의 자동 51%/50%/27%/17%, 줌 인/아웃 왕복, resize 검증
- 실제 브라우저 진단값으로 제스처당 layout commit·전체 release·page render 호출 수와 앵커 오차 기록
- `cargo fmt --all -- --check`
- `git diff --check`

Studio 화면 전용 TypeScript 변경이므로 Rust source/test가 바뀌지 않는 한 Rust Clippy 묶음과 Native
Skia·WASM 전체 빌드는 기본 범위에서 제외한다. 렌더 엔진 출력은 바꾸지 않으므로 PDF/SVG sweep 대신
실제 browser 기능·성능 검증을 권위 근거로 사용한다.

## 위험과 완화

| 위험 | 완화 |
| --- | --- |
| 50% gate 제거로 핀치 중 열 수가 더 자주 변함 | 후보 계산과 실제 commit을 분리하고 제스처 정착 시 한 번만 commit한다. |
| 페이지 수 cap이 fixed/facing의 의도적 빈 슬롯까지 없앰 | cap과 실제 점유 중앙 정렬은 `auto`에만 적용하고 fixed topology 회귀 테스트를 둔다. |
| 미완성 마지막 행 정렬이 함께 바뀜 | 첫 행 전체가 실제 page count보다 작은 경우만 문제를 닫고 마지막 미완성 행의 열 시작 규칙은 유지한다. |
| preview 중 클릭·캐럿 좌표가 commit 좌표와 섞임 | VirtualScroll 좌표는 제스처 동안 commit snapshot에 고정하고 정착 후 한 번에 교체한다. |
| 기존 Canvas 교체 중 화면이 비거나 stale 작업이 덮어씀 | render epoch를 확인해 성공한 최신 결과만 원자 교체하고 실패·취소 시 기존 Canvas를 유지한다. |
| #6041·#6042 기능이 #6040에 섞임 | 해상도 선택과 캐시/scheduler는 hook·계측 경계만 두고 정책은 후속 layer로 남긴다. |
| public preview native stack 동작 변화 | 제출 직전 공식 문서·`gh stack view`·branch base를 재확인하고 세 PR을 bottom-up으로 병합한다. |

## 승인 게이트

1. 작업지시자가 이 수행 계획과 구현 계획을 승인한다.
2. 승인 뒤 Stage 1을 구현·검증하고 결과 승인을 기다린다.
3. Stage 1 승인·commit 뒤 Stage 2, 같은 방식으로 Stage 3과 Stage 4를 진행한다.
4. #6040 최종 결과 승인·commit 뒤에만 #6041 branch와 별도 계획을 만든다.
5. #6041 최종 결과 승인·commit 뒤에만 #6042 branch와 별도 계획을 만든다.
6. 세 branch의 전체 검증과 PR 초안 검토 뒤 remote push·`gh stack submit` 승인을 별도로 받는다.
7. 게시된 stack은 #6040→#6041→#6042 순서로 review·merge하고 각 하단 merge 후 상단의 재base CI를
   다시 확인한다.
