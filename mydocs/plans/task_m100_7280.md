# Task #7280 — 조판 책임 분리와 기여자용 규칙 관리 수행계획

- Issue: [#7280](https://github.com/edwardkim/rhwp/issues/7280)
- 작성일: 2026-09-20
- 상태: 수행계획 승인, 1단계 기준 조사 착수. 제품 코드 구현 미착수.
- 승인 범위: 2026-09-20 사용자의 “7280 이슈를 시작합니다”에 따라 기준 조사를 진행한다.
- 기준: `upstream/devel` `722fb38af361ed3508aef7ca0ac3a8fdc5d3c0db`
- 작업 브랜치: `task_m100_7280`
- 담당: `edwardkim`

## 1. 목적과 경계

기여자가 조판 기능을 구현·수정할 때 담당 모듈, 보존할 계약, 검증할 규칙을 명확히
찾을 수 있도록 구조를 정비한다. 파일 크기나 복잡도 수치 감소만으로 완료하지 않는다.

`typeset.rs`를 중심으로 규칙 판단과 상태 변경을 분리하고, 측정·페이지 분할·배치가
소비하는 공통 결과의 소유권을 명시한다. 실제 호출 관계를 조사한 뒤 추출 경계를 확정한다.
우선은 동작 보존 리팩토링이며, 발견한 규칙 오류 수정은 구조 이동과 분리한다.

관련 #7090·#7195의 증거와 승인 이력은 참고하되, #7195와 PR #7222의 미병합 코드를
기준으로 삼거나 자동 이식하지 않는다. #6976의 넘침 불변식 제안과도 범위를 구분한다.
전체 엔진 재작성이나 범용 동적 rule engine 도입은 목표가 아니다.

## 2. 기준 자료

- [복잡도 대시보드](../manual/dashboard.md)
- [기존 표 조판 규칙 정본](../tech/table_layout_rules.md)
- [시각 판정 거버넌스](../manual/verification/visual_verification_governance.md)
- [문서·Git 절차](../manual/codex/docs_and_git_workflow.md)
- 로컬 이전 계측: `output/complexity-20260920/report.md`, `metrics.json`

이전 계측은 `18a9fa85e955c220e5eb4d0143dc918a4de6be73`의 native 기본 feature 결과다.
`typeset.rs` 31,968줄, `typeset_section_with_variant` CC 186 등은 참고치이며,
이번 기준 SHA의 재계측·회귀 통과를 의미하지 않는다.

## 3. 수행 순서와 산출물

| 단계 | 수행 내용 | 산출물·종료 조건 |
| --- | --- | --- |
| 1. 기준 조사 | 고정 SHA의 함수·상태·호출 경로와 기존 테스트를 조사하고 복잡도 및 CI 기준 회귀를 확인 | 기준 SHA·환경·명령·결과, 기존 실패 분류, 수정 책임 지도 |
| 2. 구현 설계 | 문단 흐름, 저장 줄 정보, 표 분할·이어 배치, 부동 개체, 각주·미주와 페이지 상태의 경계를 설계 | 구체적 모듈·입출력 계약·의존 방향·추출 순서의 구현계획, 작업지시자 승인 |
| 3. 구조 분리 | 작은 단위로 기존 동작을 보존하며 추출. 공개 API 변경과 불필요한 호출 경로 변경을 피함 | 단위별 전후 비교, 계약 테스트, 단계별 기록·커밋 |
| 4. 규칙 관리 | 의미 기반 규칙 식별자와 근거·조건·반례·구현·소비자·테스트를 연결 | 기존 규칙 정본 정비, 기여자 변경 안내, 누락·중복 판단 경로 목록 |
| 5. 통합 검증 | 정확한 변경 SHA에서 적용 lint·회귀 및 native/WASM 시각 검증 | 결과보고, 미검증·잔여 위험, 후속 PR 준비 |

단계 전환 때 현재 단계의 변경을 커밋하고 다음 단계 문서를 시작한다.
이번 수행계획 승인 후 1단계를 진행하며, 제품 코드 수정은 2단계 구현계획 승인 이후다.
규칙 의미 변경이 필요하면 원인·독립 근거·반례·영향 범위를 별도로 제시한다.

## 4. 설계 원칙

- 규칙 판단은 명시적 입력과 결과를 갖고, 페이지 커서·소비 상태의 변경과 구분한다.
- 측정과 배치는 같은 줄 구성·메트릭·분할 결과를 소비한다. 기존 공통 결과를 우선 재사용한다.
- 문서 ID나 우연한 수치 조합으로 예외를 만들지 않는다. 불명확한 기존 예외는 확인 필요로 기록한다.
- `typeset.rs`의 역할은 실행 순서 조정으로 좁히되, 파일 이동만으로 책임 분리가 되었다고 보지 않는다.
- 기존 기술 문서를 정본으로 유지하고 기여자 안내에서 연결한다. 중복 규칙 정본을 만들지 않는다.
- 기존 결함을 보존하는 구조 이동과 올바른 동작으로 승인하는 판정은 구분한다.

## 5. 검증 계약

1. 동일한 입력·설정·환경에서 고정 baseline과 변경본을 비교한다. 서로 다른 브랜치의 테스트를
   교차 실행한 결과는 별도 진단으로 표기하며 기본 CI 결과와 섞지 않는다.
2. 기존 실패, 검출 강화로 발견한 기존 결함, 변경으로 생긴 실제 회귀를 구분한다.
3. 줄 구성·점유 높이·여백·분할 경계·잔여 내용 소유권에 대한 계약을 보호한다. 영향을 받는
   규칙의 적용 사례와 반례를 확인하고 합성 테스트를 실제 한컴 증거로 격상하지 않는다.
4. 구조 이동은 출력 보존이 기본이다. 페이지 수 외에도 표 외곽, 빈 줄, 후속 문단 위치,
   누락·중복·겹침과 줄바꿈을 확인한다. 애매한 한컴 차이는 메인테이너 시각 판정에 맡긴다.
5. 실패 은폐 목적의 baseline/golden/ignore 완화는 하지 않는다. 필요한 계약 수정은 별도 근거와 승인을 남긴다.
6. 소스 변경 후 적용 범위에 따라 native·WASM·workspace lint 및 회귀를 수행한다.
   최신 WASM과 native/SVG 증거를 구분하고, 과거 빌드의 판정을 새 SHA의 판정으로 재사용하지 않는다.

## 6. 등록·준비 기록

- 2026-09-20: 열린 이슈를 `typeset`, `리팩토링`, `refactor`로 검색하고 열린 PR을 확인했다.
- 관련 #6976은 넘침 검출·불변식 제안으로 목적이 달라 별도 이슈 #7280을 등록하며 참조했다.
- 사용자 요청에 따라 local `gh`로 이슈를 생성했다. assignee `edwardkim`, labels `enhancement`, `layout`.
- 게시 후 이슈 본문·상태·담당자를 다시 조회하여 확인했다.
- 원격 devel을 fetch하여 위 SHA에서 `task_m100_7280`을 생성했다. 기존 작업 브랜치는 보존했다.
- 제품 코드·테스트·기준값 변경, 빌드·회귀 실행, commit·push·PR 생성은 하지 않았다.
- 이후 PR 검토·제출 단계에서는 해당 역할별 절차를 다시 선택한다. 원격 push·PR 생성은 별도 승인 대상이다.
- 2026-09-20 착수: 동기화된 devel과 작업 브랜치가 같은 기준 SHA임을 확인하고 작업 브랜치로 전환했다.
  이슈는 OPEN, 담당 edwardkim이며 #7280 관련 열린 PR은 없다. 수행계획을 기록한 뒤 1단계에 진입한다.
