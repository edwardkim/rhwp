# Task #704 수행 계획서

## 이슈

- 이슈 번호: #704
- 제목: 통합재정통계 페이지 분할 — TopAndBottom TAC 1×1 wrapper + 각주 환경에서 trailing 빈 paragraph 가 0.84 px 부족으로 다음 페이지로 밀림
- 마일스톤: v1.0.0 (M100)

## 결함 요약 (이슈 등록 시점)

`samples/통합재정통계(2010.11월).hwp`, `samples/통합재정통계(2011.10월).hwp` 두 파일이 한글2022 PDF 기준 1 페이지인데 SVG 2 페이지로 분할. trailing pi=14 (빈 paragraph) 이 page 2 로 밀림. 0.84 px borderline.

추정 정정 방향(이슈 본문):
- 옵션 B: `typeset.rs:1107` fit 검사 epsilon 보강 (`remaining + 0.5 < first_line_h`)
- 대안: 누적 drift 5.9 px source 추적 후 본질 정정

## 현재 상태 (재진단 — 2026-05-13)

이슈 등록 이후 다른 task 에서 **본질적 정정이 이미 진입**한 것을 확인.

### Task #676 (커밋 `52ae6558`, PR #679, merged `bd3b63dd`)

`src/renderer/typeset.rs:1182-1199` 에 **trailing empty paragraph 가드** 가 추가됨:

```rust
if is_last_in_section
    && st.col_count == 1
    && !st.current_items.is_empty()
{
    let trimmed = para.text.replace(|c: char| c.is_control(), "");
    let is_empty_para = trimmed.trim().is_empty() && para.controls.is_empty();
    if is_empty_para {
        let total_h = st.current_height + fmt.height_for_fit;
        let fit_fail_within_safety =
            total_h > available && total_h <= available + LAYOUT_DRIFT_SAFETY_PX;
        if fit_fail_within_safety {
            st.current_items.push(PageItem::FullParagraph { para_index: para_idx });
            // height=0 흡수
        }
    }
}
```

- 시멘틱: 섹션 마지막 빈 paragraph 가 `LAYOUT_DRIFT_SAFETY_PX` 영역 내 미세 overflow 인 경우 height=0 으로 흡수 → 단독 빈 페이지 차단.
- 이슈 #704 추정 정정 방향 "옵션 B (epsilon 보강)" 과 **동일 시멘틱**.
- 대상 케이스가 정확히 통합재정통계 2010.11/2011.10 임 (Task #676 보고서 + 코드 주석).
- `tests/issue_676_trailing_empty_para.rs` 에 3 케이스 GREEN 테스트 등록:
  - `issue_676_t재정통계_2010_11_single_page` — `assert_eq!(page_count, 1)`
  - `issue_676_t재정통계_2011_10_single_page` — `assert_eq!(page_count, 1)`
  - `issue_676_t재정통계_2014_08_no_regression` — `assert_eq!(page_count, 1)` (무회귀)

### 잔존 부정합

`tests/issue_703.rs` 의 동일 케이스 테스트 2 개는 Task #703 분리 시점에 `#[ignore = "Issue #704 ..."]` 처리된 채 남아있음:

- `issue_703_tonghap_2010_11_single_page` (line 43-47) — ignored
- `issue_703_tonghap_2011_10_single_page` (line 49-54) — ignored

`cargo test --test issue_703 -- --ignored` 실행 결과:
```
running 2 tests
test issue_703_tonghap_2010_11_single_page ... ok
test issue_703_tonghap_2011_10_single_page ... ok
test result: ok. 2 passed; 0 failed
```

→ **이미 GREEN** 인데 ignore 가 풀리지 않은 상태.

## 수용 기준

1. `tests/issue_703.rs` 의 통합재정통계 ignore 2 건 제거 — 정식 테스트로 승격
2. `tests/issue_676_trailing_empty_para.rs` 의 동일 케이스 GREEN 보존 (회귀 0)
3. `cargo test --release` 전체 회귀 0 (1250+ 테스트)
4. `cargo clippy --all-targets -- -D warnings` 클린
5. Issue #704 close + 본질 정정 reference (#676/PR#679) 명시

## 처리 방침

- **신규 소스 가드 추가 불필요** — Task #676 가드가 이미 본질 정정.
- **테스트 cleanup 만 수행** — `tests/issue_703.rs` 의 `#[ignore]` 2 건 제거.
- `tests/issue_676_trailing_empty_para.rs` 와 시멘틱 중복이지만 **#703 의 분리 등록 컨텍스트 보존** 목적으로 issue_703.rs 의 테스트는 유지 (시멘틱 회귀 시 양쪽 모두 RED 가 되어 디버깅에 유리).

## 진행 절차

1. **Stage 1 (현황 검증)** — `cargo test --test issue_703 -- --ignored` 결과 캡처 + `cargo test --test issue_676_trailing_empty_para` GREEN 재확인 + 본 계획서의 진단 보강.
2. **Stage 2 (cleanup)** — `tests/issue_703.rs` 의 `#[ignore]` 2 건 제거. 주석은 "Task #676 (PR #679) 으로 해결" 로 갱신.
3. **Stage 3 (회귀 검증 + 보고서)** — `cargo test --release` 전체 + `cargo clippy --all-targets -- -D warnings` + 최종 보고서 작성. devel merge 후 `gh issue close #704`.

## 위험 / 한계

- 본 task 는 소스 코드 변경 없음 (테스트 ignore 해제만). 위험 거의 0.
- Task #676 가드는 `col_count == 1 && is_last_in_section && empty_para` 한정. 다단/중간 paragraph 영역에서 동일 패턴 발생 시 추가 케이스 필요할 수 있으나 본 task 범위 외.

## 권위 자료

- `pdf/통합재정통계(2010.11월)-2022.pdf` / `pdf/통합재정통계(2011.10월)-2022.pdf` (한컴 2022)
- Task #676 보고서 (PR #679 merged commit `bd3b63dd`)

## 관련 이슈 / PR

- Issue #676 — 본질 정정 (closed)
- PR #679 — Task #676 merge
- Issue #703 — 분석 모태
- Issue #775 — Task #703 회귀 정정 (clear)
