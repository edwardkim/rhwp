# Task #7056 수행 계획 — 조판 공통 지침과 PR review 준수 검증

- Issue: [#7056](https://github.com/edwardkim/rhwp/issues/7056)
- 근거: [PR #7040 메인테이너 검토](https://github.com/edwardkim/rhwp/pull/7040#issuecomment-5637631535)
- 기준: `upstream/devel` `8a06c99b91916b6c01756ff1bfdebe4d40dca0b1`
- 작업 branch: `docs/7056-typesetting-review-guidance`
- 사용자 지시: 이슈 등록 후 개선, PR review에서도 같은 지침의 준수 여부 확인.

## 목표와 범위

Claude와 Codex가 조판 규칙·측정과 배치의 공통 결과·독립적인 검증 근거를 동일하게 따르도록
`AGENTS.md`를 보완한다. PR review에서는 정확한 head의 코드와 증거를 대조해 원칙별 준수 여부,
미검증 범위, 보류 사유와 해제 조건을 기록한다.

수정 대상은 `AGENTS.md`, `CLAUDE.md`, PR review router, 접수·리뷰 기록, 시각·fixture 증적,
로컬 검증의 연결 문구와 이 작업 기록이다. #7040의 구현·재심사, Rust·fixture·baseline·CI workflow
변경, 실제 렌더링 검증은 포함하지 않는다. 에이전트 준수를 자동 보장했다고 주장하지 않는다.

## 수행 순서와 승인 범위

1. 동일 이슈·열린 PR 검색 후 #7056을 등록하고 `jangster77`을 assignee로 지정했다.
2. 기존 dirty checkout을 보존하고 최신 `upstream/devel`에서 별도 worktree를 만들었다.
3. 이 계획과 [파일별 구현 계획](task_m100_7056_impl.md)을 커밋한 뒤 문서 개선을 진행한다.
4. 변경 문서 공백·링크·메타데이터와 원칙별 연결·기존 규칙 보존을 검증한다.
5. 단계·최종 결과를 기록하고 로컬 커밋 및 PR 본문 초안을 준비한다.

현재 지시는 이슈 등록·문서 개선·필요한 로컬 검증을 승인한다. 이 범위의 재확인을 반복하지 않는다.
remote push·PR 생성·merge·issue close는 현재 미승인이다. collaborator self 경로에 따라 오늘할일과
번호 기반 PR review 문서는 PR 생성 단계에서 작성하며 현재 checkout의 기존 오늘할일은 수정하지 않는다.

## 완료 기준

- 공통 원칙의 정본이 한곳에 있고 Claude import와 Codex 진입 경로가 유지된다.
- 새 분기 근거, 줄 구성·메트릭 공유, 반례, 합성/한컴 증거 구분, 기준값 변경 근거가 연결된다.
- 모든 정식 PR review가 실제 diff로 적용 여부를 먼저 판단하며 비해당 이유도 남긴다.
- 적용 대상은 원칙별 `충족/미충족/미검증/비해당`과 증거를 기록한다.
- 미충족·필수 증거 누락을 CI나 원본 샘플의 개선만으로 승인하지 않는다.
- 문서 검증을 완료하고 제품 동작·시각 품질의 미실행 범위를 보고한다.
