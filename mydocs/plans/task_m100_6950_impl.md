# #6950 구현계획 — 문단 텍스트·표의 결정론적 배치 계약

- Issue: [#6950](https://github.com/edwardkim/rhwp/issues/6950)
- 작성일: 2026-09-09
- 상위: [수행계획](task_m100_6950.md)
- 근거: [Stage 1 원인 계보·반증](../working/task_m100_6950_stage1.md)
- 제품 기준 SHA: `13c92feb67d2bf0ae62349f41c5f5cd83845a4a5`
- 상태: **Stage 3 회귀 정정 수정계획 승인 완료 — 정정 A 착수**. 기존 Stage 2 계획은2026-09-09 승인받아
  구현했으나 영향 검증에서 회귀가 발견됐다. 현재 수정 대상·실행 순서는 아래5절을 따른다.
  #6950 안에서 관측 문제를 해결하는 범위는 메인테이너가 확정했다.

### 2026-09-10 메인테이너 확정 사항

문단 마지막 글자 뒤의 남은 너비에 표가 들어가지 않아 다음 줄에 배치하는 대상 동작은
**시각 판정 통과**다. 아래 계획의 일반 계약을 작은 용지 높이의 모든 표 분할 정책 구현으로
확대하지 않는다. 용지 높이를 인위적으로 줄인 합성 사례는 분할 관련 속성과 기대 동작에 대한
근거가 불충분하므로 실패 판정을 철회하고 이번 범위·완료 차단 항목에서 제외한다.
이 사례의 추가 수정이나 대체 실험은 하지 않는다. 기존 정상 분할 경로의 보호 검증과
Stage 3 필수 검증은 유지한다. 상세 결정은 [Stage 2 기록](../working/task_m100_6950_stage2.md) §9를 따른다.

## 1. 목표와 선택하지 않는 방법

문단 내 텍스트와 표를 **원본 컨트롤 순서·앵커·본문배치 속성·가용 영역에 따라 결정**하고,
확정한 동일 배치 결과를 공간 예약과 실제 출력이 소비하도록 한다. 이번 샘플의 겹침만 없애는
좌표 보정은 완료 기준이 아니다. 같은 의미의 HWP/HWPX 입력에는 같은 공통 IR 배치 규칙을 적용한다.
같은 입력·폰트/측정 결과·조판 설정에 대해 같은 결과가 나오는 것이 여기서 말하는 결정론이다.
이는 모든 한컴 버전의 출력이 동일하다는 주장과 다르다.

기존 TAC 전용 제한의 **단순 삭제**는 #6718을 악화시키므로 채택하지 않는다. 그러나 이 조건문
자체를 영구 보존하는 것도 목표가 아니다. 일반 규칙으로 역할이 대체되면 제한을 교체·제거한다.
보호 대상은 기존 정상 조판이지 과거 분기문이나 테스트의 절대 좌표 그 자체가 아니다.
호스트 저장 줄의 증가 여부만으로 사례를 구분하지 않으며, 형식/한컴 버전, 문서명, 문단 번호,
샘플 좌표를 정책 키로 삼지 않는다. 이번 문제 표를 TAC로 바꾸거나 원본 LineSeg를 수정하지 않는다.

### 1.1 확정할 조판 계약

다음은 구현·검증할 계약이며, 이미 구현됐다는 설명이나 공개 스펙의 직접 인용이 아니다.
속성의 기본 의미는 [표 배치 규칙](../tech/table_layout_rules.md), 저장 줄의 의미와 source 경계는
[Document IR LineSeg 표준](../tech/document_ir_lineseg_standard.md)을 따른다.

1. **논리 순서**: 문단의 텍스트 위치와 컨트롤 앵커를 동일 문자 축으로 대응한다. control 배열
   순서나 이미 그려진 노드 순서만으로 문단 내 배치 순서를 추정하지 않는다.
2. **배치 방식**: TAC는 텍스트와 같은 줄 흐름에서 여백 포함 상자의 가용 폭을 판단한다.
   들어가지 않으면 현재 줄을 닫고 다음 가용 줄로 진행한다. 비-TAC의 TopAndBottom은 위치 속성으로
   정한 점유 구간에서 본문을 위/아래로 흐르게 하며, 단순 인라인 폭 비교로 치환하지 않는다.
   Square의 좌우 어울림, 글앞/글뒤의 겹침 의미도 혼동하지 않는다.
3. **원점**: Paper/Page/Column/Para의 기준, 앵커가 속한 실제 줄과 쪽/단의 조각을 명시적으로
   구분한다. 현재 흐름의 y를 문단 첫 줄 또는 앵커 줄이라고 암묵적으로 간주하지 않는다.
4. **저장 줄과 재조판**: 현재 문단/조각에 유효한 저장 LineSeg인지 먼저 판정한다. 유효한 source
   좌표는 그 좌표계에서 해석하고, 없거나 편집으로 무효가 된 경우 계산된 줄 배치를 사용한다.
   저장 좌표와 재조판 높이를 근거 없이 더하지 않는다. 파일 확장자·버전으로 경로를 선택하지 않는다.
5. **공통 배치 결과**: 앵커 기준·표 상자·점유 구간·다음 흐름 위치를 한 결정의 결과로 묶는다.
   typeset이 예약한 뒤 layout이 다른 원점에서 다시 보정하지 않도록 결과를 전달한다.
   ‘같은 helper를 호출한다’만으로 서로 다른 입력 원점의 불일치를 해결했다고 인정하지 않는다.
6. **쪽 경계**: 현재 영역에 배치할 수 없으면 문서의 표 나눔 정책과 기존 페이지 조판 계약에 따라
   분할/이월한다. 겹침을 감추기 위해 표를 쪽 안에 강제로 끌어올리지 않는다. 탐색은 줄/영역의
   유한한 경계를 따라 전진하고 배치 불가능한 크기에서 같은 위치를 무한 재시도하지 않는다.

이번 샘플의 ‘본문 아래 표’는 이 계약에서 도출되어야 한다. `87.36px` 추가 또는 `603.8px` 배치를
성공 규칙으로 사용하지 않는다. #6718을 지키기 위한 continuation 전용 제외 분기도 자동 채택하지 않는다.

### 1.2 구현 책임 경계

- 파서는 구조·속성을 공통 IR로 보존한다. renderer에 포맷별 임시 판별을 추가하지 않는다.
- 배치 결정은 측정된 줄/표와 현재 frame을 입력으로 받으며 RenderNode를 읽어 역산하지 않는다.
- 공간 예약과 렌더링은 확정 결과의 소비자다. 좌표계 변환은 명시하되 배치 판단을 반복하지 않는다.
- 기존 `ObjectPlacementFrame`, `InlineFlowPlan`의 원점·확정 결과 전달 구조를 우선 재사용한다.
  현재 `InlineFlowPlan::supports`는 비-TAC 표를 지원하지 않으므로 이번 표를 억지로 TAC 경로에
  통과시키지 않는다. 필요한 일반 표 배치 결과는 해당 책임 경계에서 연결한다.
- 문단·표의 계약상 필요한 결과 전달을 ‘최소 줄 수’를 이유로 생략하지 않는다. 반대로 폰트 엔진·
  전체 IR·관련 없는 모든 renderer 경로의 일괄 재작성으로 확대하지 않는다.

## 2. Stage 2의 실행 순서

### A. 좌표 기준 확인 — 이번 구현의 선행 조건

새 전수 계측 대신 이번 fixture와 #6718, #6879 세 건에서 다음 값만 확인한다.

- 원본 첫 줄·앵커 줄, 현재 쪽 원점 및 continuation 시작 줄.
- typeset의 `para_start_height`, 현재 흐름 높이와 예약한 표 상하단.
- layout의 `para_start_y`, `para_y_for_table`, 실제 호스트 줄과 표 상하단.
- 위 기준값이 어느 PageItem 처리에서 설정·재설정되는지.

기존 진단 수단을 먼저 쓰며, 부족한 값만 임시 진단으로 기록한다. 진단 출력을 제품의 무조건 로그로
남기지 않는다. #6718에서 빈 호스트 전용 스냅이 실행됐다는 과거 설명은 전제로 삼지 않는다.

이 기록은 사례별 제외 조건을 찾는 작업이 아니다. **현재 원점이 어떤 논리 줄/물리 조각을
가리키는지 밝혀 앵커와 점유 영역을 직접 결정**하기 위한 것이다. 이미 반영된 거리를 다시 더하지
않는 것은 그 결과의 불변식이다. 단순 `max()`나 임의 간격 보정으로 대체하지 않는다.
기준점 대응은 실제 입력과 조판 계약 테스트로 검증한 후 코드에 고정한다.

이 확인으로 일반 규칙을 도출할 수 없거나 PageItem/IR의 광범위한 구조 변경이 필요하면,
샘플 예외를 추가하지 않고 발견 내용과 수정 계획을 먼저 보고한다.

### B. 조판 계약 구현과 회귀 테스트

| 대상 | 계획 |
| --- | --- |
| `src/renderer/float_placement.rs` 및 필요한 배치 결과 전달 구조 | 앵커 기준과 점유 영역을 명시한 공통 결정. 기존 frame·결과 전달 구조 재사용 우선 |
| `src/renderer/layout.rs` | 확정한 표 배치 결과를 소비. 재해석·중복 보정 제거. TAC·분할의 올바른 동작 보호 |
| `src/renderer/typeset.rs` | 줄·표의 배치 결정과 공간 예약을 연결하고 결과 전달. 실제 표만 내리는 수정 금지 |
| `tests/cases/issue_6950_paragraph_end_topbottom_anchor.rs` (신규 예정) | 실제 fixture로 본문 끝-표-후속 본문 순서, 표 개수·내용 보존, 쪽 넘침을 검사 |
| `mydocs/working/task_m100_6950_stage2.md` | A의 기준점 기록, 수정 전 실패/후 성공, 보호 사례 결과와 한계를 정리 |

기존 앵커 helper의 의미를 공통 배치 결정과 정합시킨다. renderer 전체 리팩토링이나 관련 없는
개체 배치 경로의 이동은 하지 않는다. 원본 fixture와 PDF는 이미 확보했으므로 추가 생성하지 않는다.

신규 테스트는 `603.8px`를 한컴 정답 좌표라고 고정하는 방식이 아니라 실제 텍스트와 표의
비겹침·순서를 먼저 검증한다. 쪽수 변화는 무조건 승인하지 않고 후속 배치와 정답 PDF로 확인한다.
원본에서 읽은 위치의 설명용 계산과 테스트가 보호할 의미를 구분한다.

추가로 조판 계약의 경계를 테스트한다: 앵커가 첫 줄/뒤 줄에 있는 경우, 가용 폭의 안/밖,
원점 이동, 같은 문단의 여러 컨트롤 순서, continuation, 유효한 저장 줄/계산된 줄의 구분이다.
직접 구성한 IR의 테스트는 알고리즘 불변식 검증으로 명시하고, 한컴에서 정상 저장된 문서나
독립 정답지라고 주장하지 않는다. 실제 HWP/HWPX fixture와 한컴 PDF 검증은 별도로 유지한다.
기존 테스트와 한컴 근거가 충돌하면 기대값을 기계적으로 보존/갱신하지 말고 원인을 보고한다.

### C. 집중 검증·메인테이너 확인

1. review 검증 환경에 파생 suite를 준비하고 기존 test runner로 신규 테스트를 실행한다.
   수정 전 동일 입력에서 실패하고 수정 후 통과해야 한다.
2. 기존 #6718, #6879, #6860 테스트를 실행한다. 추가로 변경 호출부와 연결된 #5807/#2439
   보호 테스트를 확인한다. 기대값을 낮춰 통과시키지 않는다.
3. 이번 입력 전체 3쪽의 텍스트/표 누락과 후속 겹침을 확인한다. 초기 페이지 수를 임의 변경하지 않는다.
4. `scripts/visual_sweep.py`로 수정 후 같은 PDF·DPI·raster 조건의 compare/overlay/review를 만든다.
   출력은 `output/6950/visual-after/`로 분리하여 수정 전 표준 증적을 덮어쓰지 않는다.
5. SVG와 표준 비교 경로, 실제 위치 변화, 남은 차이를 제시하여 메인테이너 시각 판정을 받는다.

## 3. Stage 3 — 영향 검증과 PR 준비

Stage 2 결과 승인 후 별도 진행한다. 변경 범위에 맞게 [로컬 검증 4.3](../manual/pr_review/local_validation.md)을
적용하고 Docker WASM을 빌드한다. Rust 본문에 새 테스트를 넣지 않고 `tests/cases/` 원본만 제출한다.

PR 준비 시에는 `AGENTS.md`의 파생 suite 준비 → fmt → native Clippy → WASM Clippy →
workspace build → all-targets Clippy → manifest 검사 묶음을 순차 수행한다. generated suite·manifest는
검증 증적이지 제출 소스가 아니다. 전체 회귀 범위와 실제 명령·시간은 Stage 3 보고서에 남긴다.

현재 단계에서 CI 정책 변경, 원격 push·PR 생성, GitHub comment·병합·close는 하지 않는다.
최종 보고서와 결과 승인 후 각 후속 승인 절차를 따른다.

## 4. 중단·복구 기준

- #6718 넘침 또는 #6879/#6860 보호가 깨지면 기능 범위를 늘려 덮지 않고 같은 절편의 기준점부터 재검토한다.
- 표만 맞고 후속 문단이 겹치면 실패다. 예약과 실제 배치를 별도 성공으로 계산하지 않는다.
- 원본 변조·한컴 검증 없는 합성 문서로 기대 결과를 만들지 않는다.
- 역사 비교 후 실행 파일은 소스 SHA와 해시뿐 아니라 보호 사례 동작으로도 확인한다.
- 임시 진단·역사 worktree만 정리한다. 메인테이너의 WIP, Studio pkg, 공유 캐시는 보존한다.

**승인 완료: 위 조판 계약과 책임 경계에 따른 A의 기준점 확인부터 B·C의 구현·집중 검증까지 Stage 2 진행.**
계획 범위를 벗어나는 구조 변경이 필요하면 구현 전에 변경 승인을 요청한다.

## 5. Stage 3 회귀 정정 수정계획 — 승인 요청

메인테이너 승인 완료: 아래 A~C의 조사 보완·제품 정정·테스트 보강·로컬 검증을 진행한다.
기존 링크를 보존하기 위해 절 제목은 유지한다. 원격 작업 승인은 포함하지 않는다.

### 5.1 범위와 기준

메인테이너는 명시적 개행의 기존 정상 배치와, 표 뒤 빈 문단을 거친 후속 vpos 문제를
**이 이슈 안에서 해결**하도록 결정했다. 신규 이슈·자식 이슈로 넘기지 않는다.
원본 #6950의 시각 판정 통과는 보존하며 작은 용지 높이 합성 실험은 계속 제외한다.

- 변경 전 대조군: 병합된 devel `0d36da4096fab2fef0e0a654e466fa449330d7a6`.
- 현재 후보: 제품 코드 `e2436aa2dfc55994740c4f9fe4d4cc3db67cd657`,
  작업 HEAD `acfa1c4a8fe2711de6f9711409cf2bb1635ebd42` 이후 문서만 미커밋 수정.
- 현재 후보·WASM·기존 증적을 보존한다. 전체 baseline 복귀나 과거 정상 분기의 일괄 삭제는 하지 않는다.
- 원인 근거: [Stage 3 §6~9](../working/task_m100_6950_stage3.md).
  ‘전 줄이 표보다 위’라는 판별과 일부 점유 목록만으로 만든 좌표를 최종값으로 채택해,
  기존 stacking·host 하한·쪽 귀속 처리를 생략한 것이 확인된 위험이다.

### 5.2 수정할 계약

1. **개행의 의미 보존**: 문단 경계, 문단 내부 강제 줄바꿈, 너비 부족으로 발생한 자동 줄바꿈을
   구분한다. 표 제어 문자의 위치·해당 줄·앞뒤 문단 구조에 연결한다. 문단 수나 `\n` 유무만으로
   새 규칙/옛 규칙을 선택하지 않는다. 구조 증거가 없는 경우 자동 줄바꿈이라고 추정하지 않는다.
2. **확정 전 제약 수집**: 기존 정상 흐름의 원점, 실제 host 줄 하단, 선행 개체의 점유와
   바깥 여백을 같은 현재 frame으로 대응시킨 뒤 배치를 확정한다. offset=0인 선행 표처럼
   배제 목록 밖에서 이미 소비한 흐름도 빠뜨리지 않는다. 표 개수·문서명으로 예외를 만들지 않는다.
3. **완료된 배치만 전달**: `ParagraphFloatPlacement`와 기존 frame 전달 구조를 정정해
   예약·출력·다음 흐름이 동일 결정의 결과를 쓰게 한다. host 줄 위치가 후단에서 다시 이동하는데
   표만 먼저 확정되는 순서를 해소한다. 필요한 책임이 이전되기 전에 기존 보정을 우회하지 않는다.
4. **빈 문단의 진행량 보존**: `끝의 표 → 빈 문단 → 다음 본문` 순서에서 빈 문단 자체도
   올바른 위치에 배치한다. 유효한 저장 vpos가 표와 간격을 이미 포함한 경우 중복 가산하지 않고,
   재조판에서는 현재 frame의 표 점유와 빈 문단 서식으로 흐름을 전달한다. 저장 원본은 덮어쓰지 않는다.
5. **쪽 귀속 보호**: 상단 앵커 교정과 통째/분할 판정의 의미를 분리해 정합시킨다.
   #6267처럼 기존 통째 표를 새 하단 계산만으로 분할하는 지점은 본문/종이 경계·여백·속성을
   확인해 정정한다. 두 fit 결과를 무조건 OR하거나 하단 clamp로 증상을 가리지 않는다.

### 5.3 실행 묶음과 변경 위치

| 순서 | 작업 | 완료 확인 |
| --- | --- | --- |
| 정정 A — 구조·적용 경계와 기존 흐름 복구 | synam001의 명시적 경계와 실제 host 줄 원점을 연결하고 #2439/#6797의 점유 전달 누락을 보완한다. `float_placement.rs`, `typeset.rs`, `layout.rs` 및 필요한 문단 배치 호출부에 한정한다. | synam001의 글줄·표 간격 복구, co-anchored/후속 표 비겹침, #6950 원본의 승인 배치 유지 |
| 정정 B — 빈 문단과 후속 vpos | 원본 #6950의 pi=1 표, pi=2 빈 문단, pi=3 본문을 한 흐름으로 검증·수정한다. paragraph layout/cursor의 원점 갱신·점유 소비 책임을 연결한다. 실제 관찰 대상이 다른 위치이면 그 원본 위치를 추가 고정한다. | 빈 문단의 위치·줄높이·간격과 다음 본문 시작이 원본/PDF 근거에 맞고 이중 가산 없음 |
| 정정 C — 전체 영향과 제출 게이트 | 나머지 실패를 개별 원인에 연결해 같은 변경에서 발생한 회귀를 복구하고 집중 검사 후 전체 게이트를 실행한다. | 전체23개 실패의 처리 근거, 새 회귀 없음, Docker WASM과 메인테이너 재판정 |

파서나 IR의 대규모 구조 변경이 필요하면 이번 승인 범위로 강행하지 않고 구체적 변경안을
다시 제시한다. 기존 fixture·진단 로그를 우선 쓰며 동일 전체 계측을 절편마다 반복하지 않는다.
각 묶음의 기준 코드와 결과를 기록한다. 단계 전환 시 변경을 커밋하는 기존 거버넌스는 유지한다.

### 5.4 회귀 검증 보완

- 원본 `20260909-para-table.hwpx`: 표 하나·본문 순서·3쪽 확인에 더해 pi=2 빈 문단과
  pi=3 첫 줄을 명시적으로 찾는다. 빈 문단의 **존재만** 확인하지 않고 배치·진행량을 확인한다.
  현재 저장 값은 pi=2 vpos55291, 높이1200, 줄간격672; pi=3 시작57163 HU로,
  차이1872 HU는1200+672와 같다. 이는 저장 구조 근거이며 모든 문서에 적용할 고정 간격이 아니다.
- 같은 입력의 현재 출력은 pi=2 상단590.5px, pi=3 상단786.2px이고 표는603.8..782.4px다.
  빈 줄을 표 위에 남긴 채 다음 본문만 배제 영역 아래로 미는 상태도 실패로 검출해야 한다.
- 명시적 개행/자동 줄바꿈은 같은 ‘줄 수’여도 별개 검사로 둔다. 개행이 실제 원본에서
  어떤 구조인지 확인하기 전 합성 문서의 기대값을 한컴 정답으로 삼지 않는다.
- 집중 보호: #2439, #6797 두 방향, synam001, #6267, #6718/#6879/#6860.
  기존 기대값을 완화하지 않는다. tests/cases 원본에 부족한 계약만 보강한다.
- 남은 전체 실패: #1789/#5941 → #2097/#6854/#6025 → #3738/#6764 → 코퍼스 원장 개별
  문서 순으로 원인·변화·중복 검출을 연결한다. 신규 fixture IR 왕복 실패는 별도 축으로
  base를 확인하며, 같은 renderer 원인이라고 가정하지 않는다.
- 집중 검증 통과 후 Rust lint 묶음, 전체 nextest, Native Skia3종, Docker WASM을 수행한다.
  정식 PDF가 있는 자료는 표준 compare/overlay/review를 사용한다. synam00130쪽과 원본의
  후속 문단을 Studio에서 메인테이너가 다시 판정한다. 픽셀 보조 점수만으로 통과시키지 않는다.

### 5.5 이번 승인 범위

위 A~C의 조사 보완·제품 정정·테스트 보강·로컬 검증을 승인받았다.
이번 범위 확정 지시를 원격 push·PR 생성·병합 승인으로 해석하지 않는다.
승인 시점에는 수정계획 작성까지이며, 조사·계획 기록을 보존한 뒤 정정 A부터 시작한다.
