# Task #467 수행계획서 — 확장 바탕쪽 다른 apply_to 조합 동작 미검증

**이슈**: #467 — 확장 바탕쪽: 다른 apply_to 조합 (active=Odd + ext=Both 등) 동작 미검증
**브랜치**: `local/task467`
**작성일**: 2026-05-02

## 1. 이슈 요약

Task #463 Stage 8 에서 도입한 "확장 바탕쪽 + overlap=true + 같은 apply_to → active 대체" 로직은 active master 와 같은 apply_to 케이스만 처리. 다른 apply_to 조합 (Odd + Both, Even + Both 등) 동작은 본 샘플에 없어 PDF 동작 미확인.

## 2. 수행 단계

| Stage | 내용 |
|-------|------|
| 1 | 다른 apply_to 조합 샘플 발굴 + 한컴 PDF 비교 + 수정 방향 결정 |

(단계 결과에 따라 추가 stage 또는 보류 결정)

## 3. 1차 조사 결과

`mydocs/working/task_m100_467_stage1.md` 참고.

핵심:
- `samples/exam_science.hwp` 에 다른 apply_to 조합 샘플 존재 (master[3] Even ext + master[4] Both ext overlap)
- 페이지 4 (마지막 짝수) 좌측 상단 "4(화1)" PDF 에 표시, SVG 미표시 — 누락 결함 발견
- 다만 결함 원인이 본 task 의 "다른 apply_to 조합 처리" 인지, master 컨트롤 BehindText 표 렌더링인지, 또 다른 결함인지 식별 어려움
- PDF/SVG 직접 좌표 비교 신뢰도 낮음 (메모리 `feedback_pdf_not_authoritative.md`)
- 한컴 2010/2020 환경 직접 검증 불가

## 4. 결정

**보류 (Stage 1 만 수행)**.

근거:
1. 본 task 는 본질적으로 **조사 task** — 명확한 코드 수정 방향 없음
2. master 처리 변경은 회귀 위험 큼 (메모리 `feedback_essential_fix_regression_risk.md`)
3. 식별된 결함 ("4(화1)" 누락) 이 본 task 범위와 직접 연관 모호
4. 향후 layout 리팩터링 또는 한컴 환경 직접 검증 가능 시점에 종합 처리

## 5. 산출물

- 본 수행계획서
- Stage 1 조사 보고서: `mydocs/working/task_m100_467_stage1.md`
- 최종 보고서: `mydocs/report/task_m100_467_report.md` (보류 결정)
- 코드 변경: 없음
