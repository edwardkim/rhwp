# #6856 baseline 재착수 — 구조 변형 산정과 판정 계약

- Issue: #6856, 담당 edwardkim, OPEN 확인.
- 기준: `task_m100_6856_baseline`, `b5eee9c50d95edca43431a7590e59d6101a30a67`.
- 작성일: 2026-09-08.
- 상태: 메인테이너가 2가지 일반 사각형 / 16가지 글상자 구조의 식별 구현 착수를 승인함.
- 앞선 R1/R2는 `task_m100_6856`에 보존하고 이번 결과로 사용하지 않는다.
- [기존 초안](task_m100_6856_impl.md)은 역사적 참조다. 아래 재착수 절편은 원본 구조 판정에 한정하며,
  바탕쪽 fallback·선 억제 A 제거·다른 페이지 조판 정정을 함께 수행하지 않는다.

## 1. 먼저 정할 검증 공간

사각형 기하를 고정하고 **직접 소유한 내부 문단 구조**를 검사한다. 개체 수·글자 수·이미지 수가
몇 개인지에 따른 모든 문서 배열은 무한하므로, 아래 수는 판정에 필요한 유무 동치류의 수다.
도형 종류 전체나 손상 입력 전체를 망라한 수라고 주장하지 않는다.

- S: 유효한 내부 문단 영역의 소유 구조 유무. 빈 문단도 영역 존재에 포함한다.
- B: 배경 채움 이미지 유무. 단색·패턴·그라데이션은 이번 축에서 '배경 이미지 없음'에 속한다.
- T: 해당 영역의 직접 소유 문단에 글자 데이터 유무. 공백을 trim해서 구조를 판단하지 않는다.
- I: 해당 문단에 직접 삽입한 그림 컨트롤 유무. 배경 이미지는 세지 않는다.
- O: 해당 문단에 그림 이외의 컨트롤 유무. 표·수식·도형·필드 등을 포함한다.

S가 없으면 T/I/O는 모두 없음이어야 한다. S가 있으면 T/I/O는 각각 독립적인 유무 3축이다.
따라서 `(영역 없음 1 + 영역 있음 2³) × 배경 이미지 2 = 18가지`다.

| 구조 | 글자 T | 삽입 그림 I | 기타 컨트롤 O | B 없음/있음 | 기대 구조 판정 |
| --- | --- | --- | --- | --- | --- |
| 영역 없음 | 없음 | 없음 | 없음 | 2가지 | 내부 문단 영역 없는 도형 |
| 영역 있음 | 없음 | 없음 | 없음 | 2가지 | 빈 내부 문단 영역 보존 |
| 영역 있음 | 있음 | 없음 | 없음 | 2가지 | 글자 포함 영역 보존 |
| 영역 있음 | 없음 | 있음 | 없음 | 2가지 | 그림 전용 영역 보존 |
| 영역 있음 | 있음 | 있음 | 없음 | 2가지 | 글자·그림 혼합 영역 보존 |
| 영역 있음 | 없음 | 없음 | 있음 | 2가지 | 기타 컨트롤 전용 영역 보존 |
| 영역 있음 | 있음 | 없음 | 있음 | 2가지 | 글자·기타 컨트롤 영역 보존 |
| 영역 있음 | 없음 | 있음 | 있음 | 2가지 | 그림·기타 컨트롤 영역 보존 |
| 영역 있음 | 있음 | 있음 | 있음 | 2가지 | 세 종류 혼합 영역 보존 |

글자·그림만 고려한 핵심 조합은 앞의 5행인 10가지다. 기타 컨트롤 때문에 나머지 8가지를 추가한다.
18가지는 의미상 조합이지 18개의 정상 한컴 fixture가 확보되었다는 뜻이 아니다.
표 안 그림처럼 중첩된 내용은 현재 소유자의 O에 속하고, 중첩 소유자의 구조를 별도로 검사한다.
O를 한 축으로 묶는 것은 분류 계약용이다. 표·수식 등 각각의 파서 경로를 모두 검증했다는 뜻은 아니다.

## 2. 정상 조합과 별도로 검사할 경계

다음은 18가지에 억지로 포함시키지 않고 별도 경계 시험으로 관리한다.

1. 영역 없음과 '빈 문단 하나가 있는 영역'은 다르다.
2. 영역 선언은 있지만 문단이 0개인 입력: 포맷상 유효성을 확인하고, 정상 빈 문단과 구분해 보존 여부를 정한다.
3. 영역 레코드 잘림·파싱 실패: 정상 빈 글상자로 간주하지 않는다. 기존 오류/진단 정책을 유지한다.
4. 캡션·그룹 자식·표 셀의 리스트: 현재 도형이 소유하지 않으면 현재 도형의 영역으로 가져오지 않는다.
5. 실선/선 없음 변경은 구조 판정에 영향을 주지 않는다. 이번 샘플의 실선 지정은 제품 기본값 변경이 아니다.

## 3. 구현 순서와 판정 원칙

1. **원본 → 파서 → IR 대조가 먼저다.** 기존 편람 HWP/HWPX와 메인테이너가 요청한 A4 샘플을 사용한다.
   기존 자료로 각 행의 확보/미확보를 표시하며, 전수 코퍼스 조사나 18개 파일 자동 생성을 먼저 하지 않는다.
2. HWP는 해당 도형 소유의 리스트 레코드와 경계를, HWPX는 해당 도형 소유의 drawText/subList를 확인한다.
   문단 파싱 결과가 없다는 이유만으로 원본 구조도 없었다고 판정하지 않는다.
3. IR 구조 계약을 확정한 후 하나의 공통 판정 경로를 구현한다. 기하 Rectangle은 유지하고
   내부 영역의 존재를 별도 의미로 보존한다. 중복 boolean이나 종류 필드를 추가해 기존 구조와 모순시키지 않는다.
4. 내용(T/I/O)·배경(B)은 판정 입력으로 사용하지 않는다. 시험 행을 선택하고 보존 결과를 검사하는 데만 쓴다.
5. 기존 `Option<TextBox>`를 감싸는 함수만 추가하고 문제가 해결됐다고 하지 않는다.
   잘못된 판정이 있으면 원본 해석/소유권/소비 지점 중 실제 원인을 수정하고 수정 전 실패를 증명한다.
6. 조판부호는 내부 레이아웃 노드 이름이 아니라 확정된 원본 컨트롤 의미를 사용하도록 검토한다.
   표시 문구 변경은 한컴에서 확인된 기대 판정이 있어야 진행한다. 렌더링 편의를 위한 TextBox 노드와
   한컴 UI의 개체 종류가 일치한다고 전제하지 않는다.

판정 순서 초안:

```text
현재 도형의 소유 구조를 정상적으로 읽을 수 있는가?
  아니오 → 기존 파싱 오류/진단 경로 (정상 일반 도형으로 위장 금지)
  예 → 소유 내부 문단 영역이 존재하는가?
        없음 → 내부 영역 없는 도형
        있음 → 내부 영역 있는 도형 (비어 있어도 보존)
```

메인테이너 승인으로 이번 rhwp 식별 계약은 사각형 기하 + 내부 영역 없음이면 일반 사각형,
내부 영역 있음이면 글상자로 확정한다. 한컴 COM UserDesc와 동일한 값이라고 주장하지 않는다.
첫 절편은 IR 공통 식별과 조판부호 소비를 연결하고, 파서 변경은 구조 손실이 재현된 지점에 한정한다.

## 4. 현재 증거와 한계

- A4 v3 HWP/HWPX는 한컴 직접 저장·재열기 성공, 같은 문단의 도형 3개다.
- baseline 파싱 결과: 글자 포함 내부 영역 1개, 내부 영역 없음 1개, 빈 내부 영역 1개.
  배경 이미지 없음 조건에서 서로 다른 3행만 확인됐다. 나머지 15행은 이 샘플로 검증되지 않았다.
- 배경 이미지와 그림 전용 글상자 조합은 추가 근거 확인이 필요하다.
- 한컴 COM UserDesc는 3개 모두 사각형이다. 가로 글상자 생성 액션과 내부 편집 성공을
  한컴 UI의 별도 종류 식별 성공으로 과장하지 않는다.
- 현재 코드의 TextBox 렌더 노드 → `[글상자]` 경로는 확인했다. Studio에서 세 개 모두
  표시된다는 관찰은 실행 WASM/열린 파일을 확인한 재현이 아직 필요하다.

## 5. 승인 후 절편의 완료 기준

- 18가지 검증 행과 추가 경계의 기대 결과, 실제 근거 유무를 구분한다.
- 구조 판정의 입력에 글자 수·이미지 유무·배경·선 스타일이 들어가지 않는다.
- 실제 잘못된 판정 또는 구조 손실의 실패 시험을 확보한 지점만 제품 변경한다.
- 새 회귀는 `tests/cases/`에 추가하고 source 본문에 넣지 않는다.
- 내부 모델 시험, 한컴 파일 유효성, 한컴 UI 판정, Studio 표시 결과를 별도로 보고한다.
- PR 전체 검증·WASM 갱신·원격 게시·commit/push는 이번 산정 단계에서 수행하지 않는다.

이 변형 공간과 판정 계약에 대한 구현 착수 승인을 받았다. 기존 baseline과 보존 브랜치는 유지한다.

## 6. Stage 3 이후 수정안 — 빈 문단과 판정 불가 구조

- 상태: **2026-09-08 메인테이너가 P1~P3 및 권고안 A의 구현을 승인함**.
- 선행 구현 `157717f6d`, 경계 시험·보고서 보존 `9f43ff368`.
- 근거: [Stage 3 경계 조사](../working/task_m100_6856_restart_stage3.md).
- 최종 목표는 그대로다. **조판 전에 내부 영역의 소유 구조를 보존하여 사각형과 글상자를
  구분한다.** 손상 문서 전체의 복구 엔진을 만드는 작업으로 확장하지 않는다.

### 6.1 기존 계획에서 구체화할 부분

정상 구조의 의미는 변경하지 않는다. 내부 영역 없음은 일반 사각형, 내부 영역 있음은 글상자다.
다만 구조를 끝까지 읽지 못한 입력을 이 두 상태 중 하나로 정상 판정하지 않는다.

추가 확인 결과, **HWP와 HWPX 모두** 문서 열기 상위 경로에 섹션 오류를 경고 후 빈 섹션으로
대체하는 처리가 있다. HWP의 `parse_paragraph_list`도 개별 문단 오류를 생략할 수 있다.
따라서 하위 함수에 `Err`만 추가하고 완료하면 안 된다.

권고안 A는 이번에 특정한 **소유 내부 영역의 확정적 구조 오류만 문서 열기 호출자까지 전달**하는
방식이다. 그 입력은 이전처럼 부분 문서로 열리는 대신 오류를 반환하게 된다. 이것은 호환성 변화이며
본 계획의 명시적 승인 대상이다. 다른 파싱 오류의 기존 복구 정책은 변경하지 않는다.
단순 경고 뒤 일반 사각형으로 처리하거나, 임의의 빈 글상자로 대체하는 방식은 채택하지 않는다.

### 6.2 처리 계약

| 입력 상태 | 처리 계약 | 확인할 근거 |
| --- | --- | --- |
| 소유 내부 영역 없음 | 일반 사각형 | 캡션·형제·그룹 자식을 영역으로 가져오지 않음 |
| 정상 내부 문단 1개 이상 | 글상자 | 내용·배경·선 종류와 무관 |
| HWPX 내부 `<p/>` | 빈 문단과 문단 속성을 보존 | `<p></p>`와 동일 IR, 뒤 형제 소비 없음 |
| 닫히지 않은 HWPX 내부 영역 | 구조 오류 반환 | section 함수뿐 아니라 전체 문서 열기에서도 오류 |
| HWP 소유 LIST_HEADER 필수 데이터의 확정적 잘림 | 구조 오류 반환 | 실제 소유권과 해당 헤더 변형의 필수 길이를 먼저 확인 |
| HWP 문단을 선언했지만 해당 문단 구조가 누락됨 | 구조 오류 반환 | 직접 소유 레코드만 계산, 그룹·캡션·셀 문단과 혼동 금지 |
| 영역 선언은 완전하지만 문단이 0개 | 유효성 확인 전 정상/손상 어느 쪽으로도 새 판정하지 않음 | 아래 P2의 한정된 근거 조사에서 결론 확정 |

HWP 헤더를 A4에서 관찰한 33바이트로 일괄 강제하지 않는다. 포맷 변형·확장 필드와 스펙 정오표를
확인한다. 선언 문단 수와 모든 자손 문단 수의 단순 비교도 하지 않는다.

### 6.3 승인 후 수행 순서

**P1 — 빈 문단 보존**

- `src/parser/hwpx/section.rs`에서 자기닫힘 문단은 하위 reader를 진행시키지 않고 문단 속성을
  공통 경로로 읽는다. 기존 문단 속성 해석을 복제한 별도 표를 만들지 않는다.
- 빈 문단의 `id`, 문단 모양·스타일 참조, 쪽/단 나눔 등 기존 파서가 해석하는 속성을 보존한다.
- `<p/>` 뒤 문단·형제 도형이 삼켜지지 않는 #5797 보호를 유지한다.
- `tests/cases/`에서 두 빈 문단 표기의 동등성, 속성 보존, 형제 보존을 수정 전 실패/수정 후 통과로 확인한다.

**P2 — 소유 영역의 검증 경계와 오류 전달**

- 기존 편람 HWP/HWPX·한컴 생성 A4 및 기존 raw 계측을 우선 재사용한다. 10k 전수조사와 새 정상
  샘플 대량 생성은 하지 않는다.
- HWP의 LIST_HEADER 소유 깊이·필수 데이터·문단 수 해석과 선언 0문단 허용 근거를 한 번의
  집중 조사로 정리한다. HWPX 빈 drawText도 XML 표기와 스키마 유효성을 구분한다.
- 0문단 허용 근거가 확인되면 영역 존재를 보존하는 테스트와 판정을 확정한다. 근거가 없으면
  임의 복구/거부를 구현하지 않고 해당 결정만 메인테이너에게 보고한다. 조사를 무기한 확대하지 않는다.
- 확정적 잘림/누락은 포맷별 타입 있는 구조 오류로 전달한다. 오류 메시지 문자열 검색으로
  치명적 오류를 선별하거나 모든 오류를 일괄 치명적으로 바꾸지 않는다.
- HWP의 문단 오류 생략 및 일반/lenient 섹션 대체, HWPX 섹션 대체가 **이 오류 종류만**
  삼키지 않게 연결한다. 오류 전파를 위해 필요한 변경은 파서 안에 한정한다.
- 사각형 외 도형까지 공유 경로의 영향이 생기는 경우 적용 범위를 먼저 명시한다.
  renderer에 파일 형식·버전 분기나 손상 여부 추정 조건을 추가하지 않는다.

**P3 — 수용 검증과 완료 판단**

- 모델 18조합, 조판부호 ON/OFF, 새 소유권 시험 3개 및 기존 #6852 선 회귀를 다시 확인한다.
- 잘림/누락 테스트는 section 진입점과 실제 컨테이너를 통한 문서 열기 진입점을 모두 검사한다.
  중첩 글상자·그룹에서도 오류가 생략되지 않아야 한다. 정상 CFB 경로와 lenient CFB 경로의
  동일 계약도 확인한다. 시험 컨테이너는 오류 주입용이며 한컴 정상 문서라고 주장하지 않는다.
- 정해진 편람 쌍·A4 쌍에서 열기 성공과 내부 영역 수·소유권 유지, 조판부호 판정을 대조한다.
  이미지 전용/빈 영역은 텍스트 길이를 기준으로 판정하지 않는지 계속 보호한다.
- 정상 샘플이 새 오류로 거부되면 오류를 우회하거나 기준값을 바꾸지 않고 원인을 조사한다.
- 이 구간 종료 시 해결/보류 조건을 하나의 결과보고서로 제출한다. 새로운 자식 이슈나 추가
  렌더링 개선 절편을 자동 생성하지 않는다.

### 6.4 영향 범위와 종료 조건

- 예상 변경: HWPX 문단·drawText 파서, HWP 소유 영역 검증·오류 타입 및 두 형식의 오류 전파,
  `tests/cases/`의 집중 회귀. 기존 판정 함수·기하 enum·레이아웃 선 정책은 유지한다.
- 성능: 원본 파일을 다시 여는 전수 사전 파싱을 추가하지 않는다. 이미 읽은 레코드/XML의 소유
  범위에서 검사하며, 선언 문단 수만큼 무조건 할당하지 않는다. 기존 깊이·크기 제한을 유지한다.
- 수용: 정상 사각형/빈·이미지 전용 글상자의 구분 보존, 빈 문단 표기 동등성, 확정적 손상의 명시적
  오류 반환, 정상 지정 샘플의 내용·영역 보존. 미확정 0문단을 숨긴 채 완료 판정하지 않는다.
- 전체 PR 게이트·Docker WASM 교체·원격 push·PR 생성은 이번 수정계획 작성 승인에 포함하지 않는다.
  PR 준비 단계에서는 해당 범위의 필수 Rust 검증을 생략하지 않는다.

### 승인 요청

**P1~P3와 권고안 A의 제한적 오류 전달 정책을 승인 요청한다.**
특히 확정적으로 손상된 내부 영역이 있으면 문서 열기를 실패시키는 호환성 변화를 명시적으로 승인받는다.
메인테이너의 “승인합니다.”로 위 구현과 제한적 오류 전달 정책의 승인을 받았다.
미확정 0문단의 근거 부족 시 별도 보고 조건은 유지한다.

### 6.5 한컴 손상 판정에 따른 0문단 처리 확정

메인테이너는 `a4-zero-paragraph.hwp`를 한컴편집기에서 확인하여 파일손상 판정을 보고했고,
**“한컴에서와 동일하게 손상된 처리로 판정해야 합니다.”**라고 구현을 지시했다.
이에 HWP 사각형의 직접 소유 LIST_HEADER가 문단 수 0을 선언하면 기존
`DrawingTextStructure` 오류로 거부한다. 일반/lenient 문서 열기까지 오류를 전달한다.
목록 없는 일반 사각형, 빈 문단 1개가 있는 글상자는 허용한다.
이 지시는 이 HWP 소유 영역 경계에 한정하며 HWPX·표·캡션의 정책을 확대하지 않는다.
§6.2와 P2의 0문단 결정 보류는 이 지시로 해소되었다.
