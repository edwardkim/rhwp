---
kind: plan
status: done
canonical: mydocs/plans/task_m100_6053.md
last_verified: 2026-08-26
---

# #6053 계획서 — B2-UI: 차트 행·열·라벨 구조 편집

- **Issue**: [#6053](https://github.com/edwardkim/rhwp/issues/6053)
- **부모**: [#3683](https://github.com/edwardkim/rhwp/issues/3683) Track B —
  B1(#4100 엔진 → #4694 UI) → B2 스파이크(#5447) → B2-엔진(#5652) → **B2-UI(이 문서)** → B3
- **브랜치**: `task6053` (`upstream/devel` `70ebacc4c` 기준). **이 문서의 줄 번호는 이 커밋 기준이다.**
- **범위**: `rhwp-studio/` + `mydocs/`. **Rust 변경 0줄** — 엔진(#5652)의 `structure` 플래그가
  `wasm_api.rs:3844-3853` JSON 패스스루와 `wasm-bridge.ts:2140-2146` 을 그대로 통과한다.

## 1. 배경

studio 는 B1 범위에서 멈춰 있었다. `chart-data-dialog.ts` 가 숫자 값만 고칠 수 있었고, 코드
주석이 *"행·열 추가/삭제 버튼이 없고 계열명 헤더가 비활성인 것 자체가 B2 와의 경계"* 라고 적어
두었다. 이 작업이 그 경계를 걷어낸다.

## 2. 착수 시점 실측 — 이슈 본문을 바로잡은 것

이슈는 로컬 `devel` 이 110 커밋 뒤처진 시점에 쓰였다. `upstream/devel` `70ebacc4c` 실측 결과
전제 셋이 바뀌었다.

| 이슈의 전제 | 실측 |
|---|---|
| "S4(메뉴 사전 판정)는 #6037 merge 후에 붙인다" | **#6037 은 이미 devel 에 있다.** PR #6052 자체는 unmerged close 지만 통합 PR #6072(merge `ee7e8a6ed`)로 반영됐다. `chart.rs:237-239` 가 `"plot"`·`"hasUpDownBars"` 를 방출하고 `:580` 에 `candleAnchorBroken` 이 서 있으며, `pieSeriesCountFixed`·`stockSeriesCountFixed` 는 제거됐다 → **S3·S4 를 합쳤다** |
| "R1: 중간 행 삭제 × 빈 값 칸" | 더 넓다. `plan_edits`(`chart.rs:794-932`)는 **위치 기반 겹침 치환 + 꼬리 증감**만 한다 — 중간 삽입·삭제는 아래·오른쪽 칸 전부를 제자리 치환 대상으로 만든다. 사고가 셋이다: 빈 값(`valueNotPatchable` `:684`), 계열 이름 밀림(`seriesNameNotPatchable` `:710`), 신설 계열 이름 필수(`seriesNameRequired` `:768`) |
| `chart-data-target.ts` 가 `src/ui/` | 실제는 `rhwp-studio/src/core/chart-data-target.ts` (줄 번호 `:151-163` 은 정확) |
| `is_number` 가 `patch.rs:271` | `is_safe_text` 만 `crates/rhwp-ooxml-chart/src/patch.rs:268-275`(pub). `is_number` 는 `chart.rs:302-308` 의 비공개 `fn` |

**넷째 — 이슈가 "wasm-pack 게이트가 한 번도 필요 없다"고 한 것은 CI 게이트 한정이다.** `pkg/` 는
gitignore 된 빌드 산출물이고 봉투 JSON 은 `.wasm` 안 Rust 가 런타임에 조립한다. 재빌드 전에는
`plot`/`hasUpDownBars` 가 studio 에서 `undefined` 이고, 기존 `scripts/frontend-wasm-bindings.test.mjs`
는 export **이름만** 대조해 이 스테일을 잡지 못한다.

## 3. 설계

### 3-1. 그리드 상태 모델을 DOM 에서 분리한다

B1 은 `valueInputs: (HTMLInputElement|null)[][]` 가 곧 상태였다. 행·열 수가 원본에 고정됐고
(`:109`,`:188`), `null` 하나가 "원본에 칸 없음"(`:157`)과 "원본이 빈 값"(`:167`)을 겸했다. 셋 다
**DOM 이 진실**이라는 한 병의 증상이다.

신규 순수 모듈 `src/core/chart-grid-model.ts` 가 **불변 모델을 진실로, DOM 을 투영으로** 세운다.
`CellOrigin` 4종(`value`/`empty`/`absent`/`new`)이 겸용하던 두 뜻을 가른다. 구조 연산 4종은 전부
`(GridModel, …) => GridModel` 이다. 새 칸 기본값은 `'0'` — 빈 문자열은 코어 `is_number` 가 거부한다.

미변경 문자열 보존(`4.30`)은 모델이 원본 문자열을 그대로 담고 손대지 않는 것으로 유지된다.
`trim()`·`Number()` 왕복이 이 경로에 한 번도 없다.

> **`src/core/` 의 값 import 는 상대경로 + `.ts` 확장자여야 한다.** `npm test` 는 `node --test` 로
> 타입 스트리핑만 하므로 `@/` alias 를 값 import 에 쓰면 그 모듈을 읽는 단위 시험이
> `ERR_MODULE_NOT_FOUND` 로 죽는다(선례 `font-substitution.ts:13`).

### 3-2. `structure` 는 페이로드에서 유도한다

`needsStructure(data, values, labels, names)` 를 새로 두되 규칙을 발명하지 않는다 — 코어
`validate_values` 의 네 거부(`seriesCountMismatch`·`valueCountMismatch`·`seriesNameMismatch`·
`categoryMismatch`)가 하나라도 설 페이로드면 true, **그 넷의 부정으로만** 정의한다.

항상 켜면 안 되는 이유: `structure: true` 는 개수 불일치를 "의도"로 해석하므로, 그리드 조립에
버그가 생기면 엔진이 거부하지 않고 **조용히 계열을 잘라내는 문서 변형**이 된다. B1 의 네 거부가
그 사고를 막는 마지막 그물이고 UI 가 재현할 수 없다. 부수 효과로 무편집 왕복이 자동 보장된다 —
아무것도 안 건드리면 `structure` 키조차 실리지 않아 페이로드가 B1 과 글자 단위로 같다.

### 3-3. 다이얼로그 전용 우클릭 메뉴

기존 `ContextMenu` 는 `registry.get(cmdId)` 가 없으면 항목을 버리는 커맨드 레지스트리 구동이라
그리드 조작 6종의 전역 커맨드 등재가 필요하고, `row.dataset.cmd`(`context-menu.ts:50`)가
automation e2e 의 `[data-cmd]` 드리프트 스캔 표면에 올라탄다. 그리드 조작은 다이얼로그 밖에서
뜻이 없으므로 신규 `src/ui/local-context-menu.ts` 를 두고 **CSS 만 재사용**한다 — 신규 CSS 0줄
(`.context-menu` z-index 20000 > `.modal-overlay` 10000).

**키 처리가 이 모듈의 핵심이다.** `ModalDialog` 는 `document` capture 에서 모든 키를
`stopPropagation()` 하고(`dialog.ts:113`) ESC 를 닫기로, **비-입력 요소의 Enter 를 `.dialog-btn-primary`
클릭으로** 바꾼다(`:105-110`). 메뉴 항목은 `<div>` 라, 막지 않으면 메뉴에서 누른 Enter 가
다이얼로그를 확인해 버린다. 그래서 `window` capture 에 달고(capture 는 `window → document` 순)
ESC·Enter 둘 다 전파를 끊는다. **이슈에 없던 실측이다.**

### 3-4. 사전 비활성은 봉투에서 유도되는 것만

| 조건 | 비활성 | 코어 대응 |
|---|---|---|
| `labelsMultiLevel === true` | 행 3항목 | `multiLevelLabelsUnsupported` |
| `labelsShared === false` | 행 3항목 | `sharedCategoryRequired`/`sharedXRequired` |
| 행 1개 / 계열 1개 | 행 삭제 / 계열 삭제 | `lastPointDeleteRefused`/`lastSeriesDeleteRefused` |
| `hasUpDownBars` 이고 대상이 첫·끝 계열 | 그 계열 삭제 + 양끝 **바깥** 삽입 | `candleAnchorBroken`(계열 수가 바뀔 때만 발화 → 중간 삽입·삭제는 통과) |

`plot === 'pie' | 'ofPie'` 는 **막지 않고** 안내한다 — #6037 이 파손이 아니라 무효과로 확정했다.

그 밖의 거부(중간 삽입 시 빈 값 자리 이동, `c:tx` 부재 등)는 **막지 않고 코어 dryRun 이 판정한다.**
코어 규칙을 UI 가 재현하기 시작하면 둘이 어긋나고, 과잉 차단이 미달 차단보다 나쁘다. 다만
`seriesNameRequired` 만은 UI 가 예방한다 — 신설 계열에 기본 이름을 채워 보내면 이 거부가 서지 않는다.

## 4. 변경 파일

| 파일 | 변경 |
|---|---|
| `rhwp-studio/src/core/chart-grid-model.ts` | 신규 — 불변 그리드 모델 |
| `rhwp-studio/src/core/chart-data-target.ts` | `structure`·`plot`·`hasUpDownBars`·`needsStructure`·`labelsStructurallyEditable`·`unsafeTextIssue`·`buildChartEdits`/`hasAnyEdit` 확장 (전부 additive) |
| `rhwp-studio/src/ui/local-context-menu.ts` | 신규 — CSS 재사용 로컬 메뉴 |
| `rhwp-studio/src/ui/chart-data-dialog.ts` | 재작성 |
| `rhwp-studio/src/styles/chart-data-dialog.css` | `.chart-data-text`·`.chart-data-new` 2종 (접두어 재사용 → UI 규칙 표 갱신 불필요) |
| `rhwp-studio/tests/*` 4종 | 모델·페이로드·배선·메뉴 가드 |
| `rhwp-studio/e2e/chart-data-structure-issue6053.test.mjs` + MANIFEST + package.json | 본선 e2e 와 등록 |

**무변경**: 코어·크레이트·`wasm_api.rs`·`wasm-bridge.ts`·`mutation-method-registry.ts`·
`ui/context-menu.ts`·`samples/**`·`gym/**`. 뮤테이션 원장의
`'src/ui/chart-data-dialog.ts': 3` 도 그대로다(dryRun 선탐침을 늘리지 않았다).

## 5. 단계

| 단계 | 내용 | 게이트 |
|---|---|---|
| S0 | devel FF · worktree · Node 22.15 · `pkg/` 재빌드 | 봉투에 `plot`/`hasUpDownBars` 실측 |
| S1 | 모델 + 페이로드 계약 | 기존 `chart-data-target.test.ts` **무수정** 통과 |
| S2 | 로컬 메뉴 | 소스 가드 |
| S3 | 다이얼로그 재작성 + 사전 판정 | 배선 계약 · 원장 3 유지 |
| S4 | e2e + MANIFEST | `check_e2e_manifest.py` · 실브라우저 |
| S5 | 문서 · 최종 게이트 · PR | §검증 |

## 6. 위험과 대응

| # | 위험 | 대응 |
|---|---|---|
| R1 | 중간 삽입/삭제 × 빈 값·`c:tx` 부재 → 코어 거부 | 띄우고 dryRun 이 판정. 코어 한국어 `message` 를 그대로 노출한다. 엔진 변경으로 도망가지 않는다 |
| R2 | `pkg/` 스테일로 사전 판정이 조용히 무력화 | S0 재빌드 + **e2e 가 `plot`/`hasUpDownBars` 부재를 즉시 실패시킨다** |
| R3 | `window` capture 가 `document` capture 보다 먼저 도는가 | e2e 4단계가 실측 |
| R4 | B1 e2e 가 `.chart-data-grid`·첫 `td input` 에 묶여 있다 | 클래스명 유지, 그리드 앞에 `td` 를 넣지 않음 |
| R5 | `check_e2e_manifest.py` 선행 부채 3건 | clean devel 에서 동일 재현 확인 — 보고서에 명시 |
| R6 | studio 스위트 선행 실패 | 착수 시점 기준선 확보 후 대조 |

## 7. 검증

`local_validation.md` §4.3 "rhwp-studio만 변경" 행 = TypeScript 검사 · npm test · 실제 browser 동작.

```bash
cd rhwp-studio && npx tsc --noEmit
cd rhwp-studio && npx tsc --project tsconfig.ci-unit.json --noEmit
npm --prefix rhwp-studio test                      # Node 22.15+
python scripts/check_e2e_manifest.py
cd rhwp-studio && npm run e2e:issue-6053
cd rhwp-studio && node e2e/run-with-vite.mjs -- node e2e/issue-4694-chart-data-edit.test.mjs --mode=headless
cargo fmt --all -- --check                          # PR 직전 HARD GATE
```

**신규 HWP 픽스처 0건** — `묶은세로막대형`(본선) · `2차원원형`(원형 안내) · `시가고가저가종가`
(캔들 양끝) 로 덮는다.

## 8. 범위 밖

B3 종류 변환 · 신규 차트 생성(#3683 결정으로 v1.0.0 밖) · 한컴 재판정(쓰기 경로가 엔진
`set_chart_data_*` 와 동일 바이트이고 #5652 S5 가 판정 단위를 닫았다. #4694 도 같은 논리로
재판정하지 않았다) · 차트 시각 회귀 게이트(#3938 이 지목한 렌더 축의 기존 공백).
