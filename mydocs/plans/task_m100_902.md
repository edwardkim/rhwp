# Task #902 수행 계획서 — WMF unit scale 정합 (한컴 사적 WMF SetWindowExt 비표준 비례)

**이슈**: [edwardkim/rhwp#902](https://github.com/edwardkim/rhwp/issues/902)
**브랜치**: `local/task902` (base: `upstream/devel @ 72a6bbc3`)
**원본 task**: #896 Stage 9 분리 (Task #896 머지 완료)
**선행 task**: #860 (WMF SetWindowExt 자동 확장)

## 1. 증상

`samples/hwp3-sample16.hwp` 페이지 18 의 paragraph 394 picture (WMF, "주전산센터 목표시스템 구성(안)") 안의 텍스트가 rhwp-studio 와 한컴 viewer (HWP5 변환본 .hwpx) 비교 시 크기/간격 미세 차이.

| 항목 | rhwp | 한컴 viewer |
|------|------|------------|
| 한글 글리프 표시 | ✅ 정상 (Task #896 fix) | ✅ |
| 텍스트 크기 | 작음 (가까이 모임) | 큼 (적당 간격) |

## 2. ROOT CAUSE 진단 (Task #896 Stage 9 결과)

### 2.1 WMF binary 분석

```
rec[0] SETMAPMODE mode=8 (MM_ANISOTROPIC)
rec[1] SETWINDOWEXT (y=72, x=56)    ← 단 1회 호출, 매우 작은 값
rec[2] SETWINDOWORG (y=4161, x=6333)
SETVIEWPORTEXT/ORG: 호출 안 됨
SCALEWINDOWEXT/VIEWPORTEXT: 호출 안 됨
```

### 2.2 핵심 모순

- WMF spec MM_ANISOTROPIC: SetWindowExt + SetViewportExt 의 비율로 unit scale 결정 (수학적 정의)
- 한컴 사적 WMF: `SetWindowExt(56, 72)` 단 1회 + `SetViewportExt` 미호출
- 실 element 좌표는 (0, 0, 6333, 4212) 사용 — WindowExt 와 **비표준 비례**

### 2.3 현재 rhwp 동작

- `src/wmf/converter/svg/mod.rs:75~94` (Task #860): viewBox 자동 확장 → (0, 0, 6333, 4212)
- font-size 117 (in element 좌표) → effective ~11.24 px (작게 표시)

## 3. 새 자료 — 다양한 sample + PDF 추가됨

본 task 수행 시점 (5/15) 의 가용 자료:

### 3.1 HWP3 sample WMF 포함 후보

| Sample | HWP3 | HWP5 변환본 | HWPX 변환본 | PDF 2022 |
|--------|------|------------|-------------|----------|
| hwp3-sample14 | ✓ | hwp3-sample14-hwp5.hwp | hwp3-sample14-hwp5.hwpx | hwp3-sample14-hwp5-2022.pdf |
| hwp3-sample16 | ✓ | hwp3-sample16-hwp5.hwp | hwp3-sample16-hwp5.hwpx | hwp3-sample16-hwp5-2022.pdf |
| hwp3-sample17 | ✓ | hwp3-sample17-hwp5.hwp | hwp3-sample17-hwpx.hwpx | hwp3-sample17-hwp5-2022.pdf |
| hwp3-sample18 | (확인) | ? | ? | hwp3-sample18-hwp5-2022.pdf |
| hwp3-sample19 | (확인) | ? | ? | hwp3-sample19-hwp5-2022.pdf |

### 3.2 활용 전략

- **WMF binary 다중 sample pattern 분석** (Task #902 권장사항 1)
- **한컴 viewer PDF 비교** (Task #902 권장사항 2) — 시각 정합 권위 자료
- **HWP3 ↔ HWP5 ↔ HWPX 변환 결과 비교** — WMF 처리 일관성 확인

## 4. Fix 방향 후보 (이슈 본문 인용)

| 후보 | 처리 | 위험 |
|------|------|------|
| α | WMF SetMapMode + WindowExt + ViewportExt 의 정밀 ratio 처리 | 매우 높 (다른 WMF 회귀) |
| β | viewBox 결정 알고리즘 변경 (Task #860 자동 확장) | 매우 높 (Task #860 fixture 회귀) |
| γ | font-size scale factor 추가 (한컴 사적 ratio) | 매우 높 (정확값 부재) |
| δ | 한컴 WMF spec 정밀 분석 + 별도 unit scale 결정 알고리즘 | 매우 큼 (시간 매우 들) |

새 자료 활용으로 **다중 sample pattern 분석** 추가:

### ε. 다중 sample WMF pattern 분석 + 정밀 ratio 도출
- 여러 sample 의 WMF binary 의 (WindowExt, 실 element 좌표, font-size) ratio 측정
- 한컴 viewer PDF 의 텍스트 크기 측정 → 정합 ratio 계산
- 공통 pattern 식별 후 fix 알고리즘 도출

## 5. 진행 전략

### 5.1 진단 절차

1. **다중 sample WMF binary 수집** (sample14, sample16, sample17/18/19 등):
   - 각 sample 의 `samples/*.hwp` 에서 WMF picture 추출
   - WMF rec[0~N] 의 SETMAPMODE / SETWINDOWEXT / SETVIEWPORTEXT 패턴 측정
2. **rhwp 의 현재 SVG 출력 측정**:
   - 각 sample 의 WMF picture SVG 출력 (viewBox, font-size, text positioning)
3. **한컴 viewer PDF 의 텍스트 측정**:
   - 각 sample 의 PDF 페이지 에서 WMF picture 의 글자 크기 / 위치 pdftohtml 추출
4. **공통 pattern 식별**:
   - WindowExt vs element 좌표 ratio
   - font-size 의 실제 rendering scale
   - SETMAPMODE 8 (MM_ANISOTROPIC) 특수 케이스
5. **Fix 알고리즘 후보 검증**: pattern 에서 도출된 ratio 가 sample 별 정합되는지

### 5.2 가설 검증

| 가설 | 검증 |
|------|------|
| H1: 한컴 사적 WMF 는 단순 ratio (예: 6333/56 = 113×) | sample 다수 측정 |
| H2: 한컴은 default SetViewportExt 가정 (예: paper unit) | spec 확인 + sample |
| H3: SetWindowExt 의 의미가 WMF spec 와 다른 사적 의미 | reverse engineering |
| H4: font-size 가 별도 unit | font 크기 추출 + ratio |

## 6. 예상 Stage (3~5)

- **Stage 1** — 다중 sample WMF binary + PDF 추출 + pattern 분석
- **Stage 2** — Fix 알고리즘 후보 도출 (α/ε 중)
- **Stage 3** — Fix 적용 + sample16 한컴 정합
- **Stage 4** — 광범위 WMF 회귀 검증 (모든 WMF 사용 sample)
- **Stage 5** — 통합 + 최종 보고서 + PR

## 7. 위험 평가

| 위험 | 영향 | 완화 |
|------|------|------|
| WMF 처리 변경 → 모든 WMF 사용 sample 회귀 | 매우 높음 | 다수 sample + golden SVG 회귀 점검 |
| Task #860 fixture (sample14 등) 회귀 | 높 | 본 task 완료 시 sample14 동시 정합 |
| 한컴 사적 ratio 가 sample 별 다를 가능성 | 중 | 다수 sample 측정 + 공통 pattern 확인 |
| 정확한 ratio 부재 시 timeboxed 후 close | - | 부분 정합 PR 또는 won't-fix |

## 8. 산출물 예정

- 수행 계획서: 본 파일
- 구현 계획서: `mydocs/plans/task_m100_902_impl.md`
- Stage 보고서: `mydocs/working/task_m100_902_stage{N}.md`
- 최종 보고서: `mydocs/report/task_m100_902_report.md`

## 9. 작업지시자 결정 요청

1. 본 수행 계획서 자체 승인 → 구현 계획서 작성
2. **base 결정**: `upstream/devel @ 72a6bbc3` (최신, Task #898/#900/#901 머지 후)
3. **timebox**: ~3일 (한컴 사적 WMF spec 분석 시간 매우 들음 — 정확 ratio 발견 못할 시 부분 정합 또는 won't-fix)
