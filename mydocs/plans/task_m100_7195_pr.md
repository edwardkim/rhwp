---
kind: investigation
status: active
---

# #7195 PR 준비 — 잔여 실패 공개 및 게시 전 판단

## 현재 경계

아래 준비 기록 이후 2026-09-17 작업지시자가 실패 공개 목적의 push·Draft 생성을 승인했다.
[Draft PR #7222](https://github.com/edwardkim/rhwp/pull/7222)을 head `369fb09514`에서 게시했고
API로 본문·Draft 상태·head/base를 확인했다. [접수 기록](../pr/archives/pr_7222_review.md)의
판정은 머지 보류다. 아래 미실행 문구는 승인 전 준비 시점이며 Ready·merge·close는 여전히 미실행이다.

2026-09-17 작업지시자의 진행 승인으로 결과보고·PR 초안을 준비한다.
실패한 검사를 PASS로 바꾸거나 별도 이슈 등록만으로 제출 게이트를 면제하지 않는다.
이번 단계의 원격 push·댓글·PR 생성·merge·close는 하지 않는다.

- 내부 task 제출 준비: `docs_and_git_workflow.md`의 Internal Task PR Approval 적용.
- 후속 self-review 기본 경로: `collaborator_self_merge.md` (실제 PR 번호를 얻은 뒤).
- 현재 적용 보조: `local_validation.md`, `visual_fixture_evidence.md`.
- 읽은 진입점: `AGENTS.md`, `CLAUDE.md`, 프로젝트 Codex memory, mydocs/manual 지도,
  `docs_and_git_workflow.md`, `pr_review_workflow.md`, `pr_review/README.md` 및 위 자식 문서.
- 정식 review 접수·번호 기반 파일·오늘할일은 아직 시작하지 않았다. 해당 단계에서
  `intake_and_review.md`를 읽고 최신 head 기준으로 self-review한다.
- 검증 제품 SHA: `d54459747d136cf072943d9006659027c5ad274a`.
- 기준 devel: `fcbd00e0fabc4b309a887357033f92e2d511cd75`.
- 준비 시작 head: `b502715ea`. 후속 문서-only commit은 제품 검증 결과를 바꾸지 않는다.

결과는 [결과보고서](../report/task_m100_7195_report.md)에 모았다.
원격 조회에서 `task_m100_7195` PR과 동명 upstream branch는 없었다.
`git merge-tree --write-tree upstream/devel HEAD`는 exit0,
준비 시작 head의 통합 tree는 `d7cf25bfc19e260a5d866db90ad3cbe14c633f98`였다.
게시 전 최신 base와 최종 문서 head에서 다시 확인한다.

## 제출 판단

정상 Open/merge 후보 준비 완료가 아니다. 전체 회귀30건과 Native Skia lib3건이 실패했다.
후자는 전자의 부분집합이다. 실패를 해결하거나 개별 근거를 갖춘 계약 변경·승인이 필요하다.
현재 원격 CI의 통과를 예상하거나 보증하지 않는다.

장기 작업을 조기 공유하려면 **실패를 공개한 Draft PR**이 가능한 선택이다.
작업지시자가 이 목적과 push·Draft 생성에 동의한 경우에만 아래 명령을 실행한다.
정상 Open PR을 열기 위해 실패를 숨기거나 자동으로 ignore하지 않는다.

## PR 본문 초안

제목: `fix(layout): 중첩 셀 여백·분할 소유 및 저장 프레임 경계 정비 (#7195)`

### 변경 요약

- ignored 조판 계약5건을 조사하고, 승인된 규칙에 맞게 기본 회귀 검사로 복귀시켰습니다.
  처음부터 통과하던2건은 기대값을 유지하면서 입력 오류의 거짓 통과를 제거했습니다.
- 중첩 표의 owner 파생 폭에 유효 셀 안 여백을 적용하고, 분할 후 이미 소비한 줄의
  중복 paint를 제거했습니다.
- 저장 프레임의 마지막 줄 상자와 trailing 간격을 구분하여 분할 예산·실제 배치의 경계를
  정비했습니다. 교체된86712 입력의 승인된4–8·11·12쪽을 보호합니다.
- 빈 문단 뒤 중첩 표가 셀 맨 위로 되돌아가는 원점 문제를 수정했습니다.
- **부분 개선입니다. 전체 회귀 실패와 giant 문서의 잔여 시각 차이는 해결하지 않았습니다.**

### 관련 이슈

Refs #7195. 자동 종료하지 않습니다.
잔여 실패: #7009, #7095, #7140, #7204, #7205, #7206, #7207, #7208, #7209.
Studio 글자 크기 재설정 관측은 #7202입니다.

### 검증

- 제품·테스트 `d54459747`, base `fcbd00e0fa`. 후속 변경은 기록 문서뿐입니다.
- fmt, Native/WASM/workspace-all-targets Clippy, workspace build, base 고정 suite/unit 정책: PASS.
- 최종 집중 검사37 PASS /1 FAIL. 전체 nextest9932 PASS /30 FAIL /47 skipped (exit100).
- Native Skia lib3929 PASS /3 FAIL /11 ignored. 그림2 PASS, PDF4 PASS.
- Docker WASM 빌드 및 실제 Studio 제공 파일 확인 완료. 원점 변경 Native/fresh WASM39–42쪽
  직접 출력 대조 완료. 시각 전체 통과를 의미하지 않습니다.
- 교체 입력 명시 보안 검사: 최종 결과는 결과보고서를 따릅니다.
- 30개 실패의 정확한 테스트 이름과 후속 이슈는
  `mydocs/report/task_m100_7195_report.md`에 기록했습니다.

stage5→stage6의 신규 실패0/해결0이며, 통합 전 stage4대비로는 giant49/48쪽 계약2건이
추가되었습니다. 30건 전부 devel 기존 결함이라고 주장하지 않습니다.
기존 기준값/golden 변경은 없고, 명시 승인된 #3931 쪽수 검사1건 외 추가ignore도 없습니다.

### 조판 근거와 미해결 범위

`mydocs/working/task_m100_7195_stage3.md`의 소비 경로·컷/높이 표와
stage5 충돌 통합, stage6 원점 red/green 증거를 참조합니다.
본문 fit, 줄 소유/원문 보존, 예산 부족 반례를 검사했지만 모든 rowspan·편집 경로를
새로 입증한 것은 아닙니다. 합성 NO_LS 계약은 한컴 실물 출력 증거와 구분합니다.

86712는 사용자가 교체한 원본을 커밋했습니다. 교체 전 PDF를 새 입력의 정답으로
재사용하지 않았습니다. giant의 PDF41쪽과 rhwp40쪽은 내용으로 대응하며 잔여 차이를
후속 #7140으로 남깁니다. 기준 출력 차이·회귀를 페이지수만으로 수용하지 않습니다.

성능: 별도 정량 벤치마크 미실행. 이번 테스트 실행 시간을 제품 성능 개선으로 해석하지 않습니다.
대표 review PNG의 안정 경로 보존·최종 사람 판정·최신 CI는 실제 PR의 후속 검토 조건입니다.

## 실행 명령 초안 — 미실행

아래는 **실패 공개 목적의 Draft 게시를 별도 승인받은 경우에만** 사용한다.
본문 부분을 UTF-8 파일로 분리하고 최신 base/head·충돌·정책·입력·증적을 재확인한 뒤 실행한다.

```sh
git push upstream HEAD:task_m100_7195
gh pr create --repo edwardkim/rhwp --base devel --head task_m100_7195 \
  --draft --title 'fix(layout): 중첩 셀 여백·분할 소유 및 저장 프레임 경계 정비 (#7195)' \
  --body-file <확정한-UTF-8-PR본문.md>
```

Ready 전환·merge·#7195 close는 별개 승인과 충족된 검증 근거가 필요하다.
