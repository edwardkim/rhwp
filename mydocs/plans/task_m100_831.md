# Task #831 수행계획서

**이슈**: https://github.com/edwardkim/rhwp/issues/831 (OPEN, PR #835 후속)
**브랜치**: `local/task831` (PR #875 머지 가정 — `local/task873` 에서 분기)
**작성일**: 2026-05-13
**제목**: 머리말/꼬리말 picture 회전/대칭 버튼 미동작 (Issue #831 후속 — PR #835 미정정 부분)

## 1. 이슈 요약

`samples/hwp3-sample11.hwp` 머리말 영역의 그림 객체를 선택 후 도구 상자의 회전/대칭 4개 버튼 (왼쪽회전 / 오른쪽회전 / 좌우대칭 / 상하대칭) 클릭 시 무동작.

HWP3, HWP5, HWPX 포맷 모두 공통.

**선행 의존**:
- PR #835 (#831 본문 picture 정정, merged)
- PR #832 (#825 머리말 picture lookup 인프라, merged) — `getHeaderFooterPictureProperties` / `setHeaderFooterPictureProperties` 추가됨
- PR #875 (Task #873 HWP5/HWPX 변환본 image 정합) 머지 가정

## 2. 결함 영역

### 결함 — getProps/setProps 가 머리말/꼬리말 분기 미적용

[`rhwp-studio/src/command/commands/insert.ts:397-410`](rhwp-studio/src/command/commands/insert.ts#L397):

```typescript
function getProps(services, ref): Record<string, unknown> {
  if (ref.type === 'shape') {
    return services.wasm.getShapeProperties(ref.sec, ref.ppi, ref.ci);
  }
  return services.wasm.getPictureProperties(ref.sec, ref.ppi, ref.ci);
  // ↑ ref.headerFooter 무시 — 본문 lookup 만 호출
}

function setProps(services, ref, props): void {
  if (ref.type === 'shape') { ... }
  else { services.wasm.setPictureProperties(...); }
  // ↑ 동일
}
```

`ref.headerFooter` 가 있는 picture (머리말/꼬리말) 의 경우:
- `getPictureProperties` lookup 실패 → `props` 가 빈 객체 또는 stale
- `setPictureProperties` 도 본문 only → 머리말/꼬리말 IR 미갱신
- 결과: rotationAngle/horzFlip/vertFlip 변경 무동작

## 3. 사전 진단 (Stage A 검증 영역)

### 가설 A — `ref.headerFooter` 가 selectedPictureRef 에 정확히 전달됨

PR #832 에서 cursor 의 `selectedPictureRef.headerFooter` 가 line/shape/image 3 path 모두 전파되도록 정정. picture-props-dialog 에서 동일 분기 동작 확인.

### 가설 B — wasm-bridge 의 `getHeaderFooterPictureProperties` 가 rotation/flip 필드 반환

PR #832 의 `get_header_footer_picture_properties_native` (Rust 측) 가 본문 picture 와 동일 필드 (rotationAngle, horzFlip, vertFlip 포함) 반환하는지 확인.

### 가설 C — `setHeaderFooterPictureProperties` 가 rotation/flip 필드 적용

`set_header_footer_picture_properties_native` 가 props 의 rotationAngle/horzFlip/vertFlip 을 IR 에 적용하는지 확인.

## 4. 진행 절차

### 4.1 Stage A: 본질 정밀 진단

- `ref.headerFooter` 의 실제 구조 확인 (sec, outerParaIdx, outerControlIdx, kind 등)
- `getHeaderFooterPictureProperties` 반환 필드 + setter 적용 필드 확인
- `applyRotationDelta` / `toggleFlip` 동작 trace (debug print 또는 console.log)
- 정정 위치 확정

**산출**: `mydocs/working/task_m100_831_stage_a.md`

### 4.2 Stage B: 정정 구현 + 검증

- `getProps` / `setProps` 에 `ref.headerFooter` 분기 추가
- `applyRotationDelta` / `toggleFlip` 영향 검증 (분기 추가 외 변경 없음)
- 검증:
  1. cargo test --release --lib (회귀 0 — Rust 측 변경 없음 가정)
  2. tsc --noEmit (TypeScript 타입 검증)
  3. e2e 테스트 또는 수동 시각 검증:
     - hwp3-sample11.hwp 머리말 picture 선택 + 회전/대칭 4 버튼 모두 동작
     - 본문 picture (sample11 본문 또는 다른 sample) 회귀 0
     - HWP5 / HWPX 동등 검증

**산출**: `mydocs/working/task_m100_831_stage_b.md`

### 4.3 Stage C: 종합 보고서 + 커밋

**산출**: `mydocs/report/task_m100_831_report.md`
**커밋**: `Task #831: 머리말/꼬리말 picture 회전/대칭 버튼 정정 (closes #831)`

## 5. 검증 기준

- hwp3-sample11.hwp 머리말 picture 회전/대칭 4 버튼 정상 동작
- 본문 picture 회귀 0
- HWP5/HWPX 동등
- tsc --noEmit 에러 0
- cargo test 회귀 0

## 6. 위험 + 회피

| 위험 | 회피 |
|---|---|
| 본문 picture 의 ref.headerFooter 가 falsy 보장 | typeof check + undefined 명확 분기 |
| 머리말/꼬리말 picture 의 sec/ppi/ci 가 inner indices (= 머리말 내부) | wasm-bridge 의 setter signature (outerPara/outerCtrl/innerPara/innerCtrl) 정합 |
| WASM 빌드 필요 (rhwp-studio 변경) | rhwp-studio 만 변경 → tsc 만 검증 (WASM 빌드는 작업지시자 환경) |

## 7. 본 task 범위 외

- Picture 외 객체 (Shape/Line/Equation/Group) 머리말/꼬리말 회전/대칭 (별도 본질, 본 task 범위 외)
- Picture 의 다른 dialog 속성 (size, position 등) — 이미 PR #832 으로 정합
- Rust 측 (parser/renderer) 변경 — wasm-bridge 의 기존 API 재사용

## 8. 의존성

- 선행: PR #835 (Task #831 본문 정정), PR #832 (Task #825 머리말 인프라), PR #875 (Task #873)
- 후행: 없음

## 9. 마일스톤

M100 (v1.0.0) — rhwp-studio 머리말/꼬리말 picture 편집 정합

## 10. 권위 자료

- `samples/hwp3-sample11.hwp` (머리말 DCT 로고 picture)
- 한컴오피스 2022 동작 (머리말 편집 모드 + picture 회전/대칭 정상)
- PR #832 (`get/setHeaderFooterPictureProperties` 인프라)
- PR #835 (본문 picture 회전/대칭 정정 — 본 task 의 머리말/꼬리말 확장 base)

## 11. CLAUDE.md 규칙 준수

- rhwp-studio 영역만 변경 (parser / renderer / Document IR 무수정)
- 기존 wasm-bridge API 재사용 (`get/setHeaderFooterPictureProperties`)
- HWP3/HWP5/HWPX 포맷 무관 — rhwp-studio 의 ref 분기로 일괄 처리

## 12. 승인 요청

본 수행계획서 승인 후 → 구현계획서 → Stage A 부터 진행.

📋 **Task #831 수행계획서 승인 요청드립니다.**
