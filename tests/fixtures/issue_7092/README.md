# Issue #7092 fixture — 가운뎃점 `·`(U+00B7) 전진폭

`soil-quarry-permit-standard.hwp`
(sha256 `60433ba0f761ff9be54c8a17baeb26a3d24a50082afdea103adaa61226987a2b`, 100,352 B)

공개 법령 별표(산지관리법 시행령 [별표 8] 토석채취허가기준)다. 본문 글꼴이 `HY신명조`
(`H2MJSM.TTF`)이고 `·` 를 문서 전체에서 30회 쓴다. 그 글꼴은 `·` 를 **전각(1024/1024)**
으로 지니고 있고 한/글 정본도 같은 face 로 0.999em 을 준다.

`samples/` 가 아니라 `tests/fixtures/` 에 둔다 — `samples/` 신규 항목은 코퍼스 래칫
여섯의 모수를 흔든다.

런타임 분류기는 이 파일 이름으로 분기하지 않는다.
